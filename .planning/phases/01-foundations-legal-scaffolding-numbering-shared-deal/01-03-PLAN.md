---
phase: 01-foundations-legal-scaffolding-numbering-shared-deal
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - LICENSE
  - NOTICES.md
  - SOURCES.md
autonomous: true
requirements: [LEG-01, LEG-02, LEG-03]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The repository carries an MIT license covering all of its original content, and a reader can tell from `NOTICES.md` where the copyright grant stops and the trademark position starts."
    - "`LICENSE` grants MIT rights in this repository's original content and `NOTICES.md` states in one sentence that the grant conveys no rights in any third-party trademark, so the two instruments touch without overlapping. (edge: LEG-01/adjacency, explicit)"
    - "The repository uses exactly one root `LICENSE` file and no per-file copyright headers, and `NOTICES.md` states that all repository content is MIT-licensed unless a file states otherwise — so a file carrying no header is covered, not undefined. (edge: LEG-01/empty, explicit)"
    - "`NOTICES.md` contains a precedence sentence stating that `LICENSE` governs copyright in original content while `NOTICES.md` governs trademark and attribution, so a reader holding both never has to guess which applies. (edge: LEG-01/ordering, explicit)"
    - "`NOTICES.md` carries exactly three `###` framework subsections that never merge, and the MEDDIC-family subsection states that ownership of those marks is claimed by multiple parties and is contested rather than naming one holder. (edge: LEG-02/adjacency, explicit)"
    - "All three framework statements are present in `NOTICES.md` at the end of Phase 1, before any framework-derived rule content exists anywhere in the repository — the notice precedes the content it covers. (edge: LEG-02/empty, explicit)"
    - "The three framework subsections appear in a fixed, documented order and each carries the same four labelled elements in the same order plus a review date, so a later diff shows a content change and never a reordering. (edge: LEG-02/ordering, explicit)"
    - statement: "`SOURCES.md` defines what counts as reproducing framework text versus paraphrasing it, states that the judgement is semantic and is not performed by any tool in this project's stack, and names Phase 6's LEG-04 gate as its owner — while defining byte-exact equality for the one string that is mechanically checked, the attribution pointer, as an exact comparison after stripping surrounding whitespace with no Unicode normalisation and no case folding. (edge: LEG-03/encoding)"
      verification: backstop
    - "Every framework-derived concept that ships in any later phase can be traced to a source listed in `SOURCES.md`, and vendor training-portal material is named as out of bounds."
  artifacts:
    - LICENSE
    - NOTICES.md
    - SOURCES.md
  key_links:
    - "`LICENSE` <-> `NOTICES.md` precedence sentence: two legal instruments with disjoint scope, each naming its own boundary so neither is read as covering the other."
    - "`NOTICES.md` framework statements -> the attribution pointer defined in the same file: one canonical statement per rights-holder, one canonical pointer, N files carrying it."
    - "`SOURCES.md` approved-source list -> Phase 6's LEG-04 legal review gate: the gate reviews a concrete list rather than reconstructing provenance from memory."
    - "`SOURCES.md` reproduction boundary -> every framework-derived file authored in Phases 2 and 3: the boundary is written before the content it constrains."
  prohibitions:
    - statement: "No file in this repository reproduces proprietary framework text. Concepts are restated in this repository's own words and every framework-derived concept traces to a source listed in SOURCES.md (LEG-03)."
      status: resolved
      verification: judgment
    - statement: "NOTICES.md must not attribute the MEDDIC-family marks to a single owner, and must not assert or deny the validity of any mark or the outcome of any litigation. The repository does not adjudicate trademark status (D-13)."
      status: resolved
      verification: judgment
    - statement: "Framework concepts are never sourced from vendor training-portal material, paywalled course content, certification-programme handouts, or internal enablement decks, regardless of how readily a model reproduces them (D-12)."
      status: resolved
      verification: judgment
---

<objective>
Ship the repository's legal posture — MIT license, three individually named framework statements,
and an auditable paraphrase boundary — before any framework-derived content is written.

Purpose: `.planning/research/PITFALLS.md:66-93` establishes that this repository has three
rights-holders where the sibling project has one, and that the MEDDIC family has a documented
history of trademark enforcement against community content of exactly the kind this repository
will publish. A single footer line does not scale to that. Writing the notices before the content
they cover is the whole point of putting them in Phase 1.

Output: `LICENSE`, the framework statements and precedence sentence in `NOTICES.md`, and
`SOURCES.md` — the approved-source list and the reproduction boundary Phase 6's legal review gate
will review.
</objective>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-CONTEXT.md
@.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-01-SUMMARY.md
@.claude/CLAUDE.md

Read-only reference implementation on this machine (another project's checkout — never write into it):
`~/devoteam/.claude/plugins/marketplaces/simple-english/LICENSE` — a plain MIT file, the format precedent.
`~/devoteam/.claude/plugins/marketplaces/simple-english/README.md` final section — the one-rights-holder
disclaimer posture this repository has to scale to three. Do not copy its wording.

Research grounding (read the named line ranges, not the whole files):
`.planning/research/PITFALLS.md:66-93` — the three-mark trademark analysis and the nominative-fair-use boundary.
`.planning/research/PITFALLS.md:259-266` — why a ruling about one spelling in the MEDDIC family must not be read as covering another.
`.planning/research/ARCHITECTURE.md:46-56` — what a three-rights-holder repository needs that the sibling project does not.
`.planning/research/STACK.md:250-266` — repo hygiene and the non-affiliation posture.
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Every symbol below is NEW. `NOTICES.md` was created in plan 01-01 carrying only its title, one
orienting sentence, and the attribution-pointer block; everything else listed here is added by this
plan.

**Files created in this plan:** `LICENSE`, `SOURCES.md`. **File extended:** `NOTICES.md`.

**Named sections created in `NOTICES.md`:** `## Scope of this file`, `## Framework statements`,
with exactly three `###` subsections in this fixed order: `### Command of the Message`,
`### MEDDIC, MEDDICC, and related marks`, `### Challenger`.

**Labelled elements created inside each `NOTICES.md` framework subsection, in this fixed order:**
`**Mark:**`, `**Rights-holder:**`, `**Non-affiliation:**`, `**Paraphrase boundary:**`, and a
`Last reviewed:` line.

**Named sections created in `SOURCES.md`:** `## The rule`, `## What counts as reproduction`,
`## Message articulation sources`, `## Qualification checklist sources`,
`## Commercial teaching sources`, `## Out of bounds`.

**Table columns created in each `SOURCES.md` source section:** `Title` then `Author or publisher`
then `Kind` then `Status` then `Where`.

**Status vocabulary created:** `confirmed` and `unverified`, used in the `Status` column of every
`SOURCES.md` source table.

**Note on headings and marks:** per CONTEXT.md D-11, framework marks appear in headings only inside
`NOTICES.md`, where naming the mark is the factual attribution itself. `SOURCES.md` headings are
generic and name the frameworks in body prose only.
</artifacts_this_phase_produces>

<planner_assumptions>
## Flagged planner assumptions

- **A-04 (one-way rating without a preceding checkpoint, by design).** Task 1 carries a one-way
  reversibility rating for the MIT grant and no `checkpoint:decision` precedes it.
  This is deliberate, not an omission. PROJECT.md's Constraints section and CONTEXT.md D-15 both
  record the developer choosing MIT, so the door is already walked, and
  `gsd-core/references/planner-reversibility.md` names "gating a decision already made" as an
  anti-pattern. The rating is kept so the record of which doors this run walked through survives;
  the checkpoint is omitted so the run does not re-ask a settled question. A structural-validator
  warning on this task is expected and is answered here.
- **A-05 (source confirmation is deliberately manual).** `SOURCES.md` rows are verified by a
  reviewer opening each recorded location, not by an automated fetch. The verify step has no
  network access, and an automated fetch would be the wrong instrument anyway: the question is
  whether the location is the source the row names, which is a read, not a status code.
</planner_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: MIT license and the scope boundary between copyright and trademark</name>
  <reversibility rating="one-way">Publishing under MIT is a grant that cannot be withdrawn from copies already taken; CONTEXT.md and PROJECT.md both record the developer choosing MIT, so the rating is recorded here for the file and no checkpoint re-asks a decision already made.</reversibility>
  <files>LICENSE, NOTICES.md</files>
  <read_first>
    - `NOTICES.md` as it stands after plan 01-01 — read the existing title, orienting sentence, and attribution-pointer block; this task adds sections above the pointer block and must not disturb the pointer string, which the checker compares byte for byte.
    - `.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-CONTEXT.md` decision D-15 and the LEG-01 requirement text in `.planning/REQUIREMENTS.md`.
    - `~/devoteam/.claude/plugins/marketplaces/simple-english/LICENSE` — the plain MIT file format this repository mirrors. Read it for shape only.
    - `.planning/research/PITFALLS.md` lines 66-93, specifically the line stating that a code license and a trademark disclaimer are different legal instruments.
  </read_first>
  <action>
Create `LICENSE` at the repository root as the unmodified MIT license text. Line 1 is exactly
`MIT License`. The copyright line reads `Copyright (c) 2026 Proof First contributors`. Do not add a
preamble, a badge, a scope note, or any sentence that is not part of the standard MIT text — a
modified MIT file is no longer recognisably MIT to a tool or a reader. Everything this repository
needs to say beyond the grant goes in `NOTICES.md`.

Add `## Scope of this file` to `NOTICES.md`, positioned immediately after the existing orienting
sentence and before the existing attribution-pointer section, carrying four things in prose:

One, the precedence sentence: `LICENSE` governs copyright in this repository's original content,
and this file governs trademark and attribution, so the two never conflict and neither is read as
covering the other's territory.

Two, the no-trademark-grant sentence: the MIT grant conveys copyright permissions in this
repository's original content and grants no rights in any third-party trademark, service mark, or
trade name.

Three, the coverage-default sentence: all content in this repository is MIT-licensed unless a file
states otherwise, and this repository uses one root `LICENSE` file rather than per-file copyright
headers — so a file carrying no header is covered by the root license, not left undefined.

Four, one line stating that the framework statements below ship before any framework-derived
content exists in the repository, because a notice that arrives after the content it covers has
already failed.

Do not change the attribution-pointer fenced block or its carriers list. The `pointer-missing` and
`pointer-duplicated` checks compare that string byte for byte after stripping surrounding
whitespace, so reflowing it breaks the build.
  </action>
  <acceptance_criteria>
    - `LICENSE` exists at the repository root and its first line is exactly `MIT License`.
    - `LICENSE` contains the literal string `Copyright (c) 2026 Proof First contributors`.
    - `LICENSE` contains the literal string `THE SOFTWARE IS PROVIDED "AS IS"`.
    - Exactly one license file exists at the repository root: `find . -maxdepth 2 -name 'LICENSE*' -not -path './.git/*' | wc -l` prints 1.
    - `NOTICES.md` contains the literal heading `## Scope of this file`.
    - `NOTICES.md` contains a sentence naming both `LICENSE` and trademark in the same line, establishing precedence.
    - `NOTICES.md` contains the literal string `grants no rights in any third-party trademark`.
    - `NOTICES.md` contains the literal string `unless a file states otherwise`.
    - `grep -cF 'Concepts here are paraphrased from publicly described sales frameworks.' NOTICES.md` still returns exactly 1, proving the pointer block is undisturbed.
    - `python3 tools/check_repo.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>head -1 LICENSE</automated>
    <fails_when>non-zero exit because `LICENSE` does not exist, or the printed line is anything other than `MIT License`</fails_when>
    <automated>grep -cF 'Copyright (c) 2026 Proof First contributors' LICENSE</automated>
    <fails_when>exit 1 because no line matches, or a printed count other than 1</fails_when>
    <automated>find . -maxdepth 2 -name 'LICENSE*' -not -path './.git/*' | wc -l</automated>
    <fails_when>a printed count other than 1 — a second license file at the root means two conflicting grants</fails_when>
    <automated>python3 -c "import sys;t=open('NOTICES.md').read();need=['## Scope of this file','grants no rights in any third-party trademark','unless a file states otherwise'];miss=[s for s in need if s not in t];print('missing:',miss);sys.exit(1 if miss else 0)"</automated>
    <fails_when>non-zero exit, or the printed `missing:` list is not empty</fails_when>
    <automated>grep -cF 'Concepts here are paraphrased from publicly described sales frameworks.' NOTICES.md</automated>
    <fails_when>exit 1 because the pointer string is absent, or a printed count other than 1</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any line beginning with `pointer-missing` or `pointer-duplicated` appears in stdout</fails_when>
  </verify>
  <done>A reader opening the repository root finds one MIT license and one file that tells them, in three sentences, what the license covers, what it does not cover, and what governs the part it does not.</done>
</task>

<task type="auto">
  <name>Task 2: Three separately named framework statements</name>
  <reversibility rating="costly">These statements are the public legal posture of the repository; changing a rights-holder attribution after publication means correcting a statement that forks and search indexes have already copied (CONTEXT.md D-13).</reversibility>
  <files>NOTICES.md</files>
  <read_first>
    - `NOTICES.md` as it stands after Task 1 — read the scope section and the attribution-pointer block before adding below them.
    - `.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-CONTEXT.md` decision D-13 in full — it is the authoritative specification for what each statement may and may not say.
    - `.planning/STATE.md` Blockers section — the standing blocker recording that MEDDIC-family trademark status beyond one ruling is unresolved and must not be assumed safe by extension.
    - `.planning/research/PITFALLS.md` lines 66-93 and 259-266 — the enforcement history and the specific warning against reading a ruling about one spelling as covering another.
  </read_first>
  <action>
Add `## Framework statements` to `NOTICES.md`, positioned after the attribution-pointer section.
Above the subsections, state in one line that the three statements appear in a fixed order and that
each carries the same four labelled elements in the same order, so a future diff shows a content
change and never a reordering.

Write exactly three `###` subsections, in this order and with these exact headings:
`### Command of the Message`, `### MEDDIC, MEDDICC, and related marks`, `### Challenger`.
These are the only headings anywhere in the repository permitted to carry a framework mark — D-11
keeps marks out of filenames, directory names, and every other heading, and names `NOTICES.md` as
the one factual-attribution carve-out.

Each subsection carries, in this order, five lines: `**Mark:**` naming the mark or marks;
`**Rights-holder:**`; `**Non-affiliation:**`; `**Paraphrase boundary:**`; and `Last reviewed:`
followed by an ISO date.

For `### Command of the Message`, the rights-holder is Force Management. The non-affiliation line
states that this project is not affiliated with, endorsed by, sponsored by, or connected to Force
Management. The paraphrase boundary line states that this repository restates concepts associated
with the framework in its own words and reproduces no training material, no course content, and no
proprietary diagram.

For `### MEDDIC, MEDDICC, and related marks`, the rights-holder line must state that ownership of
these marks is claimed by multiple parties and is contested, and must not attribute them to a
single holder — this research could not support a single-owner claim and stating one wrongly in a
public notices file is worse than not stating one. Add one line stating that this repository makes
no claim about the validity of any mark in this family and no claim about the outcome of any
proceeding, and that a determination about one spelling in the family is not treated here as
covering another. The non-affiliation line states that this project is not affiliated with,
endorsed by, or sponsored by any party claiming rights in these marks. The paraphrase boundary line
matches the shape of the other two.

For `### Challenger`, the rights-holder is Challenger Inc. and its trademark successors. The
non-affiliation and paraphrase-boundary lines match the shape of the other two.

Every statement uses no more of each mark than is needed to identify what the concepts derive from.
Do not write a sentence anywhere in this file that positions this project as a version, an
implementation, an edition, or an automation of any named framework — that framing is what removes
the nominative-fair-use defence, and it is the specific warning sign
`.planning/research/PITFALLS.md:66-93` names.
  </action>
  <acceptance_criteria>
    - `NOTICES.md` contains exactly three `###` headings inside the `## Framework statements` section, and they are, in order, `### Command of the Message`, `### MEDDIC, MEDDICC, and related marks`, `### Challenger`.
    - `NOTICES.md` contains the literal strings `Force Management` and `Challenger Inc.`.
    - `NOTICES.md` contains the literal string `claimed by multiple parties` in the MEDDIC-family subsection.
    - Each of the three subsections contains all four labels `**Mark:**`, `**Rights-holder:**`, `**Non-affiliation:**`, `**Paraphrase boundary:**` and a line beginning `Last reviewed:` — twelve labelled lines and three review dates in total.
    - `grep -c 'Last reviewed:' NOTICES.md` returns 3 or more.
    - `grep -cF 'not affiliated with' NOTICES.md` returns 3 or more, one per framework.
    - `python3 tools/check_repo.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import re,sys;t=open('NOTICES.md').read();s=t.split('## Framework statements')[1];h=re.findall(r'^### (.+)$',s,re.M);print(h);sys.exit(0 if h==['Command of the Message','MEDDIC, MEDDICC, and related marks','Challenger'] else 1)"</automated>
    <fails_when>non-zero exit, which occurs when the printed heading list is not exactly the three expected headings in that order, or when the `## Framework statements` heading is absent and the split raises IndexError</fails_when>
    <automated>python3 -c "import re,sys;t=open('NOTICES.md').read();s=t.split('## Framework statements')[1];blocks=re.split(r'^### ',s,flags=re.M)[1:];bad=[b.splitlines()[0] for b in blocks if not all(k in b for k in ('**Mark:**','**Rights-holder:**','**Non-affiliation:**','**Paraphrase boundary:**','Last reviewed:'))];print('incomplete:',bad);sys.exit(1 if bad else 0)"</automated>
    <fails_when>non-zero exit, or the printed `incomplete:` list is not empty — a named subsection is missing one of its five required lines</fails_when>
    <automated>grep -cF 'claimed by multiple parties' NOTICES.md</automated>
    <fails_when>exit 1 because no line matches, or a printed count of 0 — the MEDDIC-family statement names a single owner instead of recording contested ownership</fails_when>
    <automated>grep -cF 'not affiliated with' NOTICES.md</automated>
    <fails_when>exit 1 because no line matches, or a printed count below 3</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any line beginning with `pointer-missing` or `pointer-duplicated` appears in stdout</fails_when>
    <manual>A reviewer reads `NOTICES.md` end to end and confirms two things a grep cannot express: that no sentence positions this project as a version, implementation, edition, or automation of any named framework, and that no sentence asserts or denies the validity of a mark or the outcome of a proceeding. Record the reading and its date in the plan SUMMARY. Phase 6's LEG-04 gate repeats this against current sources before public launch.</manual>
  </verify>
  <done>A lawyer auditing only this file learns, per framework, which mark is at issue, who claims it, that this project is unconnected to them, and exactly how far the paraphrase goes — and learns for the contested family that this repository takes no position on ownership.</done>
</task>

<task type="auto">
  <name>Task 3: The approved-source list and the reproduction boundary</name>
  <reversibility rating="reversible">A source can be added, corrected, or marked unverified at any time without breaking a published contract; the file is a working register, not a citable identifier space.</reversibility>
  <files>SOURCES.md</files>
  <read_first>
    - `NOTICES.md` as it stands after Task 2 — the paraphrase-boundary lines written there are the summary; this file is the operative detail, and the two must not contradict each other.
    - `.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-CONTEXT.md` decisions D-11 and D-12 — D-11 keeps marks out of the headings of this file, D-12 specifies what this file is for.
    - `.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-CONTEXT.md` deferred-ideas entry on textual anti-plagiarism checking — this file makes LEG-03 auditable and deliberately does not attempt to verify it mechanically.
    - `.planning/REQUIREMENTS.md` LEG-03 and LEG-04 — what this phase owns and what Phase 6 owns.
  </read_first>
  <action>
Create `SOURCES.md` at the repository root with a `Last reviewed:` ISO date line under the title and
six level-two sections in this order.

`## The rule` states the operative rule in three sentences: every framework-derived concept that
ships anywhere in this repository must trace to a source listed in this file; a concept with no
listed source does not ship; and adding a concept means adding its source in the same change.

`## What counts as reproduction` defines the boundary in plain terms, and is the section Phase 6's
LEG-04 gate reviews. State that a concept restated in this repository's own words, at a level of
generality a listed source states publicly, is paraphrase. State that a contiguous run of a
source's own wording, a source's own ordered list reproduced in its order, a source's diagram or
figure, and a term coined by a source and adopted here as this repository's own label are each
reproduction, and none of them ships. Then state the limit of this definition honestly, in the
posture PROJECT.md commits to: this is a semantic judgement, no tool in this project's stated stack
performs it, and `tools/check_repo.py` does not attempt it. Name the one string in this repository
for which equality is defined mechanically — the attribution pointer in `NOTICES.md`, compared byte
for byte after stripping leading and trailing whitespace, with no Unicode normalisation and no case
folding — and state plainly that everything else on this boundary is read by a human, with Phase
6's LEG-04 gate as the owner.

`## Message articulation sources`, `## Qualification checklist sources`, and
`## Commercial teaching sources` each open with one body line naming which framework the sources
below describe — the framework marks belong in body prose here, never in these headings, per D-11 —
followed by a table with columns `Title`, `Author or publisher`, `Kind`, `Status`, `Where`.

Populate each table only with sources whose existence can actually be confirmed: the frameworks'
originators' own published books and their own public web pages, and encyclopedia-level third-party
descriptions. `Kind` is one of `book`, `public page`, or `reference entry`. `Status` is `confirmed`
only when the entry has been checked and a public location is recorded in `Where`; otherwise
`Status` is `unverified` and `Where` reads `to confirm at LEG-04`. Do not write a URL that has not
been checked — inventing a citation in the file that exists to prevent invented citations is the
exact failure this repository is built to avoid, and an honest `unverified` row is worth more than
a plausible fabricated one. Each table carries at least two rows.

`## Out of bounds` names, as a bullet list, the material that is never a source regardless of how
readily it can be obtained or how readily a model reproduces it: vendor training-portal content,
paywalled course material, certification-programme handouts, internal sales-enablement decks, and
any leaked or redistributed copy of the above. Close with one line stating that availability is not
permission.
  </action>
  <acceptance_criteria>
    - `SOURCES.md` exists at the repository root and contains all six literal headings: `## The rule`, `## What counts as reproduction`, `## Message articulation sources`, `## Qualification checklist sources`, `## Commercial teaching sources`, `## Out of bounds`.
    - `SOURCES.md` contains a line beginning `Last reviewed:`.
    - No `SOURCES.md` heading line contains any of the strings `Command of the Message`, `MEDDIC`, `MEDDICC`, or `Challenger`: `grep -E '^#{1,6} ' SOURCES.md | grep -cE 'Command of the Message|MEDDIC|Challenger'` returns 0. <!-- planner-discipline-allow: Command of the Message, MEDDIC, MEDDICC, Challenger — this gate is region-scoped twice over: it reads only heading lines (`^#{1,6} `) and only `SOURCES.md`. Task 2 of this plan writes these same literals into `NOTICES.md` headings by design, which is the D-11 carve-out and a different file, so the gate cannot be self-invalidated by any action in this plan. -->
    - Each of the three source sections contains a table with at least two data rows.
    - Every source table data row's `Status` cell is exactly `confirmed` or exactly `unverified`.
    - Every row whose `Status` is `unverified` has a `Where` cell reading `to confirm at LEG-04` and contains no `http` substring.
    - `SOURCES.md` contains the literal string `no tool in this project` in the reproduction-boundary section.
    - `SOURCES.md` contains the literal string `LEG-04`.
    - `SOURCES.md` contains the literal string `training-portal` in the out-of-bounds section.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import sys;t=open('SOURCES.md').read();need=['## The rule','## What counts as reproduction','## Message articulation sources','## Qualification checklist sources','## Commercial teaching sources','## Out of bounds','Last reviewed:','LEG-04','training-portal','no tool in this project'];miss=[s for s in need if s not in t];print('missing:',miss);sys.exit(1 if miss else 0)"</automated>
    <fails_when>non-zero exit, or the printed `missing:` list is not empty</fails_when>
    <automated>grep -E '^#{1,6} ' SOURCES.md | grep -cE 'Command of the Message|MEDDIC|Challenger'</automated>
    <fails_when>a printed count above 0, meaning a framework mark reached a heading in this file and broke the D-11 filename-and-heading boundary</fails_when>
    <automated>python3 -c "import re,sys;t=open('SOURCES.md').read();rows=[l for l in t.splitlines() if re.match(r'^\|',l) and ('confirmed' in l or 'unverified' in l)];bad=[l for l in rows if 'unverified' in l and 'http' in l];print(len(rows),'rows;','bad:',bad);sys.exit(1 if len(rows)<6 or bad else 0)"</automated>
    <fails_when>non-zero exit, which occurs when fewer than 6 status-bearing rows exist across the three tables, or when the printed `bad:` list is not empty because a row marked unverified carries a URL</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any violation line is printed</fails_when>
    <manual>A reviewer opens each `Where` location recorded on a row whose `Status` is `confirmed` and confirms it resolves to the source the row names. Any row that does not resolve is downgraded to `unverified` with `Where` set to `to confirm at LEG-04`. This cannot be automated because the verify step has no network access, and it is the check that keeps the file from becoming the fabricated-citation problem it exists to prevent.</manual>
  </verify>
  <done>Phase 6's legal review gate has a concrete list to review rather than a provenance reconstruction exercise, Phases 2 and 3 have a written boundary before they author a single framework-derived line, and no row in the file claims a source that was not checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan writes three Markdown and text files into a public repository. There is no runtime, no
network surface, no authentication, no data store, and no user input, so ASVS L1 controls for
session management, access control, input validation, output encoding, and cryptography have no
counterpart here and are not padded into the register. The boundaries that genuinely exist are
legal-exposure boundaries, and they are this plan's entire subject.

| Boundary | Description |
|----------|-------------|
| Framework source material -> repository content | Wording owned by a rights-holder could cross into a public MIT-licensed repository through paraphrase that is too close. |
| Rights-holder identity -> published attribution | Naming the wrong holder, or naming one holder for a contested mark, is itself an exposure. |
| Copyright grant -> trademark position | An MIT file read as covering trademark, or a notices file read as licensing content, misstates what the repository actually grants. |
| Cited source -> reader trust | A fabricated or unchecked citation in the file that exists to prevent fabricated citations destroys the repository's credibility premise. |

## STRIDE Threat Register (ASVS L1, blocking on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering / Information Disclosure | Framework-derived content reaching a public repository | high | mitigate | Task 3 writes the reproduction boundary and the approved-source list before any framework-derived content is authored in Phase 2 or 3, and names Phase 6's LEG-04 gate as the owner of the semantic judgement no tool performs. |
| T-01-02 | Spoofing / Repudiation | The three framework statements in `NOTICES.md` | high | mitigate | Task 2 writes three separate, individually named statements with a fixed element order; the MEDDIC-family statement records contested ownership rather than naming a holder, and the reviewer check confirms no sentence asserts mark validity or a proceeding outcome. |
| T-01-03 | Repudiation | The `LICENSE` and `NOTICES.md` boundary | medium | mitigate | Task 1 writes an unmodified MIT `LICENSE` plus the precedence, no-trademark-grant, and coverage-default sentences, so neither instrument is read as covering the other's territory and a header-less file is covered rather than undefined. |
| T-01-09 | Spoofing | A fabricated citation in `SOURCES.md` | medium | mitigate | Task 3 forbids writing an unchecked URL, requires `unverified` plus `to confirm at LEG-04` for any unconfirmed row, and the automated check fails any unverified row carrying a URL. |
| T-01-10 | Elevation of Privilege | Framing that positions this project as a version of a named framework | medium | mitigate | Task 2 forbids version, implementation, edition, and automation framing, and the reviewer check reads for it — this framing is what removes the nominative-fair-use defence. |

**Supply chain:** no package-manager install tasks exist in this plan. Nothing is installed, so the
package-legitimacy gate has no applicable subject.
</threat_model>

<verification>
Run from the repository root after all three tasks complete:

- `head -1 LICENSE` prints exactly `MIT License`.
- `find . -maxdepth 2 -name 'LICENSE*' -not -path './.git/*' | wc -l` prints 1.
- `NOTICES.md` carries `## Scope of this file`, the undisturbed attribution-pointer block, and exactly three `###` framework subsections in the fixed order, each with its five required lines.
- `SOURCES.md` carries all six sections, at least six status-bearing source rows, and no framework mark in any heading.
- `python3 tools/check_repo.py --self-test` exits 0 with no `FAIL`, and `python3 tools/check_repo.py` exits 0.
- All three `<manual>` reviewer checks are recorded as performed in the SUMMARY.
</verification>

<success_criteria>
- The repository ships an MIT license covering all original content, and a reader can tell in one file where that grant stops.
- All three frameworks are named individually, with non-affiliation and trademark language, before any framework-derived content ships.
- The contested mark family is recorded as contested, with no ownership claim and no position on any proceeding.
- The paraphrase boundary is written down, its limits are stated honestly, and its owner is named.
- No citation in the repository claims a source that was not checked.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-03-SUMMARY.md` when done.
Record in it: the three `Last reviewed:` dates written into `NOTICES.md`, the count of `confirmed`
versus `unverified` rows in `SOURCES.md`, and the outcome of all three reviewer checks, so Phase 6's
LEG-04 gate starts from a known state.
</output>
