---
phase: 01-foundations-legal-scaffolding-numbering-shared-deal
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - README.md
autonomous: true
requirements: [LEG-01, LEG-02, LEG-03]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A reader landing on the repository learns what it is, what exists today, what does not exist yet, and that no measured claim has been published — without encountering a single number that no benchmark produced."
    - "`README.md` documents the target layout including the skill folder path, without creating any of the files it describes, so the phase boundary at `before any rule content is drafted` stays visible in the repository itself."
    - "`README.md` carries the attribution pointer string exactly once, proving the carriers mechanism defined in `NOTICES.md` works against a real file rather than only against fixtures."
    - "No framework mark appears in any filename, any directory name, or any Markdown heading anywhere in the repository outside `NOTICES.md`."
    - "`python3 tools/check_repo.py --self-test` and `python3 tools/check_repo.py` both exit 0 against the completed phase, with every Phase 1 file present."
  artifacts:
    - README.md
  key_links:
    - "`README.md` -> the `### Files required to carry it` list in `NOTICES.md`: README is the first real carrier, so the `pointer-missing` check stops being fixture-only and starts enforcing against shipped content."
    - "`README.md` repository-layout section -> `skills/proof-first/` (D-16) and the semantic-versioning policy in `NUMBERING.md` (D-17): the two things Phase 4's distribution manifests depend on, frozen here and documented rather than pre-created."
    - "`README.md` -> `LICENSE`, `NOTICES.md`, `SOURCES.md`, `NUMBERING.md`: the four root files a reader auditing the repository's legal and numbering posture needs, linked from the entry point."
  prohibitions:
    - statement: "No number, badge, percentage, or measured claim appears in README.md, or anywhere else in this repository, that is not sourced from a benchmark that has actually run and whose raw results are committed. LEG-05 and PROJECT.md's evidence constraint both bind here, and the benchmark does not exist yet."
      status: resolved
      verification: judgment
  edge_coverage_note: "All 14 spec-less probe edge items are owned by plans 01-01, 01-02, and 01-03 per the ledger in 01-01-PLAN.md. This plan introduces no new requirement and therefore owns no edge item; nothing is dropped."
---

<objective>
Write the repository's entry point and close the phase with a full-repo integrity sweep.

Purpose: two things have to be true before Phase 2 starts drafting rule content. First, a reader
arriving at the repository must be told what exists and what does not, with no number that no
benchmark produced — the repository cannot demand evidence it does not itself provide, and the
easiest place to break that rule is a README written before the measurement exists. Second, the
attribution-pointer mechanism defined in `NOTICES.md` has to be proven against a real carrier file
rather than only against the checker's own fixtures, and `README.md` is the only listed carrier
that exists in Phase 1.

Output: a claim-free `README.md` documenting the target layout, and a recorded sweep proving the
whole phase is internally consistent.
</objective>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-CONTEXT.md
@.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-01-SUMMARY.md
@.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-02-SUMMARY.md
@.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-03-SUMMARY.md
@.claude/CLAUDE.md

Read-only reference implementation on this machine (another project's checkout — never write into it):
`~/devoteam/.claude/plugins/marketplaces/simple-english/README.md` — read it to see what a finished
README of this class looks like, and note that its badge and its headline number both trace to a
committed results file. This repository has no such file yet, which is exactly why this README
carries no number. Do not copy its wording.

Research grounding (read the named line ranges, not the whole files):
`.planning/research/ARCHITECTURE.md:88-126` — the recommended directory tree this README documents.
`.planning/research/STACK.md:250-266` — repo hygiene and why the README is built on before/after pairs later, not now.
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Every symbol below is NEW.

**File created in this plan:** `README.md`.

**Named sections created in `README.md`:** `## What this is`, `## Status`,
`## Repository layout`, `## Rule numbering`, `## Versioning`, `## License and notices`.

**Paths documented but deliberately not created (D-15):** `skills/proof-first/SKILL.md`,
`skills/proof-first/references/checklist.md`,
`skills/proof-first/references/completeness-audit.md`,
`skills/proof-first/references/artifact-patterns.md`,
`skills/proof-first/references/deletion-test.md`, `output-styles/proof-first.md`,
`prompts/system-prompt.md`, `examples/before-after.md`, `evals/`, `.claude-plugin/`.

**Files that exist at the end of Phase 1 and are listed as such in `README.md`:** `LICENSE`,
`NOTICES.md`, `SOURCES.md`, `NUMBERING.md`, `README.md`, `examples/deal-brief.md`,
`tools/check_repo.py`, `.github/workflows/ci.yml`.

**Constraint carried by this plan:** `README.md` is scanned by the `undefined-id` check in
`tools/check_repo.py`, so it must not contain any specific numbered rule ID.
</artifacts_this_phase_produces>

<planner_assumptions>
## Flagged planner assumptions

- **A-06 (one-way rating without a preceding checkpoint, by design).** Task 1 carries a one-way
  reversibility rating for the `skills/proof-first/` path and no
  `checkpoint:decision` precedes it. This is deliberate. CONTEXT.md D-16 records the developer
  choosing that exact path, including the reason — the folder name must equal the frontmatter
  `name` the Agent Skills specification requires — so the door is already walked, and
  `gsd-core/references/planner-reversibility.md` names "gating a decision already made" as an
  anti-pattern. The rating is kept so the record survives; the checkpoint is omitted so the run
  does not re-ask a settled question. A structural-validator warning on this task is expected and
  is answered here.
- **A-07 (this plan owns no edge item).** All 14 spec-less probe edge items are owned by plans
  01-01, 01-02, and 01-03 per the ledger in `01-01-PLAN.md`. This plan introduces no new
  requirement, so it mints no new edge predicate. Nothing was dropped; the union across the four
  plans is 14 of 14.
- **A-08 (the phase-closing legal read cannot be automated).** LEG-03's core question — whether a
  restatement is paraphrase or reproduction — is the semantic judgement `SOURCES.md` itself
  declares no tool in this project's stack performs. Task 2 expresses it as a reviewer check with a
  concrete instruction rather than as an automated command that would only appear to check it.
</planner_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: A claim-free README that documents the target layout</name>
  <reversibility rating="one-way">The skill folder path `skills/proof-first/` documented here is baked into every published install command, the plugin manifest, and the marketplace entry from Phase 4 onward; CONTEXT.md D-16 records the developer choosing it, so the rating is recorded for the file and no checkpoint re-asks a settled decision.</reversibility>
  <files>README.md</files>
  <read_first>
    - `NOTICES.md` as completed in plan 01-03 — read the attribution-pointer fenced block and copy the string from there character for character; the `pointer-missing` and `pointer-duplicated` checks compare it exactly after stripping surrounding whitespace.
    - `NUMBERING.md` as written in plan 01-01 — read the namespaces section and the versioning section so this README summarises them rather than restating them differently.
    - `.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-CONTEXT.md` decisions D-15, D-16, D-17, and the `<specifics>` line stating that README says what the repository is and states that no measured claims exist yet.
    - `tools/check_repo.py` module docstring and the `undefined-id` check — read the list of scanned paths so this README does not cite a rule ID the check will reject.
    - `~/devoteam/.claude/plugins/marketplaces/simple-english/README.md` — read for shape only, to see the finished form this file grows into once a benchmark exists.
  </read_first>
  <action>
Create `README.md` at the repository root with a title, a one-line strapline, and six level-two
sections in this order.

`## What this is` describes the project in three or four sentences: a public, MIT-licensed agent
skill for technical presales writing — RFP and RFI responses, solution proposals, executive
summaries, and demo and discovery material — that replaces adjectives with specificity and
evidence. Name the audience: presales engineers, solution architects, and bid teams. Say it is
vendor-neutral and has no dependencies.

`## Status` is the honest-state section and is the reason this file exists in Phase 1. State that
the repository is in early construction; list what exists today as a bullet list naming
`NUMBERING.md`, `examples/deal-brief.md`, `LICENSE`, `NOTICES.md`, `SOURCES.md`,
`tools/check_repo.py`, and the CI workflow; then list what does not exist yet as a second bullet
list naming the skill itself, the reference files, the distribution manifests, the output style,
the paste-able system prompt, the worked before-and-after examples, and the evaluation harness.
Close the section with one sentence stating plainly that no measured claim is published in this
repository yet, that the benchmark has not run, and that any number this README ever carries will
be sourced from committed benchmark results and will state the model versions and the date it was
produced. Do not soften that sentence into a promise about how good the skill will be.

`## Repository layout` documents the target tree in a fenced block, marking clearly which entries
exist today and which are planned — documented, never pre-created, per D-15. The skill lives at
`skills/proof-first/`, and state in one line why: the folder name equals the frontmatter `name`
field the Agent Skills specification requires, and a single-folder upload is that one directory
zipped with the folder as its root. State in a second line that `NOTICES.md`, `SOURCES.md`,
`NUMBERING.md`, `examples/`, `tools/`, and `evals/` stay at the repository root and never ship to
an installed user.

`## Rule numbering` is two or three sentences pointing at `NUMBERING.md` as the authoritative
registry. Name the two prefixes — the prose-rule prefix and the completeness-audit prefix — and say
that ranges are reserved per section before drafting so a rule added later never disturbs an
existing citation. Do not write any specific numbered rule identifier anywhere in this file: the
`undefined-id` check in `tools/check_repo.py` scans `README.md`, no identifier is allocated yet,
and a worked example of the format would fail the build.

`## Versioning` is two sentences pointing at the versioning section of `NUMBERING.md` for what a
patch, a minor, and a major bump each mean, and stating that a release is matched by a git tag.

`## License and notices` states that everything original here is MIT-licensed and links `LICENSE`,
`NOTICES.md`, and `SOURCES.md` with one line each on what the reader will find in them. End this
section with the attribution pointer string, copied verbatim from the fenced block in `NOTICES.md`,
written exactly once in the whole file.

Two things must not appear anywhere in this file. No image, badge, or shield of any kind — every
badge in a repository of this class carries a number, and no number here has been measured. No
percentage, ratio, score, or count presented as a result. Ordinary structural counts that describe
the repository, such as naming how many root files exist, are not results and are permitted, but
prefer prose over figures throughout so the boundary stays obvious to a reader.
  </action>
  <acceptance_criteria>
    - `README.md` exists at the repository root and contains all six literal headings: `## What this is`, `## Status`, `## Repository layout`, `## Rule numbering`, `## Versioning`, `## License and notices`.
    - `grep -cF 'Concepts here are paraphrased from publicly described sales frameworks.' README.md` returns exactly 1.
    - `README.md` contains the literal string `skills/proof-first/`.
    - `README.md` links all four root documents by filename: it contains the literal strings `LICENSE`, `NOTICES.md`, `SOURCES.md`, and `NUMBERING.md`.
    - `README.md` contains no Markdown image syntax: `grep -F '![' README.md` finds no match.
    - `README.md` contains no percentage token: `grep -E '[0-9]+(\.[0-9]+)?%' README.md` finds no match.
    - `README.md` contains no rule identifier: `grep -E 'PF-[0-9]+\.[0-9]+|MC-[0-9]+' README.md` finds no match.
    - `README.md` contains a sentence stating that no measured claim is published yet.
    - `python3 tools/check_repo.py` exits 0, proving README is accepted as an attribution-pointer carrier and cites no undefined identifier.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any line beginning with `pointer-missing`, `pointer-duplicated`, or `undefined-id` appears in stdout</fails_when>
    <automated>grep -cF 'Concepts here are paraphrased from publicly described sales frameworks.' README.md</automated>
    <fails_when>exit 1 because the pointer string is absent, or a printed count other than 1</fails_when>
    <automated>grep -nF '![' README.md; test $? -eq 1</automated>
    <fails_when>non-zero exit from `test`, which occurs when grep matched — the matching lines are printed above and each is a badge or image that carries an unmeasured claim</fails_when>
    <automated>grep -nE '[0-9]+(\.[0-9]+)?%' README.md; test $? -eq 1</automated>
    <fails_when>non-zero exit from `test`, which occurs when grep matched — the printed lines carry a percentage that no committed benchmark produced</fails_when>
    <automated>grep -nE 'PF-[0-9]+\.[0-9]+|MC-[0-9]+' README.md; test $? -eq 1</automated>
    <fails_when>non-zero exit from `test`, which occurs when grep matched — the printed lines cite a rule identifier that is not in the Allocated IDs table</fails_when>
    <automated>python3 -c "import sys;t=open('README.md').read();need=['## What this is','## Status','## Repository layout','## Rule numbering','## Versioning','## License and notices','skills/proof-first/','NUMBERING.md','NOTICES.md','SOURCES.md','LICENSE'];miss=[s for s in need if s not in t];print('missing:',miss);sys.exit(1 if miss else 0)"</automated>
    <fails_when>non-zero exit, or the printed `missing:` list is not empty</fails_when>
  </verify>
  <done>A reader arriving at the repository learns what it is, what is built, what is not built, and that nothing here has been measured yet — and the attribution-pointer mechanism is now enforced against a real shipped file rather than only against the checker's fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Full-repository integrity sweep and the phase-closing legal read</name>
  <files>README.md</files>
  <read_first>
    - Every file this phase produced: `NUMBERING.md`, `NOTICES.md`, `SOURCES.md`, `LICENSE`, `README.md`, `examples/deal-brief.md`, `tools/check_repo.py`, `.github/workflows/ci.yml`. The sweep is a read of the phase as a whole, so read the phase as a whole.
    - `.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-01-SUMMARY.md`, `01-02-SUMMARY.md`, and `01-03-SUMMARY.md` — the reviewer checks already performed in earlier plans, so this task does not repeat them and does record whether each was actually done.
    - `.planning/ROADMAP.md` Phase 1 success criteria — the four statements this sweep confirms are true.
  </read_first>
  <action>
Run the sweep and fix what it finds. This task changes `README.md` only if the sweep surfaces a
defect in it; every other finding is recorded and, where it is a defect in a file another plan
owns, fixed in place with the fix recorded in this plan's SUMMARY.

Confirm the phase's file set is complete: `LICENSE`, `NOTICES.md`, `SOURCES.md`, `NUMBERING.md`,
`README.md`, `examples/deal-brief.md`, `tools/check_repo.py`, and `.github/workflows/ci.yml` all
exist, and no empty placeholder file was created for any path the README documents as planned but
not yet built.

Confirm the checker is green in both modes against the completed phase, and that its self-test
still covers all nine violation codes.

Confirm the heading and filename boundary: no framework mark appears in any filename, in any
directory name, or in any Markdown heading anywhere in the repository outside `NOTICES.md`, which
is the single carve-out where naming the mark is the factual attribution itself.

Confirm the four ROADMAP Phase 1 success criteria are true by pointing each at the artifact that
makes it true, and record that mapping in the SUMMARY: the next-free-ID lookup answered by
`NUMBERING.md`; every example fact citable from `examples/deal-brief.md`; the three individually
named framework statements plus the MIT grant; and the paraphrase boundary written and its owner
named.

Perform the phase-closing legal read: read every file this phase produced and confirm that nothing
in the repository reproduces framework text — every framework concept appearing anywhere is stated
in this repository's own words and traces to a source listed in `SOURCES.md`. This is the semantic
judgement `SOURCES.md` states no tool performs. Record the read and its date in the SUMMARY, and
record that Phase 6's LEG-04 gate repeats it against current sources before public launch. If the
read finds anything that reads as a reproduction rather than a restatement, rewrite it in this task
and record what was changed.
  </action>
  <acceptance_criteria>
    - All eight Phase 1 files exist: `test -f LICENSE && test -f NOTICES.md && test -f SOURCES.md && test -f NUMBERING.md && test -f README.md && test -f examples/deal-brief.md && test -f tools/check_repo.py && test -f .github/workflows/ci.yml` exits 0.
    - No zero-byte file exists outside `.git`: `find . -type f -empty -not -path './.git/*' | wc -l` prints 0.
    - `python3 tools/check_repo.py --self-test` exits 0 with no `FAIL` in the output, and its pass summary names all nine violation codes.
    - `python3 tools/check_repo.py` exits 0 with no violation line printed.
    - No Markdown heading outside `NOTICES.md` and outside `.planning/` matches `Command of the Message`, `MEDDIC`, or `Challenger`.
    - No file or directory path outside `.git`, `.planning`, and `.claude` matches `meddic`, `meddpicc`, `challenger`, or `command-of-the-message` case-insensitively.
    - The SUMMARY records the phase-closing legal read, its date, and the four success-criteria-to-artifact mappings.
  </acceptance_criteria>
  <verify>
    <automated>test -f LICENSE && test -f NOTICES.md && test -f SOURCES.md && test -f NUMBERING.md && test -f README.md && test -f examples/deal-brief.md && test -f tools/check_repo.py && test -f .github/workflows/ci.yml && echo ALL_PRESENT</automated>
    <fails_when>non-zero exit, or `ALL_PRESENT` is absent from the output — one of the eight Phase 1 files is missing</fails_when>
    <automated>find . -type f -empty -not -path './.git/*' | wc -l</automated>
    <fails_when>a printed count above 0 — an empty placeholder file was created, which D-15 forbids; the paths are visible by rerunning the find without the pipe</fails_when>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, or `FAIL` appears in the output, or fewer than nine distinct violation codes are named in the pass summary</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any violation line is printed</fails_when>
    <automated>grep -rEn '^#{1,6} ' --include='*.md' --exclude-dir=.git --exclude-dir=.planning --exclude-dir=.claude . | grep -v '^\./NOTICES\.md:' | grep -E 'Command of the Message|MEDDIC|Challenger'; test $? -eq 1</automated>
    <fails_when>non-zero exit from `test`, which occurs when the final grep matched — the printed lines are headings outside NOTICES.md carrying a framework mark, which breaks the D-11 boundary</fails_when>
    <automated>find . -not -path './.git/*' -not -path './.planning/*' -not -path './.claude/*' | grep -iE 'meddic|meddpicc|challenger|command-of-the-message'; test $? -eq 1</automated>
    <fails_when>non-zero exit from `test`, which occurs when grep matched — the printed paths carry a framework mark in a filename or directory name</fails_when>
    <manual>A reviewer reads all eight Phase 1 files end to end and confirms that every framework concept appearing anywhere is stated in this repository's own words rather than reproduced, and that each traces to a source listed in `SOURCES.md`. This is the LEG-03 judgement `SOURCES.md` itself states no tool in this project's stack performs. Record the read, the date, and any rewrite made as a result in the plan SUMMARY. Phase 6's LEG-04 gate repeats this against current sources before public launch.</manual>
    <manual>A reviewer opens `README.md` and confirms that no sentence makes a claim about how effective the skill is, how much it improves anything, or how it compares to any alternative. The only permitted forward-looking statement is that a benchmark has not run and that any future number will be sourced from committed results with model versions and a date. This closes the LEG-05 posture early and is a judgement no grep expresses.</manual>
  </verify>
  <done>The phase closes with one command proving the scaffolding is internally consistent, a recorded human read proving nothing reproduces framework text, and a recorded mapping from each ROADMAP success criterion to the artifact that satisfies it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan writes one Markdown file and reads the rest of the repository. There is no runtime, no
network surface, no authentication, no data store, and no user input, so ASVS L1 controls for
session management, access control, input validation, output encoding, and cryptography have no
counterpart here and are not padded into the register. The boundaries that genuinely exist are
publication boundaries: this is the file most readers will see first, and it is the last gate
before the phase is called done.

| Boundary | Description |
|----------|-------------|
| Repository -> first-time reader | The README sets what a reader believes the project has already proven. A claim here that no measurement supports is the exact hypocrisy the project exists to prevent. |
| Framework source material -> published repository | The phase-closing read is the last point in Phase 1 at which a too-close paraphrase can be caught before Phase 2 builds on top of it. |
| Filenames and headings -> forks, scrapers, and search indexes | A mark in a path or a heading propagates through forks and indexes in a way body prose does not. |
| Attribution pointer -> its first real carrier | Until README exists, the pointer mechanism is only proven against fixtures. |

## STRIDE Threat Register (ASVS L1, blocking on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering / Information Disclosure | Framework-derived wording anywhere in the phase's eight files | high | mitigate | Task 2's phase-closing legal read covers every file the phase produced, with any finding rewritten in the same task and recorded in the SUMMARY; Phase 6's LEG-04 gate repeats it against current sources before public launch. |
| T-01-11 | Spoofing | Unmeasured claims or badges in `README.md` | high | mitigate | Task 1 forbids images, badges, percentages, and result-shaped figures; three negative checks enforce it mechanically and a reviewer check covers the claims a grep cannot express. |
| T-01-05 | Tampering | The attribution pointer across carrier files | medium | mitigate | `README.md` becomes the first real carrier, and `pointer-missing` and `pointer-duplicated` now enforce against shipped content rather than fixtures alone. CI runs the check on every push. |
| T-01-12 | Information Disclosure | Framework marks in filenames, directory names, and headings | medium | mitigate | Task 2 sweeps every path and every Markdown heading outside `NOTICES.md`, with `NOTICES.md` as the single documented carve-out per D-11. |
| T-01-13 | Repudiation | An empty placeholder file for a documented-but-unbuilt path | low | mitigate | Task 2 fails on any zero-byte file outside `.git`, enforcing D-15's documented-not-pre-created rule. |

**Supply chain:** no package-manager install tasks exist in this plan or anywhere in this phase.
Nothing is installed, so the package-legitimacy gate has no applicable subject and no supply-chain
row is minted. The first phase that adds an install step carries the gate.
</threat_model>

<verification>
Run from the repository root after both tasks complete:

- All eight Phase 1 files exist and none is zero-byte.
- `python3 tools/check_repo.py --self-test` exits 0 with no `FAIL`, naming all nine violation codes.
- `python3 tools/check_repo.py` exits 0 with no violation line.
- `README.md` carries the attribution pointer exactly once, no image syntax, no percentage token, and no rule identifier.
- No Markdown heading outside `NOTICES.md` and no path outside the tooling directories carries a framework mark.
- Both `<manual>` reviewer checks in Task 2 are recorded as performed in the SUMMARY.
</verification>

<success_criteria>
- The repository has an entry point that tells the truth about what exists and states that nothing has been measured.
- The target layout is documented and none of it is pre-created.
- The attribution-pointer mechanism is proven against a real file.
- The four ROADMAP Phase 1 success criteria each map to a named artifact, recorded in the SUMMARY.
- One command proves the whole phase is internally consistent, and it runs in CI.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-04-SUMMARY.md` when done.
Record in it: the four success-criteria-to-artifact mappings, the date of the phase-closing legal
read and anything it changed, the outcome of both reviewer checks, and the final list of paths
`README.md` documents as planned but not yet built, so Phase 4 inherits the exact set.
</output>
