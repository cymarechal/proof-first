---
phase: 01-foundations-legal-scaffolding-numbering-shared-deal
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/check_repo.py
autonomous: true
gap_closure: true
requirements: [LEG-03]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`python3 tools/check_repo.py` exits non-zero and prints a line beginning `pointer-missing README.md` when the attribution-pointer line is removed from a scratch copy of README.md — the defect the check exists to catch produces a signal against production content, not only against a self-test fixture (D-14)."
    - "`python3 tools/check_repo.py` exits 0 when none of the carrier files named in NOTICES.md exist on disk, exits non-zero with `pointer-missing <carrier>` when a named carrier exists and contains zero occurrences of the pointer line, and exits non-zero with `pointer-duplicated <carrier>` when it contains two or more. (edge: LEG-03/empty, explicit)"
    - "`python3 tools/check_repo.py` exits non-zero with a `pointer-unparseable NOTICES.md` line when NOTICES.md exists but its `## Attribution pointer` section yields no usable pointer definition — either no fenced block, an empty fenced block, or a carrier entry that is not a repository-relative path. An unparseable pointer definition is itself a violation, never silent success (D-14, D-05)."
    - "The pointer comparison is Python str equality over UTF-8-decoded, strip()-ed lines — code-point equality with no Unicode normalisation applied — so a carrier line differing only by U+00A0 where the canonical string has U+0020 is reported as `pointer-missing`, and the module docstring states this. (edge: LEG-03/encoding, explicit)"
    - "`python3 tools/check_repo.py --self-test` exits 0, names `pointer-unparseable` among its verified codes, and its inline NOTICES.md fixtures carry an explanatory prose paragraph between the `## Attribution pointer` heading and the fenced block, mirroring the shape production NOTICES.md actually has (D-05)."
    - "`python3 tools/check_repo.py` exits 0 against the repository exactly as shipped — the fix closes a false negative and introduces no false positive against content the code review and the verifier already confirmed correct."
    - statement: "LICENSE exists at repo root, is non-empty, and is the unmodified MIT text; no plan in this gap-closure run adds a mechanical check for it, so its absence or emptiness produces no signal from tools/check_repo.py. (edge: LEG-01/empty)"
      verification: backstop
    - statement: "NOTICES.md `## Framework statements` carries exactly three `###` subsections, one per framework; no tool in this phase detects a missing subsection. (edge: LEG-02/empty)"
      verification: backstop
    - statement: "Marks in the MEDDIC family (MEDDIC, MEDDICC, MEDDPICC) merge into one `###` subsection rather than separating into three, per D-13, and the merge is stated in that subsection's Mark line; no tool in this phase enforces the merge. (edge: LEG-02/adjacency)"
      verification: backstop
    - statement: "The three `###` subsections under `## Framework statements` appear in the fixed order Command of the Message, MEDDIC family, Challenger, as NOTICES.md's own prose asserts; that ordering claim is currently unenforced by any tool. (edge: LEG-02/ordering)"
      verification: backstop
  artifacts:
    - tools/check_repo.py
  key_links:
    - "NOTICES.md `## Attribution pointer` section -> `parse_notices()` via `split_sections()`: the shared section splitter becomes the single extraction path, so prose between the heading and the fenced block never breaks extraction (01-PATTERNS.md pattern 1)."
    - "`parse_notices()` -> `run_notices_checks()` -> `main()` exit code: a pointer that cannot be extracted becomes a violation tuple that reaches stdout and exit 1, instead of an early empty return (01-PATTERNS.md pattern 2)."
    - "`NOTICES_CHECK_CODES` -> `ALL_CHECK_CODES` -> the `self_test()` coverage loop: a new code is covered automatically only once it is appended to its family list, so that list is the registration seam (01-PATTERNS.md pattern 3)."
    - "README.md, the one shipped carrier -> `check_pointer()`: README's content is now actually inspected, which is what turns `pointer-missing` from fixture-only into enforcing against shipped content (01-04 key link, previously falsified)."
  prohibitions:
    - statement: "MUST NOT claim coverage the tool does not have — neither by reporting a violation code as self-test-covered when that code cannot fire against the repository's real document shapes, nor by leaving a known checker blind spot out of the module docstring's declared ceiling."
      status: resolved
      verification: judgment
    - statement: "MUST NOT reach a green run by weakening what is enforced: no loosening the attribution-pointer comparison from exact stripped-line equality to substring or fuzzy matching, no removing a path from NOTICES.md's required-carriers list, and no editing the frozen pointer string or the frozen PF/MC reserved ranges to fit the parser (D-14, D-01, D-02)."
      status: resolved
      verification: judgment
    - statement: "MUST NOT write to, delete from, or otherwise mutate any file in the developer's working tree or the real repository when running any self-test or mutation mode — every mutation is applied only to a throwaway copy under a temporary directory."
      status: resolved
      verification: judgment
---

<objective>
Make the attribution-pointer check actually enforce LEG-03's no-drift guarantee against the
repository's real documents, instead of only against a fixture whose shape production NOTICES.md
was never going to have.

Purpose: this is the BLOCKER from 01-VERIFICATION.md and CR-01 from 01-REVIEW.md. `parse_notices()`
anchors its fenced-block regex directly to the `## Attribution pointer` heading, but both 01-01 and
01-03 required (and NOTICES.md as shipped carries) an explanatory prose paragraph between the two.
The regex never matches, `pointer` resolves to None, and `check_pointer()` returns an empty list
unconditionally for a None pointer. Deleting the entire pointer line from README.md today produces
`check_repo: 0 violations`, exit 0. D-14's promise — one canonical string, N pointers, drift is a
build failure — is currently unenforceable, and it becomes load-bearing the moment Phase 2 and
Phase 4 add `skills/proof-first/SKILL.md` and its two reference files as carriers.

Output: a `tools/check_repo.py` whose pointer path is parsed with the house `split_sections()`
idiom, fails loud on an unusable pointer definition via a new `pointer-unparseable` code, refuses
carrier paths that leave the repository, states its string-equality semantics, and whose self-test
fixtures mirror the production document shape.
</objective>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md

@.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-PATTERNS.md
@.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-VERIFICATION.md
@.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-REVIEW.md
@.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-CONTEXT.md

@tools/check_repo.py
@NOTICES.md
@README.md
</context>

## Nyquist validation

`workflow.nyquist_validation` is `true`, but no `01-VALIDATION.md` exists for this phase and none
can be produced for this run: VALIDATION.md is derived from RESEARCH.md's `## Validation
Architecture` section, and `--gaps` skips research by design. The plans in this gap-closure run
therefore carry no Nyquist Dimension-8 validation requirements. This is a stated absence, not an
oversight.

## Source Coverage Audit

Covers the whole gap-closure plan set (01-05, 01-06, 01-07). Sources audited: the ROADMAP phase
goal, REQUIREMENTS.md phase IDs, 01-VERIFICATION.md and 01-REVIEW.md findings (the RESEARCH-tier
input for a `--gaps` run, since no RESEARCH.md exists), and 01-CONTEXT.md decisions.

| Source | Item | Status | Where |
|---|---|---|---|
| GOAL | Scaffolding frozen so nothing downstream forces a renumbering or a retrofit | COVERED | 01-05 (attribution), 01-06 (numbering + figures), 01-07 (regression harness) |
| REQ | CAT-07 | COVERED | 01-06, 01-07 |
| REQ | EX-01 | COVERED | 01-06, 01-07 |
| REQ | LEG-01 | NOT CLAIMED (no gap) | Verified SATISFIED by 01-03/01-VERIFICATION; no plan here touches LICENSE. Padding a `requirements` field for it would be manufactured work. Its three probe rows are carried in this plan's `must_haves`. |
| REQ | LEG-02 | NOT CLAIMED (no gap) | Verified SATISFIED by 01-03/01-VERIFICATION; no plan here touches the framework statements. Its three probe rows are carried in this plan's `must_haves`. |
| REQ | LEG-03 | COVERED | 01-05, 01-07 |
| VERIFICATION | BLOCKER: pointer parser cannot match production NOTICES.md shape | COVERED | 01-05 Task 1 |
| VERIFICATION | Missing item: fail-loud on an unparseable pointer definition | COVERED | 01-05 Task 1 |
| VERIFICATION | Missing item: self-test fixture mirroring the real document shape | COVERED | 01-05 Task 1 and Task 2 |
| VERIFICATION | WARNING: `in_canonical` never resets when `## Canonical figures` is the last heading | COVERED | 01-06 Task 1 |
| REVIEW | WR-01: MC IDs validated against one aggregate range, not per dimension block | COVERED | 01-06 Task 2 |
| REVIEW | WR-02: `unlisted-figure` value-collision blind spot, undisclosed | COVERED | 01-06 Task 3 (disclosure + pinned behaviour; the stronger key-binding fix is deferred, rationale in 01-06) |
| CONTEXT | D-05 stdlib-only checker with fixture-based self-tests, tolerant of absent files | COVERED | 01-05 Task 1, 01-06 all tasks, 01-07 Task 1 |
| CONTEXT | D-10 Canonical figures table is the machine-readable interface | COVERED | 01-06 Task 1 and Task 3 |
| CONTEXT | D-14 one canonical attribution string, N pointers, drift is a build failure | COVERED | 01-05 Task 1 and Task 2 |
| CONTEXT | D-01 / D-02 PF sections and MC dimension blocks are independently enforced | COVERED | 01-06 Task 2 |
| CONTEXT | D-15 no empty placeholder files | COVERED | no new files are created by this plan set; only `tools/check_repo.py` and `.github/workflows/ci.yml` are modified |

No unplanned items. Exclusions: 01-CONTEXT.md `## Deferred Ideas` (five items, all scoped to later
phases); LEG-04 and LEG-05 (Phase 6); the two `.planning/WINDOWS.md` open items (name-collision web
search, independent read-through) which need live network access and are already routed to Phase 6.

## Flagged assumptions (probe rows carried forward, not dropped)

Two of the 14 spec-less edge-probe rows have no defensible predicate and are recorded here as
explicit assumptions rather than silently dropped or padded with a hollow criterion:

- **LEG-01/adjacency** — unresolved. The probe asks what happens when two things are exactly equal
  or just touch. LEG-01 is satisfied by exactly one unmodified MIT `LICENSE` file at repo root;
  there is no second element for it to touch or merge with, so the question has no meaning here.
- **LEG-01/ordering** — unresolved. Same reason: a single file has no output ordering semantics.

The remaining twelve rows are authored into `must_haves`: eight as explicit truths (six in 01-06 and
01-07, two here) and four as `verification: backstop` markers in this plan. Observation recorded
while dispositioning LEG-02/ordering, without acting on it: NOTICES.md's own prose asserts that its
three framework subsections appear in a fixed order, and nothing enforces that claim — the same
asserted-but-unenforced shape as the BLOCKER. It is not a verifier-found gap and LEG-02 is
SATISFIED, so no work is manufactured for it in this run.

## Artifacts this plan produces

Symbols created, renamed, or given new behaviour in `tools/check_repo.py`:

| Symbol | Kind | Change |
|---|---|---|
| `parse_notices(path)` | function | rewritten body; return shape `(pointer, carriers)` unchanged |
| `POINTER_SECTION` | module constant | new — the `Attribution pointer` heading text used as the `split_sections()` key |
| `CARRIERS_MARKER` | module constant | new — the `Files required to carry it` sub-heading text |
| `_carrier_is_repo_relative(carrier)` | function | new — rejects absolute paths, drive-letter paths, and any path with a `..` segment |
| `run_notices_checks(repo_root)` | function | modified — emits `pointer-unparseable` instead of returning silently |
| `pointer-unparseable` | violation code | new — appended to `NOTICES_CHECK_CODES`, documented in the module docstring |
| `_bad_notices()` | fixture | rewritten to production shape (heading, prose paragraph, fence) and given the lookalike carrier |
| `_good_notices()` | fixture | rewritten to production shape |
| `_unparseable_notices()` | fixture | new — a NOTICES.md whose Attribution pointer section has no fenced block |
| `_escaping_notices()` | fixture | new — a NOTICES.md whose carriers list names a path outside the repository |
| `self_test()` | function | modified registration block only; the coverage-assertion loop is unchanged |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end attribution-pointer enforcement against production content — one path, wired</name>
  <reversibility rating="costly">The new `pointer-unparseable` code enters the checker's output vocabulary that the module docstring, `NOTICES_CHECK_CODES`, the self-test coverage loop and CI logs all reference; renaming it later means changing all four in one coordinated edit.</reversibility>
  <files>tools/check_repo.py</files>
  <read_first>
    - `tools/check_repo.py` — the whole file. Current `parse_notices()` (~315-331), `check_pointer()` (~334-347), `run_notices_checks()` (~353-358), and the `split_sections()` (58-75) / `strip_fences()` (54-55) / `FENCE_RE` (51) helpers this task must reuse rather than replace.
    - `NOTICES.md` — the production document shape this parser must handle: `## Attribution pointer` heading, then a three-line explanatory prose paragraph, then the fenced block, then the `### Files required to carry it` bullet list, then trailing prose, then `## Framework statements`.
    - `README.md` — the single shipped carrier; the pointer line is the sole occurrence of the canonical string.
    - `.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-PATTERNS.md` — patterns 1 (the `split_sections()` parser idiom), 2 (the fail-loud `check_undefined_id()` counter-example and why a file-level `path.exists()` short-circuit is legitimate while a parse-failure short-circuit is not), 3 (the `(subject, "<code> <message>")` tuple convention and why the code prefix is load-bearing for `main()`'s sort), and 4 (the fixture-registration convention).
    - `.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-VERIFICATION.md` — the `gaps:` frontmatter block, specifically its three `missing` items.
  </read_first>
  <behavior>
    - Given production-shaped NOTICES.md text (heading, prose paragraph, fenced block), `parse_notices()` returns the fenced block's stripped content as the pointer.
    - Given a NOTICES.md whose `## Attribution pointer` section contains no fenced block, `parse_notices()` returns a None pointer and `run_notices_checks()` returns one `pointer-unparseable` violation.
    - Given a NOTICES.md whose Attribution pointer section is absent entirely, the same `pointer-unparseable` violation is returned.
    - Given a carrier file that exists and contains zero lines equal to the pointer, one `pointer-missing` violation naming that carrier is returned.
    - Given a carrier path that does not exist on disk, no violation is returned (D-15: absence is not failure, only contradiction is).
    - Given the repository exactly as shipped, zero violations are returned.
  </behavior>
  <action>
Rewrite the pointer half of `parse_notices()` to use the house parser idiom instead of a regex anchored to the heading line, then make an unusable pointer definition fail loud.

Parser. Add two module-level constants next to the existing `PF_ID_RE`/`MC_ID_RE`/`FENCE_RE` block: `POINTER_SECTION` holding the section heading text `Attribution pointer`, and `CARRIERS_MARKER` holding the sub-heading text `Files required to carry it`. In `parse_notices()`, call `split_sections(text)` first and take the body via `sections.get(POINTER_SECTION, '')`, exactly as `parse_deal_brief()` takes `sections.get('Canonical figures', '')`. Search that body — not the whole document — for the first fenced block, reusing the same fence shape `FENCE_RE` already encodes, and take the fence's inner content stripped. A body with no fence, or a fence whose stripped inner content is empty, yields a None pointer. Do not keep a whole-document regex for this: a lazy quantifier scanning an entire attacker-influenceable Markdown file is both the bug being fixed and threat T-01-03 in this plan's threat model.

Carriers. Scope carrier extraction to the same section body rather than `text.find(...)` over the whole document: locate the `CARRIERS_MARKER` line inside the body and read bullet entries from the lines after it, keeping the existing bullet-matching shape (a leading `-` or `*`, an optional pair of backticks around the path). Stop at the first line that starts with `#`. Scoping carriers to the Attribution pointer section means a bullet list added anywhere else in NOTICES.md can never be mistaken for a carrier.

Fail loud. In `run_notices_checks()`, keep the existing `notices_path.exists()` gate exactly as it is — a file-level absence short-circuit is correct house style and D-15 requires it. Below that gate, when `parse_notices()` yields a None pointer, return a single violation tuple whose subject is `NOTICES.md` and whose message begins with the literal code token `pointer-unparseable` followed by a space, matching the `(subject, "<code> <message>")` convention every other check uses; the message must name NOTICES.md and say the Attribution pointer section yields no usable pointer definition. Only when a pointer was extracted does `check_pointer()` run. `check_pointer()` itself keeps its per-carrier `p.exists()` skip.

Registration. Append `pointer-unparseable` to `NOTICES_CHECK_CODES` so `ALL_CHECK_CODES` and the self-test coverage loop pick it up without any change to that loop. Add a matching entry to the module docstring's violation-code list, in the same one-line style as the existing nine entries, describing the no-usable-pointer-definition trigger.

Fixtures, so the self-test stays green in the same commit. Rewrite `_bad_notices()` and `_good_notices()` to the production shape: heading, then an explanatory prose paragraph on its own lines, then the fenced block. Keep their existing carrier lists and their existing pointer text so the rest of the self-test is unchanged. Add a new zero-arg fixture `_unparseable_notices()` returning a NOTICES.md whose Attribution pointer section has the heading and the prose paragraph but no fenced block at all, and register it in `self_test()` under a third scratch root so `pointer-unparseable` fires there and stays silent on both existing roots. Follow the `_bad_deal_brief()`/`_good_deal_brief()` fixture convention: zero-arg function returning a triple-quoted string, written into the scratch tree by `_write()`.

Note on the third root: the existing coverage loop compares one bad-code set against one good-code set. Extend the bad-code set by unioning in the codes produced by running the checks against the new unparseable root, so the loop body itself needs no edit. The good root must stay free of `pointer-unparseable`.
  </action>
  <acceptance_criteria>
    - `grep -A8 'def parse_notices' tools/check_repo.py | grep -q 'split_sections'` succeeds: the pointer is extracted from a section body, not by a regex anchored to the heading line.
    - `grep -n 'NOTICES_CHECK_CODES = ' tools/check_repo.py | grep -q 'pointer-unparseable'` succeeds.
    - `sed -n '1,45p' tools/check_repo.py | grep -q 'pointer-unparseable'` succeeds: the new code is documented in the module docstring's code list.
    - `grep -c '^def _unparseable_notices' tools/check_repo.py` reports 1.
    - With the pointer line deleted from a scratch copy of README.md, the checker run against that copy exits 1 and prints a line beginning `pointer-missing README.md`.
    - With the `## Attribution pointer` heading line deleted from a scratch copy of NOTICES.md, the checker run against that copy exits 1 and prints a line beginning `pointer-unparseable NOTICES.md`.
    - Against the repository as shipped, the checker prints exactly `check_repo: 0 violations` and exits 0.
    - `python3 tools/check_repo.py --self-test` exits 0 and its summary line names `pointer-unparseable` among the verified codes.
    - `python3 -c "import ast,sys; t=ast.parse(open('tools/check_repo.py').read()); mods={(n.module or '').split('.')[0] for n in ast.walk(t) if isinstance(n,ast.ImportFrom)} | {a.name.split('.')[0] for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names}; sys.exit(0 if mods <= {'argparse','re','sys','tempfile','pathlib','shutil','os','ast'} else 1)"` exits 0: no third-party import was introduced.
  </acceptance_criteria>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or stdout is not the single line `check_repo: 0 violations`</fails_when>
    <automated>d=$(mktemp -d) && cp -R NOTICES.md NUMBERING.md examples tools "$d"/ && grep -v 'Concepts here are paraphrased' README.md > "$d/README.md" && python3 "$d/tools/check_repo.py" | grep -q '^pointer-missing README.md'</automated>
    <fails_when>non-zero exit from the pipeline — the mutated-copy run printed no line beginning `pointer-missing README.md`, which is exactly the silent-success behaviour this task exists to remove</fails_when>
    <automated>d=$(mktemp -d) && cp -R NUMBERING.md README.md examples tools "$d"/ && sed '/^## Attribution pointer$/d' NOTICES.md > "$d/NOTICES.md" && python3 "$d/tools/check_repo.py" | grep -q '^pointer-unparseable NOTICES.md'</automated>
    <fails_when>non-zero exit from the pipeline — the run against a NOTICES.md with no Attribution pointer section printed no `pointer-unparseable NOTICES.md` line, meaning an unusable pointer definition still resolves to silent success</fails_when>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, or the `self-test PASS` summary line omits `pointer-unparseable`</fails_when>
  </verify>
  <done>The attribution-pointer path runs end to end against production documents: NOTICES.md's real shape parses, a missing pointer in the real README.md produces `pointer-missing` and exit 1, an unusable pointer definition produces `pointer-unparseable` and exit 1, the repository as shipped stays clean, and `--self-test` is green with the new code covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden the same path — carrier-path containment, string-equality semantics, and duplication proven against production content</name>
  <files>tools/check_repo.py</files>
  <read_first>
    - `tools/check_repo.py` — as modified by Task 1: `parse_notices()`, `check_pointer()` (the `line.strip() == pointer` comparison and the count branches), `run_notices_checks()`, the module docstring's violation-code list, and the fixture block.
    - `NOTICES.md` — the `### Files required to carry it` bullet list (four repository-relative paths, three of which do not exist yet) and the trailing sentence stating that a listed path that does not exist yet is skipped while a listed path that does exist must carry the string exactly once.
    - `.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-PATTERNS.md` — pattern 3, the violation-tuple convention, and the Shared Constraints section on stdlib-only imports and both CI modes staying green.
  </read_first>
  <behavior>
    - Given a carriers list entry that is an absolute path, a Windows drive-letter path, or contains a `..` segment, `run_notices_checks()` returns a `pointer-unparseable` violation naming that entry and reads no file for it.
    - Given a carrier file containing the pointer line twice, one `pointer-duplicated` violation naming that carrier and the count is returned.
    - Given a carrier file whose line differs from the pointer only by U+00A0 in place of U+0020, one `pointer-missing` violation is returned — no Unicode normalisation is applied.
    - Given a carrier file where the pointer line carries leading or trailing whitespace, no violation is returned — the comparison strips each line first.
  </behavior>
  <action>
Close the two remaining honesty gaps on the pointer path: an unbounded carrier path, and an undocumented equality rule.

Containment. Add a module-level helper `_carrier_is_repo_relative(carrier)` returning False for a path that is absolute, that begins with a drive letter followed by a colon, or that contains a `..` path segment; True otherwise. Apply it in `run_notices_checks()` after the pointer is extracted and before `check_pointer()` runs: any carrier entry failing the test produces a `pointer-unparseable` violation whose subject is that carrier entry and whose message names the entry and says a required-carrier path must be repository-relative. Those entries are not passed to `check_pointer()`, so no file outside the repository is ever opened. This closes threat T-01-01 in this plan's threat model, and it is why the new code's documented meaning covers two triggers rather than one.

Docstring. Extend the `pointer-unparseable` entry to name both triggers (no usable pointer definition in the Attribution pointer section; a required-carrier entry that is not a repository-relative path). Extend the `pointer-missing` entry with a declared-ceiling sentence stating the comparison rule: each line of the carrier is UTF-8 decoded and stripped, then compared to the canonical string with Python string equality — code-point equality, with no Unicode normalisation and no case folding — so a visually identical line built from different code points is reported as missing. Write these in the same one-line-per-clause style as the existing nine entries.

Fixtures. In `_bad_notices()`, add a fifth carrier entry `carrier-lookalike.md`, and in the `self_test()` registration block write that file with a single line that reproduces the canonical fixture pointer with the space before the final word replaced by a non-breaking space (U+00A0), so `pointer-missing` fires for a reason other than plain absence. Add a new zero-arg fixture `_escaping_notices()` returning a production-shaped NOTICES.md whose carriers list names one path with a leading `..` segment, and register it under its own scratch root so the containment branch is exercised. Keep the good root clean of both.

Sorting. Do not change `main()`'s sort key. The new messages must keep `<code> ` as their literal first token so the `(code, subject)` sort in `main()` keeps working; this is why the code prefix convention is load-bearing rather than cosmetic.
  </action>
  <acceptance_criteria>
    - `grep -c '^def _carrier_is_repo_relative' tools/check_repo.py` reports 1.
    - `grep -c '^def _escaping_notices' tools/check_repo.py` reports 1.
    - `sed -n '1,50p' tools/check_repo.py | grep -q 'repository-relative'` succeeds: the containment trigger is documented in the module docstring.
    - `sed -n '1,50p' tools/check_repo.py | grep -qi 'normalisation\|normalization'` succeeds: the docstring states that no Unicode normalisation is applied.
    - With a second copy of the pointer line appended to a scratch copy of README.md, the checker run against that copy exits 1 and prints a line beginning `pointer-duplicated README.md`.
    - With one carriers-list entry in a scratch copy of NOTICES.md rewritten to a path containing a `..` segment, the checker run against that copy exits 1, prints a line beginning `pointer-unparseable`, and no `pointer-missing` line names a path outside the repository.
    - `python3 tools/check_repo.py --self-test` exits 0 and its summary line names all ten codes.
    - Against the repository as shipped, the checker prints exactly `check_repo: 0 violations` and exits 0.
  </acceptance_criteria>
  <verify>
    <automated>d=$(mktemp -d) && cp -R NOTICES.md NUMBERING.md examples tools "$d"/ && { cat README.md; grep 'Concepts here are paraphrased' README.md; } > "$d/README.md" && python3 "$d/tools/check_repo.py" | grep -q '^pointer-duplicated README.md'</automated>
    <fails_when>non-zero exit from the pipeline — the run against a README carrying the pointer twice printed no `pointer-duplicated README.md` line</fails_when>
    <automated>d=$(mktemp -d) && cp -R NUMBERING.md README.md examples tools "$d"/ && sed 's|^- `README.md`$|- `../escaped.md`|' NOTICES.md > "$d/NOTICES.md" && python3 "$d/tools/check_repo.py" | grep -q '^pointer-unparseable'</automated>
    <fails_when>non-zero exit from the pipeline — the run against a NOTICES.md naming a carrier outside the repository printed no `pointer-unparseable` line, meaning the checker followed the escaping path instead of rejecting it</fails_when>
    <automated>python3 tools/check_repo.py --self-test && python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit from either command, or the `self-test PASS` summary line names fewer than ten codes, or the live run's stdout is not `check_repo: 0 violations`</fails_when>
  </verify>
  <done>A required-carrier entry that is not repository-relative is rejected with `pointer-unparseable` and never opened; a carrier holding the pointer twice produces `pointer-duplicated` against production content; the docstring states the code-point-equality comparison rule and both `pointer-unparseable` triggers; all ten codes are covered by `--self-test` and the live repo stays clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository Markdown files -> `tools/check_repo.py` | The only untrusted input crossing into executable code. On a public MIT repo whose CI triggers on `pull_request`, a fork can supply arbitrary NOTICES.md, README.md, and `examples/**/*.md` content that this script parses inside a CI runner. |
| `tools/check_repo.py` -> CI job exit status | The script's exit code is the whole enforcement surface; a false zero is the failure this plan exists to remove. |

There is no network boundary, no authentication, no persistence, no user account, and no
deserialization of untrusted formats in this phase. An ASVS L1 model for a stdlib-only Markdown
linter is short by construction, and the rows below are the whole applicable set rather than an
inflated surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | `check_pointer()` carrier path join `repo_root / carrier` | medium | mitigate | Task 2 adds `_carrier_is_repo_relative()`; an absolute, drive-letter, or `..`-bearing carrier entry is rejected with `pointer-unparseable` and never opened. `pathlib`'s `/` operator silently discards the base for an absolute right-hand side, so without this guard a carriers-list entry of `/etc/passwd` makes the CI runner read that file. |
| T-01-02 | Denial of Service | `Path.read_text()` on every `examples/**/*.md` and every carrier | low | accept | Files are repository-committed Markdown read once each into an ephemeral CI runner; a size cap would add a failure mode with no attacker benefit, since a fork can already make the runner do more work by committing more files. Rationale recorded rather than a cap imposed. |
| T-01-03 | Denial of Service | whole-document regex with a lazy quantifier over attacker-influenceable Markdown | low | mitigate | Task 1 replaces the heading-anchored whole-document regex with the line-scanning `split_sections()` helper and bounds the fence search to one section body, so no lazy quantifier runs across an entire uploaded document. |
| T-01-04 | Information Disclosure | violation messages printed to CI logs | low | accept | Messages print only a carrier path, an ID token, or a figure token — all values the submitter already controls. With T-01-01 mitigated, no content from outside the repository can reach a message. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable by construction and enforced: this plan adds no package-manager install task, `tools/check_repo.py` imports only the standard library, and `.github/workflows/ci.yml` contains zero `pip install` lines. Task 1's acceptance criteria include an AST import scan asserting the stdlib-only set. No `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is required. |

Blocking threshold is `high`. The one `high`-severity row is mitigated by construction; no
`critical` threat applies.
</threat_model>

<verification>
1. `python3 tools/check_repo.py --self-test` exits 0 and names ten codes, including `pointer-unparseable`.
2. `python3 tools/check_repo.py` exits 0 against the repository as shipped, printing exactly `check_repo: 0 violations`.
3. The three adversarial mutations — pointer line deleted from README.md, pointer line duplicated in README.md, Attribution pointer section removed from NOTICES.md — each produce a non-zero exit and the expected code line when run against a scratch copy of the repository.
4. A carriers-list entry naming a path outside the repository produces `pointer-unparseable` and no file outside the copy is read.
5. `tools/check_repo.py` imports only the Python standard library.
6. `git diff --stat` shows `tools/check_repo.py` as the only modified file.
</verification>

<success_criteria>
- The BLOCKER from 01-VERIFICATION.md is closed: deleting the attribution pointer from README.md no longer produces `check_repo: 0 violations`.
- All three `missing` items from the verification gap are implemented: a parser tolerant of prose between heading and fence, fail-loud behaviour on an unparseable pointer definition, and self-test fixtures whose NOTICES.md mirrors the real file's shape.
- LEG-03's no-drift mechanism is enforcing rather than asserted, so it will hold when Phase 2 and Phase 4 add `skills/proof-first/SKILL.md` and its two reference files as carriers.
- No third-party dependency is introduced and both CI modes stay green.
</success_criteria>

<output>
Create `.planning/phases/01-foundations-legal-scaffolding-numbering-shared-deal/01-05-SUMMARY.md` when done
</output>
