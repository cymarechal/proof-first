---
phase: 02-rule-catalog-integrity-skill-md-core
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - skills/proof-first/SKILL.md
  - skills/proof-first/references/checklist.md
  - NUMBERING.md
autonomous: true
requirements: [CAT-01, CAT-02]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`## PF-1 — Structure` in SKILL.md presents nine rules distributed across the seven Command of the Message sub-blocks NUMBERING.md froze, each rule's ID drawn from its own sub-block's reserved range (CAT-01)."
    - "Every rule in the PF-1 section carries one `**Replace with:**` line naming what evidence to attach in place of the deleted text, so the count of `**Replace with:**` lines equals the count of `### PF-` headings across the whole file (CAT-02, D-01)."
    - "A subtractive instruction and its constructive half never occupy two different PF IDs: no PF ID appears in more than one Allocated IDs row and no ID is defined by two headings in SKILL.md, which `dup-id` and `catalog-id-drift` both enforce."
    - "A rule authored with no constructive half is a defect the file makes visible: the `**Replace with:**` line count and the `### PF-` heading count are equal at every commit in this plan."
    - "The `**Replace with:**` marker is matched as an exact ASCII literal at the start of a line, with no Unicode normalisation and no case folding, so a visually similar variant is counted as absent."
    - "The check-mode report's two labelled category groups always appear in the fixed order integrity first, then prose, and SKILL.md states that order as a rule rather than leaving it to the model — two groups that compare equal in importance have a specified, reproducible position (D-20, D-21)."
    - "SKILL.md's stated rule-count sentence reads `This catalog contains 12 rules in 4 numbered sections.` and matches the PF rows in NUMBERING.md's Allocated IDs table and the rows in references/checklist.md exactly."
    - "SKILL.md is at or below 320 lines at the end of this plan, leaving budget for the 19 rules the two remaining authoring plans add before CAT-08's 500-line ceiling."
  prohibitions:
    - "MUST NOT reproduce a contiguous run of any framework source's own wording, any of its ordered lists in source order, or a term it coined adopted here as this repository's own label — the seven sub-block names come from NUMBERING.md's already-shipped table, and every rule body is this repository's own paraphrase at the level of generality a SOURCES.md-listed source states publicly."
    - "MUST NOT make an unmeasured claim about this skill's own effect — no rule body asserts a win rate, score, improvement, or effectiveness figure for using the skill, because no benchmark has run yet."
    - "MUST NOT present a clean check report as legal, compliance, or contractual clearance."
    - "MUST NOT produce prose that is literally true but arranged to leave a technical evaluator with a false impression of what the vendor has, holds, or has measured."
  artifacts:
    - path: "skills/proof-first/SKILL.md"
      provides: "The PF-1 Command of the Message spine, nine rules across seven sub-blocks"
      contains: "This catalog contains 12 rules in 4 numbered sections."
    - path: "NUMBERING.md"
      provides: "Nine new PF-1 Allocated IDs rows and updated Allocated / Next free columns"
      contains: "| PF-1.25 |"
    - path: "skills/proof-first/references/checklist.md"
      provides: "Twelve PF rows, ascending by ID"
      contains: "| PF-1.13 |"
  key_links:
    - from: "skills/proof-first/SKILL.md"
      to: "NUMBERING.md"
      via: "each PF-1 rule heading sits inside its sub-block's reserved range"
      pattern: "### PF-1\\.\\d+ — "
    - from: "skills/proof-first/SKILL.md"
      to: "examples/deal-brief.md"
      via: "every ✗/✓ pair cites only facts and Canonical figures rows from the deal brief"
      pattern: "Halverton|Kestrel|settlement"
---

<objective>
Author `PF-1`, the structural persuasion engine: nine rules distributed across the seven Command
of the Message sub-blocks `NUMBERING.md` froze at four slots each, every one carrying a ✗/✓ pair
drawn from the shared deal brief.

Purpose: this is the section that makes specificity substitute for adjectives. It is the mechanism
behind the phase's hardest risk — a subtractive, deletion-tested catalog that produces correct,
evidenced, dead prose. `PF-1` is the positive half of that answer, and CAT-02's constructive
pairing has to hold on every one of these rules or the catalog teaches deletion without
replacement.

Output: nine new rules in `SKILL.md`, nine new `NUMBERING.md` Allocated IDs rows, nine new
`checklist.md` rows, and a mid-draft line count measured against RESEARCH Pattern 1's budget while
two authoring plans still remain to trim.
</objective>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rule-catalog-integrity-skill-md-core/02-CONTEXT.md
@.planning/phases/02-rule-catalog-integrity-skill-md-core/02-RESEARCH.md
@.planning/phases/02-rule-catalog-integrity-skill-md-core/02-01-PLAN.md
@.planning/phases/02-rule-catalog-integrity-skill-md-core/02-01-SUMMARY.md
@NUMBERING.md
@SOURCES.md
@examples/deal-brief.md
</context>

<interfaces>
<!-- Frozen by 02-01. Do not re-derive; read these and comply. -->

The nine IDs this plan allocates, from 02-01-PLAN.md's `<rule_allocation_map>`:

| Sub-block (range) | IDs this plan uses | Title |
|---|---|---|
| Before scenario (PF-1.1-PF-1.4) | PF-1.1 | Name the current state in the buyer's own terms |
| Before scenario | PF-1.2 | Name what the current state costs |
| After scenario (PF-1.5-PF-1.8) | PF-1.5 | State the after-state as an observable change, paired with its before |
| Required Capabilities (PF-1.9-PF-1.12) | PF-1.9 | State the capability the buyer needs, not the product that has it |
| Metrics (PF-1.13-PF-1.16) | PF-1.13 | Name the measure and its current baseline |
| Metrics | PF-1.14 | Name where the baseline came from |
| Proof Points (PF-1.17-PF-1.20) | PF-1.17 | Attach one comparable, verifiable proof to each capability claim |
| Differentiators (PF-1.21-PF-1.24) | PF-1.21 | Claim a differentiator only where a named alternative cannot do it |
| Positive Business Outcomes (PF-1.25-PF-1.28) | PF-1.25 | Tie the outcome to the stated priority of the person who owns it |

Every sub-block keeps unfilled slots. Under-filling is the intended use of the reserved ranges
(D-03): a v1.1 rule takes the next free number inside its own sub-block without disturbing a
citation.

Frozen output vocabulary these rule bodies must use (02-01, P-05/P-06):
- `[PF-<id> GAP: <what is missing> — <why it is missing>]`
- `[PF-<id> REVIEW (<commitment|reference|competitor|compliance>): <what needs a human decision>]`
- `[PF-3.3: customer's term, retained — <source>]`
- Register heading `## Unresolved before this document is sent`, columns `Marker | Rule | What is needed`.
- Report category headings `## Integrity flags` then `## Prose violations`.
- Stated-count sentence template `This catalog contains {N} rules in {M} numbered sections.`

Rule body shape (02-CONTEXT.md § Specific Ideas — the single source of truth): a
`### PF-<section>.<n> — <Title>` heading, one or two statement lines, a `**Replace with:**` line,
then a blank-line-separated ✗ line and ✓ line(s). Keep an exampled body at or under twelve lines;
RESEARCH Pattern 1's arithmetic depends on it.

Facts available to a ✗/✓ pair (`examples/deal-brief.md`, read it rather than recalling it): the
850-VM VMware estate, the 40 Oracle Database instances, the nightly settlement batch overrunning
its 6-hour window with no measured baseline, the $2,300,000 current annual run rate, the 8-month
examination window against Kestrel's 14-month comparable programme, the 2026-10-30 submission
date, the 55/25/20 top-level scoring weights, the five scored RFP questions, the five named people
and what each is measured on, and the four inconvenient facts. Any currency amount, percentage, or
ISO date a rule cites must already have a Canonical figures row — `unlisted-figure` now scans
`skills/` and will fail the build otherwise.
</interfaces>

<edge_probe_accounting>
Phase-level: **34 probe-surfaced edge items = 25 authored into `must_haves` + 9 flagged planner
assumptions.** The authoritative per-item table is in `02-01-PLAN.md`
§ `<edge_probe_accounting>`; the arithmetic is restated here so this plan can be checked on its own.

**This plan owns 4 of the 34, all authored as explicit truths, none flagged:**

| Req | Category | Probe question | Authored as |
|---|---|---|---|
| CAT-02 | adjacency | When two things are exactly equal or just touch, do they merge, collide, or separate? | The truth that a subtractive instruction and its constructive half never occupy two PF IDs, and that no ID is defined twice — `dup-id` and `catalog-id-drift` both fire on a collision. |
| CAT-02 | empty | What is the result for empty, single-element, or null input? | The truth that a rule with no constructive half is a defect: the `**Replace with:**` count equals the `### PF-` heading count at every commit. |
| CAT-02 | encoding | Whose definition of length/equality applies? | The truth that the `**Replace with:**` marker is an exact ASCII line-initial literal, no normalisation, no case folding. |
| CAT-02 | ordering | When elements compare equal, is output order specified and stable? | The truth that the two report category groups appear in a fixed order — integrity first, then prose — stated in SKILL.md as a rule (D-20, D-21). |

Prohibition accounting (phase-level): **4 kept prohibitions = 4 authored descriptor-less into
`must_haves.prohibitions` + 0 flagged**, plus 1 canon-referral drop recorded in `02-01-PLAN.md`.
</edge_probe_accounting>

<artifacts_this_phase_produces>
The full phase-wide list of new symbols, IDs, codes, and paths is in `02-01-PLAN.md`
§ `<artifacts_this_phase_produces>` and is authoritative. Newly created by **this plan**:

**New rule IDs (9):** PF-1.1, PF-1.2, PF-1.5, PF-1.9, PF-1.13, PF-1.14, PF-1.17, PF-1.21, PF-1.25.

**New files:** none. **New violation codes:** none. **New `tools/check_repo.py` symbols:** none.

**Modified existing artifacts:** `skills/proof-first/SKILL.md` (new `## PF-1 — Structure` section,
updated stated-count sentence), `NUMBERING.md` (nine Allocated IDs rows, PF-1's `Allocated` and
`Next free` columns), `skills/proof-first/references/checklist.md` (nine rows).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: PF-1 opening half — the before state, the after state, and the needed capability</name>
  <files>skills/proof-first/SKILL.md, skills/proof-first/references/checklist.md, NUMBERING.md</files>
  <read_first>
- `skills/proof-first/SKILL.md` as 02-01 left it — the frontmatter, the two-mode framing, the marker-vocabulary section, the reference pointers, the stated-count sentence, and the three existing rule bodies whose shape these nine must match exactly.
- `NUMBERING.md` — the `## PF-1 sub-blocks` table (the seven element names and their four-slot ranges), the PF reserved ranges table's `Allocated` and `Next free` columns, and the `## Allocated IDs` rows 02-01 added.
- `examples/deal-brief.md` in full — `## The deal in one paragraph`, `## Parties`, `## Estate and target platforms`, `## People and roles`, `## Pain points`, `## Timeline`, `## Inconvenient facts`, `## Customer source material`, and the `## Canonical figures` table. Every fact a ✗/✓ pair cites comes from here; anything beyond it is an invented fact and a defect.
- `SOURCES.md` — the reproduction boundary and the message-articulation source table these concepts trace to.
- `skills/proof-first/references/checklist.md` — the `## PF rules` table's exact column shape.
- `.planning/phases/02-rule-catalog-integrity-skill-md-core/02-CONTEXT.md` § Specific Ideas (rule shape) and D-01, D-02, D-06.
  </read_first>
  <action>
Add a `## PF-1 — Structure` catalog section to `SKILL.md`, positioned between the existing PF-0 and
PF-2 sections so the file reads in section order. Open it with a short paragraph naming the seven
sub-blocks in the order `NUMBERING.md`'s table lists them and stating that a sub-block with no rule
in this version is intentional headroom, not an omission. Then author four rules.

- `### PF-1.1 — Name the current state in the buyer's own terms`: the before state is described in
  the words the buyer used about their own estate, not in the vendor's category language. Its
  `**Replace with:**` half names the substitution: the buyer's own phrasing from supplied source
  material, or the concrete system, count, or process the buyer named. The ✗ line shows a
  category-language description of the current estate; the ✓ line shows the same sentence rebuilt
  from the brief's own numbers and named systems.
- `### PF-1.2 — Name what the current state costs`: a before state with no stated cost is
  scene-setting, not a case. The `**Replace with:**` half requires the cost the buyer already
  states — a run-rate figure, a manual process, a constrained rollout — or a `GAP` marker if the
  buyer has never quantified it. The ✗/✓ pair uses a cost the brief actually records.
- `### PF-1.5 — State the after-state as an observable change, paired with its before`: an after
  state ships adjacent to the before state it replaces, so the contrast is on the page rather than
  in the reader's head. The `**Replace with:**` half requires the paired form. State plainly that
  this pairing is the presence requirement `PF-5.1` later makes checkable. The ✗ line shows an
  unpaired after state; the ✓ line shows the pair.
- `### PF-1.9 — State the capability the buyer needs, not the product that has it`: the capability
  is expressed as what the buyer must be able to do; a product name enters only as the means, after
  the capability. The `**Replace with:**` half names the reordering. The ✗/✓ pair uses one of the
  brief's real target platforms and one of the champion's stated needs.

Register all four in `NUMBERING.md`'s `## Allocated IDs` table, ascending by ID, with `Defined in` =
`SKILL.md` and `Added in` = `v0.1.0`, and update the PF reserved ranges table's `Allocated` count
and `Next free` value for PF-1 per the file's own next-free-ID procedure. Add the same four rows to
`references/checklist.md`'s `## PF rules` table, ascending by ID. Update the stated-count sentence
to `This catalog contains 7 rules in 4 numbered sections.`

Keep every rule body at or under twelve lines. Cite no currency amount, percentage, or ISO date
that has no Canonical figures row.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>grep -c '^### PF-' skills/proof-first/SKILL.md</automated>
    <fails_when>the printed number is anything other than 7</fails_when>
    <automated>grep -cF '**Replace with:**' skills/proof-first/SKILL.md</automated>
    <fails_when>the printed number is anything other than 7, which means at least one rule shipped with no constructive half</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py` prints exactly `check_repo: 0 violations` and exits 0, which proves every new PF-1 ID resolves against the Allocated IDs table (`undefined-id`), sits inside PF-1's reserved range (`range-id`), appears in all three ID files (`catalog-id-drift`), and cites no figure lacking a Canonical figures row (`unlisted-figure`).
- `grep -c '^### PF-' skills/proof-first/SKILL.md` prints `7` and `grep -cF '**Replace with:**' skills/proof-first/SKILL.md` prints `7` — one constructive half per rule, no exceptions (CAT-02, D-01).
- `grep -c '^### PF-1\.' skills/proof-first/SKILL.md` prints `4`, and `grep -oE '^### PF-1\.[0-9]+' skills/proof-first/SKILL.md` lists exactly `### PF-1.1`, `### PF-1.2`, `### PF-1.5`, `### PF-1.9` in that order.
- `grep -c '^## PF-1 — Structure' skills/proof-first/SKILL.md` prints `1`, and that heading appears between the `## PF-0` and `## PF-2` headings — confirmed by `grep -n '^## PF-' skills/proof-first/SKILL.md` showing ascending section numbers by line.
- `grep -cF 'This catalog contains 7 rules in 4 numbered sections.' skills/proof-first/SKILL.md` prints `1`.
- `awk '/^## Allocated IDs/{f=1;next} /^## /{f=0} f && /^\| PF-/' NUMBERING.md | wc -l` prints `7`, and `awk '/^## Allocated IDs/{f=1;next} /^## /{f=0} f && /^\| PF-/ {split($0,c,"|"); gsub(/ /,"",c[2]); print c[2]}' NUMBERING.md | sort -c` exits 0 — rows are present and in ascending order.
- `awk '/^## PF rules/{f=1;next} /^## /{f=0} f && /^\| PF-/' skills/proof-first/references/checklist.md | wc -l` prints `7`.
- `awk '/^## PF reserved ranges/{f=1;next} /^## /{f=0} f && /^\| PF-1 \|/' NUMBERING.md` shows `4` in the Allocated column and `PF-1.10` in the Next free column — one above the highest allocated PF-1 ID, per the file's own next-free procedure.
- Each of the four new rule bodies contains one line starting with `✗` and at least one starting with `✓`, verified by `awk '/^## PF-1 — Structure/{f=1;next} /^## PF-2/{f=0} f' skills/proof-first/SKILL.md | grep -c '^✗'` printing `4` and the same range piped to `grep -c '^✓'` printing `4` or more.
- `wc -l skills/proof-first/SKILL.md` prints a figure at or below 260.
  </acceptance_criteria>
  <done>PF-1.1, PF-1.2, PF-1.5, and PF-1.9 are authored in a `## PF-1 — Structure` section, each with a statement, a `**Replace with:**` half, and a ✗/✓ pair drawn from the deal brief; all four are registered in NUMBERING.md and indexed in checklist.md; the live checker is clean.</done>
</task>

<task type="auto">
  <name>Task 2: PF-1 closing half — metrics, proof points, differentiators, business outcomes, then measure the budget</name>
  <files>skills/proof-first/SKILL.md, skills/proof-first/references/checklist.md, NUMBERING.md</files>
  <read_first>
- `skills/proof-first/SKILL.md` as Task 1 left it — the four PF-1 rules whose shape and length these five must match, and the running line count.
- `NUMBERING.md` — the `## PF-1 sub-blocks` ranges for Metrics, Proof Points, Differentiators, and Positive Business Outcomes, and the `Allocated` / `Next free` values Task 1 set.
- `examples/deal-brief.md` — `## Pain points` (the unmeasured settlement overrun), `## Timeline` (the 8-month examination window against the 14-month comparable programme), `## Inconvenient facts` (all four), `## People and roles` (who is measured on what), `### Economic buyer stated priorities`, `### Decision criteria`, and the `## Canonical figures` table.
- `.planning/phases/02-rule-catalog-integrity-skill-md-core/02-RESEARCH.md` § Architecture Patterns, Pattern 1 — the line-budget arithmetic and the three named remedies if the count runs over.
- `.planning/phases/02-rule-catalog-integrity-skill-md-core/02-CONTEXT.md` D-02, D-06, D-09, D-10, and the settled rule shape in § Specific Ideas.
  </read_first>
  <action>
Author the remaining five PF-1 rules in the same `## PF-1 — Structure` section, in ascending ID
order after PF-1.9.

- `### PF-1.13 — Name the measure and its current baseline`: a metric with no baseline is a target,
  not a metric. The `**Replace with:**` half requires the buyer's own stated figure, or an explicit
  `GAP` marker where they have not measured it. The ✓ line must show the marker in place rather
  than a plausible substitute number — the settlement-batch overrun is the brief's ready-made case,
  since the brief records the 6-hour window as a fact and the overrun as unmeasured.
- `### PF-1.14 — Name where the baseline came from`: a baseline with no stated provenance cannot be
  checked by the evaluator, so the sentence names the source — the buyer's own figure, a named
  measurement, or the document it came from. The `**Replace with:**` half names the attribution
  form. Its ✗/✓ pair uses a figure the brief attributes to a named person or document.
- `### PF-1.17 — Attach one comparable, verifiable proof to each capability claim`: one proof, named
  and checkable, beats three unnamed ones. The `**Replace with:**` half requires a comparable the
  vendor can actually produce, or a `GAP` marker naming what proof is missing. Draw the ✗/✓ pair
  from Kestrel's own comparable prior programme, which the brief records with a duration.
- `### PF-1.21 — Claim a differentiator only where a named alternative cannot do it`: a
  differentiator that every bidder could also claim is a feature, not a differentiator, and a claim
  about what a named rival cannot do is an integrity matter that `PF-2.16` flags. The
  `**Replace with:**` half requires the claim be restated as what this vendor does, evidenced,
  without asserting what a rival cannot — and states that a comparison naming a rival carries a
  `REVIEW (competitor)` marker.
- `### PF-1.25 — Tie the outcome to the stated priority of the person who owns it`: an outcome is
  addressed to the person measured on it, in the terms they used. The `**Replace with:**` half
  requires naming the role and the priority that role stated. The brief's economic buyer states her
  own priorities verbatim; use them.

Register all five in `NUMBERING.md`'s Allocated IDs table ascending by ID, update PF-1's
`Allocated` count and `Next free` value, add the five rows to `references/checklist.md`, and update
the stated-count sentence to `This catalog contains 12 rules in 4 numbered sections.`

Then run the mid-draft line-count checkpoint RESEARCH Pattern 1 requires — while two authoring
plans still remain to trim, not after the catalog is finished. Record the measured line count and
the projected total in the plan summary. Projection: 19 rules remain across 02-03 and 02-04, of
which 13 carry an example. If the measured count plus that projection exceeds 470, do not drop a
rule — shorten existing exampled bodies, per Pattern 1's stated remedies. Report the measured
figure either way.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>grep -c '^### PF-' skills/proof-first/SKILL.md</automated>
    <fails_when>the printed number is anything other than 12</fails_when>
    <automated>grep -cF '**Replace with:**' skills/proof-first/SKILL.md</automated>
    <fails_when>the printed number is anything other than 12</fails_when>
    <automated>wc -l skills/proof-first/SKILL.md</automated>
    <fails_when>the printed line count exceeds 320, which means the projected 19 remaining rules cannot fit under the 500-line ceiling without trimming</fails_when>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output</fails_when>
    <automated>python3 tools/check_repo.py --mutation-test</automated>
    <fails_when>non-zero exit, any line beginning "mutation-test FAIL", or a CONTROL line reporting more than 0 violations</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py --self-test && python3 tools/check_repo.py --mutation-test && python3 tools/check_repo.py` — the exact CI job order — exits 0 with no `FAIL` line and a CONTROL line reading `0 violations`.
- `grep -c '^### PF-' skills/proof-first/SKILL.md` prints `12`, `grep -cF '**Replace with:**' skills/proof-first/SKILL.md` prints `12`, and `grep -oE '^### PF-1\.[0-9]+' skills/proof-first/SKILL.md` lists exactly `### PF-1.1`, `### PF-1.2`, `### PF-1.5`, `### PF-1.9`, `### PF-1.13`, `### PF-1.14`, `### PF-1.17`, `### PF-1.21`, `### PF-1.25` in that order.
- `grep -cF 'This catalog contains 12 rules in 4 numbered sections.' skills/proof-first/SKILL.md` prints `1`, and `grep -cF 'This catalog contains 7 rules' skills/proof-first/SKILL.md` prints `0` — the sentence was updated, not duplicated.
- `awk '/^## Allocated IDs/{f=1;next} /^## /{f=0} f && /^\| PF-/' NUMBERING.md | wc -l` prints `12`, and `awk '/^## PF rules/{f=1;next} /^## /{f=0} f && /^\| PF-/' skills/proof-first/references/checklist.md | wc -l` prints `12`.
- `awk '/^## PF reserved ranges/{f=1;next} /^## /{f=0} f && /^\| PF-1 \|/' NUMBERING.md` shows `9` in the Allocated column and `PF-1.26` in the Next free column.
- Every allocated PF-1 ID sits inside its own sub-block's declared range: PF-1.1 and PF-1.2 inside `PF-1.1-PF-1.4`, PF-1.5 inside `PF-1.5-PF-1.8`, PF-1.9 inside `PF-1.9-PF-1.12`, PF-1.13 and PF-1.14 inside `PF-1.13-PF-1.16`, PF-1.17 inside `PF-1.17-PF-1.20`, PF-1.21 inside `PF-1.21-PF-1.24`, PF-1.25 inside `PF-1.25-PF-1.28`. Verified by reading `awk '/^## PF-1 sub-blocks/{f=1;next} /^## /{f=0} f && /^\| [A-Z]/' NUMBERING.md` against the ID list above.
- `wc -l skills/proof-first/SKILL.md` prints a figure at or below 320, and the measured figure plus the 19-rule projection is recorded in the plan summary.
- `grep -cF 'REVIEW (competitor)' skills/proof-first/SKILL.md` prints at least `1` — PF-1.21 names the marker its comparison raises, using the frozen category vocabulary.
- Every ✓ line that needs a number the buyer has never measured carries a `GAP` marker rather than a substitute figure, verified by `grep -c 'GAP:' skills/proof-first/SKILL.md` printing at least `2` across PF-1.13 and PF-1.17.
  </acceptance_criteria>
  <done>All nine PF-1 rules are authored, registered, and indexed; the catalog states twelve rules in four sections; the full CI job order is green; and the mid-draft line count is measured and recorded with two authoring plans still available for trimming.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paraphrased framework concepts → published repository | The seven Command of the Message element names and the spine's concepts cross from a proprietary, trademarked framework into an MIT-licensed public repository. This plan authors more framework-adjacent prose than any other in the phase. |
| deal-brief facts → worked examples in the skill folder | Facts cross from `examples/deal-brief.md` into `SKILL.md`, where a fabricated or drifted figure would become a shipped, published claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-03 | Tampering | PF-1 rule prose in `skills/proof-first/SKILL.md` | high | mitigate | Every concept traces to a source already listed in `SOURCES.md`'s message-articulation table; the element names come from `NUMBERING.md`'s already-shipped sub-block table rather than from a source document; each rule body is authored as this repository's own paraphrase at the level of generality the listed public sources state. `SOURCES.md:18-22` records that this boundary is a semantic judgment no tool in this stack performs, so the residual read is batched into the end-of-phase UAT and formally cleared at Phase 6's LEG-04. |
| T-2-05 | Information Disclosure / Tampering | figures cited in ✗/✓ pairs | low | mitigate | `unlisted-figure` now scans `skills/`, so every currency amount, percentage, and ISO date in a worked example must trace to a Canonical figures row in the fictional deal brief. Its declared ceiling remains bare counts, so an acceptance criterion additionally requires each cited fact to come from a named deal-brief section. |
| T-2-06 | Tampering | a `GAP` marker replaced by a plausible substitute number | high | mitigate | PF-1.13 and PF-1.17 are authored specifically against facts the brief records as unmeasured, and an acceptance criterion requires at least two `GAP:` markers in the file — so the rules that most invite a fabricated baseline demonstrate the marker instead of a number. |
| T-2-04 | Tampering | a check named as covered that cannot fire | high | mitigate | Task 2 re-runs the full CI job order including `--mutation-test`, so the thirteen codes proven live in 02-01 are re-proven against the grown content rather than assumed to still hold. |
| T-2-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no package in any ecosystem and adds no code — it authors Markdown only. `02-RESEARCH.md` § Package Legitimacy Audit records N/A for the whole phase. |
</threat_model>

<verification>
- `python3 tools/check_repo.py --self-test && python3 tools/check_repo.py --mutation-test && python3 tools/check_repo.py` runs green after each task.
- Twelve rules, twelve `**Replace with:**` lines, twelve Allocated IDs rows, twelve checklist rows — four counts that must agree.
- The mid-draft line count is measured and recorded, not deferred to the end of the phase.
</verification>

<success_criteria>
- A writer reading `## PF-1 — Structure` sees the Command of the Message spine as nine numbered rules, each naming what evidence to attach rather than only what to delete.
- Every PF-1 ID sits inside its own element's four-slot sub-block, with headroom deliberately unfilled.
- The stated rule count, the registry, and the checklist agree, enforced by `catalog-id-drift`.
- The line budget is a measured figure at this point in the phase, not a projection.
</success_criteria>

<output>
Create `.planning/phases/02-rule-catalog-integrity-skill-md-core/02-02-SUMMARY.md` when done.
</output>
