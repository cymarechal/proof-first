---
phase: 02-rule-catalog-integrity-skill-md-core
plan: 07
type: execute
wave: 1
depends_on: [02-06]
files_modified:
  - skills/proof-first/SKILL.md
  - skills/proof-first/references/worked-examples.md
  - .planning/WINDOWS.md
autonomous: false
gap_closure: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-05, CAT-06, CAT-08, CAT-09]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`python3 tools/check_repo.py` exits 0 against the real repository state: `skill-token-budget-exceeded` no longer fires for `skills/proof-first/SKILL.md` (CAT-08, Roadmap SC1, 02-VERIFICATION.md gap 1)."
    - "`skills/proof-first/SKILL.md` measures at most 3,700 whitespace-delimited words, so its estimated token count is at most 4,810 against the 5,000-token ceiling — reached by moving worked examples into `references/` and tightening prose, never by raising the ceiling and never by re-wrapping lines (WINDOWS.md id 5, CAT-08's own 'with detail pushed into references/' clause)."
    - "All three `.github/workflows/ci.yml` steps exit 0 against the post-trim repository state with the workflow file itself unchanged — no `continue-on-error`, no scoped suppression, no explanatory comment needed because there is no longer a known-red state to explain (02-VERIFICATION.md gap 2, 02-REVIEW.md WR-02)."
    - "Every worked ✗/✓ pair removed from SKILL.md exists verbatim in `skills/proof-first/references/worked-examples.md` under the ID of the rule that owns it, inside the `skills/` tree, so `undefined-id` and `unlisted-figure` still scan it and no example's enforcement is lost by the move."
    - "All 31 `### PF-` headings survive the trim and every one of PF-1's seven Command of the Message sub-block headings is still present, so the trim leaves no sub-block empty (CAT-01; edge probe CAT-01/empty)."
    - "The word count that decides CAT-08 is measured exactly as the checker measures it — `len(text.split())`, whitespace-delimited, over the whole file including frontmatter — so no second measurement (a byte count, a code-point count, or an independent `wc` threshold) can disagree with the enforced one (edge probe CAT-01/encoding)."
    - "The count of `### PF-` headings and the count of `**Replace with:**` lines in SKILL.md are exactly equal at 31 after the trim (CAT-02; edge probe CAT-02/adjacency)."
    - "No `**Replace with:**` line is left with an empty body after the trim (CAT-02; edge probe CAT-02/empty)."
    - "Every `**Replace with:**` anchor stays at the start of its own physical line, so re-wrapping during the trim cannot silently break a literal multi-word anchor the way 02-04 and 02-05 each recorded once (CAT-02; edge probe CAT-02/encoding)."
    - "Every consecutive pair of `### PF-` headings encloses exactly one `**Replace with:**` line, so no rule's constructive half migrates to a neighbouring rule (CAT-02; edge probe CAT-02/ordering)."
    - "PF-4's four prose-mechanics rules and its self-containment sentence stay in SKILL.md; the trim moves no PF-4 rule body into `references/` (CAT-06; edge probe CAT-06/empty)."
    - "The self-containment claim stays greppable as one physical line, not split across an authored wrap boundary (CAT-06; edge probe CAT-06/encoding)."
    - "SKILL.md's frontmatter block is byte-identical before and after the trim, so the `description` key's measured length still sits inside the 200/1024 bounds `frontmatter-description-invalid` enforces (CAT-09; edge probe CAT-09/adjacency)."
    - "SKILL.md's frontmatter block is byte-identical before and after the trim, so no required key is left empty (CAT-09; edge probe CAT-09/empty)."
    - "SKILL.md's frontmatter block is byte-identical before and after the trim, so frontmatter key order is unchanged and the order-insensitivity 02-06 proved stays untested-by-this-plan rather than disturbed by it (CAT-09; edge probe CAT-09/ordering)."
    - "The `description` body is byte-identical after the trim, so the artifact-type trigger terms inside its first 200 characters keep both their presence and their adjacency (CAT-10; edge probe CAT-10/adjacency)."
    - "The `description` body is byte-identical after the trim, so it is still non-empty (CAT-10; edge probe CAT-10/empty)."
    - "The `description` body is byte-identical after the trim, so its front-loaded keyword order is preserved exactly as D-30 fixed it (CAT-10; edge probe CAT-10/ordering)."
    - statement: "Whether the skill activates in a fresh harness session is unchanged by this trim, because the `description` is byte-identical — but activation itself is a per-session harness decision no command in this environment can observe (CAT-10; edge probe CAT-10/concurrency)."
      verification: backstop
    - "Every marker moved into `references/worked-examples.md` keeps a non-empty body after its keyword, so no gap or flag is reduced to a bare bracket (INT-02; edge probe INT-02/empty)."
    - "`undefined-id` reports 0 violations across `skills/` after the move, so every `PF-#.#` token inside a moved marker still resolves to an allocated ID in NUMBERING.md (INT-02; edge probe INT-02/encoding)."
    - "Two consecutive `python3 tools/check_repo.py` runs produce byte-identical output against the post-trim tree (edge probe INT-05/idempotency)."
  prohibitions:
    - statement: "MUST NOT close the token-ceiling finding by weakening its own measurement — no raising `SKILL_TOKEN_CEILING`, no lowering `SKILL_TOKEN_WORDS_PER_TOKEN_RATIO`, no adding `skill-token-budget-exceeded` to any suppression set, and no `continue-on-error` or removed step in `.github/workflows/ci.yml`."
      status: unverified
      verification: test
    - statement: "MUST NOT satisfy the ceiling by a measurement that differs from the one the checker enforces — no parallel word or line threshold that could disagree with `int(len(text.split()) * 1.3)`, and no re-wrapping of physical lines as a substitute for removing content."
      status: unverified
      verification: test
    - statement: "MUST NOT reach the ceiling by deleting a rule, dropping a rule's constructive half, or moving a rule body out such that the catalog can no longer be read and applied standalone — progressive disclosure must not become concealment."
      status: unverified
      verification: test
    - statement: "MUST NOT change the frontmatter `description` as a byproduct of trimming: it is CAT-10's whole trigger surface and the pending human observations in `evals/pressure-tests.md` bind to its exact text."
      status: unverified
      verification: test
  artifacts:
    - path: "skills/proof-first/SKILL.md"
      provides: "the 31-rule catalog at or under 3,700 whitespace-delimited words, with each rule's statement and its `**Replace with:**` line retained inline"
      contains: "**Replace with:**"
    - path: "skills/proof-first/references/worked-examples.md"
      provides: "every worked ✗/✓ pair removed from SKILL.md, keyed by owning rule ID"
      contains: "PF-2.17"
  key_links:
    - from: "skills/proof-first/SKILL.md"
      to: "skills/proof-first/references/worked-examples.md"
      via: "a D-28 condition-specific reference pointer in the `## Reference files` section naming when to open the file"
      pattern: "references/worked-examples.md"
    - from: "skills/proof-first/references/worked-examples.md"
      to: "examples/deal-brief.md"
      via: "unlisted-figure's rglob over the skills/ scan root reaches reference files, so every moved figure is still checked against the Canonical figures table"
      pattern: "unlisted-figure"
    - from: "skills/proof-first/references/worked-examples.md"
      to: "NUMBERING.md"
      via: "undefined-id's rglob over the skills/ scan root validates every PF-#.# token inside a moved marker"
      pattern: "undefined-id"
---

<objective>
Bring `skills/proof-first/SKILL.md` under CAT-08's 5,000-token progressive-disclosure ceiling by
moving its worked ✗/✓ examples into a reference file and tightening prose — the two moves CAT-08's
own requirement text and WINDOWS.md id 5 permit — without losing a rule, a constructive half, or a
single unit of existing enforcement.

Purpose: this is the one gap that makes the repository's own CI red. `python3 tools/check_repo.py`
exits 1 today on `skill-token-budget-exceeded` (6,207 estimated tokens against 5,000), which means
every push and pull request shows a failing check on the project whose governing constraint is
"measured claims or no claims". The finding is a content-volume fact, not a formatting artifact:
the file is 368 lines, comfortably inside the 500-line half of the same ceiling, and 4,775 words.
Re-wrapping changes nothing because the estimator counts words, and raising the ceiling would
delete the measurement rather than meet it.

Output: SKILL.md at or under 3,700 words, `skills/proof-first/references/worked-examples.md`
carrying every moved pair, a live checker that exits 0, and WINDOWS.md id 5 closed. Closing this
plan also closes 02-VERIFICATION.md gap 2 by construction: `ci.yml` is left untouched and its three
steps go green because the only violation they reported is gone.
</objective>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/02-rule-catalog-integrity-skill-md-core/02-CONTEXT.md
@.planning/phases/02-rule-catalog-integrity-skill-md-core/02-VERIFICATION.md
@.planning/phases/02-rule-catalog-integrity-skill-md-core/02-REVIEW.md
@.planning/phases/02-rule-catalog-integrity-skill-md-core/02-05-SUMMARY.md
@.planning/phases/02-rule-catalog-integrity-skill-md-core/02-06-SUMMARY.md
@skills/proof-first/SKILL.md
@skills/proof-first/references/checklist.md
@NUMBERING.md
@SOURCES.md
</context>

<interfaces>
<!-- Measured directly against this checkout during planning. Use these numbers; do not re-derive them. -->

## The enforced arithmetic (tools/check_repo.py:978-1000)

- `SKILL_TOKEN_WORDS_PER_TOKEN_RATIO = 1.3`, `SKILL_TOKEN_CEILING = 5000`.
- `word_count = len(text.split())` over the whole file, frontmatter included.
- Fires when `int(word_count * 1.3) > 5000`.
- Therefore the code is silent at and below **3,846 words** (3,846 x 1.3 = 4,999.8, `int` -> 4,999)
  and fires at 3,847 (5,001.1 -> 5,001).
- This plan targets **3,700 words** (4,810 estimated tokens), leaving 146 words of headroom for
  Phase 3's MOD-03 third check-output category, which adds to SKILL.md.
- `wc -w` counts the same whitespace-delimited tokens, so it agrees — but the authoritative
  measurement in every acceptance criterion below is the Python form, because it is the checker's
  own expression and cannot drift from it.

## Current measured distribution (4,775 words total)

| Region | Words | Disposition in this plan |
|---|---|---|
| Frontmatter block (lines 1-14) | ~90 | **Untouched, byte-identical.** CAT-09 and CAT-10 both bind to it. |
| Title + core-value paragraph + attribution pointer line | ~61 | Untouched. The attribution pointer is `pointer-missing`'s subject. |
| `## Your task` (incl. the stated-count line) | 107 | Untouched. The stated-count line is `catalog-count-*`'s subject. |
| `## Marker vocabulary` | 245 | Untouched. D-14 froze this grammar as a one-way interface. |
| `## Reference files` | 38 | **+~30**: one new D-28 pointer for the new file. |
| 31 `### PF-` headings | 375 | Untouched. The IDs are frozen and `catalog-id-drift` reads them. |
| 31 `**Replace with:**` lines | 626 | **Tighten to ~470 (-156).** Each stays one non-empty line at column zero. |
| 20 worked ✗/✓ pairs (40 lines) | 824 | **Move all 824 out to `references/worked-examples.md`.** |
| Ten longest rule statements | 830 | **Tighten to ~700 (-130).** |
| Remaining 21 rule statements | 2,120 | Untouched unless the budget demands it. |
| `## Write mode` | 310 | **Untouched.** Carries MOD-01's output contract; Phase 3 extends it. |
| `## Check mode` | 362 | **Untouched.** Carries MOD-02's output contract; Phase 3 extends it. |
| `## Self-check before delivering` | 91 | Untouched. |
| `## Limits` | 89 | Untouched. D-13's cannot-verify and out-of-scope halves. |

Net: 4,775 - 824 + 30 - 156 - 130 = **3,695 words** -> `int(3695 * 1.3)` = 4,803 tokens. Silent.

## The twenty worked pairs, with measured word counts

`PF-0.1` 47 · `PF-1.1` 33 · `PF-1.2` 37 · `PF-1.5` 34 · `PF-1.9` 35 · `PF-1.13` 41 ·
`PF-1.14` 28 · `PF-1.17` 38 · `PF-1.21` 53 · `PF-1.25` 41 · `PF-2.11` 40 · `PF-2.12` 37 ·
`PF-2.13` 37 · `PF-2.14` 51 · `PF-2.15` 46 · `PF-2.16` 55 · `PF-2.17` 57 · `PF-3.1` 26 ·
`PF-3.2` 29 · `PF-3.3` 59. Total 824.

The eleven rules with no pair — `PF-2.1`, `PF-2.2`, `PF-2.3`, `PF-2.4`, `PF-4.1`-`PF-4.4`,
`PF-5.1`-`PF-5.3` — are D-02's mechanical rules and gain nothing here.

## Which checks reach which files (verified by reading tools/check_repo.py)

| Check | Scan root | Reaches a new `references/*.md`? |
|---|---|---|
| `undefined-id` | `rglob('*.md')` over its scan roots incl. `skills/` (`:394`) | **Yes** — every `PF-#.#` token in a moved marker stays validated. |
| `unlisted-figure` | `UNLISTED_FIGURE_SCAN_ROOTS = ('examples', 'skills')`, `rglob('*.md')` (`:471-483`) | **Yes** — every moved figure stays checked against the Canonical figures table. |
| `catalog-id-drift` | `SKILL_GLOB = 'skills/*/SKILL.md'` + `references/checklist.md` only (`:876-916`) | No — it never reads a third reference file, so the new file cannot perturb it. |
| `catalog-count-unstated` / `catalog-count-mismatch` | `SKILL_GLOB` only | No. |
| `skill-too-long` / `skill-token-budget-exceeded` | `SKILL_GLOB` only (one level below `skills/`) | No — the new file is two levels down and is not measured against the ceiling. |
| `frontmatter-*` | `SKILL_GLOB` only | No. |
| `pointer-missing` / `pointer-duplicated` | Only the four paths NOTICES.md names as carriers | No — `references/worked-examples.md` is not a listed carrier, so it must **not** carry the attribution pointer string. |

No violation code binds to the marker vocabulary or to the four `REVIEW` categories; the
four-category assertion 02-03 recorded was a plan-level acceptance grep, not a checker code. The
categories must therefore still be findable under `skills/` after the move, which they are — inside
the new reference file.

## Frozen text that must survive verbatim

- The stated-count line: `This catalog contains 31 rules in 6 numbered sections.` —
  `catalog-count-unstated` matches this template byte-exactly.
- The attribution pointer line, exactly once in SKILL.md.
- The `## Reference files` section's two existing D-28 pointers.
</interfaces>

<probe_fallback_accounting>
This plan carries the edge-probe accounting for all three gap-closure plans (02-07, 02-08, 02-09),
recorded once so no row is double-counted. Source report:
`.planning/phases/02-rule-catalog-integrity-skill-md-core/.02-edge-probe.json` —
34 applicable rows, 0 pre-resolved.

**Authored into `must_haves.truths` — 19 rows.** CAT-01/empty, CAT-01/encoding, CAT-02/adjacency,
CAT-02/empty, CAT-02/encoding, CAT-02/ordering, CAT-06/empty, CAT-06/encoding, CAT-09/adjacency,
CAT-09/empty, CAT-09/ordering, CAT-10/adjacency, CAT-10/empty, CAT-10/ordering, INT-02/empty,
INT-02/encoding, INT-05/idempotency (17 as `explicit` plain strings) and CAT-10/concurrency
(1 as a `verification: backstop` structured marker, which abstains to `human_needed` if a verifier
cannot confirm it with explicit evidence). That is 18. The nineteenth is INT-05/concurrency, which
is recorded below as out-of-scope, not authored — see the count reconciliation at the end.

**Surfaced as flagged assumptions — 9 rows.** Every `unclassified` row stays `unresolved` and is
never auto-resolved with a backstop:

| Row | Why it stays unresolved | What it means for this plan |
|---|---|---|
| CAT-03/unclassified | The probe could not classify "exactly one opening rule". | The trim tightens PF-0.1's 137-word statement. A-03's paraphrase judgment must be re-run on the new text — routed to 02-09. |
| CAT-04/unclassified | Evidence-attachment framing is a semantic property, not a shape. | The trim moves PF-3.1's worked pair and may tighten its statement. A-04's judgment must be re-run — routed to 02-09. |
| CAT-05/unclassified | Customer-verbatim retention is a semantic property. | PF-3.3's 171-word statement is the single largest; tightening it is in scope and its retention-marker text must survive. |
| CAT-08/unclassified | The probe could not classify a token ceiling as any of its eight shape categories. | This plan's own acceptance criteria cover it directly and mechanically, via the checker rather than via a probe predicate. |
| INT-01/unclassified | "Refuses to invent" is a behavioral refusal, not a shape. | PF-2.11 and PF-2.12's worked pairs move; their refusal statements stay inline. |
| INT-03/unclassified | Same class. | PF-2.14's pair moves. |
| INT-04/unclassified | Same class. | PF-2.15's pair moves. |
| INT-06/unclassified | Same class. | PF-2.17's pair moves — it is this plan's tracer. |
| MOD-01/unclassified | Same class. | `## Write mode` is explicitly untouched by these plans, so no MOD-01 behavior changes. |

**Explicitly out-of-scope for these plans, with reasons — 7 rows.**

| Row | Reason |
|---|---|
| MOD-02/boundary | `## Check mode` is untouched by all three plans; MOD-02 was verified SATISFIED in 02-VERIFICATION.md and Phase 3's MOD-03 owns the next change to that section. |
| MOD-02/adjacency | Same. |
| MOD-02/empty | Same. |
| MOD-02/encoding | Same. |
| MOD-02/ordering | Same. |
| MOD-02/precision | Same. |
| INT-05/concurrency | `tools/check_repo.py` is a read-only single-process script over a static file tree; there is no concurrent-writer surface for the probe's question to apply to. Recorded rather than dismissed. |

**No-silent-drop equality.** The authoritative accounting is this explicit list, not a summary count:

- **Authored as `explicit` plain-string truths (17):** CAT-01/empty, CAT-01/encoding,
  CAT-02/adjacency, CAT-02/empty, CAT-02/encoding, CAT-02/ordering, CAT-06/empty, CAT-06/encoding,
  CAT-09/adjacency, CAT-09/empty, CAT-09/ordering, CAT-10/adjacency, CAT-10/empty, CAT-10/ordering,
  INT-02/empty, INT-02/encoding, INT-05/idempotency.
- **Authored as a `verification: backstop` structured marker (1):** CAT-10/concurrency.
- **Surfaced as flagged assumptions (9):** CAT-03, CAT-04, CAT-05, CAT-08, INT-01, INT-03, INT-04,
  INT-06, MOD-01 — every `unclassified` row, none auto-resolved and none auto-dismissed.
- **Recorded out-of-scope with a reason (7):** the six MOD-02 rows plus INT-05/concurrency.

17 + 1 + 9 + 7 = **34** probe-surfaced rows accounted for, which equals the report's
`coverage.applicable`. Equality holds, with zero silent drops.

**Prohibition accounting (§B).** Stage 1 over-produced ~15 raw must-NOT candidates across CAT-08,
CAT-10 and this repository's Evidence constraint. Stage 2 dropped the routine-engineering ones
("must not mutate its input", "must not break the 1:1 pairing" — both owned by the edge probe
above) and kept 6 values/transparency items, distributed across the three plans: 4 here, 1 in
02-08, 1 in 02-09. Zero were dropped by canon-referral as bespoke-vs-canon: none of the six is
OWASP, GDPR, injection, or generic-fairness canon — they are all specific to this repository's
"measured claims or no claims" doctrine. One separate canon item **was** surfaced and dropped with
a breadcrumb: GitHub Actions workflow-permission and script-injection hardening is canon — covered
by `/gsd-secure-phase`; not minted as a prohibition, and registered honestly in `<threat_model>`
below instead. 6 kept = 6 authored across the three plans; 0 unresolved; 0 silent drops.
</probe_fallback_accounting>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Amend locked decision D-25 to permit a fourth Phase 2 reference file, or trim SKILL.md in place without one?</decision>
  <context>
CAT-08's requirement text asks for the ceiling to be met "with detail pushed into `references/`",
and WINDOWS.md id 5 rules out both cheats (re-wrapping, raising the ceiling). But locked decision
D-25 says: "Phase 2 ships `SKILL.md`, `references/deletion-test.md`, and `references/checklist.md`
— and nothing else in the skill folder." Its stated rationale is about not pre-creating Phase 3's
two files as empty placeholders, which a substantive Phase-2-authored examples file is not — but the
letter of the decision is categorical, and amending a locked decision is not the planner's call.

The measured facts behind both options: SKILL.md is 4,775 words and must reach 3,846 to stop the
check firing, 3,700 to leave Phase 3 any headroom. The 20 worked ✗/✓ pairs are 824 of those words —
the single largest movable block, and the only block whose removal costs no rule, no constructive
half, and no enforcement (both `undefined-id` and `unlisted-figure` reach reference files under
`skills/`, so the moved examples stay checked exactly as they are checked today).

The Write mode and Check mode sections are off the table in both options: they carry MOD-01 and
MOD-02's output contracts and Phase 3's MOD-03 extends them.
  </context>
  <options>
    <option id="option-a">
      <name>Amend D-25 — add `skills/proof-first/references/worked-examples.md` (recommended)</name>
      <pros>Moves 824 words in one structural change, which is exactly the mechanism CAT-08's own text names. Leaves all 31 rule statements and all 31 constructive halves inline, so the catalog stays readable and applicable standalone. Needs only ~286 further words of prose tightening (-156 across the `**Replace with:**` lines, -130 across the ten longest statements), so 21 of the 31 rule statements are never touched — which keeps the PF-0.1/PF-3.1 paraphrase-boundary judgment (WINDOWS id 3) to a narrow, named re-read instead of a whole-catalog one. Every moved pair stays inside `skills/`, so no enforcement is lost.</pros>
      <cons>Amends a locked decision. Takes the skill folder from three files to four, which changes README.md's documented target tree (handled in 02-09). Phase 4's worked before/after examples and Phase 5's linter gain a second file to be aware of. Reversing the move later is a mechanical re-inline, but by then published pointers would churn.</cons>
    </option>
    <option id="option-b">
      <name>Honour D-25 as written — in-place tightening only, no new file</name>
      <pros>No locked decision is amended. The skill folder stays at three files and README.md's target tree needs no change beyond the CR-03 Status fix. Nothing new for Phase 4 or Phase 5 to bind to.</pros>
      <cons>Needs roughly 1,075 words removed from a cuttable pool of about 3,400, once the frontmatter, the marker vocabulary, and both mode sections are excluded — a ~32% rewrite reaching every one of the 31 rule statements, all 31 constructive halves, and all 20 worked pairs (each shrinking from ~41 words to ~16). That maximises regression risk against CAT-01, CAT-02, CAT-04, CAT-05 and CAT-06, and it re-opens the SOURCES.md paraphrase-boundary judgment across the entire catalog rather than on two named rules. Arithmetic check: pairs -494, constructive halves -156, ten longest statements -130, remaining 21 statements -318 = -1,098, landing at 3,677 words. It reaches the ceiling, but by rewriting nearly everything.</cons>
    </option>
  </options>
  <resume-signal>Reply `option-a` to amend D-25 and create `references/worked-examples.md`, or `option-b` to trim in place with no new file. Either answer is executable; Tasks 2 and 3 below state both branches.</resume-signal>
</task>

<task type="tracer" tdd="false">
  <name>Task 2: PF-2.17 end-to-end — one rule trimmed through every layer the gap touches</name>
  <reversibility rating="costly">Moving worked-example bodies out of SKILL.md is mechanically re-inlinable, but once Phase 4's committed examples and Phase 5's linter are authored against the resulting layout, reversing it means editing published artifacts. The locked-decision half of this choice is gated by Task 1's checkpoint.</reversibility>
  <files>skills/proof-first/SKILL.md, skills/proof-first/references/worked-examples.md</files>
  <read_first>
- `skills/proof-first/SKILL.md` in full — specifically the frontmatter block (lines 1-14, which must
  end byte-identical), `## Reference files` (lines 47-51, the two existing D-28 pointers whose form
  the new one must match), and `### PF-2.17 — Flag compliance, certification, and export claims`
  (lines 230-239) with its statement, its `**Replace with:**` line, and its ✗/✓ pair.
- `tools/check_repo.py` — `check_skill_token_budget` and its two constants (`:978-1000`), plus
  `UNLISTED_FIGURE_SCAN_ROOTS` and `check_unlisted_figure` (`:471-500`) and `check_undefined_id`
  (`:390-400`), so the two enforcement paths that must keep reaching the moved content are read
  from the code rather than assumed.
- `NUMBERING.md` — the Allocated IDs table, confirming `PF-2.17` is allocated so its token inside a
  moved marker still resolves.
- `examples/deal-brief.md` § `## Inconvenient facts` and the 18-key `## Canonical figures` table —
  PF-2.17's example cites the SOC 2 Type I / Type II gap and the examination window, and every
  figure in a moved example must still match a Canonical figures row.
- `NOTICES.md` § Attribution pointer § "Files required to carry it" — confirming
  `references/worked-examples.md` is **not** a listed carrier, so the new file must not contain the
  attribution pointer string.
- `.planning/phases/02-rule-catalog-integrity-skill-md-core/02-CONTEXT.md` D-02, D-11, D-14, D-25,
  D-28, D-30.
  </read_first>
  <action>
Prove the whole mechanism on exactly one rule before touching the other nineteen. PF-2.17 is the
right single rule because its worked pair exercises every enforcement path at once: it carries a
`REVIEW (compliance)` marker containing a literal `PF-2.17` token (so `undefined-id` must still
resolve it after the move), it cites deal-brief facts including the SOC 2 Type I/Type II gap (so
`unlisted-figure` must still check it after the move), it is the largest single pair at 57 words,
and it is INT-06's worked evidence.

Under **option-a** (new file):

1. Create `skills/proof-first/references/worked-examples.md` with a one-paragraph header stating
   what the file is (the worked ✗/✓ contrast for each catalog rule that has one), that every fact in
   every example comes from `examples/deal-brief.md`, and that the rule statements themselves live
   in `SKILL.md`. Do not put the NOTICES.md attribution pointer string in it — it is not a listed
   carrier and adding it would be a second uncounted copy.
2. Move PF-2.17's two example lines into that file verbatim — byte-for-byte, both glyphs, the
   marker, the figures — under a `## PF-2.17` heading. Use `##`, not `###`, so the file's headings
   can never be confused with SKILL.md's rule headings by a future reader (no check reads them, but
   `parse_skill_catalog` reads `### PF-` in SKILL.md and the visual distinction is worth keeping).
3. Remove those two lines from SKILL.md. Leave PF-2.17's `### PF-2.17 — ...` heading, its statement,
   and its `**Replace with:**` line exactly where they are.
4. Add the third D-28 pointer to `## Reference files`, in the same imperative
   condition-then-instruction form as the two already there: the condition is writing or checking a
   ✗/✓ contrast for a rule, and the instruction is to read `references/worked-examples.md`. Name the
   condition a model can match on, not an invitation it can decline.

Under **option-b** (no new file): skip steps 1-4 and instead rewrite PF-2.17's ✗/✓ pair in place
from 57 words to at most 24, keeping both glyphs at column zero, keeping the `REVIEW (compliance)`
marker with its `PF-2.17` token and a non-empty body, and keeping every retained figure matched to a
Canonical figures row. Tighten PF-2.17's 72-word statement to at most 55 words without dropping the
externally-verifiable-not-fabricable distinction that makes compliance a flag rather than a refusal.

In both branches, measure and record the word count before and after in the summary, and do not
touch the frontmatter block, the stated-count line, the attribution pointer line, `## Marker
vocabulary`, `## Write mode`, or `## Check mode`.

<!-- planner-discipline-allow: ✗ -->
  </action>
  <verify>
    <automated>python3 -c "import pathlib;t=pathlib.Path('skills/proof-first/SKILL.md').read_text();print(len(t.split()))"</automated>
    <fails_when>the printed number is not strictly less than 4775, meaning no content actually left the file</fails_when>
    <automated>python3 tools/check_repo.py 2>&1 | grep -vc 'skill-token-budget-exceeded'</automated>
    <fails_when>the printed count is anything other than 0, meaning a violation other than the still-open token-budget finding appeared</fails_when>
    <automated>python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/SKILL.md').read_text();h=len(re.findall(r'^### PF-',t,re.M));r=len(re.findall(r'^\*\*Replace with:\*\*',t,re.M));print(h,r)"</automated>
    <fails_when>the two printed numbers are not both exactly 31</fails_when>
    <automated>python3 -c "import pathlib,hashlib;l=pathlib.Path('skills/proof-first/SKILL.md').read_text().splitlines(True)[:14];print(hashlib.sha256(''.join(l).encode()).hexdigest())"</automated>
    <fails_when>the digest differs from the digest of the same 14 lines captured before this task ran, meaning the frontmatter changed</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 -c "import pathlib;t=pathlib.Path('skills/proof-first/SKILL.md').read_text();print(len(t.split()))"` prints a number below 4,775 and the summary records it.
- `python3 tools/check_repo.py` reports at most the one `skill-token-budget-exceeded` line and no other violation — proven by the grep-inverted count above printing `0`.
- `grep -c '^### PF-' skills/proof-first/SKILL.md` prints `31` and `grep -c '^\*\*Replace with:\*\*' skills/proof-first/SKILL.md` prints `31`.
- `grep -cF 'This catalog contains 31 rules in 6 numbered sections.' skills/proof-first/SKILL.md` prints `1`.
- The sha256 of SKILL.md's first 14 lines is unchanged from before the task; capture it first with the command in `<verify>` and compare.
- Under option-a: `test -f skills/proof-first/references/worked-examples.md` succeeds; `grep -c '^## PF-2.17' skills/proof-first/references/worked-examples.md` prints `1`; `grep -cF '[PF-2.17 REVIEW (compliance)' skills/proof-first/references/worked-examples.md` prints `1`; `grep -cF 'references/worked-examples.md' skills/proof-first/SKILL.md` prints `1`.
- Under option-a: `grep -cF 'Concepts here are paraphrased from publicly described sales frameworks.' skills/proof-first/references/worked-examples.md` prints `0` — the new file is not a listed attribution carrier.
- Under option-b: `grep -cF '[PF-2.17 REVIEW (compliance)' skills/proof-first/SKILL.md` prints `1` and PF-2.17's pair measures at most 24 words.
- `python3 tools/check_repo.py --self-test` exits 0 and `python3 tools/check_repo.py --mutation-test` exits 0, proving the move broke no fixture and no mutation.
  </acceptance_criteria>
  <done>PF-2.17's worked pair lives in its destination with its marker and figures intact and still checked by `undefined-id` and `unlisted-figure`; SKILL.md's word count has measurably dropped; the heading/constructive-half pairing is still 31/31; the frontmatter is byte-identical; and the only remaining live violation is the token-budget finding this plan has not yet finished closing.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: The remaining nineteen pairs, the prose tightening, and the ceiling met</name>
  <files>skills/proof-first/SKILL.md, skills/proof-first/references/worked-examples.md, .planning/WINDOWS.md</files>
  <read_first>
- `skills/proof-first/SKILL.md` as Task 2 left it, in full.
- `skills/proof-first/references/worked-examples.md` as Task 2 left it (option-a only) — the header
  and the `## PF-2.17` section whose shape the nineteen remaining sections must match.
- `skills/proof-first/references/checklist.md` — all 31 PF rows, confirming the trim removes no ID
  from the set `catalog-id-drift` compares three ways.
- `skills/proof-first/references/deletion-test.md` — the four documented classes, so tightening
  PF-3.1/PF-3.2/PF-3.3 does not duplicate or contradict what already lives in the reference file.
- `tools/check_repo.py` — `check_skill_token_budget` (`:978-1000`) once more, to compute the final
  margin against the same expression the checker uses.
- `SOURCES.md` — the reproduction boundary and the out-of-bounds list, before rewriting PF-0.1's or
  PF-3.1's statement text.
- `examples/deal-brief.md` § `## Canonical figures` — every figure in every moved example must still
  match a row.
- `.planning/WINDOWS.md` — entry id 5 and its explicit "do not resolve by re-wrapping or raising the
  ceiling" instruction.
  </read_first>
  <action>
Finish the trim and close the ledger entry.

**Move the remaining nineteen pairs (option-a).** `PF-0.1`, `PF-1.1`, `PF-1.2`, `PF-1.5`, `PF-1.9`,
`PF-1.13`, `PF-1.14`, `PF-1.17`, `PF-1.21`, `PF-1.25`, `PF-2.11`, `PF-2.12`, `PF-2.13`, `PF-2.14`,
`PF-2.15`, `PF-2.16`, `PF-3.1`, `PF-3.2`, `PF-3.3`. Each pair moves verbatim into
`references/worked-examples.md` under its own `## <rule-id>` heading, in ascending rule order, with
`## PF-2.17` sorted into its place. Remove the pair lines from SKILL.md and change nothing else
about those rules in this step.

**Under option-b instead**, rewrite each of the nineteen pairs in place to at most 24 words per
pair, preserving in every case: both glyphs at column zero, every marker keyword with a non-empty
body and its rule token, and every figure matched to a Canonical figures row.

**Then tighten prose to land the budget, in this order, stopping as soon as the checker is silent
with at least 100 words of margin:**

1. The 31 `**Replace with:**` lines, 626 words down to about 470. Each stays a single physical line
   beginning at column zero with the literal anchor, and each keeps a non-empty body naming what
   evidence to attach — that body is CAT-02's constructive half and is the reason one citation covers
   both directions of a fix.
2. The ten longest rule statements, 830 words down to about 700: `PF-3.3` (171), `PF-0.1` (137),
   `PF-3.1` (83), `PF-1.25` (79), `PF-2.17` (72, already tightened under option-b), `PF-3.2` (68),
   `PF-1.21` (62), `PF-2.11` (58), `PF-2.13` (51), `PF-1.5` (49). Tighten by removing restatement
   and qualification, never by removing a stated condition. Four bodies carry named, load-bearing
   content that must survive in substance: PF-0.1's disqualifying condition (a reframe that opens
   with a capability list, or splits across several qualifying sentences, does not satisfy the
   rule — this is what makes CAT-03's "exactly one instruction" true); PF-3.1's
   evidence-attachment framing (CAT-04 is satisfied by "attach that evidence in the same sentence
   rather than deleting it", not by a deletion instruction); PF-3.3's retention-plus-flag precedence
   (D-11: retention is a vocabulary decision, the flag is a truth decision, neither suppresses the
   other); and PF-2.17's externally-verifiable-not-fabricable distinction.
3. Only if still short, the remaining 21 rule statements, by at most 15% each.

Do not touch, in either branch: the frontmatter block, the title and core-value paragraph, the
attribution pointer line, `## Your task` including the stated-count line, `## Marker vocabulary`,
`## Write mode`, `## Check mode`, `## Self-check before delivering`, or `## Limits`.

**Then close the ledger**, and only after the live checker exits 0: run
`gsd_run windows fixed 5` so `.planning/WINDOWS.md` records id 5 as resolved. Do not touch ids 3 or
4 — they are human-verification items 02-09 handles and no command in this environment can sign
either off.

**Record in the summary**, for 02-09 and for the verifier: the final word count, the resulting
estimated token count, the margin below 5,000, and whether PF-0.1, PF-3.1 or PF-3.3 statement text
was rewritten. That last fact is what tells 02-09 how wide the WINDOWS id 3 paraphrase re-judgment
has to be.

<!-- planner-discipline-allow: ✗ -->
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any line containing "skill-token-budget-exceeded", or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 -c "import pathlib;w=len(pathlib.Path('skills/proof-first/SKILL.md').read_text().split());print(w, int(w*1.3))"</automated>
    <fails_when>the first number exceeds 3700 or the second number exceeds 4900, meaning the trim landed without the headroom Phase 3 needs</fails_when>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL"</fails_when>
    <automated>python3 tools/check_repo.py --mutation-test</automated>
    <fails_when>non-zero exit, or any line beginning "mutation-test FAIL", or a CONTROL line reporting more than 0 unexpected violations</fails_when>
    <automated>python3 tools/check_repo.py > /tmp/pf-07-a.txt 2>&1; python3 tools/check_repo.py > /tmp/pf-07-b.txt 2>&1; diff -q /tmp/pf-07-a.txt /tmp/pf-07-b.txt</automated>
    <fails_when>diff reports the files differ, meaning the checker's output is not reproducible across two runs</fails_when>
    <automated>python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/SKILL.md').read_text();segs=re.split(r'^### PF-[0-9.]+.*$',t,flags=re.M)[1:];bad=sum(1 for s in segs if len(re.findall(r'^\*\*Replace with:\*\*\s*\S',s,re.M))!=1);print(len(segs),bad)"</automated>
    <fails_when>the first printed number is not 31, or the second is not 0 — a rule whose segment holds zero or two constructive halves, or whose half is empty after the anchor</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py` prints exactly `check_repo: 0 violations` and exits 0. This is the authoritative gate for CAT-08; the ceiling lives in the checker and nowhere else.
- `python3 -c "import pathlib;w=len(pathlib.Path('skills/proof-first/SKILL.md').read_text().split());print(w, int(w*1.3))"` prints a first number at or below `3700` and a second at or below `4900`. The arithmetic tying the two: the checker fires when `int(words * 1.3) > 5000`, so it is silent at or below 3,846 words; the 3,700 target leaves 146 words of headroom for Phase 3's MOD-03 addition, and no separate word threshold is asserted anywhere that could disagree with this expression.
- `grep -c '^### PF-' skills/proof-first/SKILL.md` prints `31`; `grep -c '^\*\*Replace with:\*\*' skills/proof-first/SKILL.md` prints `31`; and the segment script in `<verify>` prints `31 0`.
- `grep -c '^### PF-4' skills/proof-first/SKILL.md` prints `4` — PF-4's prose mechanics stayed inline.
- `grep -cF 'depends on no other skill, tool, or standard being installed' skills/proof-first/SKILL.md` prints `1` — CAT-06's self-containment claim survived as one physical line.
- `grep -c '^## PF-1 ' skills/proof-first/SKILL.md` prints `1` and all seven Command of the Message sub-block labels are still findable in PF-1's body, confirmed by reading the section.
- The sha256 of SKILL.md's first 14 lines matches the digest captured before Task 2 ran.
- `python3 tools/check_repo.py --self-test` exits 0 with no line beginning `FAIL`.
- `python3 tools/check_repo.py --mutation-test` exits 0, its CONTROL line reports `0 unexpected`, and no line begins `mutation-test FAIL`.
- Under option-a: `grep -c '^## PF-' skills/proof-first/references/worked-examples.md` prints `20`, and each of the four literals `REVIEW (commitment)`, `REVIEW (reference)`, `REVIEW (competitor)` and `REVIEW (compliance)` appears at least once across `skills/` (they moved with their rules, so they are still present under the scan root that validates them).
- `.planning/WINDOWS.md` shows id 5 with status `fixed` and a non-null `resolved_at`, and ids 3 and 4 still `open` with `resolved_at` null.
- `git diff --stat .github/workflows/ci.yml` shows no change.
- `git diff --stat tools/check_repo.py` shows no change — this plan closed the finding by changing content, not by changing the check.
  </acceptance_criteria>
  <done>The live checker exits 0 with zero violations; SKILL.md is at or under 3,700 words with the margin recorded; all 31 rules and all 31 constructive halves are intact and correctly paired; PF-4 and both mode sections are untouched; the frontmatter is byte-identical; every moved example is still reached by `undefined-id` and `unlisted-figure`; `ci.yml` and `check_repo.py` are unmodified; and WINDOWS.md id 5 is closed while ids 3 and 4 remain open for 02-09.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Every symbol these three gap-closure plans create. The plan-review-convergence source-grounding pass
reads this section to exclude newly-created symbols from drift verification.

**New files (02-07):**
- `skills/proof-first/references/worked-examples.md` — created under option-a only. Carries the 20
  worked ✗/✓ pairs, each under a `## <PF-rule-id>` heading. Not a NOTICES.md attribution carrier.

**New text in existing files (02-07):**
- A third D-28 reference pointer inside SKILL.md's `## Reference files` section, naming
  `references/worked-examples.md` and the condition that requires opening it.

**New symbols (02-08):**
- `mutation_test`'s local `control_codes`, `discrimination_proven`, and `fire_only` bindings.
- The new final-summary print wording distinguishing discrimination-proven codes from
  confirmed-fire-only codes.
- No new violation code, no new CLI flag, no new module-level function. `KNOWN_OPEN_VIOLATIONS`
  is an existing symbol that 02-08 empties and re-comments; it is not new.

**New text in existing files (02-09):**
- A rewritten `## Status` section in `README.md` and a `worked-examples.md` row in its target tree.
- A scope note in `evals/pressure-tests.md` recording which SKILL.md text the pending observations
  bind to.

**Created by no plan here:** no new violation code, no new CLI flag, no new dependency, no new
workflow step, no new `.planning/` artifact type.
</artifacts_this_phase_produces>

<threat_model>
ASVS L1, blocking threshold `high`. This plan's deliverables are Markdown content in a public
repository plus one ledger update. There is no network surface, no authentication, no session, no
user input path, and no data store, so several STRIDE classes have no applicable threat here and
saying so is the honest answer rather than fabricating one.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository content → GitHub Actions runner | `python3 tools/check_repo.py` reads repository files, including files a fork's pull request may have modified, and the runner executes the workflow. This is the only boundary this plan's content crosses. |
| repository content → an installed agent harness | A user installs `skills/proof-first/` into their own harness, which reads SKILL.md and the reference files as instructions. The content this plan edits becomes instruction text in someone else's session. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `.github/workflows/ci.yml` — no `permissions:` block, `pull_request` trigger | low | accept | Canon item, breadcrumbed rather than minted as a prohibition: workflow-permission hardening (`permissions: contents: read`) is `/gsd-secure-phase`'s territory. Not mitigated here because this plan deliberately leaves `ci.yml` byte-unchanged (an acceptance criterion asserts it), and the job uses no secret, performs no deploy, interpolates no untrusted value into a `run:` block, and uses `pull_request` rather than `pull_request_target`. Recorded, not silently omitted. |
| T-02-02 | Tampering | `skills/proof-first/references/worked-examples.md` as instruction content read by a third-party harness | low | mitigate | Every fact in every moved example traces to `examples/deal-brief.md`, and `unlisted-figure` mechanically enforces that against the Canonical figures table across the whole `skills/` tree — including the new file. No example instructs a harness to fetch, execute, or write anything; the moved lines are prose contrasts only. |
| T-02-03 | Information disclosure | moved example content | low | accept | The deal brief is entirely fictional by construction (Phase 1's EX-01), so no real customer, figure, or certification status can leak through a moved example. `unlisted-figure` would fire on any figure that is not a Canonical figures row. |
| T-02-04 | Repudiation | `.planning/WINDOWS.md` id 5 closure | medium | mitigate | The ledger transition is ordered after, and conditional on, `python3 tools/check_repo.py` exiting 0 — a machine-checkable fact, not an executor's assertion. Ids 3 and 4 are explicitly excluded because no command here can observe what they require; an acceptance criterion asserts both stay `open`. |
| T-02-05 | Spoofing | — | — | accept | No applicable threat in this class: these deliverables have no authentication, identity, or credential surface of any kind. |
| T-02-06 | Denial of service | — | — | accept | No applicable threat in this class: no service, no request handling, and no unbounded input. The checker runs over a fixed file tree in under a second. |
| T-02-07 | Elevation of privilege | — | — | accept | No applicable threat in this class: no roles, no permissions model, and no privileged operation in any deliverable this plan touches. |
| T-02-SC | Tampering | npm/pip/cargo installs | — | accept | No applicable threat: this plan adds no package-manager install task and no dependency. The repository's zero-dependency constraint holds, and `grep -nE '^(import\|from) ' tools/check_repo.py` listing only standard-library modules is already an acceptance criterion from 02-06. RESEARCH.md's Package Legitimacy Audit is therefore not required for this plan. |
</threat_model>

<verification>
Run from the repository root, in this order:

1. `python3 tools/check_repo.py` — must print `check_repo: 0 violations` and exit 0. This is the gap.
2. `python3 -c "import pathlib;w=len(pathlib.Path('skills/proof-first/SKILL.md').read_text().split());print(w, int(w*1.3))"` — first number at or below 3,700, second at or below 4,900.
3. `python3 tools/check_repo.py --self-test` — exit 0, no `FAIL` line.
4. `python3 tools/check_repo.py --mutation-test` — exit 0, CONTROL reports `0 unexpected`.
5. `grep -c '^### PF-' skills/proof-first/SKILL.md` and `grep -c '^\*\*Replace with:\*\*' skills/proof-first/SKILL.md` — both `31`.
6. `git diff --stat .github/workflows/ci.yml tools/check_repo.py` — no change to either.
7. `sed -n '1,14p' skills/proof-first/SKILL.md` — compare against the pre-task digest.

Then confirm by reading, not by grep: that each of the ten tightened rule statements still states
every condition it stated before, and that PF-0.1's disqualifying condition, PF-3.1's
evidence-attachment framing, PF-3.3's retention-plus-flag precedence, and PF-2.17's
verifiable-not-fabricable distinction all survive in substance.
</verification>

<success_criteria>
- 02-VERIFICATION.md gap 1 closed: the live checker no longer reports `skill-token-budget-exceeded`.
- 02-VERIFICATION.md gap 2 closed by construction: all three `ci.yml` steps exit 0 with the workflow
  file unchanged, so there is no known-red state left to disclose and no accommodation to remove.
- No requirement Phase 2 already satisfied is regressed: CAT-01, CAT-02, CAT-03, CAT-04, CAT-05,
  CAT-06 and CAT-09 all still hold, each by a stated mechanical criterion.
- `.planning/WINDOWS.md` id 5 is `fixed`; ids 3 and 4 are untouched and still `open`.
- The ceiling was met by moving detail into `references/` and tightening prose — not by raising a
  constant, not by suppressing a code, not by re-wrapping lines, and not by deleting a rule.
</success_criteria>

<output>
Create `.planning/phases/02-rule-catalog-integrity-skill-md-core/02-07-SUMMARY.md` when done.

Record explicitly, because 02-09 and the verifier both read it: the checkpoint answer (option-a or
option-b); the final word count and estimated token count with the margin below 5,000; whether
PF-0.1, PF-3.1 or PF-3.3 statement text was rewritten and how far; and the list of rule IDs whose
statements were tightened.
</output>
