---
phase: 02-rule-catalog-integrity-skill-md-core
plan: 09
type: execute
wave: 2
depends_on: [02-07]
files_modified:
  - README.md
  - evals/pressure-tests.md
autonomous: true
gap_closure: true
requirements: [CAT-03, CAT-04, CAT-05, CAT-10]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`README.md` no longer contradicts itself about this phase's own deliverable: its `## Status` prose and its target-tree diagram agree, and the prose lists `skills/proof-first/SKILL.md`, both of its reference files, and `evals/pressure-tests.md` among what exists (02-REVIEW.md CR-03, 02-VERIFICATION.md anti-pattern row 1)."
    - "`README.md`'s target tree matches the skill folder on disk after 02-07: every reference file that exists is listed without a `(planned)` tag and every file still unwritten keeps it."
    - "`README.md` publishes no measured claim: the Status section still states that no benchmark has run and that any future number will name its model versions and date (LEG-05, PROJECT.md's Evidence constraint)."
    - "`evals/pressure-tests.md` records which SKILL.md text its pending observations bind to, so a reader can tell whether an observation recorded later was made against the current `description` or an earlier one (CAT-10, D-31)."
    - "The `description` frontmatter field is byte-identical to its pre-02-07 state, so no pressure-test row is invalidated by the trim and the 14 phrasings remain runnable as written (CAT-10)."
    - "`.planning/WINDOWS.md` ids 3 and 4 remain `open` with a null `resolved_at`: neither is signed off by any task in this plan, because neither can be observed from this execution environment."
  prohibitions:
    - statement: "MUST NOT record an observation, a human sign-off, or a ledger `fixed` status for work no human actually performed — no filled-in Observed/Date/Harness cell, no claimed paraphrase-boundary judgment, and no ledger transition for WINDOWS ids 3 or 4."
      status: unverified
      verification: test
  artifacts:
    - path: "README.md"
      provides: "a `## Status` section whose prose agrees with its own tree diagram and with the repository on disk"
      contains: "skills/proof-first/SKILL.md"
    - path: "evals/pressure-tests.md"
      provides: "a scope note binding the pending observations to a named SKILL.md state, with every Observed cell still unfilled"
      contains: "not yet observed"
  key_links:
    - from: "README.md"
      to: "skills/proof-first/"
      via: "the Status prose and the target tree both enumerate the skill folder's actual contents after 02-07"
      pattern: "references/"
    - from: "evals/pressure-tests.md"
      to: "skills/proof-first/SKILL.md"
      via: "the scope note names the frontmatter description the 14 phrasings were authored against"
      pattern: "description"
---

<objective>
Close the two documentation defects 02-REVIEW.md and 02-VERIFICATION.md left behind the three
headline gaps, and prepare — without faking — the two items that only a human can finish.

`README.md` is the blocker-adjacent one: this phase's own diff correctly removed the `(planned)`
tags from its tree diagram but left the prose two sections above it asserting "the skill itself has
not been written yet". The file now contradicts itself about the 31-rule catalog this phase wrote,
and a reader who trusts prose over a diagram three lines below it concludes the opposite of reality.

`evals/pressure-tests.md` is the honest one that must stay honest: all 14 rows read "not yet
observed" because this environment cannot start a fresh harness session, install a skill into it,
and read back activation. This plan does not change that. It adds the one thing missing — a record
of which SKILL.md state the pending observations bind to — so that an observation made later is
attributable, and so that 02-07's trim cannot silently invalidate evidence gathered afterwards.

Purpose: WINDOWS.md ids 3 and 4 are human-verification items, not automatable work. The failure mode
this plan is written to prevent is an executor closing them with an auto task that claims a human
signed off. Both are therefore routed to `<human-check>` blocks under this project's
`human_verify_mode=end-of-phase` setting, and an acceptance criterion asserts both ledger entries
are still open when the plan finishes.

Output: a README that agrees with itself and with the repository, a pressure-test file whose pending
state is attributable, and two human items correctly left open with their scope narrowed to exactly
what 02-07 changed.
</objective>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/02-rule-catalog-integrity-skill-md-core/02-CONTEXT.md
@.planning/phases/02-rule-catalog-integrity-skill-md-core/02-VERIFICATION.md
@.planning/phases/02-rule-catalog-integrity-skill-md-core/02-REVIEW.md
@.planning/phases/02-rule-catalog-integrity-skill-md-core/02-05-SUMMARY.md
@.planning/phases/02-rule-catalog-integrity-skill-md-core/02-07-SUMMARY.md
@README.md
@evals/pressure-tests.md
@SOURCES.md
</context>

<interfaces>
<!-- Read directly from the files during planning. -->

## `README.md` as it stands (107 lines)

Six sections: `## What this is`, `## Status`, `## Repository layout`, plus rule-numbering,
versioning and license sections. The contradiction is entirely inside `## Status` (lines 14-36):

- Line 15-16 assert the skill "has not been written yet".
- The "What exists today" list names six items and omits `skills/proof-first/SKILL.md`, both
  reference files, and `evals/pressure-tests.md`.
- The "What does not exist yet" list names "The skill itself" and "Its reference files" first, and
  "The evaluation harness" last.
- The final paragraph — no measured claim is published, the benchmark has not run, any future number
  will name its model versions and date — is **correct and must survive verbatim in substance**. It
  is LEG-05's posture and PROJECT.md's Evidence constraint, and it is the one part of this section
  that was never wrong.

The tree diagram (lines 47-77) is already correct for the pre-02-07 state: `skills/proof-first/`
carries no tag, `checklist.md` and `deletion-test.md` are untagged, `completeness-audit.md` and
`artifact-patterns.md` are `(planned)`, and `evals/pressure-tests.md` is listed.

02-REVIEW.md CR-03 supplies a full suggested replacement for the Status section. Use it as the
source for what belongs in each list, then reconcile against the repository on disk — CR-03 was
written before 02-07, so its list does not know about `references/worked-examples.md`.

## The `(planned)` inventory the tree must match after 02-07

| Path | State after 02-07 | Tree tag |
|---|---|---|
| `skills/proof-first/SKILL.md` | exists | none |
| `skills/proof-first/references/checklist.md` | exists | none |
| `skills/proof-first/references/deletion-test.md` | exists | none |
| `skills/proof-first/references/worked-examples.md` | exists **under option-a only** — read 02-07's summary for the checkpoint answer | none if created, absent from the tree if option-b was chosen |
| `skills/proof-first/references/completeness-audit.md` | Phase 3 | `(planned)` |
| `skills/proof-first/references/artifact-patterns.md` | Phase 3 | `(planned)` |
| `output-styles/proof-first.md` | Phase 4 | `(planned)` |
| `prompts/system-prompt.md` | Phase 4 | `(planned)` |
| `examples/before-after.md` | Phase 4 | `(planned)` |
| `.claude-plugin/` | Phase 4 | `(planned)` |
| `evals/pressure-tests.md` | exists | none |

## `evals/pressure-tests.md` as it stands (62 lines)

14 phrasings: 9 must-fire, 5 must-not-fire. Every Observed cell reads `not yet observed`. Row
detection in the acceptance script 02-05 used is sensitive to a cell that opens with a quote
character — 02-05's summary records this as a fix it had to make. Do not re-introduce surrounding
quotation marks on phrasing cells.

02-REVIEW.md WR-03 notes that the blanket justification for the must-not-fire rows overstates what
SKILL.md's Limits section actually names: two of the five rows fall outside the frontmatter's
positive scope rather than being named in Limits. **WR-03 is not in this plan's scope** — it was not
listed as a gap and not listed among the findings routed here. It is recorded in this interface block
so a future plan can pick it up, not so this one closes it.

## Checks that read these two files

- `pointer-missing` / `pointer-duplicated` read `README.md` — it is one of NOTICES.md's four listed
  attribution carriers and must carry the pointer string exactly once.
- `undefined-id` reads `README.md` for `PF-#.#` and `MC-#` tokens against NUMBERING.md's Allocated
  IDs table. Do not cite a rule ID in the Status prose that is not allocated.
- `unlisted-figure` does **not** read `README.md` for figures; its scan roots are `examples` and
  `skills`. It does not read `evals/` either. Neither file is figure-checked — which is a reason to
  publish no number in either, not a licence to.
</interfaces>

<probe_fallback_accounting>
The edge-probe accounting for all three gap-closure plans is recorded once, in 02-07's
`<probe_fallback_accounting>` section — all 34 rows, with 18 authored into `must_haves.truths`,
9 surfaced as flagged assumptions and 7 recorded out-of-scope with reasons. No row is re-counted
here. The one prohibition this plan carries is the sixth of the six kept by the §B precision stage.

Two of 02-07's flagged assumptions are discharged into this plan's human-check blocks rather than
into a mechanical criterion: CAT-03/unclassified and CAT-04/unclassified, whose paraphrase-boundary
judgments (flagged assumptions A-03 and A-04) must be re-run on any PF-0.1 or PF-3.1 text 02-07
rewrote. They remain flagged, not resolved — a human-check block is how a flagged assumption is
carried, not how it is closed.
</probe_fallback_accounting>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Make README.md agree with itself and with the repository</name>
  <files>README.md</files>
  <read_first>
- `README.md` in full — the whole `## Status` section (lines 14-36) including its final
  no-measured-claim paragraph, and the whole tree diagram (lines 47-77) with every existing
  `(planned)` and `(exists)` tag, so the two are reconciled against each other rather than edited
  independently.
- `.planning/phases/02-rule-catalog-integrity-skill-md-core/02-REVIEW.md` § CR-03 in full — it
  quotes the stale prose and supplies a complete suggested replacement for both lists.
- `.planning/phases/02-rule-catalog-integrity-skill-md-core/02-07-SUMMARY.md` — the checkpoint
  answer, which decides whether `references/worked-examples.md` exists and therefore belongs in both
  the prose list and the tree.
- The skill folder on disk: `ls -1 skills/proof-first/ skills/proof-first/references/` — the
  authority for what the tree must say, ahead of any list written before 02-07 ran.
- `NOTICES.md` § Attribution pointer — README.md is a listed carrier and must keep the pointer
  string exactly once; do not disturb the line that carries it.
- `.planning/REQUIREMENTS.md` LEG-05 and `.planning/PROJECT.md` § Constraints § Evidence — the
  reason the no-measured-claim paragraph survives.
  </read_first>
  <action>
Rewrite `README.md`'s `## Status` section so its prose states what the repository actually contains,
and reconcile the tree diagram against the skill folder on disk.

Replace the opening assertion. The current text says the foundational scaffolding is in place and
the skill has not been written. Replace it with a sentence stating that the repository is under
active construction and that the rule-ID registry, the `skills/proof-first/` rule catalog, and the
integrity checker are in place.
<!-- planner-discipline-allow: has not been written yet -->
The exact stale literal to remove is `has not been written yet`; it must not survive anywhere in the
file.

Rewrite "What exists today" to list, each with a short clause naming what it is:
`NUMBERING.md`, `skills/proof-first/SKILL.md`, `skills/proof-first/references/checklist.md`,
`skills/proof-first/references/deletion-test.md`, `skills/proof-first/references/worked-examples.md`
(only if 02-07's summary records option-a), `examples/deal-brief.md`, `evals/pressure-tests.md`,
`LICENSE`, `NOTICES.md`, `SOURCES.md`, and `tools/check_repo.py`. For `evals/pressure-tests.md`,
state that it carries the trigger-pressure-test method and that no observation has been recorded
yet, and point the reader at the file itself — this is the one entry where the honest qualifier is
load-bearing, because CAT-10 is unverified and the README must not imply otherwise.

Rewrite "What does not exist yet" to: the distribution manifests (`.claude-plugin/`), the output
style, the paste-able system prompt, the worked before-and-after examples, the two Phase 3 reference
files (`completeness-audit.md`, `artifact-patterns.md`), and a run evaluation benchmark. Remove
"The skill itself", "Its reference files" and the bare "The evaluation harness" entry.

Keep the final paragraph's substance exactly: no measured claim is published in this repository, the
benchmark has not run, and any number this README ever carries will be sourced from committed
benchmark results and will state the model versions and the date it was produced. Do not soften it,
do not move it out of the Status section, and do not add a number anywhere in the file.

Then reconcile the tree diagram: add a `worked-examples.md` line under `references/` with no tag if
02-07 chose option-a, leave `completeness-audit.md` and `artifact-patterns.md` tagged `(planned)`,
and leave every other tag as it is. The tree's `(exists)` and `(planned)` tags and the prose lists
must name the same set of files afterwards.

Cite no rule ID in the new prose — `undefined-id` scans README.md, and there is no reason for a
Status section to name one.
  </action>
  <verify>
    <automated>grep -c 'has not been written yet' README.md</automated>
    <fails_when>the printed count is anything other than 0 — the self-contradicting assertion survived</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>grep -cF 'Concepts here are paraphrased from publicly described sales frameworks. Not affiliated with or endorsed by any framework rights-holder. See NOTICES.md.' README.md</automated>
    <fails_when>the printed count is anything other than 1 — the attribution pointer was lost or duplicated by the rewrite</fails_when>
    <automated>python3 -c "import re,pathlib,subprocess;s=pathlib.Path('README.md').read_text().split('## Status')[1].split('\n## ')[0];need=['skills/proof-first/SKILL.md','references/checklist.md','references/deletion-test.md','evals/pressure-tests.md'];missing=[n for n in need if n not in s];print(missing)"</automated>
    <fails_when>the printed list is not empty — a shipped artifact is still absent from the Status prose</fails_when>
    <automated>python3 -c "import pathlib,re;t=pathlib.Path('README.md').read_text();s=t.split('## Status')[1].split('\n## ')[0];print(len(re.findall(r'PF-\d+\.\d+|MC-\d+',s)))"</automated>
    <fails_when>the printed count is not 0 — a rule ID was cited in the Status prose, which undefined-id must then validate for no reason</fails_when>
  </verify>
  <acceptance_criteria>
- `grep -c 'has not been written yet' README.md` prints `0`.
- The Status prose names `skills/proof-first/SKILL.md`, `references/checklist.md`, `references/deletion-test.md` and `evals/pressure-tests.md`, proven by the membership script in `<verify>` printing an empty list.
- The README mentions the fourth reference file exactly as often as it exists: run
  `python3 -c "import pathlib;r=pathlib.Path('README.md').read_text();e=pathlib.Path('skills/proof-first/references/worked-examples.md').exists();n=r.count('worked-examples.md');print(n, 2 if e else 0)"`
  and require the two printed numbers to be equal — under option-a that is two mentions (one in the
  Status prose list, one in the tree diagram) against a file that exists; under option-b it is no
  mentions against a file that was never created. The file on disk is the authority, not the plan.
- `grep -c '(planned)' README.md` counts exactly the files that do not exist on disk, and `for f in $(ls -1 skills/proof-first/references/); do grep -q "$f" README.md || echo "MISSING $f"; done` prints nothing.
- No `(planned)` tag sits on a line naming a file that exists: verified by reading the tree against `ls -1 skills/proof-first/ skills/proof-first/references/ evals/`.
- The no-measured-claim paragraph is still present in the Status section and still states that the benchmark has not run and that any future number will name its model versions and date; verified by reading it.
- `README.md` contains no digit-bearing performance or effectiveness claim: `grep -nE '[0-9]+(\.[0-9]+)?%' README.md` prints nothing.
- `grep -cF 'Concepts here are paraphrased from publicly described sales frameworks.' README.md` prints `1`.
- The Status prose cites no `PF-#.#` or `MC-#` token, proven by the count script printing `0`.
- `python3 tools/check_repo.py` prints exactly `check_repo: 0 violations`; `python3 tools/check_repo.py --self-test` and `python3 tools/check_repo.py --mutation-test` both exit 0.
- `git diff --stat` shows `README.md` as the only file this task changed.
  </acceptance_criteria>
  <done>README.md's Status prose, its tree diagram, and the repository on disk all name the same set of shipped and unwritten files; the self-contradicting assertion is gone; the attribution pointer is intact and unduplicated; no measured claim was introduced; and all three checker modes are green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Make the pending trigger observations attributable, and route both human items</name>
  <files>evals/pressure-tests.md</files>
  <read_first>
- `evals/pressure-tests.md` in full — the method section, the justification paragraph above the
  must-not-fire rows, all 14 rows, and the exact current contents of every Observed, Date and
  Harness cell.
- `skills/proof-first/SKILL.md` lines 1-14 — the frontmatter `description` as it stands after
  02-07, which is what the 14 phrasings were authored against and what a later observation would be
  made against.
- `.planning/phases/02-rule-catalog-integrity-skill-md-core/02-07-SUMMARY.md` — confirmation that
  the `description` is byte-identical, and the list of rule statements that were rewritten, which is
  what bounds the paraphrase re-judgment below.
- `.planning/WINDOWS.md` — ids 3 and 4 in full, including id 3's naming of flagged assumptions
  A-03/A-04 and id 4's statement of why this environment cannot observe activation.
- `.planning/phases/02-rule-catalog-integrity-skill-md-core/02-VERIFICATION.md`
  § `human_verification` — both entries, whose `test` and `expected` fields are the source for the
  human-check text below.
- `SOURCES.md` — the reproduction boundary, the out-of-bounds list, and its statement that this
  judgment is a semantic one no tool in this project's stack performs, with Phase 6's LEG-04 as the
  formal gate.
- `.planning/phases/02-rule-catalog-integrity-skill-md-core/02-05-SUMMARY.md` — the row-detection
  quoting hazard, so the scope note's formatting does not re-introduce it.
  </read_first>
  <action>
Add attribution to the pending observations. Do not fill any of them in.

Add a short, clearly-labelled scope note to `evals/pressure-tests.md`, immediately after the method
section and before the tables, stating four things:

1. The 14 phrasings were authored against, and must be run against, the frontmatter `description` in
   `skills/proof-first/SKILL.md` as of the commit this note lands in. Name the commit-identifying
   fact plainly — the description's first line and its character length are sufficient and are
   greppable; do not paste the whole description, which would create a second copy that can drift
   from the frontmatter.
2. The `description` was not modified by the Phase 2 gap-closure trim, so every row remains runnable
   exactly as written. State this as the measured fact it is, citing 02-07's byte-identical
   frontmatter criterion.
3. An observation recorded in these tables is valid only for the `description` it was run against;
   if the `description` changes later, previously recorded observations must be re-run rather than
   carried forward.
4. No observation exists yet, and why: this execution environment cannot start a fresh harness
   session, install a skill into it, and read back whether the skill activated. This restates what
   the file already says honestly — do not weaken it into a hedge that implies the rows are probably
   fine.

Leave every Observed, Date and Harness cell exactly as it is. Do not add a row, do not remove a row,
do not reword a phrasing, and do not put surrounding quotation marks on any cell — 02-05 recorded
that a cell opening with a quote character breaks the row-detection regex its acceptance script uses.

Do not touch the must-not-fire justification paragraph: 02-REVIEW.md WR-03 has a real finding about
it, and that finding is not in this plan's scope. Changing the paragraph here would either
half-close WR-03 or obscure it.

Do not touch `.planning/WINDOWS.md`. Ids 3 and 4 stay open. Both are carried by the `<human-check>`
blocks in this task's `<verify>`, which is how this project's `human_verify_mode=end-of-phase`
setting carries a human item — not by a ledger transition an executor performs on a human's behalf.
  </action>
  <verify>
    <automated>grep -c 'not yet observed' evals/pressure-tests.md</automated>
    <fails_when>the printed count is not 14 — an Observed cell was filled in, or a row was added or removed</fails_when>
    <automated>python3 -c "import pathlib,hashlib;l=pathlib.Path('skills/proof-first/SKILL.md').read_text().splitlines(True)[:14];print(hashlib.sha256(''.join(l).encode()).hexdigest())"</automated>
    <fails_when>the digest differs from the one 02-07's summary recorded, meaning the frontmatter the scope note attributes the rows to is not the frontmatter on disk</fails_when>
    <automated>python3 -c "import json,pathlib;d=json.loads(pathlib.Path('.planning/WINDOWS.md').read_text().split('```json')[1].split('```')[0]);print([(w['id'],w['status'],w['resolved_at']) for w in d if w['id'] in (3,4)])"</automated>
    <fails_when>either entry shows a status other than "open" or a non-null resolved_at — a human item was closed by a machine</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <human-check>
**WINDOWS.md id 3 — SOURCES.md reproduction-boundary and PF-0.1 / PF-3.1 framing judgment.**

Read `SOURCES.md`'s reproduction boundary and out-of-bounds list first, then read the current text
of `PF-0.1` (the opening reframe) and `PF-3.1` (the deletion test) in `skills/proof-first/SKILL.md`,
plus `PF-3.3` if 02-07's summary records that its statement was rewritten.

Confirm: no contiguous run of any framework source's own wording is reproduced; no source's ordered
list appears in source order; and no source-coined term has been adopted as this repository's own
label. 02-07 rewrote rule statement text to meet the token ceiling, so this judgment must be re-run
on the new text even if it was made on the old — read 02-07's summary for the exact list of rule IDs
whose statements changed, and treat that list as the minimum scope.

Expected: no contiguous-reproduction and no coined-term-adoption violation found.

This is a semantic judgment no tool in this project's stack performs; Phase 6's LEG-04 is the formal
gate. Mark WINDOWS.md id 3 fixed only after making this judgment yourself.
    </human-check>
    <human-check>
**WINDOWS.md id 4 — trigger pressure-test observations (CAT-10).**

Install `skills/proof-first/` into a real harness (Claude Code, Cursor, or another Agent
Skills-compatible harness) and, in a fresh session per phrasing, run each of the 14 phrasings in
`evals/pressure-tests.md`: the 9 must-fire rows and the 5 must-not-fire rows. Record the observed
result, the date, and the harness name in each row's cells.

Expected: every must-fire phrasing activates the skill; every must-not-fire phrasing does not.

The scope note this task added states which `description` the phrasings bind to. 02-07's trim left
the `description` byte-identical, so the rows are runnable as written — but if the `description` is
edited in a later phase, these observations must be re-run rather than carried forward.

Until these cells are filled, CAT-10 cannot honestly be marked satisfied under this repository's
"measured claims or no claims" constraint. Mark WINDOWS.md id 4 fixed only after recording real
observations.
    </human-check>
  </verify>
  <acceptance_criteria>
- `grep -c 'not yet observed' evals/pressure-tests.md` prints `14` — unchanged from before this task.
- `git diff evals/pressure-tests.md` shows only added lines forming the scope note; no table row, no Observed cell, and no phrasing is modified, and the must-not-fire justification paragraph is untouched.
- The scope note states all four required points, including why no observation exists; verified by reading it.
- The scope note contains the `description`'s character length and its first line, and does not contain the full description text — so no second copy exists that could drift from the frontmatter.
- No phrasing cell begins with a quote character: `grep -c '^| "' evals/pressure-tests.md` prints `0`.
- The sha256 of `skills/proof-first/SKILL.md`'s first 14 lines matches the digest 02-07's summary recorded.
- `.planning/WINDOWS.md` ids 3 and 4 both show status `open` and a null `resolved_at`, proven by the JSON extraction command in `<verify>`; `git diff --stat .planning/WINDOWS.md` shows no change from this task.
- No task in this plan writes to any Observed, Date or Harness cell, and no task claims either human check was performed.
- `python3 tools/check_repo.py` prints exactly `check_repo: 0 violations`.
  </acceptance_criteria>
  <done>The pending observations are attributable to a named `description` state, every Observed cell still reads "not yet observed", WINDOWS ids 3 and 4 are still open with their scope narrowed to exactly what 02-07 changed, and both are carried as human-check items rather than machine-closed.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
See 02-07's `<artifacts_this_phase_produces>` section for the full cross-plan list. This plan's own
additions, restated so the source-grounding pass can exclude them:

- A rewritten `## Status` section in `README.md` and, under option-a, a `worked-examples.md` row in
  its target-tree diagram.
- A scope note in `evals/pressure-tests.md` binding the pending observations to a named SKILL.md
  frontmatter state.

No new file, no new symbol, no new violation code, no new CLI flag, no new dependency, and no ledger
transition.
</artifacts_this_phase_produces>

<threat_model>
ASVS L1, blocking threshold `high`. This plan edits two Markdown files in a public repository. There
is no code change, no network surface, no authentication, no user input path, and no data store, so
most STRIDE classes have no applicable threat and saying so is the honest answer.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository content → a reader or evaluator | `README.md` is the document a prospective user reads before installing anything. Its claims are the repository's public representations. |
| repository content → `check_repo.py` | `README.md` is one of NOTICES.md's four attribution-pointer carriers and is scanned for rule-ID tokens, so a rewrite crosses into the checker's subject set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-15 | Repudiation | `README.md` public claims | high | mitigate | **This is the defect being closed.** A self-contradicting README is a false public representation about the repository's own contents. Mitigated by reconciling the prose, the tree and the disk state, and asserted by a membership script rather than a read-through. The no-measured-claim paragraph is preserved verbatim in substance and an acceptance criterion greps for the absence of any percentage figure, so the rewrite cannot introduce an unmeasured claim while fixing a stale one. |
| T-02-16 | Repudiation | `evals/pressure-tests.md` observation cells | high | mitigate | The realistic failure here is an executor filling in an Observed cell to make a row look complete. Mitigated by a prohibition, by an acceptance criterion counting the unfilled cells at exactly 14, by a diff criterion permitting added lines only, and by routing both items to `<human-check>` instead of an auto task. |
| T-02-17 | Tampering | `README.md`'s attribution pointer line | medium | mitigate | The rewrite touches a section adjacent to a `pointer-missing` / `pointer-duplicated` subject. Mitigated by an exact-string count criterion asserting the pointer appears once, plus a full live-checker run. |
| T-02-18 | Tampering | `.planning/WINDOWS.md` ledger state | medium | mitigate | A machine closing a human-verification item is the specific tampering risk. Mitigated by a JSON extraction assertion that ids 3 and 4 are still `open` with null `resolved_at`, plus a criterion that this task produces no diff against the ledger at all. |
| T-02-19 | Information disclosure | — | — | accept | No applicable threat in this class: both files are public by design and neither gains any non-public content. The scope note deliberately omits the full `description` text to avoid creating a second copy, which is a drift concern rather than a disclosure one. |
| T-02-20 | Spoofing | — | — | accept | No applicable threat in this class: no identity, credential, or authentication surface in either file. |
| T-02-21 | Denial of service | — | — | accept | No applicable threat in this class: no service and no request handling in scope. |
| T-02-22 | Elevation of privilege | — | — | accept | No applicable threat in this class: no roles, no permissions model, no privileged operation. The GitHub Actions workflow-permissions canon item is registered as T-02-01 in 02-07's threat model and breadcrumbed to `/gsd-secure-phase`. |
| T-02-SC | Tampering | npm/pip/cargo installs | — | accept | No applicable threat: this plan adds no package-manager install task and no dependency. RESEARCH.md's Package Legitimacy Audit is therefore not required. |
</threat_model>

<verification>
Run from the repository root:

1. `grep -c 'has not been written yet' README.md` — `0`.
2. `grep -c 'not yet observed' evals/pressure-tests.md` — `14`.
3. `grep -cF 'Concepts here are paraphrased from publicly described sales frameworks.' README.md` — `1`.
4. `grep -nE '[0-9]+(\.[0-9]+)?%' README.md` — no output.
5. `ls -1 skills/proof-first/ skills/proof-first/references/ evals/` compared against README.md's tree
   diagram — the same set of files, with `(planned)` only on paths that do not exist.
6. The WINDOWS.md JSON extraction — ids 3 and 4 both `open`, both `resolved_at` null.
7. `python3 tools/check_repo.py`, `--self-test`, `--mutation-test` — all three exit 0.

Then the two `<human-check>` items in Task 2, which are the mechanism by which CAT-10 and the
SOURCES.md paraphrase judgment move from open to resolved. Neither is closable by any command in
this environment, and neither is claimed as done by this plan.
</verification>

<success_criteria>
- 02-REVIEW.md CR-03 closed: README.md's prose, its tree, and the repository agree, and the stale
  assertion is gone.
- The Status rewrite introduced no measured claim and preserved the no-measured-claim paragraph.
- `evals/pressure-tests.md`'s pending observations are attributable to a named `description` state,
  and all 14 remain unfilled.
- WINDOWS.md ids 3 and 4 are still open, with their scope narrowed to exactly the text 02-07
  changed, and both are carried as human checks rather than machine-closed.
- All three checker modes stay green, and `README.md` still carries the attribution pointer once.
</success_criteria>

<output>
Create `.planning/phases/02-rule-catalog-integrity-skill-md-core/02-09-SUMMARY.md` when done.

Record: whether option-a or option-b applied (and therefore whether `worked-examples.md` entered the
README); the final `(planned)` inventory as the tree now states it; confirmation that all 14 Observed
cells are unchanged; and the two human-check items restated verbatim so `/gsd-verify-work` can pick
them up without re-reading this plan.
</output>
