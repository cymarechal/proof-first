---
phase: 03-completeness-audit-artifact-patterns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/proof-first/references/completeness-audit.md
  - skills/proof-first/references/checklist.md
  - skills/proof-first/SKILL.md
  - NUMBERING.md
  - tools/check_repo.py
autonomous: false
requirements: [AUD-01, AUD-02, MOD-05]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A writer opening `skills/proof-first/references/completeness-audit.md` reads one document-level completeness check, `MC-1`, carrying a diagnostic statement, exactly one `**Replace with:**` line, and the attribution pointer exactly once."
    - "`MC-1` is present in all three of NUMBERING.md's Allocated IDs table, `references/completeness-audit.md`'s rule headings, and `references/checklist.md`'s `## MC rules` table, and a new CI-enforced code fails the build if that ever stops being true."
    - "`python3 tools/check_repo.py` reports 0 violations with the new reference file on disk, which proves `pointer-missing`, `pointer-duplicated`, `undefined-id`, `range-id` and `unlisted-figure` all accept MC-namespace content with no code change."
    - "A rule heading defining an `MC-` ID inside any `skills/*/SKILL.md` is a build failure, so AUD-02's never-blended requirement is enforced mechanically rather than by convention."
    - "`skills/proof-first/SKILL.md` gains exactly one new reference pointer, stating the named condition that requires opening `references/completeness-audit.md`, and the file's measured word count is re-read in the same task that adds it."
    - "`parse_checklist` reads its section name from a parameter defaulting to the existing PF section, so both PF call sites keep working unchanged and the MC call site adds no second table reader."
    - "Both `catalog-id-drift` and `mc-catalog-id-drift` compute their three-way divergence through one shared helper, so a future rule about definitional agreement cannot be fixed in one namespace and silently drift in the other."
    - "`--mutation-test` reports 24 codes discrimination-proven with a CONTROL line reading 0 violations, up from 22, and no code is reported fire-only."
  prohibitions:
    - "MUST NOT reproduce a contiguous run of any framework source's own wording, any of its ordered lists in source order, or a term coined by a source adopted here as this repository's own label — `references/completeness-audit.md` paraphrases only, at the level of generality a SOURCES.md-listed source states publicly."
    - "MUST NOT reorder, rename, or re-column NUMBERING.md's `## MC reserved blocks` table; the eight dimension names and their ID ranges are frozen inputs from Phase 1 and the paraphrase tension they carry is routed to Phase 6's LEG-04 gate, not resolved here."
    - "MUST NOT cite an `MC-` ID that this plan has not allocated, anywhere under `skills/` — including inside a range expression such as a low ID, a hyphen, and a high ID on one line, which `undefined-id` reads as two separate citation tokens."
    - "MUST NOT invent a fourth marker bracket form for a completeness gap; the marker vocabulary is closed at three forms and a missing dimension reuses the existing GAP form."
    - "MUST NOT make an unmeasured claim about this skill's own effect anywhere in the new reference file."
  artifacts:
    - path: "skills/proof-first/references/completeness-audit.md"
      provides: "The MC-namespace document-level completeness audit, its trigger condition, and its inherited-ID-order note"
      contains: "### MC-1 — "
      min_lines: 20
    - path: "skills/proof-first/references/checklist.md"
      provides: "The MC ID index section the installed skill can search"
      contains: "## MC rules"
    - path: "NUMBERING.md"
      provides: "The first MC Allocated IDs row"
      contains: "| MC-1 |"
    - path: "tools/check_repo.py"
      provides: "mc-catalog-id-drift, mc-rule-in-skill, the parameterised checklist reader, and the shared three-way diff helper"
      contains: "mc-catalog-id-drift"
    - path: "skills/proof-first/SKILL.md"
      provides: "The reference pointer naming the condition that requires opening the completeness audit"
      contains: "references/completeness-audit.md"
  key_links:
    - from: "skills/proof-first/references/completeness-audit.md"
      to: "NUMBERING.md"
      via: "every MC rule heading has a matching Allocated IDs row"
      pattern: "### MC-\\d+ — "
    - from: "skills/proof-first/references/checklist.md"
      to: "NUMBERING.md"
      via: "one MC checklist row per allocated MC ID"
      pattern: "\\| MC-\\d+ \\|"
    - from: "tools/check_repo.py"
      to: "skills/proof-first/references/completeness-audit.md"
      via: "run_catalog_checks reads the MC rule headings this file defines"
      pattern: "completeness-audit.md"
    - from: "skills/proof-first/references/completeness-audit.md"
      to: "NOTICES.md"
      via: "carries the verbatim attribution pointer line exactly once, as NOTICES.md's carrier list already requires"
      pattern: "Not affiliated with or endorsed by any framework rights-holder"
---

<objective>
Prove the whole Phase 3 pipeline on one thin, real slice: one MEDDICC-derived completeness check,
`MC-1`, authored in a new production-quality `skills/proof-first/references/completeness-audit.md`,
registered in `NUMBERING.md`, indexed in a new `## MC rules` section of `references/checklist.md`,
pointed at from `SKILL.md`, and guarded by two new violation codes proven to fire against mutated
production content in CI.

Purpose: creating `completeness-audit.md` switches on four already-shipped checks against it
(`pointer-missing`, `pointer-duplicated`, `undefined-id`, `unlisted-figure`) plus `range-id` against
the first MC row, all with zero new code. Proving that whole path green on one dimension — rather
than on eight — catches an architectural dead end after one commit instead of after three plans of
authored prose, exactly as `02-01` did for the PF namespace. The interfaces this plan freezes (the
eight-ID MC allocation map, the check-mode report section names and their fixed order, the four
artifact-family section names, the MC stated-count sentence template) are what Phase 4's committed
before/after examples and Phase 5's linter both bind to, so they are gated by a decision checkpoint
before any of them is written down.

Output: a real MC-namespace reference file with one authored dimension; `NUMBERING.md`'s first MC
Allocated IDs row; `checklist.md`'s `## MC rules` section; one `SKILL.md` reference pointer; two new
CI-enforced violation codes, each with firing and silent self-test fixtures and a registered
mutation.
</objective>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-RESEARCH.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-PATTERNS.md
@NUMBERING.md
@NOTICES.md
@SOURCES.md
@examples/deal-brief.md
@tools/check_repo.py
@skills/proof-first/SKILL.md
@skills/proof-first/references/checklist.md
@skills/proof-first/references/deletion-test.md
</context>

<interfaces>
<!-- Contracts the executor needs. Every line below was read from the named file during this -->
<!-- planning session. Use these directly; no codebase exploration is needed to start. -->

**Measured baseline, re-verified this planning session (2026-09-14) — do not re-derive:**
- `wc -l -w -c skills/proof-first/SKILL.md` prints `309 3694 22902`.
- `python3 -c "import pathlib;w=len(pathlib.Path('skills/proof-first/SKILL.md').read_text().split());print(w, int(w*1.3))"` prints `3694 4802`. Ceiling is 5000, so the margin is 198 estimated tokens, roughly 152 words.
- `python3 tools/check_repo.py` prints `check_repo: 0 violations`.
- `python3 tools/check_repo.py --self-test` passes with 22 verified codes.
- `python3 tools/check_repo.py --mutation-test` prints `mutation-test CONTROL: 0 violations` and `mutation-test PASS: 22 codes discrimination-proven`.

**Existing helpers to reuse verbatim (`tools/check_repo.py`):**
- `MC_ID_RE = re.compile(r'^MC-(\d+)$')` (line 217) — already exists, already correct for flat MC IDs.
- `split_sections(text)` (line 230) — `{heading: body}` split on `## ` headings.
- `table_rows(section_text)` (line 250) — data rows of the first Markdown table in a section, header and separator rows skipped.
- `parse_numbering(path)` (line 268) — returns `pf_ranges`, `mc_ranges`, `allocated`, `deprecated`. `mc_ranges` already parses all eight `## MC reserved blocks` rows and needs no change.
- `strip_fences(text)` (line 226).
- `SKILL_GLOB = 'skills/*/SKILL.md'` (line 224) — a fixed one-level pattern. Never widen it to `skills/**/SKILL.md`; `self_test()` writes a fixture at `bad_root/skills/SKILL.md`, one level shallower than a real skill, and a widened glob would fire against it.
- `_write(path, content)` (line 1508) — self-test fixture writer.
- `_insert_table_rows_after_heading(text, heading, new_rows)` (line 1406) — mutation helper that works even when a table has zero data rows.
- `_copy_repo_subset` / `MUTATION_SOURCES` (line 1385) — already includes `'skills'`, so the control copy already contains the reference files this plan creates.

**Functions this plan modifies, with their current PF-only hard-codes:**
- `RULE_HEADING_RE = re.compile(r'^### (PF-\d+\.\d+) — ')` (line 850). The separator is U+2014 EM DASH with one ASCII space either side. An MC sibling regex must use the identical separator or it will silently match nothing.
- `parse_checklist(path)` (line 867) reads `sections.get('PF rules', '')` — the section name is a hard-coded literal, and it is the single line this plan parameterises.
- `check_catalog_id_drift(allocated, repo_root)` (line 880) filters `allocated` with `PF_ID_RE`, compares three sets, and emits one violation per divergent ID naming the files it is inconsistent between. Its comparison body is what this plan extracts into a shared helper.

**Contracts every new check must satisfy:**
- A check function returns `list[(subject, "code-name message")]`. Both `mutation_test` and `self_test` recover the code via `line.split(' ', 1)[0]`, so the leading token of the message must be exactly the code string, unbroken by whitespace.
- New codes go into `CATALOG_CHECK_CODES` (line 1039) and their calls into `run_catalog_checks` (line 1045). `ALL_CHECK_CODES` (line 1059) concatenates that list automatically — no edit needed there.
- `MUTATIONS` (line 1394) is a list of `(code, description, mutate_fn)`. `mutation_test` fails any code in `ALL_CHECK_CODES` with no registered mutation.
- `mutation_test` only counts a code as discrimination-proven when it is **silent on the unmutated control copy and fires on the mutated copy**. A code that already fires on the control is reported FIRE-ONLY and is excluded from the proven total.
- A new bad fixture's codes must be unioned into `bad_codes` inside `self_test()` (line 2252) or the generic coverage loop reports the new code as never observed firing.

**THE SINGLE HIGHEST-RISK DETAIL IN THIS PLAN — read twice.**
`_good_numbering()` (line 1542) is the fixture behind `good_root`, `bad_catalog_root`,
`good_catalog_root`, `line500_root`, `line501_root`, `token_good_root`, `token_bad_root` and more.
Its Allocated IDs table **already contains `MC-1` and `MC-5` rows**, and none of those roots has a
`references/completeness-audit.md` or a `## MC rules` checklist section. A `mc-catalog-id-drift`
implementation that reports "allocated in NUMBERING.md, missing from the other two" without first
checking that `references/completeness-audit.md` exists will fire on `good_root` and break
`--self-test` across the whole suite. **`check_mc_catalog_id_drift` MUST return no violations when
`references/completeness-audit.md` does not exist**, matching `check_catalog_id_drift`'s own stated
"absence is not failure" posture. Do not edit `_good_numbering()` to remove those MC rows; they are
the fixture that proves `range-id`'s MC branch at both block boundaries.

**Traps this repository has already been bitten by — all four are live against this plan:**
1. `check_unlisted_figure` (line 478) scans `UNLISTED_FIGURE_SCAN_ROOTS = ('examples', 'skills')`. Every `$`-prefixed amount, every `NN%`, and every `YYYY-MM-DD` date written anywhere in `completeness-audit.md` must match a value in `examples/deal-brief.md`'s Canonical figures table exactly. Bare counts (`850`, `40`, `8`) are unconstrained. A currency token that absorbs a trailing comma from sentence punctuation is reported as unmatched — reword the sentence, never the checker (02-02's recorded fix).
2. `check_undefined_id` (line 388) scans `skills/` recursively for `MC-\d+` tokens. Writing a range as a low ID, a hyphen, and a high ID on one line produces **two** citation tokens, and the high one is not allocated by this plan. Write no MC range expression anywhere under `skills/`.
3. A literal multi-word anchor that an acceptance criterion greps for must sit entirely on one physical line. 02-04 lost two anchors to an authored line-wrap and fixed it by moving the wrap point, not the wording.
4. The attribution pointer is compared by full-line equality after stripping leading and trailing whitespace. Copy it out of `NOTICES.md`'s fenced block; never retype it.
</interfaces>

<decisions>
## Phase-wide interface choices this plan freezes (planner discretion — no `03-CONTEXT.md` exists, `/gsd-discuss-phase` did not run for this phase)

Every later Phase 3 plan reads this table and complies. None of these is routed back to the user.

| ID | Choice | Reversibility |
|----|--------|---------------|
| P3-01 | **Eight MC rules, one per dimension, each allocated at its dimension block's range start:** `MC-1` Metric, `MC-6` Economic Buyer, `MC-11` Decision Criteria, `MC-16` Decision Process, `MC-21` Paper Process, `MC-26` Pain, `MC-31` Champion, `MC-36` Competition. AUD-01 asks for the eight dimensions "as questions asked of a document" — one question per dimension satisfies it exactly, keeps every block under-filled with four slots of headroom (the same deliberate under-fill D-03 gave PF), and makes the ID-to-dimension mapping unambiguous without a sub-block table. Full map in `<mc_allocation_map>` below. | one-way |
| P3-02 | **Check-mode report sections and their fixed order:** `## Integrity flags`, then `## Prose violations`, then `## Completeness gaps`, then `## Structural ordering`. Phase 2's D-20 states the two-category structure "adds `Completeness gaps` as a third group and changes nothing else"; appending third is the only reading that leaves the two existing groups in their existing positions. The alternative — inserting completeness between integrity and prose on D-21's severity rationale — is defensible on its merits and is **recorded as rejected here**, because it would reposition an ordering Phase 2 locked and which `02-UAT.md` already signed off. | one-way |
| P3-03 | **The structural-ordering pass gets its own labelled section, not a fold into one of the three categories.** MOD-03's own wording puts it outside the three categories ("...three separately labeled categories, plus a structural ordering pass"). Its own heading, printed always, carrying the identical no-findings-line convention, makes all four outputs mechanically identical for Phase 4's examples and Phase 5's linter. Answers 03-RESEARCH Open Question 2. | one-way |
| P3-04 | **The MC checklist gets its own stated-count anti-hallucination sentence**, frozen as `This audit contains {N} checks across {M} dimensions.` on its own line in `references/completeness-audit.md`, where N is the count of MC rows in NUMBERING.md's Allocated IDs table and M is the count of distinct MC dimension blocks those rows fall into. It lands in `03-02` together with its enforcing check, at the final values 8 and 8 — not in this plan, because the frozen template is plural and a one-dimension slice would have to write ungrammatical numbers to match it. Answers 03-RESEARCH Open Question 3: yes, for symmetry with D-32's already-proven PF mechanism ("a wrong stated total is worse than none"). | costly |
| P3-05 | **`references/worked-examples.md` gains MC-namespace worked pairs**, one `## MC-<n>` section per allocated MC ID, in the identical shape its 20 PF pairs already use. MC dimensions are judgment calls, which is exactly the class D-02 reserves examples for. Lands in `03-02`. Answers 03-RESEARCH Open Question 4. | reversible |
| P3-06 | **`parse_checklist` gains a `section_name` parameter defaulting to `'PF rules'`.** Both existing call sites keep working with no edit; the MC call site passes the new section name. Chosen over a duplicated sibling function because the duplication would carry its own `table_rows` plumbing, which 03-RESEARCH's Don't Hand-Roll table names as the failure mode. Research assumption A4's fallback (a sibling function) is not taken. | reversible |
| P3-07 | **One shared three-way divergence helper** backs both `check_catalog_id_drift` and `check_mc_catalog_id_drift`. Refactoring the already-proven PF path is licensed because its existing self-test assertion and its existing `MUTATIONS` entry both re-run unchanged in this plan's verification and will catch a regression immediately. | reversible |
| P3-08 | **Five new violation codes across the phase:** `mc-catalog-id-drift` and `mc-rule-in-skill` (this plan); `mc-count-unstated` and `mc-count-mismatch` (`03-02`); `artifact-family-section-missing` (`03-03`). The discrimination-proven total moves 22 → 24 → 26 → 27. | reversible |
| P3-09 | **Phase 3's four plans execute in four sequential waves, with no parallelism.** Every plan's verify command is the whole-repository checker: `python3 tools/check_repo.py` reads every Markdown file under `skills/` and `examples/`, plus `NUMBERING.md`, `NOTICES.md`, `README.md` and `LICENSE`. Disjoint `files_modified` is therefore not sufficient isolation — a file left mid-edit by a concurrent plan produces a spurious violation in this plan's verify run. | reversible |
| P3-10 | **Threat IDs use `T-03-NN`, unique phase-wide, never restarting per plan.** Supply-chain rows are `T-03-SC1` through `T-03-SC4`, one per plan. This plan owns `T-03-01` through `T-03-06` and `T-03-SC1`; `03-02` continues at `T-03-07`, `03-03` at `T-03-12`, `03-04` at `T-03-17`. Phase 2 used two colliding schemes (`T-2-NN` in plans 01-06, `T-02-NN` in plans 07-09), both restarting per plan, and that collision caused a real incident in which a threat-register assembly matched one namespace and silently missed 22 threats in the other. | reversible |
| P3-11 | **The four artifact-family section headings in `references/artifact-patterns.md` are frozen as** `## RFP and RFI response`, `## Solution proposal`, `## Executive summary`, `## Demo and discovery material`. They mirror the four families `SKILL.md`'s Write mode already names, and `artifact-family-section-missing` checks them literally in `03-03`. | one-way |
| P3-12 | **`NUMBERING.md`'s `## MC reserved blocks` table is not touched at all** — not reordered, not renamed, and not given `Allocated`/`Next free` columns for symmetry with the PF table. `parse_numbering` reads the range from `row[1]`, so adding a column ahead of it would break MC range parsing, and 03-RESEARCH Pitfall 2 names any edit to that table's row order or names as the wrong lever for the paraphrase concern. Only the `## Allocated IDs` table gains rows. | costly |

## Accepted gaps (recorded, not silently left)

- `references/completeness-audit.md` carries no stated-count sentence for the duration of this plan (P3-04 defers it to `03-02`, where its enforcing check lands in the same task). The window is one wave long and `mc-catalog-id-drift` already guards the ID set itself throughout it.
- `SKILL.md`'s `## Reference files` list names `completeness-audit.md` at the end of this plan but will not name `artifact-patterns.md` until `03-04`. `03-03` creates that file one wave earlier. `NOTICES.md`'s carrier rule covers the pointer obligation in the meantime; the reference-list entry is documentation, not enforcement.
- `check_range_id`'s MC branch only asserts that an MC ID falls inside *some* declared dimension block, not inside the block its title implies. There is no `parse_pf_subblocks` equivalent for MC. P3-01 allocates one ID at each block's range start, which makes the mapping unambiguous by construction, so no per-dimension containment check is built this phase. Recorded as deliberately out of scope.

## Flagged planner assumptions

- **A3-01 (unresolved, routed to Phase 6 LEG-04).** The eight MC dimension names and their ID-range order, both frozen in `NUMBERING.md:66-77` and restated in `.planning/REQUIREMENTS.md`'s AUD-01 before this phase began, sit in the MEDDICC acronym's own canonical order. `SOURCES.md:14-16` names "a source's own ordered list reproduced in its order" as reproduction, and `SOURCES.md:18-22` states plainly that no tool in this project's stack performs that judgment and that Phase 6's LEG-04 gate owns it. **This phase does not attempt to resolve it.** The levers this phase does use are content levers only: every dimension's audit question, rationale and example is written in fresh, project-specific language grounded in `examples/deal-brief.md`, never restating any source's own definition of what a dimension conceptually means. `03-04` Task 3 records the tension as a `.planning/WINDOWS.md` entry addressed to LEG-04, mirroring existing id 3.
- **A3-02.** This plan's word-budget arithmetic for `SKILL.md` is a projection, not a measurement: the one pointer bullet added here is estimated at roughly 20 words against a measured 152-word margin. The task re-measures immediately after the edit rather than trusting the projection. If the measured margin after this task is below 120 words, stop and say so in the SUMMARY rather than continuing — `03-04` owns the trim that buys the budget back and it must not be pre-spent here.
- **A3-03 (unclassified).** Whether one audit question per dimension is "a completeness checklist covering" that dimension, as AUD-01 words it, or whether a dimension needs several, is a reading judgment on the authored question's own body, not a mechanical check. P3-01 fixes the ID count; the sufficiency judgment is a human read at UAT.
</decisions>

<mc_allocation_map>
The complete P3-01 map. Every plan in this phase draws its MC IDs from here; no plan invents one.
Titles are document-facing instructions, never definitions of what a methodology dimension
conceptually is — that distinction is the paraphrase lever A3-01 describes.

| ID | Dimension block | Title | Plan |
|----|-----------------|-------|------|
| MC-1 | Metric | Name the buyer's own measure and where its baseline came from | 03-01 |
| MC-6 | Economic Buyer | Name the person who signs and the priority they stated in their own words | 03-02 |
| MC-11 | Decision Criteria | Mirror the buyer's own stated evaluation criteria and their weights | 03-02 |
| MC-16 | Decision Process | State the steps, dates, and people the buyer's evaluation runs through | 03-02 |
| MC-21 | Paper Process | State the reviews the document must clear before a signature is possible | 03-02 |
| MC-26 | Pain | Name the cost the buyer already states, in the buyer's own words | 03-02 |
| MC-31 | Champion | Name the person inside the buyer who carries this internally | 03-02 |
| MC-36 | Competition | State the alternatives the buyer is weighing, without asserting what a rival cannot do | 03-02 |

Running stated-count sentence by plan (P3-04): this plan authors none; `03-02` authors
`This audit contains 8 checks across 8 dimensions.` together with the check that enforces it.
</mc_allocation_map>

<source_audit>
## Multi-Source Coverage Audit (phase-wide)

| SOURCE | ID | Feature/Requirement | Plan | Status |
|---|---|---|---|---|
| GOAL | — | A writer can classify a document by artifact family, apply that family's conventions, and get a document-level completeness verdict and trustworthy rule citations — independent of the prose rules | 03-01..03-04 | COVERED |
| REQ | AUD-01 | Document-level completeness checklist derived from MEDDICC, 8 dimensions as questions asked of a document | 03-01 (MC-1), 03-02 (the other seven) | COVERED |
| REQ | AUD-02 | Checklist in its own reference file and its own MC namespace, never blended into the prose rules | 03-01 (`mc-rule-in-skill` + the separate file) | COVERED |
| REQ | AUD-03 | Writer can run the audit independently and get a separate verdict | 03-02 (the standalone-run section of the reference file), 03-04 (the SKILL.md mode instruction) | COVERED |
| REQ | ART-01 | RFP/RFI response pattern: answer-first ordering, compliance-versus-value separation, criteria mirroring | 03-03 | COVERED |
| REQ | ART-02 | Solution proposal pattern: architecture narrative, required-capability mapping, risk treatment | 03-03 | COVERED |
| REQ | ART-03 | Executive summary pattern: problem reframe first, business case before the capability list | 03-03 | COVERED |
| REQ | ART-04 | Demo and discovery pattern: discovery notes, demo scripts, POC success criteria, follow-up | 03-03 | COVERED |
| REQ | MOD-03 | Check mode reports three separately labelled categories plus a structural ordering pass | 03-04 (P3-02, P3-03) | COVERED |
| REQ | MOD-04 | Skill classifies the artifact family before applying rules and says which one | 03-03 (the classification procedure and its fallback), 03-04 (the Check-mode instruction) | COVERED |
| REQ | MOD-05 | Check mode cites only rule numbers that exist in the shipped files | 03-01 (`mc-catalog-id-drift`), 03-02 (`mc-count-*`), 03-04 (the SKILL.md citation instruction) | COVERED |
| RESEARCH | Summary | The token budget, not the line count, is the binding SKILL.md constraint; budget the trim up front rather than discovering it at verification | 03-01 (measure after the one edit), 03-04 (the trim, as its own task with a measured gate) | COVERED |
| RESEARCH | Pattern 1 | Re-measure `wc` and re-run the checker after every SKILL.md edit, not once at the end | 03-01, 03-04 | COVERED |
| RESEARCH | Pattern 2 | Extend Write mode's existing assumed-family line to Check mode rather than inventing classification; author the no-family fallback as genuinely new content | 03-03 (fallback), 03-04 (Check-mode line) | COVERED |
| RESEARCH | Pattern 3 | Add the third category reusing the identical no-findings-line convention; the ordering pass is a distinct pass, not a fourth finding category | 03-04 (P3-02, P3-03) | COVERED |
| RESEARCH | Pattern 4 | Label generically, operationalise with this project's own deal-brief facts, never restate a source's own definition | 03-01, 03-02 | COVERED |
| RESEARCH | Pattern 5 | The concrete new-code shape: MC heading regex, parameterised checklist reader, MC drift check, optional count check, and the AUD-02 structural check | 03-01, 03-02 | COVERED |
| RESEARCH | Pattern 6 | One MC dimension end-to-end through both tracks before bulk-authoring the rest | 03-01 | COVERED |
| RESEARCH | Don't Hand-Roll | Reuse `split_sections`/`table_rows`; parameterise the shared drift comparison rather than copy-pasting it per namespace | 03-01 (P3-06, P3-07) | COVERED |
| RESEARCH | Don't Hand-Roll | No banned/allowed phrasing list as the paraphrase-safety mechanism | 03-01, 03-02 (the "does not do" section) | COVERED |
| RESEARCH | Pitfall 1 | Do not treat the remaining headroom as enough without re-measuring | 03-01, 03-04 | COVERED |
| RESEARCH | Pitfall 2 | Do not reorder or relabel the MC dimensions to fix the paraphrase concern | 03-01 (P3-12, A3-01) | COVERED |
| RESEARCH | Pitfall 3 | Do not assume `undefined-id` closes MOD-05; build the MC drift check as its own deliverable | 03-01 | COVERED |
| RESEARCH | Pitfall 4 | State which half of each requirement is CI-checkable and which is permanently manual | every plan's `<verification>` and `<output>` | COVERED |
| RESEARCH | Open Q1 | MC dimension order versus SOURCES.md's ordered-list rule | 03-01 (A3-01, carried forward), 03-04 (WINDOWS.md entry) | COVERED |
| RESEARCH | Open Q2 | Structural-ordering pass placement | 03-01 (P3-03) | COVERED |
| RESEARCH | Open Q3 | MC stated-count sentence | 03-01 (P3-04), 03-02 (authored and enforced) | COVERED |
| RESEARCH | Open Q4 | MC worked pairs in `worked-examples.md` | 03-01 (P3-05), 03-02 (authored) | COVERED |
| RESEARCH | Validation | Every new code ships the D-34 triad: wired check, firing and silent fixtures, registered mutation | 03-01, 03-02, 03-03 | COVERED |
| PATTERNS | §1 | Reproduce the paired heading-count versus `**Replace with:**`-count grep at plan level; the checker does not enforce it | 03-01, 03-02 | COVERED |
| PATTERNS | §2 | Model the MC reference file on `deletion-test.md` — trigger paragraph, worked instances, closing "does not do" section | 03-01, 03-02 | COVERED |
| PATTERNS | §6 | One phase-wide threat-ID scheme, never restarting per plan | 03-01 (P3-10) | COVERED |
| PATTERNS | §7 | `requirements-completed:` records implementation, never verification | every plan's `<output>` | COVERED |
| CONTEXT | — | No `03-CONTEXT.md` exists; `/gsd-discuss-phase` did not run for this phase. The four research discretion points are decided in `<decisions>` above as P3-02, P3-03, P3-04 and P3-05, and none is routed back to the user. | 03-01 | COVERED |

No row is MISSING.
</source_audit>

<artifacts_this_phase_produces>
Every symbol, ID and path Phase 3 creates. Nothing below exists on disk before this phase.

**New files:** `skills/proof-first/references/completeness-audit.md`,
`skills/proof-first/references/artifact-patterns.md`.

**New violation codes (5):** `mc-catalog-id-drift`, `mc-rule-in-skill`, `mc-count-unstated`,
`mc-count-mismatch`, `artifact-family-section-missing`.

**New `tools/check_repo.py` symbols:** `MC_HEADING_RE`, `MC_COUNT_SENTENCE_RE`,
`ARTIFACT_FAMILY_SECTIONS`, `parse_completeness_audit`, `_three_way_id_diff`,
`check_mc_catalog_id_drift`, `check_mc_rule_in_skill`, `check_mc_count`,
`check_artifact_family_sections`.

**New mutation functions and `MUTATIONS` entries (5):** `_mutate_mc_catalog_id_drift`,
`_mutate_mc_rule_in_skill`, `_mutate_mc_count_unstated`, `_mutate_mc_count_mismatch`,
`_mutate_artifact_family_section_missing`.

**New self-test fixture helpers:** `_mc_numbering`, `_good_completeness_audit`,
`_bad_completeness_audit`, `_mc_checklist`, `_good_artifact_patterns`,
`_bad_artifact_patterns`, and their scratch roots inside `self_test()`.

**Modified existing symbols:** `parse_checklist` (gains a `section_name` parameter),
`check_catalog_id_drift` (calls the shared helper), `CATALOG_CHECK_CODES`, `run_catalog_checks`,
the module docstring's violation-code block.

**New rule IDs (8):** MC-1, MC-6, MC-11, MC-16, MC-21, MC-26, MC-31, MC-36. No `PF-` ID is
allocated, deprecated, renumbered or retitled by this phase.

**New stable output vocabulary (bound by Phases 4-5):** the check-mode report headings
`## Completeness gaps` and `## Structural ordering` and their position in the fixed order; the four
artifact-family section headings; the MC stated-count sentence template.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Freeze the four Phase 3 one-way interfaces Phases 4 and 5 will bind to: the eight-ID MC allocation map, the check-mode report section names and their fixed order, the structural-ordering pass as its own labelled section, and the four artifact-family section headings</decision>
  <context>
No `03-CONTEXT.md` exists — `/gsd-discuss-phase` did not run for this phase, so nothing here was
locked by a user decision and all four choices are the planner's. All four become stable interfaces
the moment any content is authored against them. Phase 4's committed before/after examples print the
check-mode section headings and cite MC numbers; Phase 5's linter parses those same headings and
counts those same categories. Phase 2's D-20 and D-21 are the direct precedent: they froze the
two-category order, and `02-UAT.md` signed it off, so Phase 3 is amending a signed interface rather
than inventing a free one. Phase 1's deprecation-never-deletion rule means a merged or renumbered MC
rule strands a published citation forever. Undoing any of the four after Phase 4 ships is a migration
of committed examples plus recorded benchmark data, not a refactor. This gate exists so a human sees
the door before the agent walks through it.
  </context>
  <options>
    <option id="adopt-as-proposed">
      <name>Adopt the planner's proposal verbatim</name>
      <pros>
Eight MC rules, one per dimension, at the range starts MC-1, MC-6, MC-11, MC-16, MC-21, MC-26,
MC-31, MC-36 — every block left under-filled with four slots of headroom, and the ID-to-dimension
mapping unambiguous with no sub-block table. Check-mode sections in the fixed order Integrity flags,
Prose violations, Completeness gaps, Structural ordering — the only order that leaves Phase 2's two
existing groups exactly where D-20 and D-21 put them, which is what "adds a third group and changes
nothing else" says. The ordering pass as its own always-printing section, so all four outputs share
one shape for a linter to parse. Artifact-family sections named for the four families Write mode
already names, so a reader meets no second vocabulary.
      </pros>
      <cons>
One question per dimension is a floor, not a ceiling: a dimension later judged to need two questions
takes a minor version bump to add MC-2 (headroom exists, but the stated count changes). Appending
Completeness gaps third puts a document-level finding that can lose a deal at scoring below
line-level prose findings, which is arguably the wrong severity order — that tension is recorded in
P3-02 as knowingly accepted in favour of not repositioning a signed-off interface.
      </cons>
    </option>
    <option id="amend-before-authoring">
      <name>Amend one or more fields before any prose is written</name>
      <pros>
Nothing has been authored yet, so any of the following changes at zero cost right now: the number of
MC rules per dimension or the specific IDs chosen inside each block; the report section heading
wording; the position of `## Completeness gaps` in the fixed order (for example moving it second, on
D-21's severity rationale, accepting that this repositions `## Prose violations`); whether the
ordering pass is its own section or a fold into one of the three; the four artifact-family heading
strings. Name the field and its replacement value.
      </pros>
      <cons>
Amending the MC map changes the stated-count sentence `03-02` authors and the checklist rows every
plan writes. Amending a report heading means `03-04`'s SKILL.md text and `03-03`'s per-family
ordering definitions both change with it.
      </cons>
    </option>
  </options>
  <resume-signal>Reply "adopt" to take the proposal verbatim, or name the field and its replacement value</resume-signal>
</task>

<task type="tracer">
  <name>Task 1: End-to-end "one completeness check is CI-visible in the installed skill" — MC-1 only</name>
  <reversibility rating="one-way">This task writes the first MC ID and the MC rule-heading shape into a folder Phase 4 publishes and Phase 5's linter parses; Phase 1's deprecation-never-deletion rule means a renumbered MC-1 strands a published citation rather than being refactored away.</reversibility>
  <precondition>`NOTICES.md`'s "Attribution pointer" section yields a non-empty fenced pointer string and its "Files required to carry it" list names `skills/proof-first/references/completeness-audit.md`; `NUMBERING.md` contains a `## MC reserved blocks` table with a Metric row and an `## Allocated IDs` table with zero MC rows. All three are Phase 1 artifacts this task writes against, and all three were verified present during planning.</precondition>
  <files>skills/proof-first/references/completeness-audit.md, skills/proof-first/references/checklist.md, NUMBERING.md, skills/proof-first/SKILL.md</files>
  <read_first>
- `NUMBERING.md` in full — the `## MC reserved blocks` table (frozen, not edited by this task), the `## Allocated IDs` table's row shape and its ascending-sort rule, the range-exhaustion rule, and the next-free-ID procedure.
- `NOTICES.md` lines 24-42 — the fenced attribution pointer string (the single source of truth; never restate it from memory) and the "Files required to carry it" list that already names this task's new file.
- `SOURCES.md` in full — the reproduction boundary at lines 11-22, the "Qualification checklist sources" table the MC content traces to, and the out-of-bounds list.
- `examples/deal-brief.md` in full — every fact any example may cite, especially `## People and roles`, `## Pain points`, `## Inconvenient facts`, `## Customer source material`, and the 18-row `## Canonical figures` table.
- `skills/proof-first/references/deletion-test.md` in full — this is the shape the new file copies: a trigger paragraph naming exactly when to open it, worked instances that teach a transferable mechanism, and a closing `## What this file does not do` section that refuses to become a static list.
- `skills/proof-first/references/checklist.md` in full — the header paragraph and the `## PF rules` two-column table whose shape the new `## MC rules` table matches.
- `skills/proof-first/SKILL.md` lines 47-51 — the `## Reference files` list and the named-opening-condition shape (D-28) the new pointer bullet must match.
- This plan's `<interfaces>` block, twice — particularly the four recorded traps and the measured baseline.
  </read_first>
  <action>
Author one completeness check, MC-1, through every layer this phase touches, and stop. Do not author
MC-6 or any later dimension; `03-02` owns them and citing an unallocated MC token here is the exact
forward-reference failure `02-01` and `02-02` both hit.

`skills/proof-first/references/completeness-audit.md` (new file):
- Open with one paragraph stating the named condition that requires opening the file — running a
  document-level completeness audit, or reporting a completeness gap in check mode — mirroring
  `deletion-test.md`'s own opening. State in the same paragraph that this audit asks questions of a
  document, that its verdict is separate from the prose rules, and that its IDs live in a namespace
  disjoint from the prose catalog's (AUD-02, AUD-03).
- Carry the verbatim attribution pointer line from `NOTICES.md`'s fenced block, on its own line,
  exactly once, copied from that block rather than retyped. `NOTICES.md` already lists this path as a
  required carrier, so `pointer-missing` fires the moment the file exists without it.
- Add one short paragraph recording that the ID order across dimension blocks is inherited numbering
  scheme structure from `NUMBERING.md`, fixed before this file was written, and is not an assertion
  that this is the correct or original sequence for the methodology the dimensions derive from. This
  is the zero-token-cost defensive note A3-01 describes; it lives here, not in `SKILL.md`.
- Author MC-1 under a level-three heading whose text is the ID, a space, an em dash, a space, and the
  title from this plan's `<mc_allocation_map>` — byte-identical in separator to the PF rule headings
  `RULE_HEADING_RE` already matches, because the new MC regex uses the same separator. Its body is
  two to four sentences stating what a document missing this dimension looks like, written as a
  direct instruction grounded in this repository's own deal-brief facts. Never restate any source's
  own definition of what the dimension conceptually means — that is the distinction A3-01 turns on.
  Follow the body with exactly one bold-labelled constructive line, in the same `**Replace with:**`
  form every PF rule carries, naming the concrete substitution: the buyer's own stated figure and the
  source that produced it, or the existing GAP marker form carrying MC-1 as the raising rule token.
  Reuse the closed three-form marker vocabulary; do not invent a fourth bracket form for a
  completeness gap.
- Close with a `## What this file does not do` section stating plainly that it is not a scoring
  rubric, not a pass/fail gate substituting for a human's judgment, and not a qualification tool for
  a live deal — it asks questions of a document and reports what the document does not contain.
- Grounding material available for MC-1 without inventing anything: the economic buyer's stated
  run-rate figure, the settlement batch job's required window, and the recorded fact that the batch
  overrun has never been instrumented, so no baseline exists for it. Any `$` amount, `%` value or
  ISO date written anywhere in this file must match a Canonical figures value exactly — see trap 1.

`skills/proof-first/references/checklist.md`:
- Append a `## MC rules` section after the existing `## PF rules` section, holding a two-column
  `| ID | Rule |` table with the identical header and separator shape, and exactly one data row for
  MC-1 carrying the title from the allocation map. Do not modify the `## PF rules` section or the
  file's opening paragraph.

`NUMBERING.md`:
- Append exactly one `## Allocated IDs` row for MC-1, keeping the table's ascending sort: MC rows sort
  after PF rows under this table's existing ordering. `Title` is the title from the allocation map,
  `Defined in` is the new reference file's own filename, `Added in` is `v0.1.0` to match every
  existing row.
- Change nothing else in this file. The `## MC reserved blocks` table is frozen (P3-12), and the PF
  reserved ranges table's counters describe PF only.

`skills/proof-first/SKILL.md`:
- Add exactly one bullet to the existing `## Reference files` list, stating the named condition that
  requires opening the completeness audit — before running a document-level completeness audit, or
  before reporting a completeness gap in check mode — and naming the file. Keep it to one sentence;
  this is the only `SKILL.md` edit this plan makes, and `03-04` owns the mode-level instructions and
  the trim that pays for them.
- Add no rule heading of any kind to this file. The MC rules live in the reference file; Task 2 turns
  that into a build failure rather than a convention.
- Immediately after the edit, re-measure with the two commands in the acceptance criteria and record
  both numbers in the SUMMARY. If the remaining margin is under 120 words, stop and say so rather
  than proceeding — see A3-02.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output</fails_when>
    <automated>python3 -c "import pathlib;w=len(pathlib.Path('skills/proof-first/SKILL.md').read_text().split());print(w, int(w*1.3))"</automated>
    <fails_when>the second printed number is 5000 or greater, or the first printed number exceeds 3790</fails_when>
    <human-check>
Read `skills/proof-first/references/completeness-audit.md` end to end against `SOURCES.md`'s
reproduction boundary at lines 11-22 and confirm two things no tool in this project's stack can
check: (1) no contiguous run of any framework source's own wording, no source's ordered list in
source order, and no term coined by a source and adopted here as this repository's own label appears
anywhere in the file; (2) MC-1's body reads as a question asked of a document, grounded in the deal
brief's own facts, rather than as a restatement of what the dimension conceptually means in the
methodology it derives from — that distinction is the whole paraphrase lever available to this phase
(flagged assumption A3-01, whose residual formal clearance is Phase 6 LEG-04's).
    </human-check>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py` prints exactly `check_repo: 0 violations` and exits 0, with `skills/proof-first/references/completeness-audit.md` present on disk.
- `grep -cF 'Not affiliated with or endorsed by any framework rights-holder' skills/proof-first/references/completeness-audit.md` prints `1`.
- `grep -c '^### MC-' skills/proof-first/references/completeness-audit.md` prints `1`, and `grep -cF '**Replace with:**' skills/proof-first/references/completeness-audit.md` prints `1` — one constructive half per check, the parity `tools/check_repo.py` does not itself enforce.
- `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/references/completeness-audit.md').read_text();print(re.findall(r'^### (MC-\d+) — ',t,re.M))"` prints exactly `['MC-1']` — proving the em-dash separator matches the pattern the new checker regex will use.
- `python3 -c "import re,pathlib;t=pathlib.Path('NUMBERING.md').read_text();s=t.split('## Allocated IDs')[1].split('\n## ')[0];print([l.split('|')[1].strip() for l in s.splitlines() if re.match(r'^\|\s*MC-',l)])"` prints exactly `['MC-1']`.
- `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/references/checklist.md').read_text();s=t.split('## MC rules')[1];print([l.split('|')[1].strip() for l in s.splitlines() if re.match(r'^\|\s*MC-',l)])"` prints exactly `['MC-1']`.
- `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/references/checklist.md').read_text();s=t.split('## PF rules')[1].split('## MC rules')[0];print(len([l for l in s.splitlines() if re.match(r'^\|\s*PF-',l)]))"` prints `31` — the PF section is untouched.
- `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/SKILL.md').read_text();print(len(re.findall(r'^### PF-',t,re.M)), len(re.findall(r'^\*\*Replace with:\*\*',t,re.M)))"` prints `31 31` — no PF rule was disturbed.
- `grep -cF 'This catalog contains 31 rules in 6 numbered sections.' skills/proof-first/SKILL.md` prints `1`.
- `grep -cF 'references/completeness-audit.md' skills/proof-first/SKILL.md` prints `1`.
- `python3 -c "import pathlib;w=len(pathlib.Path('skills/proof-first/SKILL.md').read_text().split());print(w, int(w*1.3), 5000-int(w*1.3))"` prints a third number of at least 156 — the pointer bullet consumed no more than about 30 words of the measured 198-token margin.
- `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/references/completeness-audit.md').read_text();print(sorted(set(re.findall(r'MC-\d+',t))))"` prints exactly `['MC-1']` — no unallocated MC token, and no range expression producing a second token (trap 2).
- `grep -c '^## What this file does not do' skills/proof-first/references/completeness-audit.md` prints `1`.
- `ls skills/proof-first/references/` lists exactly `checklist.md`, `completeness-audit.md`, `deletion-test.md`, `worked-examples.md` — `artifact-patterns.md` is `03-03`'s deliverable and is not pre-created here.
  </acceptance_criteria>
  <done>`skills/proof-first/references/completeness-audit.md` exists, carries the attribution pointer exactly once, defines exactly one MC check whose ID is registered in `NUMBERING.md` and indexed in `checklist.md`'s new `## MC rules` section, `SKILL.md` points at it with a named opening condition, the whole-repository checker runs clean, and `SKILL.md`'s remaining word margin is measured and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Close the MC namespace's two enforcement gaps — `mc-catalog-id-drift` and `mc-rule-in-skill`, both proven live</name>
  <reversibility rating="reversible">Adding two violation codes and refactoring one comparison body is ordinary code change; both the self-test and the mutation test re-run the existing PF assertions unchanged, so a regression surfaces immediately rather than shipping.</reversibility>
  <files>tools/check_repo.py</files>
  <read_first>
- `tools/check_repo.py`'s module docstring in full — note that every violation-code entry states what it catches and its **Declared ceiling**; the two new entries must match that shape.
- `tools/check_repo.py` lines 216-262 — `PF_ID_RE`, `MC_ID_RE`, `SKILL_GLOB`, `strip_fences`, `split_sections`, `table_rows`.
- `tools/check_repo.py` lines 388-429 — `check_undefined_id` (the citation scanner the new drift check must NOT duplicate) and `run_id_checks`.
- `tools/check_repo.py` lines 843-960 — `RULE_HEADING_RE`, `parse_skill_catalog`, `parse_checklist`, `check_catalog_id_drift`, `check_catalog_count`. This is the block the new code sits beside and the shape it mirrors.
- `tools/check_repo.py` lines 1039-1062 — `CATALOG_CHECK_CODES`, `run_catalog_checks`, `ALL_CHECK_CODES`.
- `tools/check_repo.py` lines 1376-1420 — `_mutate_catalog_opening_rule_count` and the `MUTATIONS` list, for the tuple shape and the mutate-a-copy-of-the-real-repo discipline.
- `tools/check_repo.py` lines 1500-1560 and 1975-2040 — `_write`, `_good_numbering` (read this one carefully; it is the trap named in `<interfaces>`), and the `_single_opening_rule_*` fixture pair as the freshest fixture template.
- `tools/check_repo.py` lines 2090-2330 — `self_test()` in full: the scratch-root declarations, the `_write` calls, the code-set collection, the `bad_codes` union, the named per-code assertions, and the generic coverage loop.
- `skills/proof-first/references/completeness-audit.md` and `skills/proof-first/references/checklist.md` as written by Task 1 — the exact heading and table shapes the new extractors parse.
- This plan's `<interfaces>` block, especially the paragraph headed as the single highest-risk detail.
  </read_first>
  <action>
Add the two MC-namespace codes named in P3-08, each shipping the full three-part contract: the check
wired into its aggregator and registered in the code list, firing and silent self-test fixtures with
explicit named assertions, and a `MUTATIONS` entry proving discrimination against the real
repository. Reuse the existing parsing helpers; add no second Markdown table reader.

Parameterise the checklist reader (P3-06):
- Give `parse_checklist` a second parameter carrying the section name, defaulting to the existing PF
  section name so both current call sites keep working with no edit at all. Update its docstring to
  say it returns the IDs listed in the named section's table, and that the default preserves the PF
  behaviour every existing caller depends on.

Extract the shared three-way comparison (P3-07):
- Pull the "present in some, missing from others" loop out of `check_catalog_id_drift` into a module
  level helper taking the violation code string and the three labelled ID sets, and returning the
  same list of `(id, message)` tuples the current body produces. The emitted message text for the PF
  code must be byte-identical to today's, because `_mutate_catalog_id_drift` and the existing
  `catalog-id-drift` self-test assertion both still run against it unchanged.
- Rewrite `check_catalog_id_drift` to call the helper. Its behaviour, its code string and its message
  wording do not change.

Add `mc-catalog-id-drift`:
- Add an MC heading regex beside `RULE_HEADING_RE`, anchored at line start, matching a level-three
  heading followed by an MC ID and the identical em-dash-with-spaces separator. Add a
  `parse_completeness_audit(path)` function mirroring `parse_skill_catalog`'s shape — read the file,
  strip fenced blocks, return the ordered list of MC IDs its rule headings define. A heading defines a
  rule; a bracketed marker or a prose reference only cites one, and `check_undefined_id` already owns
  citations.
- Add `check_mc_catalog_id_drift(allocated, repo_root)` iterating `SKILL_GLOB` the same way
  `check_catalog_id_drift` does, and for each skill folder comparing three sets through the shared
  helper: the MC subset of `NUMBERING.md`'s Allocated IDs filtered with `MC_ID_RE`, the MC IDs
  `references/completeness-audit.md` defines by heading, and the MC IDs
  `references/checklist.md`'s `## MC rules` section lists.
- **Return no violations when `references/completeness-audit.md` does not exist**, before computing
  any set. Re-read the highest-risk paragraph in this plan's `<interfaces>` block for why: the shared
  `_good_numbering()` fixture allocates two MC rows and no existing fixture root ships a completeness
  audit file, so an implementation without this guard fires on the known-good fixture and breaks the
  whole self-test suite. State this early return and its reason in the function's own docstring, as a
  declared ceiling, not as an accident.

Add `mc-rule-in-skill`:
- Add `check_mc_rule_in_skill(repo_root)` scanning each path matching `SKILL_GLOB` for lines matching
  the new MC heading regex, and emitting one violation per match naming the file, the ID, and the fact
  that AUD-02 requires every MC rule to be defined in the completeness-audit reference file rather
  than blended into the prose catalog. This is the structural half of AUD-02 turned into a build
  failure instead of a convention.

Wire both:
- Append both code strings to `CATALOG_CHECK_CODES` and both calls to `run_catalog_checks`.
  `ALL_CHECK_CODES` concatenates that list already and needs no edit.
- Add a docstring entry for each new code in the module docstring's violation-code block, each
  stating what it catches and its declared ceiling — for the drift code, that it compares definitional
  agreement across three files and says nothing about whether a cited ID is valid, which is
  `undefined-id`'s job; for the in-skill code, that it matches heading shape only and cannot tell a
  rule definition from a heading that merely looks like one.

Self-test fixtures (one firing, one silent, for each code):
- Add fixture builders beside the existing ones: a numbering fixture allocating a small MC set inside
  the Metric block, a good completeness-audit fixture defining exactly those IDs by heading, a bad one
  omitting one of them, a matching MC checklist fixture, and a skill fixture carrying an MC-shaped
  rule heading for the in-skill code's bad case. Keep every fixture self-contained and small; do not
  reuse the real repository's 8-ID content.
- Declare the new scratch roots beside the existing ones, `_write` each fixture set under the real
  `skills/proof-first/...` layout so `SKILL_GLOB` matches one level below `skills/`, collect each
  root's code set the same way every other root does, and union every new bad root's codes into
  `bad_codes` so the generic coverage loop picks the new codes up.
- Add explicit named assertions in the same style as the opening-rule-count pair: each new code must
  be absent from its good root's codes and present in its bad root's codes, with a distinct printed
  FAIL message per direction.

Mutations (one per code, against a copy of the real repository):
- `mc-catalog-id-drift`: delete the single MC data row from `references/checklist.md`'s `## MC rules`
  table in the copied tree, mirroring `_mutate_catalog_id_drift`'s shape for the PF namespace.
- `mc-rule-in-skill`: insert one MC-shaped rule heading into the copied `skills/proof-first/SKILL.md`.
  Because `check_mc_catalog_id_drift` compares the registry, the audit file and the checklist — and
  never `SKILL.md` — this mutation fires the in-skill code alone and cannot be confused with the drift
  code.
- Register both as `MUTATIONS` tuples with a description stating exactly what the mutation does.

Add no third-party import. The file stays standard-library only.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, any line beginning "FAIL", or the printed verified-code list omitting either mc-catalog-id-drift or mc-rule-in-skill</fails_when>
    <automated>python3 tools/check_repo.py --mutation-test</automated>
    <fails_when>non-zero exit, any line beginning "mutation-test FAIL", any line beginning "mutation-test FIRE-ONLY", or a CONTROL line reporting more than 0 violations</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py --self-test && python3 tools/check_repo.py --mutation-test && python3 tools/check_repo.py` — the exact CI job order from `.github/workflows/ci.yml` — exits 0 end to end.
- `python3 tools/check_repo.py --mutation-test | grep -c '^mutation-test OK:'` prints `24`.
- `python3 tools/check_repo.py --mutation-test | tail -3 | grep -cF 'PASS: 24 codes discrimination-proven'` prints `1`.
- `python3 tools/check_repo.py --mutation-test | grep -c 'CONTROL: 0 violations'` prints `1`.
- `python3 tools/check_repo.py --self-test | tr ',' '\n' | grep -c 'mc-catalog-id-drift\|mc-rule-in-skill'` prints `2`.
- `python3 -c "import re,pathlib;t=pathlib.Path('tools/check_repo.py').read_text();print(len(re.findall(r'MUTATIONS\s*=\s*\[',t)), t.count(\"('mc-catalog-id-drift',\"), t.count(\"('mc-rule-in-skill',\"))"` prints `1 1 1`.
- `python3 -c "import re,pathlib;t=pathlib.Path('tools/check_repo.py').read_text();m=re.search(r'^def parse_checklist\(([^)]*)\):',t,re.M).group(1);print(m)"` prints a signature containing two parameters, the second carrying a default value.
- `python3 -c "import ast,sys;src=open('tools/check_repo.py').read();tree=ast.parse(src);mods={n.module.split('.')[0] for n in ast.walk(tree) if isinstance(n,ast.ImportFrom) and n.module}|{a.name.split('.')[0] for n in ast.walk(tree) if isinstance(n,ast.Import) for a in n.names};print(sorted(mods));sys.exit(0 if mods <= {'argparse','re','shutil','sys','tempfile','pathlib'} else 1)"` exits 0 — the checker is still standard-library only.
- Deleting `references/completeness-audit.md` from a scratch copy and running the checker produces no `mc-catalog-id-drift` violation, verified by `D=$(mktemp -d) && cp -R LICENSE NUMBERING.md NOTICES.md README.md examples tools skills "$D"/ && rm "$D"/skills/proof-first/references/completeness-audit.md && (cd "$D" && python3 tools/check_repo.py | grep -c 'mc-catalog-id-drift' || true)` printing `0` — the absence-is-not-failure guard holds.
- `git diff --stat` shows `tools/check_repo.py` as the only modified file in this task.
  </acceptance_criteria>
  <done>Two new violation codes are wired, self-tested in both directions, and proven to discriminate against mutated production content, taking the mutation test from 22 to 24 codes discrimination-proven with the control copy still clean; `parse_checklist` is parameterised without touching either existing call site; and both drift checks now share one comparison body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository Markdown → `tools/check_repo.py` parsers | Locally-authored, arbitrarily-shaped Markdown crosses into two new parsers with no schema library behind them. |
| a new reference file → an installing harness | `references/completeness-audit.md` ships inside the skill folder and is read by every target harness that installs the skill; a defect surfaces on a user's machine, not as a local error. |
| paraphrased qualification-methodology concepts → published repository | MC rule prose crosses from a proprietary, contested, trademarked framework family into an MIT-licensed public repository. |
| the already-proven PF check path → a shared refactored helper | A comparison body that 22 green mutation runs depend on is being moved behind a new abstraction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | a new check named as covered that cannot fire | high | mitigate | Both new codes ship the full D-34 triad: wired into `run_catalog_checks` and `CATALOG_CHECK_CODES`, a firing and a silent self-test fixture with a distinct named assertion each way, and a `MUTATIONS` entry proving discrimination against a copy of the real repository. An acceptance criterion counts the `mutation-test OK` lines at exactly 24 so a silently-dropped mutation is visible, and a second criterion asserts no code is reported FIRE-ONLY. |
| T-03-02 | Denial of Service | `self_test()`'s existing fixture roots | high | mitigate | `_good_numbering()` already allocates MC-1 and MC-5 with no completeness-audit file, so an unguarded `mc-catalog-id-drift` would fire on every known-good root and disable the entire self-test suite. The plan requires an explicit absence-early-return, states the reason in the function's own docstring, and proves it with a scratch-copy acceptance criterion that deletes the file and asserts the code stays silent. |
| T-03-03 | Tampering | MC rule prose in `references/completeness-audit.md` | high | mitigate | Every MC concept traces to a source already listed in `SOURCES.md`'s qualification-checklist table; a concept with no listed source does not ship without adding its row in the same change. The reproduction boundary is a semantic judgment `SOURCES.md:18-22` states no tool in this stack performs, so this task carries a `<human-check>` reading the file against that boundary. The residual ordered-list tension is recorded as flagged assumption A3-01 and routed to Phase 6's LEG-04 gate rather than silently accepted. |
| T-03-04 | Tampering | the refactored `check_catalog_id_drift` | medium | mitigate | The extracted helper must emit byte-identical message text for the PF code, and the existing `catalog-id-drift` self-test assertion and `_mutate_catalog_id_drift` mutation both re-run unchanged inside this task's own verify block — a regression fails the task rather than shipping. |
| T-03-05 | Tampering | new parsers reading repository paths | low | mitigate | `parse_completeness_audit` and the parameterised `parse_checklist` extract only ID tokens matched by an anchored regex and never treat an extracted value as a path. Path construction stays on the fixed one-level `SKILL_GLOB` pattern plus literal `references/` filenames, never a caller-supplied path; any future path-valued field reuses `_carrier_is_repo_relative` rather than a new, weaker check. |
| T-03-06 | Information Disclosure | worked figures inside the new reference file | low | accept | Every party, person and figure in `examples/deal-brief.md` is invented and the file says so. `unlisted-figure` already scans `skills/` recursively, so any currency amount, percentage or ISO date written into `completeness-audit.md` must trace to a row in that fictional table, and a real figure cannot enter unnoticed. |
| T-03-SC1 | Tampering | npm / pip / cargo installs | low | accept | This plan installs no package in any ecosystem. `03-RESEARCH.md`'s Package Legitimacy Audit records N/A for the whole phase and `tools/check_repo.py` stays standard-library only, asserted by an acceptance criterion in Task 2. No install task exists, so no package-legitimacy checkpoint is required. |
</threat_model>

<verification>
- `python3 tools/check_repo.py --self-test && python3 tools/check_repo.py --mutation-test && python3 tools/check_repo.py` — the exact CI job order — runs green.
- Twenty-four violation codes are discrimination-proven by `--mutation-test`, up from twenty-two, with the control copy still reporting 0 violations and no code reported fire-only.
- `skills/proof-first/references/` holds exactly four files; `artifact-patterns.md` is not pre-created.
- `SKILL.md`'s measured word count and estimated-token margin after Task 1 are recorded in the SUMMARY as numbers, not as an assertion that the budget is fine.

**Honest verification statement (03-RESEARCH Pitfall 4).** Of this plan's three requirements:
- **AUD-02** is mechanically verified in full by this plan. Its "own reference file, own namespace" half is proven by `range-id`, `dup-id` and `revived-id` on the MC row plus the new `mc-rule-in-skill` code, all CI-enforced.
- **MOD-05**'s definitional-agreement half is mechanically verified by `mc-catalog-id-drift`; its citation-validity half was already live via `undefined-id`. Its live-session half — that a real check-mode conversation never invents a number — is model behaviour no file-reading checker can observe and stays permanently manual.
- **AUD-01** is only partially addressed here: one of eight dimensions, and only its structural half. The content-quality half (does this question read as a genuine completeness finding rather than an invented deficiency) is prose-authoring correctness, verifiable only by the `<human-check>` above and by end-of-phase UAT.
The `<human-check>` in Task 1 is harvested at end-of-phase into `03-UAT.md` for the developer's batch review, matching `workflow.human_verify_mode: end-of-phase`.
</verification>

<success_criteria>
- One completeness check, MC-1, exists in a real `references/completeness-audit.md` carrying the attribution pointer exactly once, with exactly one constructive half and a trigger paragraph naming when to open the file.
- The MC ID set is identical across `NUMBERING.md`, `references/completeness-audit.md` and `references/checklist.md`, and a new CI-enforced code fails the build if that ever stops being true.
- An MC rule heading inside any `SKILL.md` is a build failure, so AUD-02's never-blended requirement is mechanical rather than conventional.
- The whole-repository checker, its self-test and its mutation test all run green, with two more codes discrimination-proven than before.
- The eight-ID MC map, the four check-mode report sections and their fixed order, and the four artifact-family headings are frozen by an explicit human decision before any of them is depended upon.
</success_criteria>

<output>
Create `.planning/phases/03-completeness-audit-artifact-patterns/03-01-SUMMARY.md` when done.

In its frontmatter, `requirements-completed:` records **implementation, not verification** — this is
the field that once propagated a false "CAT-10 Complete" into `.planning/REQUIREMENTS.md`. List
`[AUD-02, MOD-05]` only. Do **not** list AUD-01: this plan authors one of its eight dimensions and
none of its content-quality half, and the SUMMARY must say so in prose rather than letting the field
imply otherwise. State explicitly in the SUMMARY body that MOD-05's live-session half and AUD-01's
content half are model behaviour, remain unverified by any check in this repository, and are
provisional pending end-of-phase UAT.

Record in the SUMMARY, as numbers rather than assertions: `SKILL.md`'s word count and estimated-token
margin after Task 1, and the `mutation-test` discrimination-proven total.
</output>
