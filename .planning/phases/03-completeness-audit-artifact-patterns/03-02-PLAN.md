---
phase: 03-completeness-audit-artifact-patterns
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - skills/proof-first/references/completeness-audit.md
  - skills/proof-first/references/checklist.md
  - skills/proof-first/references/worked-examples.md
  - NUMBERING.md
  - tools/check_repo.py
autonomous: true
requirements: [AUD-01, AUD-03, MOD-05]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A writer opening `skills/proof-first/references/completeness-audit.md` reads eight document-level completeness checks — one for each dimension block NUMBERING.md reserves — each carrying a diagnostic statement and exactly one `**Replace with:**` line."
    - "All eight MC IDs are present in NUMBERING.md's Allocated IDs table, in the reference file's rule headings, and in `references/checklist.md`'s `## MC rules` table, and `mc-catalog-id-drift` fails the build if any one of the three sets diverges."
    - "`references/worked-examples.md` carries one keyed section per allocated MC ID, in the identical shape its PF pairs already use, and every fact in every MC pair comes from `examples/deal-brief.md`."
    - "`references/completeness-audit.md` states the stated-count sentence in its frozen template with numbers that match the registry, and two new CI-enforced codes fail the build when that sentence is absent or when its numbers disagree."
    - "A writer can ask for the completeness audit on its own and the reference file states exactly what that standalone run returns and what its separate verdict looks like."
    - "`--mutation-test` reports 26 codes discrimination-proven with a CONTROL line reading 0 violations, and no code is reported fire-only."
    - "No file under `skills/` cites an MC token outside the eight allocated IDs, so `undefined-id` stays silent with no suppression."
  prohibitions:
    - "MUST NOT reproduce a contiguous run of any framework source's own wording, any of its ordered lists in source order, or a term coined by a source adopted here as this repository's own label — every dimension's audit question is written in this repository's own words at the level of generality a SOURCES.md-listed source states publicly."
    - "MUST NOT restate what a methodology dimension conceptually means; each MC body is a question asked of a document, grounded in `examples/deal-brief.md`'s own facts."
    - "MUST NOT reorder, rename, or re-column NUMBERING.md's `## MC reserved blocks` table, and MUST NOT allocate an MC ID anywhere other than its dimension block's range start."
    - "MUST NOT allocate, deprecate, renumber, or retitle any `PF-` rule, and MUST NOT change the stated PF catalog count."
    - "MUST NOT make an unmeasured claim about this skill's own effect anywhere in the authored content."
    - "MUST NOT present a completeness verdict as deal qualification, as a score, or as a substitute for a human's judgment."
  artifacts:
    - path: "skills/proof-first/references/completeness-audit.md"
      provides: "All eight MC checks, the stated-count sentence, and the standalone-run section"
      contains: "This audit contains 8 checks across 8 dimensions."
      min_lines: 60
    - path: "skills/proof-first/references/worked-examples.md"
      provides: "One keyed worked pair per allocated MC ID"
      contains: "## MC-1"
    - path: "skills/proof-first/references/checklist.md"
      provides: "The complete MC ID index"
      contains: "| MC-36 |"
    - path: "NUMBERING.md"
      provides: "All eight MC Allocated IDs rows"
      contains: "| MC-36 |"
    - path: "tools/check_repo.py"
      provides: "mc-count-unstated and mc-count-mismatch, the MC stated-count guard"
      contains: "mc-count-mismatch"
  key_links:
    - from: "skills/proof-first/references/completeness-audit.md"
      to: "NUMBERING.md"
      via: "eight rule headings, eight Allocated IDs rows, one per dimension block"
      pattern: "### MC-\\d+ — "
    - from: "skills/proof-first/references/worked-examples.md"
      to: "skills/proof-first/references/completeness-audit.md"
      via: "one keyed example section per MC rule whose statement lives in the audit file"
      pattern: "## MC-\\d+"
    - from: "tools/check_repo.py"
      to: "skills/proof-first/references/completeness-audit.md"
      via: "check_mc_count reads the stated-count sentence and compares it to the registry"
      pattern: "This audit contains"
---

<objective>
Fill the MC namespace out to the eight dimensions `NUMBERING.md` reserves, give each one a worked
✗/✓ pair keyed the way the PF pairs already are, state what a standalone audit run returns, and close
the stated-count anti-hallucination gap with two new CI-enforced codes.

Purpose: `03-01` proved the pipeline on one dimension and left seven unbuilt. This plan is the
expansion, and it is deliberately the only plan in the phase that authors bulk MC content — the
registry, the index, the reference file and the examples file all move together in one wave so no
intermediate state ever has an allocated ID with no definition, which is the "check that cannot fire"
defect class Phase 1 spent three gap-closure plans removing.

Output: seven more MC checks and their registry and checklist rows; eight keyed MC worked pairs; the
standalone-audit section that answers AUD-03; the frozen stated-count sentence at its final values;
and two more violation codes, each with firing and silent fixtures and a registered mutation.
</objective>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-01-PLAN.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-01-SUMMARY.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-RESEARCH.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-PATTERNS.md
@NUMBERING.md
@NOTICES.md
@SOURCES.md
@examples/deal-brief.md
@tools/check_repo.py
@skills/proof-first/references/completeness-audit.md
@skills/proof-first/references/checklist.md
@skills/proof-first/references/worked-examples.md
</context>

<interfaces>
<!-- Contracts the executor needs. Read 03-01-PLAN.md's own <interfaces> block first — every -->
<!-- helper, trap and contract listed there is still live against this plan. What follows is -->
<!-- additional to it, not a replacement for it. -->

**Frozen decisions this plan complies with — do not re-derive them, read `03-01-PLAN.md`'s
`<decisions>` table:** P3-01 (the eight-ID allocation map, reproduced below), P3-04 (the stated-count
sentence template), P3-05 (MC worked pairs go in `worked-examples.md`), P3-08 (this plan owns
`mc-count-unstated` and `mc-count-mismatch`), P3-10 (threat IDs continue at `T-03-07`; this plan owns
`T-03-07` through `T-03-11` and `T-03-SC2`), P3-12 (the `## MC reserved blocks` table is untouched).

**The MC allocation map (P3-01), authoritative — no plan invents an ID:**

| ID | Dimension block | Title | Authored in |
|----|-----------------|-------|-------------|
| MC-1 | Metric | Name the buyer's own measure and where its baseline came from | 03-01 |
| MC-6 | Economic Buyer | Name the person who signs and the priority they stated in their own words | this plan, Task 1 |
| MC-11 | Decision Criteria | Mirror the buyer's own stated evaluation criteria and their weights | this plan, Task 2 |
| MC-16 | Decision Process | State the steps, dates, and people the buyer's evaluation runs through | this plan, Task 2 |
| MC-21 | Paper Process | State the reviews the document must clear before a signature is possible | this plan, Task 2 |
| MC-26 | Pain | Name the cost the buyer already states, in the buyer's own words | this plan, Task 2 |
| MC-31 | Champion | Name the person inside the buyer who carries this internally | this plan, Task 2 |
| MC-36 | Competition | State the alternatives the buyer is weighing, without asserting what a rival cannot do | this plan, Task 2 |

**The frozen stated-count sentence template (P3-04)**, on its own line in
`references/completeness-audit.md`, at its final values once all eight rows exist:

```
This audit contains 8 checks across 8 dimensions.
```

The first number is the count of MC rows in `NUMBERING.md`'s Allocated IDs table; the second is the
count of distinct MC dimension blocks those rows fall into, read from `parse_numbering`'s `mc_ranges`.

**Grounding material in `examples/deal-brief.md`, already canonical — invent nothing.** Each row below
names facts an MC body or worked pair may cite. Bare counts and names are unconstrained; every `$`
amount, `%` value and `YYYY-MM-DD` date must match a Canonical figures value exactly.

| Dimension | Facts available |
|---|---|
| Metric | The economic buyer's stated current annual run rate; the settlement batch job's required window in hours; the recorded fact that the overrun has never been instrumented, so no baseline exists. |
| Economic Buyer | Diane Osoria, Chief Financial Officer, and her three stated priorities in her own words. |
| Decision Criteria | The three-row weighted criteria table (technical, commercial, security) and the five scored RFP questions with their own separate weights. |
| Decision Process | Three shortlisted bidders; the submission date; the regulatory examination window in months; Tom Weatherly's stated same-five-questions-for-everyone process. |
| Paper Process | Three sequential reviews — security owned by Priya Raghunathan at 15 business days, procurement owned by Tom Weatherly with no duration stated by the buyer, legal owned by Gina Almeida at 10 business days. |
| Pain | The unmeasured settlement batch overrun; Oracle licensing as a rising share of run rate; the vSphere estate at capacity; manual failover across the VM estate. |
| Champion | Marcus Feld, Vice President of Infrastructure, and his verbatim discovery quote. |
| Competition | Ardent Digital as both rival bidder and incumbent; Vantage Nine Consulting; the incumbent's share of the estate; Priya Raghunathan's stated preference for extending the incumbent contract. |

**`references/worked-examples.md`'s opening paragraph is currently false for MC rules.** It reads that
"the rule statements themselves, and each rule's `**Replace with:**` line, live in `SKILL.md`". MC
rule statements live in `references/completeness-audit.md`, not `SKILL.md`. That paragraph must be
corrected in the same task that adds the first MC pair, or the file asserts something untrue about its
own content — the exact class of self-contradiction `02-09` had to close in the README.

**The absence guard, again.** `check_mc_count` must return no violations when
`references/completeness-audit.md` does not exist, for the same reason `check_mc_catalog_id_drift`
must: no existing self-test fixture root ships that file, and an unguarded `mc-count-unstated` would
fire on every one of them and disable the suite. State the early return and its reason in the
function's own docstring.

**Traps still live (full statements in `03-01-PLAN.md`'s `<interfaces>`):** `unlisted-figure` scans
`skills/` recursively and matches `$` amounts, `NN%` values and ISO dates against the Canonical
figures table; a currency token absorbing a trailing comma reads as unmatched, so reword the sentence
rather than the checker. `undefined-id` reads a range expression written as a low ID, a hyphen and a
high ID on one line as two separate citation tokens. Any literal an acceptance criterion greps for
must sit entirely on one physical line.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a second dimension travels every layer including the examples file" — MC-6 only</name>
  <reversibility rating="costly">MC-6 is a published rule number under Phase 1's deprecation-never-deletion rule, so renumbering it later strands a citation rather than refactoring one. The one-way decision itself — the eight-ID allocation map this ID comes from — was already opened and confirmed at `03-01`'s blocking-human `checkpoint:decision`, so this task executes an approved choice rather than making a new one.</reversibility>
  <precondition>`references/completeness-audit.md` exists with exactly one MC rule heading, `references/checklist.md` has a `## MC rules` section with exactly one row, `NUMBERING.md`'s Allocated IDs table has exactly one MC row, and `python3 tools/check_repo.py` prints 0 violations — all four are `03-01`'s output and all four are asserted by this task's first acceptance criterion before anything is written.</precondition>
  <files>skills/proof-first/references/completeness-audit.md, skills/proof-first/references/checklist.md, skills/proof-first/references/worked-examples.md, NUMBERING.md</files>
  <read_first>
- `skills/proof-first/references/completeness-audit.md` in full as `03-01` wrote it — the trigger paragraph, the attribution pointer line, the inherited-ID-order note, MC-1's own body and constructive line, and the closing `## What this file does not do` section. MC-6 copies MC-1's shape exactly.
- `skills/proof-first/references/worked-examples.md` in full — the opening paragraph (which this task corrects), and the `## PF-0.1` section's exact shape: a level-two heading carrying the bare rule ID, then one ✗ line and one ✓ line, each a quoted sentence.
- `examples/deal-brief.md` § People and roles and § Economic buyer stated priorities — Diane Osoria's role and her three quoted priorities, which are the only grounding MC-6 needs.
- `NUMBERING.md` § Allocated IDs and § MC reserved blocks — the Economic Buyer block's range start is MC-6.
- `SOURCES.md` lines 11-22 — the reproduction boundary this task's prose is written against.
- `03-01-PLAN.md`'s `<interfaces>` and `<decisions>` blocks in full.
  </read_first>
  <action>
Author the Economic Buyer dimension, MC-6, through all four layers, and stop. Do not author MC-11 or
anything later; Task 2 owns them, and citing an unallocated MC token before its registry row exists is
the forward-reference failure `02-01` and `02-02` both hit.

`skills/proof-first/references/completeness-audit.md`:
- Add MC-6 after MC-1, under a level-three heading whose text is the ID, a space, an em dash, a space,
  and the title from the allocation map — byte-identical in separator to MC-1's heading and to every
  PF heading, because the checker's MC heading regex matches that separator and nothing else.
- Its body is two to four sentences stating what a document missing this dimension looks like, written
  as a direct instruction about a document. Ground it in the deal brief: a proposal addressed to the
  organisation in the abstract, with no named person who signs and no priority stated in that person's
  own words, has not named its economic buyer. Never restate what the dimension conceptually means in
  the methodology it derives from — that distinction is the entire paraphrase lever this phase has
  (flagged assumption A3-01 in `03-01-PLAN.md`).
- Follow the body with exactly one `**Replace with:**` line naming the concrete substitution: the
  named role and the priority that role stated in their own words, or the existing GAP marker form
  carrying MC-6 as the raising rule token. Do not invent a fourth bracket form.

`NUMBERING.md`:
- Append one `## Allocated IDs` row for MC-6, keeping the table's ascending sort. `Defined in` is the
  completeness-audit filename, matching MC-1's row; `Added in` is `v0.1.0`.
- Change nothing else.

`skills/proof-first/references/checklist.md`:
- Append one row to the `## MC rules` table for MC-6, ascending. Touch nothing else in the file.

`skills/proof-first/references/worked-examples.md`:
- Correct the opening paragraph so it is true of both namespaces: the ✗/✓ contrast lives here, and
  each rule's own statement and constructive line live in `SKILL.md` for the prose catalog and in
  `references/completeness-audit.md` for the completeness audit. Keep the existing sentence stating
  that every fact in every example comes from the deal brief — that is still true and still
  load-bearing.
- Append a `## MC-6` section in the identical shape the PF sections use. Because MC checks are
  document-level rather than sentence-level, the ✗ line is a short passage a document would actually
  contain and the ✓ line is that passage repaired, or the same passage carrying the MC-6 GAP marker
  where the named buyer and stated priority would have gone. Every fact comes from the deal brief.
- Do not add a `## MC-1` section in this task; Task 2 adds the remaining seven pairs including MC-1's,
  so that the pair set and the final count land together.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output</fails_when>
    <human-check>
Read MC-6's body and its worked pair against `SOURCES.md`'s reproduction boundary and confirm what no
tool here can: that the body asks a question of a document grounded in the deal brief's own facts,
rather than restating what the dimension conceptually means in the methodology it derives from, and
that the ✗ passage is one a real proposal would plausibly contain rather than a straw example built
to make the rule look necessary.
    </human-check>
  </verify>
  <acceptance_criteria>
- Before any edit, `python3 tools/check_repo.py` prints `check_repo: 0 violations` — the precondition holds and this task starts from `03-01`'s green state.
- `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/references/completeness-audit.md').read_text();print(re.findall(r'^### (MC-\d+) — ',t,re.M))"` prints exactly `['MC-1', 'MC-6']`.
- `grep -cF '**Replace with:**' skills/proof-first/references/completeness-audit.md` prints `2` — heading count and constructive-half count stay in parity, the check `tools/check_repo.py` does not itself perform.
- `python3 -c "import re,pathlib;t=pathlib.Path('NUMBERING.md').read_text();s=t.split('## Allocated IDs')[1].split('\n## ')[0];print([l.split('|')[1].strip() for l in s.splitlines() if re.match(r'^\|\s*MC-',l)])"` prints exactly `['MC-1', 'MC-6']`.
- `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/references/checklist.md').read_text();s=t.split('## MC rules')[1];print([l.split('|')[1].strip() for l in s.splitlines() if re.match(r'^\|\s*MC-',l)])"` prints exactly `['MC-1', 'MC-6']`.
- `grep -c '^## MC-' skills/proof-first/references/worked-examples.md` prints `1`.
- `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/references/worked-examples.md').read_text();print(sorted(set(re.findall(r'MC-\d+',t))))"` prints exactly `['MC-6']`.
- `grep -cF 'completeness-audit.md' skills/proof-first/references/worked-examples.md` prints at least `1` — the opening paragraph now tells the truth about where MC rule statements live.
- `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/SKILL.md').read_text();print(len(re.findall(r'^### PF-',t,re.M)), len(re.findall(r'^\*\*Replace with:\*\*',t,re.M)))"` prints `31 31` and `git diff --name-only` does not list `skills/proof-first/SKILL.md` — this task does not touch the token-budgeted file at all.
  </acceptance_criteria>
  <done>MC-6 exists in the reference file with one constructive half, is registered in `NUMBERING.md` and indexed in `checklist.md`, has a keyed worked pair in `worked-examples.md` whose opening paragraph now describes both namespaces truthfully, and the whole-repository checker runs clean.</done>
</task>

<task type="auto">
  <name>Task 2: The remaining six dimensions, the remaining seven worked pairs, and the standalone-run section</name>
  <files>skills/proof-first/references/completeness-audit.md, skills/proof-first/references/checklist.md, skills/proof-first/references/worked-examples.md, NUMBERING.md</files>
  <read_first>
- `skills/proof-first/references/completeness-audit.md` as it stands after Task 1 — MC-1 and MC-6 are the shape the six new checks copy.
- `examples/deal-brief.md` in full, particularly § Pain points, § Timeline, § Inconvenient facts, § Customer source material (the scored RFP question table, the discovery quotes, the decision-criteria table and the paper-process paragraph), and the `## Canonical figures` table.
- The grounding-material table in this plan's `<interfaces>` block — it names, per dimension, exactly which deal-brief facts are available, so nothing has to be invented.
- `skills/proof-first/references/deletion-test.md` § `What this file does not do` — the refusal shape the completeness audit's own closing section already copies, for tone.
- `03-01-PLAN.md`'s `<decisions>` table, P3-02 and P3-03 — the check-mode section names the standalone-run section must agree with.
  </read_first>
  <action>
Finish the MC namespace at eight checks and eight worked pairs, and state what a standalone audit run
returns.

`skills/proof-first/references/completeness-audit.md` — add MC-11, MC-16, MC-21, MC-26, MC-31 and
MC-36 in ascending ID order after MC-6, each in the identical shape MC-1 and MC-6 established: the
level-three heading with the em-dash separator and the title from the allocation map, a two-to-four
sentence body stating what a document missing that dimension looks like, and exactly one
`**Replace with:**` line naming the concrete substitution or the GAP marker form carrying that rule's
own token. Specific authoring notes, all grounded in the deal brief:
- MC-11 mirrors the buyer's own criteria and their weights. The deal brief supplies a three-row
  weighted criteria table and a separate five-question scored table with its own weights; a document
  that reorganises those criteria into the vendor's preferred order, or that silently merges the two
  weighting schemes, has not mirrored them.
- MC-16 names the steps, dates and people the evaluation runs through. The brief supplies a
  submission date, a count of shortlisted bidders, an examination window in months, and a stated
  process of scoring every bidder against the same questions.
- MC-21 names the reviews that must clear before a signature is possible. The brief supplies three
  sequential reviews with named owners, two with stated durations in business days and one with no
  duration the buyer has stated — that third one is a live example of a dimension that is partly
  present and partly a gap, and the check's body should make clear that partial presence is reported,
  not rounded up to satisfied.
- MC-26 names the cost the buyer already states, in the buyer's own words. The brief's flagship pain
  is an overrun the buyer has never instrumented, so this check must not require a number that does
  not exist — a named pain with an explicitly stated missing baseline satisfies it, and inventing a
  figure to satisfy it is the failure the prose catalog's integrity rules already refuse.
- MC-31 names the person inside the buyer who carries this internally. The brief names a champion and
  quotes him verbatim.
- MC-36 states the alternatives the buyer is weighing. The brief names two rival bidders, one of them
  the incumbent with a stated share of the estate, and records a scored evaluator's stated preference
  for extending the incumbent contract. Word this check so it never asks a document to assert what a
  rival cannot do — the prose catalog already flags that as a review hazard, and this check must not
  pull in the opposite direction.

Add a `## Running the audit on its own` section to the same file, before the closing
`## What this file does not do` section, stating what AUD-03 promises: a writer can ask for this
audit alone; run against a document it returns the check-mode completeness section by its frozen
heading and a one-line verdict naming how many of the eight dimensions the document satisfies and
which it does not; it returns no prose findings and does not rewrite the document. Name the frozen
heading exactly as P3-02 froze it. Do not describe the structural-ordering pass here — that pass
belongs to the artifact-family conventions `03-03` authors, and `03-04` wires it into check mode.

`NUMBERING.md` — append the six remaining Allocated IDs rows in ascending order, `Defined in` matching
the existing MC rows, `Added in` `v0.1.0`. Change nothing else; the `## MC reserved blocks` table stays
exactly as it is (P3-12).

`skills/proof-first/references/checklist.md` — append the six remaining `## MC rules` rows in ascending
order. Touch nothing else.

`skills/proof-first/references/worked-examples.md` — append one keyed section per remaining MC ID,
including MC-1's, so the file ends with one section for each of the eight. Keep the sections in
ascending ID order after the existing PF sections. Each pair is document-level: the ✗ line is a short
passage a real presales document would contain, the ✓ line is that passage repaired or carrying the
right GAP marker. Every fact comes from the deal brief, and every `$` amount, `%` value or ISO date
must match a Canonical figures value exactly.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output</fails_when>
    <automated>python3 tools/check_repo.py --mutation-test</automated>
    <fails_when>non-zero exit, any line beginning "mutation-test FAIL", any line beginning "mutation-test FIRE-ONLY", or a CONTROL line reporting more than 0 violations</fails_when>
    <human-check>
Read all eight MC bodies end to end against `SOURCES.md`'s reproduction boundary and confirm three
things no tool in this project's stack can check: (1) no contiguous run of any source's own wording,
no ordered list in source order, and no source-coined term adopted here as this repository's own
label; (2) each body reads as a question asked of a document rather than as a definition of what the
dimension means; (3) MC-21 and MC-26 in particular report partial presence and an explicitly missing
baseline as findings, rather than demanding a fact the buyer has never supplied — which would put
this file in direct conflict with the prose catalog's refusal to invent numbers.
    </human-check>
  </verify>
  <acceptance_criteria>
- `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/references/completeness-audit.md').read_text();print(re.findall(r'^### (MC-\d+) — ',t,re.M))"` prints exactly `['MC-1', 'MC-6', 'MC-11', 'MC-16', 'MC-21', 'MC-26', 'MC-31', 'MC-36']`.
- `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/references/completeness-audit.md').read_text();print(len(re.findall(r'^### MC-',t,re.M)), len(re.findall(r'^\*\*Replace with:\*\*',t,re.M)))"` prints `8 8`.
- `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/references/completeness-audit.md').read_text();segs=re.split(r'^### MC-\d+.*$',t,flags=re.M)[1:];print(len(segs), sum(1 for s in segs if len(re.findall(r'^\*\*Replace with:\*\*\s*\S',s,re.M))!=1))"` prints `8 0` — every check has exactly one non-empty constructive half, none has two and none has none.
- `python3 -c "import re,pathlib;a=set(re.findall(r'^### (MC-\d+) — ',pathlib.Path('skills/proof-first/references/completeness-audit.md').read_text(),re.M));n=pathlib.Path('NUMBERING.md').read_text().split('## Allocated IDs')[1].split('\n## ')[0];nn=set(l.split('|')[1].strip() for l in n.splitlines() if re.match(r'^\|\s*MC-',l));c=pathlib.Path('skills/proof-first/references/checklist.md').read_text().split('## MC rules')[1];cc=set(l.split('|')[1].strip() for l in c.splitlines() if re.match(r'^\|\s*MC-',l));print(len(a),len(nn),len(cc));import sys;sys.exit(0 if a==nn==cc and len(a)==8 else 1)"` exits 0 and prints `8 8 8`.
- `python3 -c "import re,pathlib;print(sorted(set(re.findall(r'^## (MC-\d+)\s*$',pathlib.Path('skills/proof-first/references/worked-examples.md').read_text(),re.M)),key=lambda s:int(s[3:])))"` prints exactly `['MC-1', 'MC-6', 'MC-11', 'MC-16', 'MC-21', 'MC-26', 'MC-31', 'MC-36']`.
- `grep -c '^## ' skills/proof-first/references/worked-examples.md` prints `28` — twenty PF sections plus eight MC sections.
- `grep -c '^## Running the audit on its own' skills/proof-first/references/completeness-audit.md` prints `1`.
- `grep -cF 'Not affiliated with or endorsed by any framework rights-holder' skills/proof-first/references/completeness-audit.md` prints `1` — still exactly once after all the additions.
- `python3 -c "import re,pathlib,sys;ids=set().union(*[set(re.findall(r'MC-\d+',p.read_text())) for p in pathlib.Path('skills').rglob('*.md')]);print(sorted(ids,key=lambda s:int(s[3:])));sys.exit(0 if ids=={'MC-1','MC-6','MC-11','MC-16','MC-21','MC-26','MC-31','MC-36'} else 1)"` exits 0 — no unallocated MC token anywhere under `skills/`, and no range expression producing one.
- `git diff --name-only` does not list `skills/proof-first/SKILL.md`.
  </acceptance_criteria>
  <done>All eight MC checks exist with matching registry rows, checklist rows and worked pairs, the three ID sets are provably equal, the standalone-run section states what AUD-03 promises, and the full CI job order runs green.</done>
</task>

<task type="auto">
  <name>Task 3: The MC stated-count guard — the frozen sentence and the two codes that enforce it</name>
  <reversibility rating="costly">The stated-count sentence template is a literal string a CI check matches and Phase 5's linter may parse; changing its wording later means changing the check, the fixtures, the mutation and any artifact quoting it.</reversibility>
  <files>skills/proof-first/references/completeness-audit.md, tools/check_repo.py</files>
  <read_first>
- `tools/check_repo.py` lines 924-960 — `COUNT_SENTENCE_RE` and `check_catalog_count`. The MC version mirrors this function exactly: one regex, one scan for a matching line, one unstated violation when no line matches, one mismatch violation when the numbers disagree with the registry.
- `tools/check_repo.py` lines 268-300 — `parse_numbering`, specifically how `mc_ranges` is built, since the dimension count comes from it.
- `tools/check_repo.py` lines 1330-1420 — `_mutate_catalog_count_unstated` and `_mutate_catalog_count_mismatch`, the two mutators this task's pair copies.
- `tools/check_repo.py` lines 2098-2200 and 2280-2300 — the `count_good_root` / `count_unstated_root` / `count_mismatch_root` fixture trio and their named assertions. This is the exact three-root pattern to reproduce for MC.
- The check functions and fixtures `03-01` added, particularly `check_mc_catalog_id_drift`'s absence guard and its docstring — the same guard and the same docstring discipline apply here.
- This plan's `<interfaces>` block for the frozen sentence template and its two numbers' definitions.
  </read_first>
  <action>
Author the frozen stated-count sentence and the two codes that keep it honest, together, so no window
exists in which the sentence is present but unenforced.

`skills/proof-first/references/completeness-audit.md`:
- Add the stated-count sentence on its own line, byte-identical to the frozen template in this plan's
  `<interfaces>` block, near the top of the file where a reader meets it before the checks — the same
  placement `SKILL.md` uses for the PF catalog's own stated count. Its two numbers are both eight at
  this point.
- Add one short sentence beside it, in the file's own voice, stating that a number outside the stated
  total does not exist and must never be cited — the MC-namespace counterpart of the anti-hallucination
  instruction the prose catalog already carries. Keep it to one sentence; this file has no token budget
  pressure but it also has no room for restatement.

`tools/check_repo.py`:
- Add an anchored module-level regex beside `COUNT_SENTENCE_RE` matching the frozen MC template with
  its two numbers captured. Anchor it at both ends so a line that merely contains the phrase does not
  match.
- Add `check_mc_count(allocated, repo_root)` beside `check_catalog_count`. For each skill folder found
  through `SKILL_GLOB`, locate `references/completeness-audit.md`; **return no violations for that
  folder when the file does not exist**, and say so in the docstring as a declared ceiling, for the
  same reason `check_mc_catalog_id_drift` carries the same guard. When the file exists: scan its lines
  for one matching the regex; emit `mc-count-unstated` naming the file when none matches; otherwise
  compare the two captured numbers against the registry — the count of MC rows in the Allocated IDs
  table, and the count of distinct MC dimension blocks from `mc_ranges` that those rows fall into — and
  emit `mc-count-mismatch` naming both the stated pair and the registry pair when they disagree.
- Append both code strings to `CATALOG_CHECK_CODES` and the call to `run_catalog_checks`.
- Add a docstring entry for each new code in the module docstring's violation-code block, each stating
  what it catches and its declared ceiling — that it compares two integers against the registry and
  says nothing about whether the eight checks themselves are correct, useful, or distinct.

Self-test fixtures — reproduce the existing three-root count pattern for MC:
- A self-contained numbering fixture allocating a small MC set spread across two dimension blocks, a
  matching MC checklist section, and three completeness-audit fixtures: one carrying the correct
  sentence for that fixture's own numbers, one carrying no sentence at all, and one carrying the
  sentence with wrong numbers. Keep the fixture numbers different from the real repository's eight, so
  a fixture can never pass by coincidence.
- Declare the three scratch roots beside the existing ones, write each fixture set under the real
  `skills/proof-first/...` layout so `SKILL_GLOB` matches, collect each root's code set, and union the
  two bad roots' codes into `bad_codes`.
- Add three named assertions in the existing style: neither code fires on the good root;
  `mc-count-unstated` fires on the unstated root; `mc-count-mismatch` fires on the mismatch root.

Mutations — one per code, against a copy of the real repository:
- `mc-count-unstated`: delete the stated-count line from the copied `references/completeness-audit.md`.
- `mc-count-mismatch`: change one of the two numbers in that line in the copied file.
- Register both as `MUTATIONS` tuples with descriptions stating exactly what each does.

Add no third-party import.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, any line beginning "FAIL", or the printed verified-code list omitting either mc-count-unstated or mc-count-mismatch</fails_when>
    <automated>python3 tools/check_repo.py --mutation-test</automated>
    <fails_when>non-zero exit, any line beginning "mutation-test FAIL", any line beginning "mutation-test FIRE-ONLY", or a CONTROL line reporting more than 0 violations</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py --self-test && python3 tools/check_repo.py --mutation-test && python3 tools/check_repo.py` exits 0 end to end.
- `python3 tools/check_repo.py --mutation-test | grep -c '^mutation-test OK:'` prints `26`.
- `python3 tools/check_repo.py --mutation-test | tail -3 | grep -cF 'PASS: 26 codes discrimination-proven'` prints `1`.
- `python3 tools/check_repo.py --self-test | tr ',' '\n' | grep -c 'mc-count-unstated\|mc-count-mismatch'` prints `2`.
- `grep -cF 'This audit contains 8 checks across 8 dimensions.' skills/proof-first/references/completeness-audit.md` prints `1`.
- Changing one number in the sentence on a scratch copy fires the mismatch code, verified by `D=$(mktemp -d) && cp -R LICENSE NUMBERING.md NOTICES.md README.md examples tools skills "$D"/ && sed -i.bak 's/This audit contains 8 checks/This audit contains 7 checks/' "$D"/skills/proof-first/references/completeness-audit.md && rm -f "$D"/skills/proof-first/references/completeness-audit.md.bak && (cd "$D" && python3 tools/check_repo.py | grep -c 'mc-count-mismatch')` printing `1`.
- Deleting `references/completeness-audit.md` from a scratch copy produces no `mc-count-unstated` violation, verified by `D=$(mktemp -d) && cp -R LICENSE NUMBERING.md NOTICES.md README.md examples tools skills "$D"/ && rm "$D"/skills/proof-first/references/completeness-audit.md && (cd "$D" && python3 tools/check_repo.py | grep -c 'mc-count-unstated' || true)` printing `0`.
- `python3 -c "import ast,sys;src=open('tools/check_repo.py').read();tree=ast.parse(src);mods={n.module.split('.')[0] for n in ast.walk(tree) if isinstance(n,ast.ImportFrom) and n.module}|{a.name.split('.')[0] for n in ast.walk(tree) if isinstance(n,ast.Import) for a in n.names};print(sorted(mods));sys.exit(0 if mods <= {'argparse','re','shutil','sys','tempfile','pathlib'} else 1)"` exits 0.
- `git diff --name-only` does not list `skills/proof-first/SKILL.md`.
  </acceptance_criteria>
  <done>The completeness audit states its own total in the frozen template with numbers that match the registry, two new codes fail the build when that sentence is absent or wrong, both are self-tested in each direction and both are proven to discriminate against mutated production content, taking the mutation test to 26 codes discrimination-proven with the control copy still clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paraphrased qualification-methodology concepts → published repository | Seven more dimensions of prose cross from a proprietary, contested, trademarked framework family into an MIT-licensed public repository — this is the largest single volume of framework-derived content the phase ships. |
| repository Markdown → `tools/check_repo.py` parsers | A new stated-count parser reads a locally-authored line and turns two captured integers into a build verdict. |
| the completeness audit's own instructions → the prose catalog's integrity rules | Two shipped instruction sets now speak to the same model about the same document, and they can contradict each other. |
| a new reference file's examples → an installing harness | Eight more worked passages ship inside the skill folder and are read verbatim by every harness that installs it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | seven new MC bodies in `references/completeness-audit.md` | high | mitigate | Every dimension traces to the qualification-checklist source already listed in `SOURCES.md`, and every body is written as a question about a document grounded in `examples/deal-brief.md`'s own invented facts, never as a restatement of a source's definition. Task 2 carries a three-part `<human-check>` reading all eight bodies against `SOURCES.md:11-22`. The residual ordered-list tension is flagged assumption A3-01, routed to Phase 6 LEG-04 by `03-04`, not resolved here. |
| T-03-08 | Tampering | a completeness check that demands a fact the buyer never supplied | high | mitigate | MC-26's flagship grounding is a pain the buyer has never instrumented and MC-21's is a review step with no stated duration. A check worded to require a number in those cases would push the model toward inventing one, directly against the prose catalog's integrity rules. The action text requires both checks to treat an explicitly stated missing baseline and partial presence as satisfying findings, and the `<human-check>` asserts exactly that as its third question. |
| T-03-09 | Tampering | a new check named as covered that cannot fire | high | mitigate | Both count codes ship the full D-34 triad, with fixture numbers deliberately different from the real repository's eight so a fixture cannot pass by coincidence. The mutation-test OK line count is asserted at exactly 26 and a FIRE-ONLY line fails the task. |
| T-03-10 | Denial of Service | `self_test()`'s existing fixture roots | high | mitigate | `check_mc_count` returns no violations when `references/completeness-audit.md` is absent, guarded before any parsing and stated in its docstring, so the existing fixture roots that ship no audit file stay clean. An acceptance criterion deletes the file from a scratch copy of the real repository and asserts the code stays silent. |
| T-03-11 | Information Disclosure | eight worked passages in `references/worked-examples.md` | low | accept | Every party, person and figure in `examples/deal-brief.md` is invented and the file says so. `unlisted-figure` already scans `skills/` recursively, so any currency amount, percentage or ISO date in an MC pair must trace to a row in that fictional table. |
| T-03-SC2 | Tampering | npm / pip / cargo installs | low | accept | This plan installs no package in any ecosystem. `03-RESEARCH.md`'s Package Legitimacy Audit records N/A for the whole phase and `tools/check_repo.py` stays standard-library only, asserted by an acceptance criterion in Task 3. |
</threat_model>

<verification>
- `python3 tools/check_repo.py --self-test && python3 tools/check_repo.py --mutation-test && python3 tools/check_repo.py` — the exact CI job order — runs green.
- Twenty-six violation codes are discrimination-proven, up from twenty-four, with the control copy still reporting 0 violations and no code reported fire-only.
- The MC ID set is provably equal across all three registry-adjacent files at exactly eight IDs, one per dimension block.
- `skills/proof-first/SKILL.md` is untouched by this plan, so its measured token margin is unchanged from `03-01`'s recorded figure.

**Honest verification statement (03-RESEARCH Pitfall 4).** Of this plan's three requirements:
- **AUD-01**'s structural half — eight dimensions, eight registered IDs, eight defined checks, eight
  indexed rows — is mechanically verified in full by `mc-catalog-id-drift` and `mc-count-*`. Its
  content-quality half, whether each question reads as a genuine completeness finding rather than an
  invented deficiency, is prose-authoring correctness and is verifiable only by the two
  `<human-check>` blocks above and by end-of-phase UAT. It is **not** closed by this plan.
- **AUD-03** is only half addressed here: the shipped instruction text describing a standalone run
  exists and is grep-checkable. Whether a live session actually runs the audit alone and returns a
  separate verdict is model behaviour no file-reading checker can observe, and it is permanently
  manual.
- **MOD-05** gains a second definitional guard. Its live-session half stays permanently manual.
Both `<human-check>` blocks are harvested at end-of-phase into `03-UAT.md`, matching
`workflow.human_verify_mode: end-of-phase`.
</verification>

<success_criteria>
- Eight completeness checks exist, one per reserved dimension block, each with exactly one constructive half, and the three ID sets across the registry, the reference file and the checklist are provably identical.
- Eight keyed worked pairs exist in `worked-examples.md`, whose opening paragraph now describes both namespaces truthfully.
- The reference file states what a standalone audit run returns and what its separate verdict looks like.
- The audit states its own total in the frozen template, and a wrong or absent total is a build failure proven live by two new mutations.
- The full CI job order runs green with twenty-six codes discrimination-proven.
</success_criteria>

<output>
Create `.planning/phases/03-completeness-audit-artifact-patterns/03-02-SUMMARY.md` when done.

`requirements-completed:` records **implementation, not verification** — the field that once
propagated a false "CAT-10 Complete" into `.planning/REQUIREMENTS.md`. List `[AUD-01, MOD-05]`. Do
**not** list AUD-03: this plan ships only its instruction text, and whether a live session returns a
separate verdict is unobservable by any check in this repository. State plainly in the SUMMARY body
that AUD-01's content-quality half and AUD-03's live-behaviour half are both provisional pending
end-of-phase UAT, and name the two `<human-check>` blocks as the closure condition for the first.

Record the `mutation-test` discrimination-proven total as a number.
</output>
