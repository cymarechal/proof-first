---
phase: 03-completeness-audit-artifact-patterns
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - skills/proof-first/SKILL.md
  - README.md
  - .planning/REQUIREMENTS.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [AUD-03, MOD-03, MOD-04, MOD-05]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`skills/proof-first/SKILL.md` is trimmed of restatement before a single Phase 3 word is added to it, and the trimmed word count is measured and recorded rather than assumed."
    - "Check mode names the artifact family it is reading the document as before it reports any finding, and says so when no family fits."
    - "Check mode reports findings under four labelled sections in one fixed order — integrity flags, prose violations, completeness gaps, structural ordering — each printing its heading always and carrying the existing explicit no-findings line when it has nothing to report."
    - "A writer can ask for the completeness audit on its own, and `SKILL.md` states that the run returns the completeness section and its verdict alone, without the prose findings."
    - "`SKILL.md` names both new reference files in its `## Reference files` list, each with the named condition that requires opening it."
    - "Every one of the 31 PF rules, its constructive half, the stated catalog count, the attribution pointer, the register heading, the three marker bracket forms and the four review categories all survive the trim unchanged."
    - "`python3 tools/check_repo.py` reports 0 violations with `skill-token-budget-exceeded` silent, and the recorded estimated-token margin is at least 150."
    - "`README.md` no longer claims the two Phase 3 reference files do not exist, and its worked-pair count matches the file on disk."
    - "The MC dimension-order paraphrase tension is recorded as an open ledger entry addressed to Phase 6's LEG-04 gate, and every requirement whose closure needs a live model session is marked provisional in `.planning/REQUIREMENTS.md`."
  prohibitions:
    - "MUST NOT delete, merge, renumber or retitle any `PF-` rule, delete any `**Replace with:**` line, or change the stated catalog count, to make room in the token budget. The trim removes restatement only — a sentence whose content is already stated in full elsewhere in the same file."
    - "MUST NOT resolve the budget by re-wrapping lines, by raising a ceiling, by adding a KNOWN_OPEN_VIOLATIONS allowance, or by editing `tools/check_repo.py` at all. That file is not in this plan's scope."
    - "MUST NOT define a rule heading in either namespace inside `SKILL.md`; `mc-rule-in-skill` makes an MC heading there a build failure and the prose catalog is closed at 31 rules."
    - "MUST NOT instruct a structural-ordering finding to cite a rule number — no numbered namespace covers the artifact-family conventions."
    - "MUST NOT reorder `## Integrity flags` and `## Prose violations` relative to each other; Phase 2's D-20 and D-21 froze that order and `02-UAT.md` signed it off."
    - "MUST NOT publish a measured claim in `README.md`. No benchmark has run, and the repository makes measured claims or none."
    - "MUST NOT re-mark any requirement Complete in `.planning/REQUIREMENTS.md` on the strength of a SUMMARY's `requirements-completed` field — that field records implementation, not verification, and doing so once already propagated a false CAT-10 Complete."
  artifacts:
    - path: "skills/proof-first/SKILL.md"
      provides: "The Check-mode classification line, the four-section report order, the standalone-audit instruction, and both new reference pointers"
      contains: "## Completeness gaps"
    - path: "README.md"
      provides: "A Status section and layout tree that agree with the repository on disk"
      contains: "completeness-audit.md"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Provisional annotations on every Phase 3 requirement whose closure needs a live model session"
      contains: "records implementation, not verification"
  key_links:
    - from: "skills/proof-first/SKILL.md"
      to: "skills/proof-first/references/completeness-audit.md"
      via: "a reference pointer stating the named condition that requires opening the completeness audit"
      pattern: "references/completeness-audit.md"
    - from: "skills/proof-first/SKILL.md"
      to: "skills/proof-first/references/artifact-patterns.md"
      via: "a reference pointer stating the named condition that requires classifying the document"
      pattern: "references/artifact-patterns.md"
    - from: "README.md"
      to: "skills/proof-first/references/"
      via: "the Status list and the layout tree both describe the files actually on disk"
      pattern: "artifact-patterns.md"
---

<objective>
Buy the token budget back by removing restatement, then spend it on the four mode-level instructions
Phase 3 owes `SKILL.md` — the Check-mode classification line, the third and fourth report sections,
the standalone-audit run, and the second reference pointer — and finish by making the repository's own
documentation agree with what is now on disk.

Purpose: `SKILL.md` entered this phase with a measured margin of 198 estimated tokens, roughly 152
words, and Phase 3's mode instructions are projected at roughly 150. Phase 2 already hit this ceiling
once and needed a whole gap-closure plan to retrofit a trim after the fact. This plan does the trim
first, as its own gated step with a measured acceptance criterion, so the additions land inside a
re-measured known-good margin instead of pushing an already-tight file over.

Output: a trimmed `SKILL.md` carrying every rule it entered with plus four new mode-level
instructions; a `README.md` whose Status list and layout tree match the repository; a ledger entry
routing the MC dimension-order paraphrase tension to Phase 6's LEG-04 gate; and provisional
annotations on every Phase 3 requirement that a static checker cannot close.
</objective>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-01-PLAN.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-RESEARCH.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-PATTERNS.md
@README.md
@NOTICES.md
@skills/proof-first/SKILL.md
@skills/proof-first/references/completeness-audit.md
@skills/proof-first/references/artifact-patterns.md
</context>

<interfaces>
<!-- Read 03-01-PLAN.md's <interfaces> and <decisions> blocks first. What follows is additional. -->

**Frozen decisions this plan implements (from `03-01-PLAN.md`'s `<decisions>` table):** P3-02 (the
four report sections and their fixed order), P3-03 (the structural-ordering pass as its own labelled
section), P3-09 (sequential waves), P3-10 (threat IDs continue at `T-03-17`; this plan owns `T-03-17`
through `T-03-22` and `T-03-SC4`), plus `03-03-PLAN.md`'s P3-14 (a structural-ordering finding names
the family and the convention label and cites no rule number).

**The measured entry state, taken during this planning session (2026-09-14) against the
pre-Phase-3 file — `03-01` added roughly one pointer bullet on top of it and recorded its own
figure in `03-01-SUMMARY.md`. Read that recorded figure as the true starting point:**

| Metric | Value | Ceiling | Margin |
|---|---|---|---|
| Lines | 309 | 500 | 191 |
| Words | 3,694 | — | — |
| Estimated tokens (words x 1.3) | 4,802 | 5,000 | 198 tokens, about 152 words |

The line ceiling is comfortable and irrelevant. `check_skill_token_budget`
(`tools/check_repo.py:1020-1036`) estimates tokens as `word_count * 1.3` and is the binding
constraint. The measuring command, which is the one `02-07` used and the one to reuse verbatim:

```
python3 -c "import pathlib;w=len(pathlib.Path('skills/proof-first/SKILL.md').read_text().split());print(w, int(w*1.3))"
```

**The named trim candidates, each measured during planning, each pure restatement.** "Restatement"
here has one meaning: a sentence whose content is already stated in full elsewhere in the same file.
Nothing on this list is a rule, a constructive half, or a distinct instruction.

| Where | Words | Why it is restatement | Action |
|---|---|---|---|
| The marker-vocabulary paragraph beginning "A retained term under the third form is marked on its first occurrence only" (line 43 of the pre-Phase-3 file) | 53 | `PF-3.3`'s own body (line 207) states the first-occurrence rule and the both-markers-fire rule in full, in the rule that owns them. | delete |
| The paragraph beginning "The closed `REVIEW` category vocabulary" that closes the PF-2 section (line 189) | 45 | Each of the four mappings is stated inside the raising rule's own body at lines 167, 173, 179 and 185. | delete |
| The Check-mode paragraph beginning "Check mode never reports a contrast structure" (line 292) | 35 | `PF-5.1`, `PF-5.2` and `PF-5.3` each carry their own check-mode prohibition in their own bodies at lines 245, 251 and 257. | delete |
| The Check-mode paragraph beginning "Before emitting any rule citation, read" (line 294) | 29 | Line 29 states the citation prohibition and line 50 states the same reference pointer with the same opening condition. | delete |
| The trailing sentence of the marker-vocabulary register paragraph, "That register's full column shape is specified in Write mode below." (second sentence of line 45) | 11 | Write mode specifies the register's columns immediately below. | delete the sentence only |

**Do not delete the first sentence of that last paragraph.** It carries the literal
`## Unresolved before this document is sent`, and that is the **only** occurrence of the register
heading string anywhere in `SKILL.md`; `02-05`'s acceptance criterion greps for it and this plan's
does too.

Total on the list: 173 words. The additions are projected at about 150. If the executor's authored
additions run longer, find more restatement by the same test — never by deleting a rule.

**Invariants that must hold before and after every edit in this plan.** Each is an acceptance
criterion in both Task 1 and Task 2.

| Invariant | Command | Expected |
|---|---|---|
| 31 rules, 31 constructive halves | `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/SKILL.md').read_text();print(len(re.findall(r'^### PF-',t,re.M)), len(re.findall(r'^\*\*Replace with:\*\*',t,re.M)))"` | `31 31` |
| Stated catalog count intact | `grep -cF 'This catalog contains 31 rules in 6 numbered sections.' skills/proof-first/SKILL.md` | `1` |
| Attribution pointer, exactly once | `grep -cF 'Not affiliated with or endorsed by any framework rights-holder' skills/proof-first/SKILL.md` | `1` |
| Register heading survives | `grep -cF '## Unresolved before this document is sent' skills/proof-first/SKILL.md` | `1` |
| All four review categories survive | `grep -oE 'REVIEW \([a-z]+\)' skills/proof-first/SKILL.md \| sort -u \| wc -l` | `4` |
| Three marker bracket forms survive | `grep -c '^- `\[<rule>' skills/proof-first/SKILL.md` | `3` |
| Real heading count unchanged | `grep -c '^## ' skills/proof-first/SKILL.md` | `13` |
| No MC rule heading minted here | `python3 tools/check_repo.py` | `check_repo: 0 violations` |

The real-heading-count invariant matters because the four report section names are written **inline,
inside backticks**, exactly as `## Integrity flags` and `## Prose violations` already are. Writing one
at line start would create a real section in `SKILL.md` and change how `split_sections` reads the
file.

**The four report section names, in their frozen order (P3-02), as they must appear inline in the
Check-mode text:** `## Integrity flags`, `## Prose violations`, `## Completeness gaps`,
`## Structural ordering`.

**The existing Check-mode sentence that must be edited rather than duplicated** currently reads
"Both category headings always print." Extend it to cover four, in place. Do not add a second
always-print sentence.

**`gsd-tools windows append` signature, probed during planning:**
`--kind <kind> --phase <NN> --description "<text>"`, with `--file <path>` optional.

**Traps still live (full statements in `03-01-PLAN.md`'s `<interfaces>`):** `unlisted-figure` scans
`skills/` recursively; `undefined-id` scans `skills/`, `examples/` and `README.md` and validates every
`PF-#.#` and `MC-#` token against the registry; a literal an acceptance criterion greps for must sit
entirely on one physical line — `02-04` lost two anchors to an authored line-wrap and fixed it by
moving the wrap point, not the wording.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "the budget is bought, then spent" — the trim, then the Check-mode classification line only</name>
  <reversibility rating="costly">The trim removes shipped prose from a published skill file, and the Check-mode classification line is the sentence Phase 4's committed examples will show and Phase 5's linter will look for.</reversibility>
  <precondition>`python3 tools/check_repo.py` prints 0 violations, and `03-01-SUMMARY.md` records a measured word count and estimated-token margin for `skills/proof-first/SKILL.md`. Both are `03-01` through `03-03`'s output and the first is asserted before any edit.</precondition>
  <files>skills/proof-first/SKILL.md</files>
  <read_first>
- `skills/proof-first/SKILL.md` in full — every one of its 13 sections. This task deletes from four places in it and adds to one, and the invariant table in this plan's `<interfaces>` names exactly what must survive.
- This plan's `<interfaces>` block in full, twice — the trim-candidate table, the invariant table, and the inline-backtick rule for the report section names.
- `03-01-SUMMARY.md` — the recorded word count and margin after `03-01`'s pointer bullet. That figure, not the planning-session figure, is this task's true starting point.
- `skills/proof-first/references/artifact-patterns.md` § `Classifying the document` as `03-03` wrote it — the classification procedure and the no-family fallback this sentence points at rather than restates.
- `skills/proof-first/SKILL.md` line 263 — Write mode's existing assumed-family sentence. The new Check-mode sentence is its parallel, and it should read as the same instruction applied in the other mode, not as a second vocabulary.
  </read_first>
  <action>
Do the trim first and measure it, then add exactly one sentence and measure again. Do not begin
authoring any addition before the post-trim measurement has been taken and recorded — the whole shape
of this task exists because Phase 2 did those two steps in the other order and needed a gap-closure
plan to recover.

**Step 1 — trim.** Delete the four paragraphs and the one trailing sentence named in the
trim-candidate table in this plan's `<interfaces>`. Delete nothing else. After deleting, read the
surrounding paragraphs and fix only what the deletions broke: a dangling connective, a "below" or
"above" pointing at removed text, or a paragraph that now opens with a conjunction. Change no meaning
and add no compensating sentence — a compensating sentence is the restatement coming back under a
different name.

If the post-trim measurement in the acceptance criteria is not met, find further restatement by the
same single test: a sentence whose content is already stated in full elsewhere in the same file.
Candidates that have not been checked and may qualify: the Write-mode register illustration table,
whose two example rows demonstrate a shape the surrounding sentence already fully specifies; and the
five rule statements `02-07`'s tightening pass did not reach. Never reach for a rule, a
`**Replace with:**` line, the stated count, the marker grammar, or a distinct instruction.

**Step 2 — measure.** Run both measuring commands and record both numbers. Proceed only if the
post-trim gate in the acceptance criteria is met.

**Step 3 — add one sentence.** At the opening of the `## Check mode` section, before the existing
sentence that begins "Output is a report, never a corrected document", add one sentence stating that
check mode names the artifact family it is reading the document as before it reports any finding, and
that when no family fits it says so and applies only the family-independent rules — pointing at
`references/artifact-patterns.md` for the classification procedure rather than restating the four
families or the fallback inline. Keep it to one sentence of roughly 30 words. Write it as the
parallel of Write mode's existing assumed-family sentence so the two modes read as one instruction
applied twice, not as two conventions.

Add nothing else in this task. The third and fourth report sections, the standalone-audit
instruction, and the second reference pointer are Task 2's, and they are separated from this one so
that the budget mechanism is proven green on one addition before three more land on top of it.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 -c "import pathlib;w=len(pathlib.Path('skills/proof-first/SKILL.md').read_text().split());print(w, int(w*1.3), 5000-int(w*1.3))"</automated>
    <fails_when>the second printed number is 5000 or greater, or the third printed number is below 150</fails_when>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output</fails_when>
    <human-check>
Read the four trimmed regions in context and confirm what no tool here can: that nothing the file
uniquely said has been lost. For each deletion, name the surviving sentence elsewhere in the same
file that carries the same content, and confirm that the surrounding paragraph still reads as a
continuous instruction rather than as text with a hole in it. Then read the new Check-mode sentence
against Write mode's existing assumed-family sentence and confirm the two describe one behaviour
applied in two modes, rather than two similar behaviours a model must reconcile.
    </human-check>
  </verify>
  <acceptance_criteria>
- Before any edit, `python3 tools/check_repo.py` prints `check_repo: 0 violations`.
- After Step 1 and before Step 3, `python3 -c "import pathlib;w=len(pathlib.Path('skills/proof-first/SKILL.md').read_text().split());print(w)"` prints a number at or below `3530`, and that number is recorded in the SUMMARY.
- After Step 3, `python3 -c "import pathlib;w=len(pathlib.Path('skills/proof-first/SKILL.md').read_text().split());print(w, int(w*1.3), 5000-int(w*1.3))"` prints a third number of at least `150`.
- `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/SKILL.md').read_text();print(len(re.findall(r'^### PF-',t,re.M)), len(re.findall(r'^\*\*Replace with:\*\*',t,re.M)))"` prints `31 31`.
- `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/SKILL.md').read_text();segs=re.split(r'^### PF-[0-9.]+.*$',t,flags=re.M)[1:];print(len(segs), sum(1 for s in segs if len(re.findall(r'^\*\*Replace with:\*\*\s*\S',s,re.M))!=1))"` prints `31 0` — every rule still has exactly one non-empty constructive half.
- `grep -cF 'This catalog contains 31 rules in 6 numbered sections.' skills/proof-first/SKILL.md` prints `1`.
- `grep -cF 'Not affiliated with or endorsed by any framework rights-holder' skills/proof-first/SKILL.md` prints `1`.
- `grep -cF '## Unresolved before this document is sent' skills/proof-first/SKILL.md` prints `1`.
- `grep -oE 'REVIEW \([a-z]+\)' skills/proof-first/SKILL.md | sort -u | wc -l | tr -d ' '` prints `4`.
- ``grep -c '^- `\[<rule>' skills/proof-first/SKILL.md`` prints `3`.
- `grep -c '^## ' skills/proof-first/SKILL.md` prints `13`.
- `grep -cF 'references/artifact-patterns.md' skills/proof-first/SKILL.md` prints `1` — the new sentence points at the file, and the `## Reference files` bullet is Task 2's.
- `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/SKILL.md').read_text();s=t.split('## Check mode')[1].split('\n## ')[0];print(s.strip().splitlines()[0][:60])"` prints the opening of the new classification sentence, not the opening of the existing "Output is a report" sentence.
- `git diff --name-only` lists `skills/proof-first/SKILL.md` as the only file changed by this task.
  </acceptance_criteria>
  <done>`SKILL.md` is trimmed of the named restatement with every rule, constructive half, stated count, pointer, register heading, marker form and review category intact; check mode names the artifact family before reporting any finding; and the file's word count and estimated-token margin are measured and recorded at two points rather than assumed at one.</done>
</task>

<task type="auto">
  <name>Task 2: The third and fourth report sections, the standalone audit, and the second reference pointer</name>
  <files>skills/proof-first/SKILL.md</files>
  <read_first>
- `skills/proof-first/SKILL.md` § Check mode as it stands after Task 1 — particularly the fixed-order paragraph naming the two existing sections and the sentence stating that both headings always print. That sentence is edited in place, not duplicated.
- `skills/proof-first/SKILL.md` § Reference files — the three existing bullets and their named-opening-condition shape, which the new bullet matches.
- `skills/proof-first/references/completeness-audit.md` § `Running the audit on its own` as `03-02` wrote it — the standalone verdict this task's `SKILL.md` sentence points at rather than restates.
- `skills/proof-first/references/artifact-patterns.md` in full as `03-03` wrote it — the four `**Order:**` lines are what the structural-ordering pass reads, and the sentence stating that an ordering finding cites no rule number is the one this task must stay consistent with.
- This plan's `<interfaces>` block — the frozen report section names, the inline-backtick rule, and the invariant table.
- `03-01-PLAN.md`'s `<decisions>` P3-02, for why `## Completeness gaps` is appended third rather than inserted second.
  </read_first>
  <action>
Spend the remaining budget on the three instructions Phase 3 still owes `SKILL.md`, keeping each to
the shortest sentence that carries the whole instruction and pointing at a reference file for every
piece of detail.

**The third and fourth report sections, inside `## Check mode`.** The existing text names
`## Integrity flags` first and `## Prose violations` second, both inline in backticks. Extend it in
place:
- Add one sentence naming `## Completeness gaps` third, stating that it carries the document-level
  findings `references/completeness-audit.md` defines and that each cites its own `MC-` number.
- Add one sentence naming `## Structural ordering` fourth, stating that it reports one verdict on
  whether the document follows the order its classified artifact family expects, and that a finding
  there names the family and the convention it breaks and carries no rule number, because no numbered
  namespace covers those conventions.
- Edit the existing always-print sentence in place so it covers four sections rather than two. Do not
  add a second always-print sentence, and do not restate the no-findings-line convention — it is
  already stated once and it applies to all four sections by being stated about the report.
- Write all four section names inline inside backticks. A section name written at line start would
  become a real heading in `SKILL.md` and change what `split_sections` reads; the real-heading-count
  invariant catches that.
- Do not reorder `## Integrity flags` and `## Prose violations` relative to each other.

**The standalone audit run (AUD-03).** Add one sentence, near the new completeness sentence, stating
that a writer can ask for the completeness audit on its own, and that such a run returns
`## Completeness gaps` and its verdict alone, with no prose findings and no rewritten document.
Point at `references/completeness-audit.md` for what the verdict contains rather than restating it.

**The second reference pointer.** Add one bullet to the `## Reference files` list for
`references/artifact-patterns.md`, in the same named-opening-condition shape the three existing
bullets use — before classifying a document into an artifact family, or before applying that family's
conventions. This is the bullet `03-03` deliberately deferred so that every `SKILL.md` edit in this
phase lands in one budget-gated plan.

Measure after every one of the three additions, not once at the end. If the margin falls below the
gate, trim further restatement by the test in Task 1 before continuing — never drop one of the three
instructions and never shorten one into ambiguity.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 -c "import pathlib;w=len(pathlib.Path('skills/proof-first/SKILL.md').read_text().split());print(w, int(w*1.3), 5000-int(w*1.3))"</automated>
    <fails_when>the second printed number is 5000 or greater, or the third printed number is below 150</fails_when>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, or any line beginning "FAIL" in the output</fails_when>
    <automated>python3 tools/check_repo.py --mutation-test</automated>
    <fails_when>non-zero exit, any line beginning "mutation-test FAIL", any line beginning "mutation-test FIRE-ONLY", or a CONTROL line reporting more than 0 violations</fails_when>
    <human-check>
Read the whole `## Check mode` section end to end as a model would and confirm three things no tool
here can: (1) the four sections read as one ordered report a model can actually produce, not as four
instructions bolted on at different times; (2) the structural-ordering sentence makes it
unmistakable that no rule number is cited there, so a model reaching for a citation finds the
instruction not to; (3) nothing in the trimmed file now depends on a sentence the trim removed — read
the marker-vocabulary section, the PF-2 section's close, and the Check-mode section in particular.
    </human-check>
  </verify>
  <acceptance_criteria>
- `python3 -c "import pathlib;w=len(pathlib.Path('skills/proof-first/SKILL.md').read_text().split());print(w, int(w*1.3), 5000-int(w*1.3))"` prints a third number of at least `150`, and all three numbers are recorded in the SUMMARY.
- `python3 -c "import pathlib,sys;t=pathlib.Path('skills/proof-first/SKILL.md').read_text();names=['## Integrity flags','## Prose violations','## Completeness gaps','## Structural ordering'];pos=[t.find(n) for n in names];print(pos);sys.exit(0 if all(p>0 for p in pos) and pos==sorted(pos) else 1)"` exits 0 — all four section names are present and appear in the frozen order.
- `grep -c '^## ' skills/proof-first/SKILL.md` prints `13` — none of the four report names was written at line start.
- `grep -cF 'references/completeness-audit.md' skills/proof-first/SKILL.md` prints at least `1` and `grep -cF 'references/artifact-patterns.md' skills/proof-first/SKILL.md` prints at least `2` — one occurrence in the Check-mode classification sentence from Task 1 and one in the new `## Reference files` bullet.
- `python3 -c "import pathlib,sys;t=pathlib.Path('skills/proof-first/SKILL.md').read_text();s=t.split('## Reference files')[1].split('\n## ')[0];n=len([l for l in s.splitlines() if l.startswith('- ')]);print(n);sys.exit(0 if n==4 else 1)"` exits 0 — four reference pointers, one per reference file.
- `python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/SKILL.md').read_text();print(len(re.findall(r'^### PF-',t,re.M)), len(re.findall(r'^\*\*Replace with:\*\*',t,re.M)))"` prints `31 31`.
- `grep -cF 'This catalog contains 31 rules in 6 numbered sections.' skills/proof-first/SKILL.md` prints `1`, `grep -cF 'Not affiliated with or endorsed by any framework rights-holder' skills/proof-first/SKILL.md` prints `1`, and `grep -cF '## Unresolved before this document is sent' skills/proof-first/SKILL.md` prints `1`.
- `grep -oE 'REVIEW \([a-z]+\)' skills/proof-first/SKILL.md | sort -u | wc -l | tr -d ' '` prints `4` and ``grep -c '^- `\[<rule>' skills/proof-first/SKILL.md`` prints `3`.
- `python3 tools/check_repo.py --mutation-test | tail -3 | grep -cF 'PASS: 27 codes discrimination-proven'` prints `1` — the whole phase's enforcement is still intact after the trim.
- `python3 tools/check_repo.py > /tmp/pf-0304-a.txt 2>&1; python3 tools/check_repo.py > /tmp/pf-0304-b.txt 2>&1; diff -q /tmp/pf-0304-a.txt /tmp/pf-0304-b.txt` reports no difference — the checker's verdict on this file is deterministic across runs.
- `git diff --name-only` lists `skills/proof-first/SKILL.md` as the only file changed by this task.
  </acceptance_criteria>
  <done>Check mode reports four labelled sections in the frozen order, states that an ordering finding carries no rule number, and states that the completeness audit can be run on its own; `SKILL.md` points at all four reference files with a named opening condition each; every invariant holds; and the measured estimated-token margin is at least 150 with the whole CI job order green.</done>
</task>

<task type="auto">
  <name>Task 3: Make the repository's own documentation true — README, the ledger entry, and the provisional requirement marks</name>
  <files>README.md, .planning/WINDOWS.md, .planning/REQUIREMENTS.md</files>
  <read_first>
- `README.md` in full — the `## Status` section's two lists at lines 20-43, the layout tree at lines 54-85, and the fenced attribution pointer at lines 113-115. The pointer must stay present exactly once and the fenced block is where it lives.
- `skills/proof-first/references/worked-examples.md` — count its `## ` sections; the README states that number as prose and it is wrong the moment `03-02` lands.
- `.planning/REQUIREMENTS.md` line 21 — CAT-10's provisional annotation. It is the exact shape this task reproduces: the checkbox untouched, an italic note appended to the requirement line stating what is unverified, the warning not to re-mark Complete from a SUMMARY field, and a named closure condition.
- `.planning/WINDOWS.md` — entries 3 and 4, both open `unrun-verify` items routed to a later gate. Entry 3 is the closest analog to the one this task adds.
- `03-01-PLAN.md`'s flagged assumption A3-01 in full — that is the text this ledger entry summarises.
  </read_first>
  <action>
Reconcile the three documents that now describe a repository state that has changed, and record what
this phase could not verify rather than letting silence imply it did.

`README.md`:
- In "What exists today", add one bullet for `skills/proof-first/references/completeness-audit.md`
  describing it as the completeness audit in its own ID namespace, and one for
  `skills/proof-first/references/artifact-patterns.md` describing it as the four artifact families'
  conventions. Keep both descriptions factual and claim-free.
- Correct the worked-examples bullet's stated pair count to the number of `## ` sections actually in
  that file.
- In "What does not exist yet", remove the bullet naming the two Phase 3 reference files. Leave every
  other bullet in that list alone — the distribution manifests, the output style, the system prompt,
  the before-and-after examples and the benchmark are all still genuinely absent.
- In the layout tree, remove the "(planned)" annotation from the two reference files' lines, keeping
  the tree's column alignment consistent with its neighbours.
- Change nothing in the `## Rule numbering`, `## Versioning` or `## License and notices` sections, and
  add no measured claim anywhere. `README.md` is scanned by `undefined-id` and `check_pointer`: cite no
  rule number that is not allocated, and leave the fenced pointer block exactly as it is.

`.planning/WINDOWS.md` — append one open entry through the tool rather than by hand-editing the file,
so the table and the JSON block stay in agreement:

```
gsd-tools windows append --kind unrun-verify --phase 03 --file skills/proof-first/references/completeness-audit.md --description "<text>"
```

The description states, in one sentence each: that the eight MC dimension names and their ID-range
order were frozen in `NUMBERING.md` and `.planning/REQUIREMENTS.md` before Phase 3 began and sit in
the source methodology's own acronym order; that `SOURCES.md` names a source's own ordered list
reproduced in its order as reproduction and states that no tool in this stack performs that judgment;
that Phase 3 used content levers only, writing every dimension's question in this repository's own
words grounded in the deal brief, and deliberately did not reorder or rename anything; and that the
judgment itself is routed to Phase 6's LEG-04 gate, which is this entry's closure condition.

`.planning/REQUIREMENTS.md` — append a provisional annotation to each Phase 3 requirement whose
closure depends on something no check in this repository can observe, reproducing CAT-10's shape
exactly: leave the checkbox untouched, append an italic note to the requirement's own line stating
what is implemented, what is unverified and why, the warning that a SUMMARY's `requirements-completed`
field records implementation rather than verification, and a named closure condition. Apply it to:
- AUD-01 and ART-01 through ART-04 — implemented and structurally enforced; the content-quality half
  is a human read, closure condition the Phase 3 UAT pass.
- AUD-03, MOD-03 and MOD-04 — instruction text shipped and grep-checkable; whether a live session
  actually behaves that way is model behaviour no file-reading checker observes, closure condition a
  live harness session recorded in the Phase 3 UAT pass.
- MOD-05 — the two mechanical halves are closed by `undefined-id` and the MC drift and count checks;
  the live-session half, that a real check-mode conversation never invents a number, is permanently
  manual and shares MOD-03's closure condition.
Change no checkbox and no requirement wording. AUD-02 takes no annotation: both its halves are
mechanically enforced and it is genuinely closed.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py --self-test && python3 tools/check_repo.py --mutation-test && python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit at any stage, any line beginning "FAIL" or "mutation-test FAIL" or "mutation-test FIRE-ONLY", or a final line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 -c "import json,pathlib,sys;t=pathlib.Path('.planning/WINDOWS.md').read_text();f=chr(96)*3;rows=json.loads(t.split(f+'json')[1].split(f)[0]);p3=[r for r in rows if r['phase']=='03'];print(len(p3),[r['status'] for r in p3]);sys.exit(0 if len(p3)==1 and p3[0]['status']=='open' else 1)"</automated>
    <fails_when>non-zero exit, meaning the phase-03 ledger entry is absent, duplicated, or not open</fails_when>
    <human-check>
Read the appended `.planning/REQUIREMENTS.md` annotations against what this phase actually shipped and
confirm the one judgment a checker cannot make: that every annotation states something true and
narrower than the requirement it sits on, and that no requirement was marked more closed than the
evidence supports. Then read the new `.planning/WINDOWS.md` description and confirm it would be
actionable to a reader arriving at Phase 6's LEG-04 gate with no memory of this phase — that it names
what was frozen, who froze it, what the boundary rule says, what this phase did and did not do about
it, and what would close it.
    </human-check>
  </verify>
  <acceptance_criteria>
- `grep -cF 'references/completeness-audit.md' README.md` prints at least `2` — once in the Status list and once in the layout tree.
- `grep -cF 'references/artifact-patterns.md' README.md` prints at least `2`.
- `grep -c '(planned)' README.md` prints `4` — down from the six the file carries today, because the two Phase 3 reference files no longer carry the annotation and the four genuinely-absent entries still do.
- `python3 -c "import pathlib,sys;t=pathlib.Path('README.md').read_text();bad=[l for l in t.splitlines() if 'Phase 3 reference files' in l];print(bad);sys.exit(1 if bad else 0)"` exits 0 — the "does not exist yet" bullet naming them is gone.
- `python3 -c "import pathlib,re,sys;n=len([l for l in pathlib.Path('skills/proof-first/references/worked-examples.md').read_text().splitlines() if l.startswith('## ')]);r=pathlib.Path('README.md').read_text();m=re.search(r'the (\d+) worked',r);print(n, m.group(1));sys.exit(0 if int(m.group(1))==n else 1)"` exits 0 — README's stated pair count equals the file's actual section count.
- `grep -cF 'Not affiliated with or endorsed by any framework rights-holder' README.md` prints `1` — the pointer is still carried exactly once.
- `python3 -c "import pathlib,sys;t=pathlib.Path('.planning/REQUIREMENTS.md').read_text();ids=['AUD-01','AUD-03','ART-01','ART-02','ART-03','ART-04','MOD-03','MOD-04','MOD-05'];missing=[i for i in ids if not any(i in l and 'records implementation, not verification' in l for l in t.splitlines())];print(missing);sys.exit(1 if missing else 0)"` exits 0 and prints `[]` — every requirement needing a live session carries the provisional note.
- `python3 -c "import pathlib,sys;t=pathlib.Path('.planning/REQUIREMENTS.md').read_text();bad=[l for l in t.splitlines() if 'AUD-02' in l and 'records implementation' in l];print(bad);sys.exit(1 if bad else 0)"` exits 0 — AUD-02 carries no provisional note, because both its halves are mechanically enforced.
- `python3 -c "import re,pathlib;t=pathlib.Path('.planning/REQUIREMENTS.md').read_text();print(len(re.findall(r'^- \[[ x]\] \*\*(?:AUD|ART|MOD)-',t,re.M)))"` prints `12` — no requirement line was added, removed, or restructured.
- `python3 tools/check_repo.py --self-test && python3 tools/check_repo.py --mutation-test && python3 tools/check_repo.py` exits 0 end to end, with `PASS: 27 codes discrimination-proven` and a final line of `check_repo: 0 violations`.
- `git diff --name-only` lists exactly `README.md`, `.planning/REQUIREMENTS.md` and `.planning/WINDOWS.md` for this task.
  </acceptance_criteria>
  <done>`README.md`'s Status list, its stated worked-pair count and its layout tree all describe the repository as it is on disk; the MC dimension-order paraphrase tension is an open ledger entry addressed to Phase 6's LEG-04 gate; and every Phase 3 requirement whose closure needs a live model session carries a provisional annotation in `.planning/REQUIREMENTS.md` in CAT-10's proven shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a trim of shipped prose → the installed skill's behaviour | Deleting sentences from a file a model reads as instructions can remove a behaviour, and no test in this repository executes the skill. |
| `SKILL.md` → every installing harness | The file is loaded in full at skill-selection time by every target harness; exceeding the progressive-disclosure ceiling degrades or truncates it on a user's machine, not here. |
| check-mode instructions → the citation guarantee | A fourth report section that a model must populate creates a new place for a fabricated rule number to appear. |
| this repository's own documentation → a reader deciding whether to trust it | `README.md` and `.planning/REQUIREMENTS.md` are where an over-claim would be believed, and this project's whole premise is that it makes measured claims or none. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-17 | Denial of Service | `skills/proof-first/SKILL.md`'s progressive-disclosure ceiling | high | mitigate | The trim runs before any addition, behind its own measured gate, and the margin is re-measured after every one of the four additions rather than once at the end. `skill-token-budget-exceeded` is a live, proven CI gate that fails the build on a regression regardless, and an acceptance criterion additionally requires a residual margin of at least 150 estimated tokens so the file does not ship one word under the line. |
| T-03-18 | Tampering | behaviour silently removed by the trim | high | mitigate | The trim list is five named, individually measured regions, each with the surviving sentence that already carries its content named in the same table. A seven-command invariant table re-runs before and after every edit covering rule count, constructive-half parity, stated count, pointer, register heading, review categories, marker forms and real-heading count. Task 1's `<human-check>` requires naming, per deletion, the surviving sentence that carries the same content. |
| T-03-19 | Tampering | an invented rule number in the new fourth report section | high | mitigate | The structural-ordering instruction states explicitly that a finding there names the artifact family and the convention label and carries no rule number, consistent with `03-03`'s P3-14 and with the fact that this phase minted no third namespace. `undefined-id` independently fails the build on any unallocated `PF-` or `MC-` token anywhere under `skills/`. |
| T-03-20 | Spoofing | a report section name written as a real heading | medium | mitigate | All four names are written inline inside backticks, exactly as the two existing ones are. The real-heading-count invariant asserts `SKILL.md` still has 13 sections, so a name written at line start fails the task rather than silently changing what `split_sections` reads. |
| T-03-21 | Repudiation | a requirement recorded as closed that no evidence closes | high | mitigate | Task 3 reproduces CAT-10's provisional-annotation shape on all nine Phase 3 requirements that need a live session, with an acceptance criterion asserting each one carries the note and that AUD-02 does not. Every plan's `<output>` block restricts `requirements-completed:` to what was implemented and names what stays provisional. This closes the same defect that once propagated a false CAT-10 Complete. |
| T-03-22 | Repudiation | `README.md` describing a repository that no longer exists | medium | mitigate | Task 3 reconciles the Status list, the stated worked-pair count and the layout tree against disk, each with an acceptance criterion that reads the real file rather than trusting the edit. This is the same self-contradiction class `02-09` closed once already, and an acceptance criterion asserts the fenced attribution pointer is still carried exactly once. |
| T-03-SC4 | Tampering | npm / pip / cargo installs | low | accept | This plan installs no package in any ecosystem and does not edit `tools/check_repo.py` at all. `03-RESEARCH.md`'s Package Legitimacy Audit records N/A for the whole phase. No install task exists, so no package-legitimacy checkpoint is required. |
</threat_model>

<verification>
- `python3 tools/check_repo.py --self-test && python3 tools/check_repo.py --mutation-test && python3 tools/check_repo.py` — the exact CI job order — runs green, with 27 codes discrimination-proven and the control copy clean.
- `SKILL.md`'s word count and estimated-token margin are recorded at three points: after the trim, after Task 1's addition, and after Task 2's three additions. The final margin is at least 150 estimated tokens.
- Every invariant in this plan's `<interfaces>` table holds at the end of Task 2.
- `README.md`, `.planning/WINDOWS.md` and `.planning/REQUIREMENTS.md` each describe the repository as it actually is.

**Honest verification statement (03-RESEARCH Pitfall 4).** Of this plan's four requirements, exactly
one half of each is mechanically checkable and the other half is not, and the plan says which:
- **MOD-03**: the shipped instruction text naming four sections in a fixed order is grep-verified.
  Whether a live check-mode session actually prints four labelled sections, in that order, with a
  no-findings line in each empty one, is model behaviour no file-reading checker observes.
- **MOD-04**: the Check-mode classification sentence and the reference pointer are grep-verified.
  Whether a live session classifies a real document and states the family before applying any rule is
  model behaviour.
- **AUD-03**: the standalone-run instruction is grep-verified. Whether a live session runs the audit
  alone and returns a separate verdict is model behaviour.
- **MOD-05**: `undefined-id`, `mc-catalog-id-drift` and the two MC count codes close the shipped-file
  half completely. Whether a live session in a fresh conversation never invents a number is
  permanently manual — the same class as CAT-10's trigger-reliability half.
All three `<human-check>` blocks in this plan are harvested at end-of-phase into `03-UAT.md`, matching
`workflow.human_verify_mode: end-of-phase`, and Task 3 writes the provisional annotations that stop
any of the four being re-marked Complete off a SUMMARY field.
</verification>

<success_criteria>
- Check mode names the artifact family it classified the document as before it reports any finding, and says so when no family fits.
- Check mode reports four labelled sections in one fixed order, each always printing, with the completeness section citing `MC-` numbers and the ordering section citing none.
- A writer can ask for the completeness audit on its own and `SKILL.md` says what that returns.
- `SKILL.md` carries all 31 prose rules and all four reference pointers, and ships with at least 150 estimated tokens of margin below the progressive-disclosure ceiling, measured rather than assumed.
- The repository's own README, ledger and requirements file each state what is true, including what this phase could not verify.
</success_criteria>

<output>
Create `.planning/phases/03-completeness-audit-artifact-patterns/03-04-SUMMARY.md` when done.

`requirements-completed:` records **implementation, not verification**. List
`[MOD-03, MOD-04, MOD-05, AUD-03]` as implemented, and in the same breath state in the SUMMARY body
that every one of the four has a live-behaviour half that no check in this repository observes, that
Task 3 has annotated each of them provisionally in `.planning/REQUIREMENTS.md`, and that the closure
condition for all four is a live harness session recorded at the Phase 3 UAT pass. Do not let the
field imply more than that.

Record, as numbers rather than assertions: `SKILL.md`'s word count and estimated-token margin after
the trim, after Task 1, and at the end of Task 2; the `mutation-test` discrimination-proven total; and
the id `gsd-tools windows append` assigned to the new ledger entry.

Restate the two still-open pre-existing ledger entries (ids 3 and 4) verbatim in the SUMMARY so
`/gsd-verify-work` sees them, exactly as `02-09-SUMMARY.md` did.
</output>
