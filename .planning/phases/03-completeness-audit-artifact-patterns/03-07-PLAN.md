---
phase: 03-completeness-audit-artifact-patterns
plan: 07
type: execute
wave: 2
depends_on: ["03-06"]
gap_closure: true
autonomous: true
security_asvs_level: 1
security_block_on: high
requirements: [MOD-04, AUD-01, AUD-03, ART-03, AUD-02, ART-01, ART-02, ART-04, MOD-03, MOD-05]
files_modified:
  - skills/proof-first/SKILL.md
  - skills/proof-first/references/artifact-patterns.md
  - skills/proof-first/references/completeness-audit.md
  - tools/check_repo.py
  - .planning/WINDOWS.md

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`skills/proof-first/SKILL.md`'s `## Write mode` defines its first output part as a line that always prints and carries one of five values, the fifth being the no-family fallback — so an unclassifiable document has a sanctioned output and silence is no longer a legal reading."
    - "`skills/proof-first/SKILL.md`'s `## Self-check before delivering` runs three named passes, the first of which gates delivery on the family line being present."
    - "`skills/proof-first/SKILL.md` stays under the 5000-token ceiling, with its post-edit word count, estimated token count, and remaining margin re-measured and recorded in the SUMMARY."
    - "No source-coined dimension label appears as this repository's own unattributed noun anywhere under `skills/`, and `source-label-in-skill-content` fires if one returns."
    - "`skills/proof-first/references/completeness-audit.md`'s statement of the standalone-audit output shape names the four things a standalone run does not add, matching the criterion `AUD-03` is actually held to, and no longer asserts a stricter shape than the project accepts."
    - "`python3 tools/check_repo.py --mutation-test` reports 29 codes discrimination-proven with CONTROL reporting 0 unexpected."
    - "`WINDOWS.md` entries 7 and 9 each carry a recorded disposition rather than being left silently unchanged."
  artifacts:
    - skills/proof-first/SKILL.md
    - skills/proof-first/references/artifact-patterns.md
    - skills/proof-first/references/completeness-audit.md
    - tools/check_repo.py
    - .planning/WINDOWS.md
  key_links:
    - "`SKILL.md`'s `## Self-check before delivering` family-line pass ↔ `skill-family-line-gate-missing` — the instruction and the check that proves it is still there."
    - "`SOURCE_COINED_LABELS` in `tools/check_repo.py` ↔ every file under `skills/` — the guard that stops a fixed label from returning."
    - "`completeness-audit.md`'s standalone-audit shape sentence ↔ `SKILL.md`'s standalone-audit sentence ↔ the `AUD-03` criterion in `.planning/REQUIREMENTS.md` — three statements of one contract that must agree."
  prohibitions:
    - "A new violation code must not be counted toward the discrimination-proven total unless `--mutation-test` proves it silent on the unmutated control and firing on its own mutation. A code registered without a mutation inflates this repository's own headline integrity number."
    - "The 5000-token ceiling must not be raised, and `SKILL.md` must not be re-wrapped or reflowed, to make an addition fit. `WINDOWS.md` entry 5 states this explicitly for the same file. If the edit does not fit, the edit gets shorter."
    - "The eight frozen `MC-` dimension names and IDs, and the four frozen `ARTIFACT_FAMILY_SECTIONS` strings, must not be reworded, re-cased, pluralised, or reordered. `WINDOWS.md` entry 6 owns the ordering question and routes it to Phase 6 LEG-04."
---

<objective>
Pull the three content levers this gap-closure run owns, and put a mechanical guard behind two of
them.

- **GAP A (`MOD-04`, `WINDOWS.md` entry 8):** make the artifact-family line unconditional in
  `SKILL.md` — not by stating the ordering rule a third time, which is what 03-05 did and what did
  not move the rate, but by two different levers: promoting the no-family fallback to a *value* of
  the family line so an unclassifiable document has a sanctioned output, and adding the family
  line to the mechanical self-check the model runs immediately before returning anything.
- **GAP B (`WINDOWS.md` entry 9):** remove the last source-coined dimension label used as this
  repository's own unattributed noun in shipped skill content, and add the check that stops it
  coming back.
- **GAP C (`WINDOWS.md` entry 7):** make `completeness-audit.md`'s statement of the standalone-audit
  output shape agree with the criterion `AUD-03` is actually held to.

Purpose: 03-05 shipped correct instruction text and the measured rate did not move. This plan
therefore treats "the edit is present" as *not* evidence of closure — it is a necessary lever, and
`03-08-PLAN.md` owns the measurement that decides whether it worked.
Output: edited shipped skill content, two new discrimination-proven violation codes, and two
WINDOWS entries with recorded dispositions.

**This plan does not close `MOD-04`.** It must not mark `MOD-04` `[x]`, must not edit
`REQUIREMENTS.md`, and must not touch `WINDOWS.md` entry 8. Those belong to `03-08-PLAN.md`, after
a measurement exists.
</objective>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-VERIFICATION.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-UAT.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-05-SUMMARY.md
@.planning/WINDOWS.md
@SOURCES.md
@.claude/CLAUDE.md
</context>

<inherited_decisions>
The `<assumption_delta_decision>` block in `03-06-PLAN.md` is binding on this plan and is not
re-decided here. Its operative conclusion: the primary noun is **the artifact-family line**, an
always-emitted output element carrying one of five values; the four named families and the
no-family fallback are all values of it. Task 1 implements that promotion.

The 12-row spec-less probe table and its no-silent-drop equality also live in `03-06-PLAN.md`.
Rows 3 (`AUD-03`), 6-8 (`ART-03`) and 11 (`MOD-04`) are the ones this plan's tasks touch; none is
resolved by this plan and all stay flagged.
</inherited_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Make the artifact-family line unconditional in SKILL.md — a fifth value and a delivery gate</name>

  <reversibility rating="costly">This changes the definition of write mode's first output part, which Phase 4's committed before/after examples and Phase 5's linter will both bind to. Undoing it after those land means a coordinated edit across the examples, the linter, and this file — not a one-file revert. It is not one-way: nothing is published, no data is migrated, and the repo is pre-launch until Phase 6.</reversibility>

  <read_first>
    - `skills/proof-first/SKILL.md` lines 22-31 (`## Your task`), 259-272 (`## Write mode`), 274-292 (`## Check mode`), 294-301 (`## Self-check before delivering`) — all four regions state something about the family line, and the edit must not create a fifth restatement. Read all four before changing any.
    - `.planning/phases/03-completeness-audit-artifact-patterns/03-VERIFICATION.md` — truth 5 and the `gaps:` block state exactly what failed and why stating the rule again is not the fix.
    - `.planning/WINDOWS.md` entry 8 — the closure condition this lever is aimed at, and entry 5, which forbids resolving a token overrun by re-wrapping or raising the ceiling.
    - `.planning/phases/03-completeness-audit-artifact-patterns/03-05-SUMMARY.md` `## Token Budget` — the measurement method and the 190-token figure this task spends from.
    - `skills/proof-first/references/artifact-patterns.md` `## Classifying the document` region — the no-family fallback's shipped spelling, so the value named in `SKILL.md` matches the reference file byte for byte.
  </read_first>

  <files>skills/proof-first/SKILL.md</files>

  <action>
Two surgical edits to `skills/proof-first/SKILL.md`. Make no other change to this file in this task.

**Edit 1 — `## Write mode`, the section's first sentence (currently line 261).** Replace the whole sentence, which today defines the first output part as one line naming the artifact family with the four families in a parenthetical. The replacement states three things in this order: that output is exactly three parts, in order — the artifact-family line, the prose itself, then the trailing register; that the family line always prints and carries one of five values, naming an RFP answer, a proposal section, an executive summary, demo or discovery material, and the `**No family fits:**` value followed by the family-independent rules only; and that a document the session cannot place takes the fifth value rather than taking silence. Spell the `**No family fits:**` value exactly as `references/artifact-patterns.md` spells it — a reworded copy stops matching both the reference file and the new check.

The point of this edit is the promotion recorded in `03-06-PLAN.md`'s `<assumption_delta_decision>`: the fallback stops being an escape from emitting the line and becomes the line's fifth value. Before this edit, a session facing an unclassifiable document had no sanctioned output for part one, and emitting nothing satisfied every sentence it had read — which is what 2 of 16 post-fix sessions did.

**Edit 2 — `## Self-check before delivering`.** Change the lead-in so it reads three named passes rather than two. Insert a new numbered item 1 ahead of the existing two, and renumber the existing subtractive pass to 2 and the existing additive sweep to 3, leaving their bodies untouched. The new item 1 is the family-line pass: it says to confirm the response's first line names the artifact family or states the `**No family fits:**` value, and that a response failing this is not ready to return — the line goes in before anything else does.

This is the lever that is different in kind from what 03-05 shipped. `## Your task` and `## Write mode` already state the rule as an up-front instruction, and the rule held in 14 of 16 sessions. The self-check is the only place in this file the model is told to run a mechanical pass immediately before returning output, and the family line is currently absent from it. Adding it there converts the requirement from something read at the start of a long turn into something checked at the end of one.

**Token budget.** This addition is paid for out of the 190-token margin measured in `03-VERIFICATION.md` against the file on disk (3700 words, 4810 estimated tokens at words × 1.3, against the 5000-token ceiling `skill-token-budget-exceeded` enforces). Measure before and after with the one-liner in `<verify>`, and record both figures plus the remaining margin in the SUMMARY. The floor for this task is a remaining margin of at least 100 tokens. If the post-edit margin falls below 100, do not ship as written and do not touch the ceiling: shorten the two added passages themselves — the five-value list can drop its trailing gloss clauses and still name all five values — then re-measure. `WINDOWS.md` entry 5 forbids resolving a budget overrun by re-wrapping or raising the ceiling for this exact file.

Leave `## Your task` line 29 and `## Check mode` line 276 exactly as they are. Both already state the ordering correctly; a third and fourth restatement is the move that already failed.
  </action>

  <acceptance_criteria>
    - Source assertion: `## Write mode`'s first sentence names all five values of the family line, including the no-family value spelled identically to `references/artifact-patterns.md`.
    - Source assertion: `## Self-check before delivering` states three named passes and its first numbered item names the artifact family and the no-family value.
    - Source assertion: the existing subtractive pass and additive sweep bodies are unchanged apart from their leading digit.
    - Source assertion: `## Your task` still contains its no-rule-ID-before-the-family sentence and `## Check mode` still contains its classification sentence — this task adds a lever, it does not remove either existing one.
    - CLI assertion: the measured estimated token count is below 5000 and the remaining margin is at least 100 tokens, both recorded in the SUMMARY as numbers.
    - CLI assertion: all 31 `PF-` rule headings and all 31 `**Replace with:**` lines are still present — the catalog is untouched.
  </acceptance_criteria>

  <verify>
    <automated>python3 -c "import pathlib;w=len(pathlib.Path('skills/proof-first/SKILL.md').read_text().split());t=int(w*1.3);print('WORDS',w,'TOKENS',t,'MARGIN',5000-t)"</automated>
    <fails_when>a MARGIN value below 100, or a TOKENS value of 5000 or above</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any line other than "check_repo: 0 violations"</fails_when>
    <automated>grep -c 'three named passes' skills/proof-first/SKILL.md</automated>
    <fails_when>any count other than 1</fails_when>
    <automated>grep -cF 'No family fits' skills/proof-first/SKILL.md</automated>
    <fails_when>any count below 2, meaning the no-family value is missing from either the write-mode five-value list or the self-check family-line pass</fails_when>
    <automated>grep -c '^### PF-' skills/proof-first/SKILL.md</automated>
    <fails_when>any count other than 31</fails_when>
    <automated>grep -cF '**Replace with:**' skills/proof-first/SKILL.md</automated>
    <fails_when>any count other than 31</fails_when>
    <automated>grep -cF 'no rule ID is cited and no finding is reported before the artifact family is named' skills/proof-first/SKILL.md</automated>
    <fails_when>any count other than 1, meaning 03-05's instruction sentence was displaced instead of supplemented</fails_when>
  </verify>

  <done>`SKILL.md` defines the artifact-family line as an always-printed element with five values including the no-family fallback, and gates delivery on it in the first of three self-check passes, inside the token budget with the remaining margin recorded as a number.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the last source label from shipped skill content, and align the standalone-audit shape statement with the criterion it is held to</name>

  <reversibility rating="reversible">Two sentence-level edits in two reference files; either reverts with a one-line change.</reversibility>

  <read_first>
    - `skills/proof-first/references/artifact-patterns.md` lines 90-108 — the whole `## Executive summary` section, so the replacement reads correctly in its sentence and not just in isolation.
    - `skills/proof-first/references/completeness-audit.md` lines 122-131 — the `## Running the audit on its own` section in full.
    - `skills/proof-first/SKILL.md` line 288 — the standalone-audit sentence in `## Check mode`, which states the same contract and is the wording the reference file must be brought into agreement with.
    - `SOURCES.md` lines 11-22 — the reproduction boundary, specifically the clause about a term coined by a source and adopted here as this repository's own label.
    - `.planning/WINDOWS.md` entries 7 and 9 — the two entries this task disposes of, including entry 9's own proposed replacement wording.
    - `.planning/phases/03-completeness-audit-artifact-patterns/03-VERIFICATION.md` `## Post-Verification Resolution` — the orchestrator decision that `AUD-03` keeps its `[x]`. That decision is settled and is the reason this task aligns the text to the criterion rather than tightening the criterion to the text.
  </read_first>

  <files>skills/proof-first/references/artifact-patterns.md, skills/proof-first/references/completeness-audit.md, .planning/WINDOWS.md</files>

  <action>
**Edit 1 — GAP B, `skills/proof-first/references/artifact-patterns.md`, the `**Business case:**` paragraph at lines 101-105.** The sentence beginning with the named person currently follows her name with a two-word appositive that is a frozen `NUMBERING.md` registry label from the qualification-checklist family, used here as this repository's own unattributed noun. Replace that appositive with the phrase this same section already uses twice — `the person who signs` — so the sentence reads as the named person, the person who signs, stating she is measured on run-rate reduction and a clean regulatory examination. Change nothing else in the sentence and nothing else in the file. This is `WINDOWS.md` entry 9's own proposed fix, and it makes the paragraph consistent with its own opening line and with the `**Order:**` line above it.

`WINDOWS.md` entry 6 owns the separate question of the frozen registry labels in `NUMBERING.md` itself, and routes it to Phase 6 LEG-04. Do not touch `NUMBERING.md`, do not touch any `MC-` heading, and do not touch the eight dimension names in the registry. This edit is one appositive in one shipped sentence.

After the edit, close `WINDOWS.md` entry 9 with `gsd-tools windows fixed 9`.

**Edit 2 — GAP C, `skills/proof-first/references/completeness-audit.md`, the last sentence of `## Running the audit on its own` at lines 127-129.** The sentence currently ends by asserting the standalone run answers the question this file asks and nothing more. A real standalone-audit session additionally printed an unrequested artifact-family heading, and the orchestrator has already ruled that acceptable because it is not one of the four things the `AUD-03` criterion forbids — the criterion held 9 times out of 9. The shipped text is therefore stricter than the contract the project actually holds this behaviour to, which is the inconsistency `WINDOWS.md` entry 7 records.

Rewrite the closing clause so the file states the same boundary `SKILL.md` line 288 and the `AUD-03` criterion state: a standalone run returns no prose findings and never rewrites the document, and adds no integrity-flags section, no prose-violations section, and no structural-ordering verdict; naming the artifact family it read the document as is permitted and is not a finding. Keep the three section names inline as backticked text inside a sentence — never at the start of a line — because `tools/check_repo.py`'s `split_sections` helper splits this file on lines beginning with a level-two heading marker, and a heading name at line start would be read as a new section and could disturb the `MC-` dimension-block count `mc-count-mismatch` enforces.

After the edit, dispose of `WINDOWS.md` entry 7. The disposition is `fixed`, not `waived`: the inconsistency the entry records is the shipped text contradicting the accepted criterion, and this edit removes the contradiction. Run `gsd-tools windows fixed 7`.

Do not edit `SKILL.md` in this task — Task 1 owns that file and a second writer in the same plan would make the token measurement ambiguous.
  </action>

  <acceptance_criteria>
    - Source assertion: a case-insensitive word-boundary scan of everything under `skills/` for the seven frozen source-coined dimension labels returns zero matching lines.
    - Source assertion: the `**Business case:**` paragraph in `artifact-patterns.md` still names the same person and still states run-rate reduction and a clean regulatory examination — the fix replaces an appositive, it does not delete the fact.
    - Source assertion: `completeness-audit.md`'s `## Running the audit on its own` section names all three forbidden report sections inline and states that naming the artifact family is permitted.
    - Source assertion: no line in `completeness-audit.md` begins with a level-two heading marker followed by any of the three forbidden section names.
    - Source assertion: all eight `MC-` headings and the stated check count in `completeness-audit.md` are unchanged.
    - CLI assertion: `gsd-tools windows status` reports entries 7 and 9 as `fixed`.
    - CLI assertion: `python3 tools/check_repo.py` still prints `check_repo: 0 violations`.
  </acceptance_criteria>

  <verify>
<!-- planner-discipline-allow: economic buyer -->
<!-- planner-discipline-allow: paper process -->
<!-- planner-discipline-allow: decision criteria -->
<!-- planner-discipline-allow: decision process -->
<!-- planner-discipline-allow: champion -->
<!-- planner-discipline-allow: competition -->
<!-- planner-discipline-allow: pains? -->
    <automated>grep -rniE '\b(economic buyer|paper process|decision criteria|decision process|champion|competition|pains?)\b' skills/ | wc -l</automated>
    <fails_when>any number other than 0, meaning a frozen source label is still used as this repository's own noun in shipped skill content</fails_when>
    <automated>grep -c 'the person who signs' skills/proof-first/references/artifact-patterns.md</automated>
    <fails_when>any count below 3, meaning the replacement phrase was not added alongside the section's two existing uses</fails_when>
    <automated>grep -c 'run-rate reduction and a clean regulatory' skills/proof-first/references/artifact-patterns.md</automated>
    <fails_when>any count other than 1, meaning the fact was deleted rather than the appositive replaced</fails_when>
    <automated>grep -c '^## ' skills/proof-first/references/artifact-patterns.md</automated>
    <fails_when>any count other than 6, the pre-edit number of level-two headings in this file, meaning a section boundary moved</fails_when>
    <automated>grep -cE '^## (Integrity flags|Prose violations|Structural ordering)' skills/proof-first/references/completeness-audit.md</automated>
    <fails_when>any count other than 0, meaning a forbidden section name was written at line start and will be parsed as a real heading</fails_when>
    <automated>grep -c '^### MC-' skills/proof-first/references/completeness-audit.md</automated>
    <fails_when>any count other than 8</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any line other than "check_repo: 0 violations"</fails_when>
  </verify>

  <done>Shipped skill content contains no frozen source-coined dimension label used as this repository's own noun, the standalone-audit shape statement agrees with the criterion `AUD-03` is held to, and `WINDOWS.md` entries 7 and 9 both read `fixed`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Two new violation codes — the family-line gate and the source-label guard — discrimination-proven</name>

  <reversibility rating="reversible">Two code names added to one Python file and its two test harnesses; removal is a mechanical deletion of the same seven touch points.</reversibility>

  <read_first>
    - `tools/check_repo.py` lines 1195-1253 — `ARTIFACT_FAMILY_SECTIONS`, `ARTIFACT_FAMILY_REQUIREMENT` and `check_artifact_family_sections`. This is the analog to copy: it is the most recent code added to this file, it guards the same reference file, and its docstring demonstrates the declared-ceiling convention and the file-absence guard.
    - `tools/check_repo.py` lines 205-232 — the module docstring's per-code descriptions, including the declared-ceiling paragraph style.
    - `tools/check_repo.py` lines 1331-1355 — `CATALOG_CHECK_CODES` and `run_catalog_checks`, the two registration points.
    - `tools/check_repo.py` lines 1723-1780 — `_mutate_artifact_family_section_missing` and the tail of the `MUTATIONS` list, the mutation registration point.
    - `tools/check_repo.py` lines 2476-2500 — `_good_artifact_patterns` and `_bad_artifact_patterns`, the self-test fixture-builder convention.
    - `tools/check_repo.py` lines 2600-2620 and 2755-2780 and 2900-2930 — where self-test fixture roots are declared, written, and asserted. Read `_good_skill()` before writing the family-line check and confirm whether it contains a self-check section; the guard condition depends on the answer.
    - `SOURCES.md` lines 11-22 — the boundary the source-label check approximates, and the sentence stating no tool in this stack performs that judgement, which the check's declared ceiling must restate rather than contradict.
  </read_first>

  <behavior>
    - `skill-family-line-gate-missing` is silent against a SKILL.md whose self-check section names both the artifact family and the no-family value.
    - `skill-family-line-gate-missing` fires against a SKILL.md whose self-check section is present but names neither.
    - `skill-family-line-gate-missing` is silent against a SKILL.md with no self-check section at all — the declared ceiling, required so every pre-existing synthetic fixture stays silent.
    - `source-label-in-skill-content` is silent against the repository's shipped skill content as Task 2 leaves it.
    - `source-label-in-skill-content` fires, naming the file, the label, and the line number, when any frozen label is inserted into a file under a skill folder.
    - `source-label-in-skill-content` does not fire on the ordinary-English word for a measurement, which is deliberately excluded from the frozen list.
  </behavior>

  <files>tools/check_repo.py</files>

  <action>
Add two violation codes to `tools/check_repo.py`, each wired through all seven touch points the existing codes use: the module docstring's per-code description, the check function, `CATALOG_CHECK_CODES`, `run_catalog_checks`, a self-test known-good fixture, a self-test known-bad fixture plus its two assertions, and a mutation function plus its `MUTATIONS` row.

**Code 1 — `skill-family-line-gate-missing`.** For each path matching `SKILL_GLOB`, split the file with `split_sections` and look up the self-check section. If that section is absent, return no violation and move on — declare this as the ceiling in the docstring, for the same reason `check_artifact_family_sections` declares its file-absence guard: every existing self-test fixture root writes a synthetic `_good_skill()` that has no such section, and an unguarded implementation would fire on all of them and disable the whole suite. When the section is present, require its body to contain both the phrase naming the artifact family and the no-family value string that `03-07` Task 1 put there. Fire once, naming the file and which of the two anchors is missing, and cite `MOD-04` as the requirement the gate serves.

Declared ceiling for this code, stated in its docstring: it asserts the instruction text is present. It cannot assert a live session obeys it — that is a model-behaviour property no file-reading checker observes, and `evals/conformance/run_conformance.py` is the instrument for it. Say this plainly; do not let the code's existence imply `MOD-04` is mechanically verified.

**Code 2 — `source-label-in-skill-content`.** Add a module-level frozen tuple `SOURCE_COINED_LABELS` holding the seven labels the two independent readers flagged in `03-UAT.md` test 5 — the two-word buyer-authority label, the two-word paperwork-step label, the two-word criteria label, the two-word process label, the single-word internal-advocate label, the single-word rival-set label, and the single-word problem label in both its singular and plural form. For each path matching `SKILL_GLOB` and each `references/*.md` beside it, read the file, apply `strip_fences`, and scan case-insensitively with word boundaries for each label. Fire once per file-and-label pair, naming the relative path, the label, and the first line number where it occurs, and citing `SOURCES.md`'s clause about a term coined by a source and adopted here as this repository's own label.

Three declared ceilings for this code, stated in its docstring. First: it is a literal-string scan over a closed, enumerated list, not the semantic paraphrase judgement `SOURCES.md` states no tool in this stack performs — it catches a known label returning, and a novel one has no string to match. Second: the ordinary-English word for a measurement is deliberately excluded from the list, because this repository's own `MC-1` and integrity rules use it as ordinary business English and including it would fire on legitimate content; that exclusion is a stated ceiling, not an oversight. Third: it scans shipped skill content only — `NUMBERING.md`'s frozen registry labels are out of scope and owned by `WINDOWS.md` entry 6, which routes them to Phase 6 LEG-04.

**Self-test fixtures.** Prefer reusing an existing known-good root as each code's silent side over adding a redundant one — `artifact_good_root` already writes a clean `artifact-patterns.md` and can carry the silent assertion for the source-label code. Add one known-bad root per code, following the `artifact_bad_root` pattern exactly: write the same known-consistent `NUMBERING.md`/`SKILL.md`/`checklist.md` trio the neighbouring roots use, so each new root isolates the one new code under test and no unrelated code fires into its assertion set. Then add the two assertions per code that the existing suite uses — the code must be absent from the good root's code set and present in the bad root's.

**Mutations.** `_mutate_skill_family_line_gate_missing` deletes the family-line pass item from the copied real `SKILL.md`'s self-check section, leaving the section heading and the other two passes in place, so the mutation targets exactly what the check reads. `_mutate_source_label_in_skill_content` inserts one frozen label into the copied real `references/artifact-patterns.md`. Register both in `MUTATIONS` with a one-line description in the same voice as the existing rows.

If any pre-existing self-test fixture trips either new code, fix the fixture or widen the check's absence guard — never weaken the assertion that fires against the real repository files. A check that has been relaxed until it passes is the failure mode this repository's own mutation-test exists to prevent.

After wiring, the self-test's printed verified-codes list must name 29 codes and `--mutation-test` must report 29 discrimination-proven with CONTROL reporting 0 unexpected. A code that appears in `ALL_CHECK_CODES` with no registered mutation fails the run by design — `mutation_test` checks for exactly that.
  </action>

  <acceptance_criteria>
    - Behaviour assertion: all six cases in `<behavior>` hold, proven by `--self-test` exiting 0 and `--mutation-test` reporting both new codes as discrimination-proven.
    - CLI assertion: `--self-test` prints a verified-codes list containing 29 comma-separated codes, including both new names.
    - CLI assertion: `--mutation-test` prints `mutation-test PASS: 29 codes discrimination-proven` and a CONTROL line reporting 0 unexpected violations.
    - CLI assertion: the bare run still prints `check_repo: 0 violations` — both new codes are silent against the repository as Tasks 1 and 2 leave it.
    - Source assertion: each new code appears at all seven touch points — module docstring, check function, `CATALOG_CHECK_CODES`, `run_catalog_checks`, a self-test bad fixture, its two self-test assertions, and a `MUTATIONS` row.
    - Source assertion: `SOURCE_COINED_LABELS` contains seven entries and does not contain the ordinary-English word for a measurement.
    - CLI assertion: the checker is deterministic — two consecutive bare runs produce byte-identical output.
  </acceptance_criteria>

  <verify>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, any line beginning with "FAIL", or a verified-codes list that does not name both skill-family-line-gate-missing and source-label-in-skill-content</fails_when>
    <automated>python3 tools/check_repo.py --self-test 2>&1 | tr ',' '\n' | grep -c '[a-z]-'</automated>
    <fails_when>any number below 29, meaning a code was dropped from the self-test's coverage list</fails_when>
    <automated>python3 tools/check_repo.py --mutation-test</automated>
    <fails_when>non-zero exit, a final line other than "mutation-test PASS: 29 codes discrimination-proven", or a CONTROL line reporting anything other than 0 unexpected</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any line other than "check_repo: 0 violations"</fails_when>
    <automated>grep -c 'skill-family-line-gate-missing' tools/check_repo.py</automated>
    <fails_when>any count below 5, meaning the code name is missing from one or more of its seven touch points</fails_when>
    <automated>grep -c 'source-label-in-skill-content' tools/check_repo.py</automated>
    <fails_when>any count below 5, meaning the code name is missing from one or more of its seven touch points</fails_when>
    <automated>python3 tools/check_repo.py > /tmp/a.txt 2>&1; python3 tools/check_repo.py > /tmp/b.txt 2>&1; diff -q /tmp/a.txt /tmp/b.txt</automated>
    <fails_when>non-zero exit, or any "differ" line from diff</fails_when>
  </verify>

  <done>Two new violation codes are wired through all seven touch points and proven silent-on-control and firing-on-mutation, raising the discrimination-proven total from 27 to 29 with the CONTROL still clean.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols this plan creates or changes. The source-grounding pass must treat every new name here as
newly created, not as drift from an existing symbol.

**New `check_repo.py` violation codes**
- `skill-family-line-gate-missing`
- `source-label-in-skill-content`

**New `check_repo.py` Python symbols**
- `SOURCE_COINED_LABELS` (module-level frozen tuple, seven entries)
- `check_skill_family_line_gate()`
- `check_source_label_in_skill_content()`
- `_mutate_skill_family_line_gate_missing()`
- `_mutate_source_label_in_skill_content()`
- two new self-test known-bad fixture roots and their fixture-builder functions

**Changed `SKILL.md` content (no new headings)**
- `## Write mode` first sentence — redefined as a five-value always-printed family line
- `## Self-check before delivering` — lead-in changes from two named passes to three; a new
  numbered item 1, the family-line pass; the existing two items renumbered 2 and 3

**Changed reference-file content (no new headings)**
- `references/artifact-patterns.md` `## Executive summary` `**Business case:**` — one appositive replaced
- `references/completeness-audit.md` `## Running the audit on its own` — closing clause rewritten

**No new files.** No requirement checkbox changes. `WINDOWS.md` entries 7 and 9 move to `fixed`;
entry 8 is untouched by this plan.
</artifacts_this_phase_produces>

<threat_model>
Security enforcement is active at ASVS L1, blocking on `high`. This plan edits three Markdown
files and one stdlib Python checker. There is no network surface, no authentication, no
persistence, and no untrusted input: every file the checker reads is committed repository content.
The honest register is short and contains no high-severity threat.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository content → checker | `check_repo.py` reads committed Markdown and makes pass/fail claims about it. The content is trusted; the claim is what must not be wrong. |
| shipped skill text → live model session | `SKILL.md` and `references/*.md` become instructions a downstream session executes. Wording is the only control surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Spoofing | the discrimination-proven count printed by `--mutation-test` | medium | mitigate | A code registered in `ALL_CHECK_CODES` with no mutation would raise the printed total without proving anything, spoofing this repository's own integrity number. `mutation_test` already fails the run when a code has no registered mutation; Task 3 registers a mutation for both new codes and the `<verify>` block asserts the exact PASS line, not merely a zero exit. |
| T-03-05 | Tampering | `SOURCE_COINED_LABELS` scope | low | mitigate | A label list narrowed until nothing can match would be a decorative check. The mutation function inserts a real label into the real reference file and the run fails unless the code fires, so the list is proven to have teeth against the actual shipped content, not only against a synthetic fixture. |
| T-03-06 | Repudiation | `WINDOWS.md` entry disposition | low | mitigate | Marking an entry `fixed` with no recorded reason leaves no audit trail. Task 2 states the disposition and its rationale in the SUMMARY, and the ledger's own `resolved_at` timestamp is written by `gsd-tools windows fixed`. |
| T-03-07 | Information disclosure | shipped skill text | low | accept | `SKILL.md` and the reference files ship publicly under MIT. They contain no secret and reference only the fictional deal in `examples/deal-brief.md`. Accepted: public disclosure is the artifact's purpose. |
| T-03-08 | Elevation of privilege | — | — | accept | No privilege boundary exists in this change set. There is no process, no account, and no permission model to elevate within. Recorded so the absence is deliberate rather than unexamined. |

**Package legitimacy:** no `npm`, `pip`, or `cargo` install task exists in this plan. `check_repo.py`
is standard library only and this plan adds no import. There is no `T-03-SC` row because there is
no dependency to audit.

**No `high` or `critical` threat is present, so the `security_block_on: high` gate does not fire.**
</threat_model>

<verification>
Run all four after the last task commit:

- `python3 tools/check_repo.py --self-test` — 29 codes, exit 0.
- `python3 tools/check_repo.py --mutation-test` — 29 codes discrimination-proven, CONTROL 0 unexpected, exit 0.
- `python3 tools/check_repo.py` — `check_repo: 0 violations`, exit 0.
- `python3 evals/conformance/run_conformance.py --self-test` — exit 0; this plan must not disturb `03-06`'s scorer.

Token re-measurement, recorded as a number in the SUMMARY:
`python3 -c "import pathlib;w=len(pathlib.Path('skills/proof-first/SKILL.md').read_text().split());t=int(w*1.3);print(w,t,5000-t)"`

Ledger state:
`gsd-tools windows status` — entries 7 and 9 `fixed`; entry 8 still `open`, untouched by this plan.
</verification>

<success_criteria>
- The artifact-family line is defined as unconditional in `SKILL.md`, with the no-family fallback as its fifth value and a delivery gate in the self-check — two levers different in kind from the restatement that already failed.
- `SKILL.md` remains under the 5000-token ceiling with at least 100 tokens of margin, re-measured and recorded.
- Zero frozen source-coined dimension labels remain in shipped skill content, and a discrimination-proven check fires if one returns.
- `completeness-audit.md`'s standalone-audit shape statement agrees with `SKILL.md` line 288 and with the `AUD-03` criterion.
- The discrimination-proven code count rises from 27 to 29 with a clean CONTROL.
- `WINDOWS.md` entries 7 and 9 read `fixed`; entry 8 is left for `03-08-PLAN.md`.
- `MOD-04` is still `[ ]` and `REQUIREMENTS.md` is untouched — this plan pulls the lever, it does not claim the outcome.
</success_criteria>

<output>
Create `.planning/phases/03-completeness-audit-artifact-patterns/03-07-SUMMARY.md` when done.
Record in it: the pre-edit and post-edit `SKILL.md` word count, estimated token count and margin as
three numbers; the exact self-test verified-codes line; the exact mutation-test PASS line; and the
disposition rationale written for `WINDOWS.md` entries 7 and 9.
</output>
