---
phase: 03-completeness-audit-artifact-patterns
plan: 08
type: execute
wave: 3
depends_on: ["03-07"]
gap_closure: true
autonomous: true
security_asvs_level: 1
security_block_on: high
requirements: [MOD-04, AUD-01, AUD-02, AUD-03, ART-01, ART-02, ART-03, ART-04, MOD-03, MOD-05]
files_modified:
  - evals/conformance/RESULTS-mod04.md
  - .planning/WINDOWS.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/03-completeness-audit-artifact-patterns/03-UAT.md

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A write-mode conformance rate for the post-03-07 skill is measured over at least 16 scoreable live sessions spanning both `claude-sonnet-5` and `claude-opus-5` and all five committed fixtures, and is recorded with its exact numerator and denominator."
    - "Every unscoreable session is listed by model, fixture and repeat index with the reason it was excluded from the denominator."
    - "`evals/conformance/RESULTS-mod04.md` records the git blob SHA of the measured `skills/proof-first/SKILL.md`, the model ids, the date, and the one command that reproduces the run."
    - "`WINDOWS.md` entry 8 carries a disposition determined by the measured rate under the rule pre-committed in this plan, not by a judgement made after seeing the number."
    - "`.planning/REQUIREMENTS.md`'s `MOD-04` line states the measured post-run rate, and its checkbox state matches that rate under the same pre-committed rule."
    - "`AUD-01` and `ART-01` through `ART-04` are still `[ ]` and still annotated as owned by Phase 6 LEG-04 — this run performs no human paraphrase read and claims none."
    - "No line in `.planning/REQUIREMENTS.md` carries a checked box beside an UNVERIFIED annotation."
  artifacts:
    - evals/conformance/RESULTS-mod04.md
    - .planning/WINDOWS.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/03-completeness-audit-artifact-patterns/03-UAT.md
  key_links:
    - "`RESULTS-mod04.md`'s headline rate ↔ `REQUIREMENTS.md`'s `MOD-04` annotation ↔ `WINDOWS.md` entry 8 ↔ `03-UAT.md`'s gap G-03-2 — four records of one number that must agree digit for digit."
    - "the measured SKILL.md blob SHA ↔ the 03-07 commit — a rate that cannot be attributed to a skill revision is not a measurement of that revision."
  prohibitions:
    - "`MOD-04` must not be marked Complete on the strength of an edit being present. 03-05 shipped correct instruction text and the measured rate did not move; presence of an edit is the evidence that has already failed once in this phase."
    - "A measured residual must not be dropped, rounded, softened, or described only in prose. It is disclosed with its exact numerator and denominator in all four records, or the claim is not made at all."
    - "`AUD-01` and `ART-01` through `ART-04` must not be marked from a subagent read, an executor re-read, or this plan's own conformance run. Two independent subagent reads have already happened and are recorded as explicitly not a substitute for a human. Phase 6 LEG-04 owns them."
---

<objective>
Measure whether `03-07`'s two levers actually moved MOD-04's live-session conformance rate, then
record the result honestly in all four places that track it.

This phase has reverted a false-Complete three times. The one rule that prevents a fourth is that
the acceptance criterion here is the **measurement**, never the edit. `03-07` pulled the levers;
this plan finds out what they were worth and writes down the answer whatever it is.

Purpose: `WINDOWS.md` entry 8 states its own closure condition — "a write-mode conformance run
that names the family in every session, or a further SKILL.md change that makes the family line
unconditional" — and `03-VERIFICATION.md` adds a third admissible outcome, "an accepted, disclosed
residual failure rate." All three paths need the same thing first: a number.
Output: a measured rate in `evals/conformance/RESULTS-mod04.md`, and a disposition applied to
`WINDOWS.md` entry 8, `REQUIREMENTS.md` `MOD-04`, and `03-UAT.md` gap G-03-2 under a rule this plan
commits to **before** the number is known.
</objective>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/WINDOWS.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-UAT.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-VERIFICATION.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-06-SUMMARY.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-07-SUMMARY.md
@.claude/CLAUDE.md
</context>

<pre_committed_decision_rule>
**This rule is fixed before the measurement runs. Task 2 applies it mechanically. Do not
substitute a judgement made after seeing the number — that is the p-hacking this plan's
prohibitions bar.**

Let `M` be the count of scoreable sessions and `N` the count scored `conformant`. The prior
independently-measured rate is 14/16 = 0.875.

| Branch | Condition | `WINDOWS` entry 8 | `REQUIREMENTS` `MOD-04` | `03-UAT` G-03-2 |
|---|---|---|---|---|
| **1 — closed** | `M >= 16` and `N == M` | `fixed` | `[x]`, annotation states `N`/`M`, both model ids, the five fixtures, the SKILL.md blob SHA, and the reproduction command | `status: resolved` with the post-fix evidence block replaced by the new measurement |
| **2 — improved, not closed** | `M >= 16` and `N < M` and `N/M > 0.875` | stays `open`; description replaced with the new rate, the date, and the statement that a rate below 1.0 does not satisfy a truth that says "in every session" | stays `[ ]`, annotation states `N`/`M` alongside the prior 14/16 | stays `partially_resolved`, evidence block gains the new measurement |
| **3 — not improved** | `M >= 16` and `N/M <= 0.875` | stays `open`; description replaced with the new rate plus the explicit finding that a second instruction-text change did not move it, so closure now requires a lever other than instruction wording | stays `[ ]`, annotation states `N`/`M`, the prior 14/16 and the prior 5/6, and names instruction text as a measured-exhausted lever | stays `partially_resolved`, evidence block records all three measurements in sequence |
| **4 — under-sampled** | `M < 16` after the attempt cap | stays `open`; description states the achieved `M`, why the cap was hit, and that no rate is claimed at this sample size | stays `[ ]`, annotation states that the re-run was attempted and under-sampled, with `M` | stays `partially_resolved`, evidence block records the attempt and the shortfall |
| **5 — harness unavailable** | the `<precondition>` fails | stays `open`; description gains one line naming the blocker | stays `[ ]`, annotation unchanged apart from a line naming the blocker and the date | unchanged apart from a blocker line |

Branch 3 additionally requires a paired baseline: re-run the identical matrix with `--skill-src`
pointed at the pre-`03-07` `skills/proof-first`, materialised from git, so the "did not move it"
claim is made with one instrument on both sides rather than across two instruments. Branches 1, 2,
4 and 5 do not run a paired baseline — the closure criterion in those branches is absolute, not a
delta, and 20 extra live sessions would buy nothing.

In every branch, `AUD-01` and `ART-01` through `ART-04` stay `[ ]` and their annotations are not
touched except to keep them accurate. This run performs no human paraphrase read.
</pre_committed_decision_rule>

<requirement_dispositions>
Every one of the phase's ten requirement IDs, with what this gap-closure run does about it.
Coverage means visible disposition, not invented work.

| ID | This run | Why |
|---|---|---|
| `MOD-04` | **Advanced.** `03-07` Task 1 pulled two instruction levers; this plan measures the result and disposes of it under the pre-committed rule. | The phase's one FAILED truth, `WINDOWS.md` entry 8, no later-phase owner. |
| `AUD-01` | **Partially advanced, not closed.** `03-07` Task 2 removed the last source-coined label from shipped skill content and `03-07` Task 3 added the check that stops it returning. The content-quality and paraphrase-boundary half is untouched. | The remaining half is a human read against `SOURCES.md`, owned by Phase 6 LEG-04 (`WINDOWS.md` entries 3, 6). Two subagent reads are explicitly recorded as not a substitute. Stays `[ ]`. |
| `ART-03` | **Partially advanced, not closed.** The `## Executive summary` section is the file region `03-07` Task 2 edited. | Its content-quality half is the same Phase 6 LEG-04 human read. Stays `[ ]`. |
| `ART-01`, `ART-02`, `ART-04` | **Not touched.** | Structurally verified already and mechanically enforced by `artifact-family-section-missing`. Their open half is the same Phase 6 LEG-04 human read. Stays `[ ]`. |
| `AUD-03` | **Consistency-only.** `03-07` Task 2 brought `completeness-audit.md`'s shape statement into agreement with the criterion `AUD-03` is held to, closing `WINDOWS.md` entry 7. | Already `[x]` on 9/9 live evidence; the orchestrator's `03-VERIFICATION.md` post-verification resolution settled the completion-bar question and is not relitigated. No checkbox change. |
| `AUD-02` | **Not touched.** | Already `[x]`, mechanically enforced by `mc-rule-in-skill`, no open half. |
| `MOD-03` | **Not touched.** | Already `[x]` on 9/9 live sessions with the wording defect fixed. No open half. |
| `MOD-05` | **Not touched.** | Already `[x]`; shipped-file half mechanically enforced, live half clean across 18 sessions and 224 citations. No open half. |

The 12-row spec-less probe table and its no-silent-drop equality live in `03-06-PLAN.md`
`<flagged_assumptions>`. Row 11 (`MOD-04`) is the one this plan acts on; it stays flagged, because
a live-session ordering property is not a data-shape edge the probe's taxonomy can resolve.
</requirement_dispositions>

<tasks>

<task type="auto">
  <name>Task 1: Measure the post-edit write-mode conformance rate over at least 16 scoreable live sessions</name>

  <reversibility rating="reversible">A measurement run and a results file; re-running replaces it.</reversibility>

  <precondition>`claude` resolves on PATH and `03-06-SUMMARY.md` records that the tracer's live session completed — the runner's live half was proven in `03-06` Task 1. If `03-06-SUMMARY.md` instead records a precondition failure, or if a single read-only `claude -p "reply with the single word ok" --model claude-sonnet-5` run from a temp directory returns non-zero or empty, do not attempt the matrix: go directly to Task 2 and apply branch 5.</precondition>

  <read_first>
    - `evals/conformance/run_conformance.py` — the flags, the scoring rule, and the results-block format `03-06` established. Do not change any of them; the prohibition against tuning a scorer after it has scored live sessions binds here.
    - `.planning/phases/03-completeness-audit-artifact-patterns/03-UAT.md` `## Method` — the recipe, including the caveat about exactly one writer per output file and the note that skill auto-discovery must stay on.
    - `.planning/phases/03-completeness-audit-artifact-patterns/03-07-SUMMARY.md` — confirms the two SKILL.md levers landed and records the commit whose blob SHA this run must measure.
    - `.planning/WINDOWS.md` entry 8 — the measurement this run is aimed at, including the 14/16 and 5/6 figures it will be compared against.
    - `evals/conformance/RESULTS-mod04.md` — the existing file, which already holds `03-06`'s instrument-proving run against the pre-edit skill. Append; do not overwrite it, the proving run is part of the audit trail.
  </read_first>

  <files>evals/conformance/RESULTS-mod04.md</files>

  <action>
Run the committed runner over the full matrix against the post-`03-07` skill.

Matrix: both models `claude-sonnet-5` and `claude-opus-5`, all five fixtures `A-rfp-answer`, `B-proposal-section`, `C-exec-summary`, `D-demo-discovery`, `E-ambiguous`, two repeats each. That is 20 attempted sessions, sized so that at least 16 remain scoreable after the usual attrition — the 16-session re-check `03-UAT.md` records also needed reruns after two sessions hit a usage limit.

Invoke with `--skill-src skills/proof-first` (the working tree, which now carries `03-07`'s edits), `--models claude-sonnet-5,claude-opus-5`, `--fixtures A-rfp-answer,B-proposal-section,C-exec-summary,D-demo-discovery,E-ambiguous`, `--repeats 2`, and `--out evals/conformance/RESULTS-mod04.md`. Leave `--transcript-dir` at its default so raw transcripts land in a temp directory outside the repository and never enter git — `T-03-02` in `03-06-PLAN.md`'s threat model, and Phase 5 `EVAL-12` owns the separate question of committing raw generations.

Do not pass `--bare`. The measurement is whether the skill activates and holds on its own description; naming it in a system prompt would measure something else. The runner already omits the flag and `03-06` Task 1's `<verify>` asserts its absence.

If fewer than 16 sessions come back scoreable, re-run only the unscoreable cells, one at a time, up to a hard cap of 30 total attempted sessions. Record every attempt, scoreable or not. When the cap is reached with fewer than 16 scoreable, stop and record the achieved `M` — do not report a rate as though the 16-session bar had been met, and do not drop attempts to make the arithmetic nicer.

Write the results block so a reader can audit any single verdict without re-running anything: date, both model ids, every fixture-and-repeat cell with its verdict and the scorer's evidence string, the git blob SHA from `git rev-parse HEAD:skills/proof-first/SKILL.md`, the headline `conformant N of M scoreable sessions` line, and a separate list of every unscoreable session with its exclusion reason.

Add an honest-caveats block to the results file, as a permanent part of it rather than something trimmed once the numbers look good. State at minimum: both models are Anthropic-hosted, so the run says nothing about a non-Anthropic harness; `claude -p` exposes no temperature or seed flag, so the run is not deterministic and a repeat can differ; the user-level configuration under the home directory still loads because `--bare` is deliberately off, matching the condition the prior measurements ran under; and the prior 14/16 figure was produced by the ad-hoc recipe rather than by this runner, so a cross-measurement comparison carries an instrument difference that a same-instrument paired baseline would remove.

Do not edit `WINDOWS.md`, `REQUIREMENTS.md`, or `03-UAT.md` in this task. Task 2 owns every disposition, and separating the measurement from its interpretation is what keeps the number from being written to fit a conclusion.
  </action>

  <acceptance_criteria>
    - CLI assertion: `evals/conformance/RESULTS-mod04.md` contains a headline line of the form `conformant N of M scoreable sessions` with `M` at least 16, or — if the attempt cap was reached first — an explicit under-sampled line stating the achieved `M` and that no rate is claimed.
    - Source assertion: the results file names both `claude-sonnet-5` and `claude-opus-5` and all five fixture stems.
    - Source assertion: the results file contains a 40-character git blob SHA for the measured `SKILL.md`.
    - Source assertion: every unscoreable session appears in the exclusion list with a stated reason; the count of listed exclusions equals attempted sessions minus `M`.
    - Source assertion: the honest-caveats block names all four caveats enumerated in the action.
    - Source assertion: `evals/conformance/run_conformance.py` is byte-identical to the version `03-06` committed — the scorer was not touched between writing it and scoring with it.
    - Source assertion: `03-06`'s instrument-proving run block is still present in the results file.
  </acceptance_criteria>

  <verify>
    <automated>grep -cE 'conformant [0-9]+ of [0-9]+ scoreable sessions|under-sampled' evals/conformance/RESULTS-mod04.md</automated>
    <fails_when>a count of 0, meaning neither a headline rate nor an explicit under-sampled statement was written</fails_when>
    <automated>grep -c 'claude-opus-5' evals/conformance/RESULTS-mod04.md</automated>
    <fails_when>a count of 0, meaning the second model was never exercised and the run is single-model</fails_when>
    <automated>grep -cE '[0-9a-f]{40}' evals/conformance/RESULTS-mod04.md</automated>
    <fails_when>a count of 0, meaning no measured SKILL.md blob SHA was recorded and the rate cannot be attributed to a revision</fails_when>
    <automated>git diff --exit-code HEAD -- evals/conformance/run_conformance.py</automated>
    <fails_when>non-zero exit, meaning the scorer was modified during or after the measurement run</fails_when>
    <automated>python3 evals/conformance/run_conformance.py --self-test</automated>
    <fails_when>non-zero exit, or any line beginning with "FAIL"</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any line other than "check_repo: 0 violations"</fails_when>
  </verify>

  <done>A conformance rate for the post-`03-07` skill exists as a number with its denominator, its excluded sessions, its model ids, its measured blob SHA, its reproduction command, and its caveats — or an explicit, recorded statement that the run was under-sampled or the harness unavailable.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the pre-committed disposition to the four records that track MOD-04</name>

  <reversibility rating="costly">Writing an accepted or disclosed residual into `REQUIREMENTS.md` and the ledger sets the completion bar Phase 6's launch gate inherits and that `README` claims will later cite. Undoing it means re-opening a requirement other artifacts already reference. It is not one-way: nothing is published and the repository is pre-launch until Phase 6.</reversibility>

  <read_first>
    - `evals/conformance/RESULTS-mod04.md` — Task 1's headline line is the only input to the branch selection. Read the number before reading anything else, then stop and identify the branch.
    - This plan's `<pre_committed_decision_rule>` — the branch table, fixed before the number was known.
    - `.planning/REQUIREMENTS.md` lines 34-43 and 47-51 and the traceability table at lines 148-159 — the `MOD-04` annotation, the `AUD-01`/`ART-01..04` annotations that must stay untouched, and the status rows.
    - `.planning/WINDOWS.md` entry 8 — the description this task replaces or leaves, depending on the branch.
    - `.planning/phases/03-completeness-audit-artifact-patterns/03-UAT.md` gap G-03-2 — the `status` and `post_fix_evidence` fields the branch table addresses.
    - `.planning/phases/03-completeness-audit-artifact-patterns/03-VERIFICATION.md` `deferred:` block — the record that `AUD-01` and `ART-01..04` are Phase 6 LEG-04's, which this task must not contradict.
  </read_first>

  <files>.planning/WINDOWS.md, .planning/REQUIREMENTS.md, .planning/phases/03-completeness-audit-artifact-patterns/03-UAT.md</files>

  <action>
Read the headline line from `evals/conformance/RESULTS-mod04.md`, compute `N` and `M`, and select exactly one branch from `<pre_committed_decision_rule>`. State the selected branch and the arithmetic that selected it in the SUMMARY before making any edit, so the choice is auditable and cannot be reconstructed after the fact.

Then apply that branch's row to all four records, using the same numerator and denominator in every one. Disagreement between these four files is how this phase's earlier false-Completes survived review.

**`.planning/WINDOWS.md` entry 8.** In branch 1 run `gsd-tools windows fixed 8`. In branches 2, 3, 4 and 5 the entry stays open and its description is replaced with the branch's text, carrying the new measured rate and the date. Use the `gsd-tools windows` sub-commands rather than hand-editing both the Markdown table and the JSON block, which must stay in agreement.

**`.planning/REQUIREMENTS.md`.** Edit the `MOD-04` line only. Set the checkbox per the branch and rewrite the annotation so a reader learns the current state from the annotation alone, without cross-referencing `03-UAT.md` — that staleness is the exact warning `03-VERIFICATION.md` raised against the previous annotation. The annotation states: the measured `N`/`M`, both model ids, the five fixtures, the measured `SKILL.md` blob SHA, the one command that reproduces the run, and the `WINDOWS.md` entry number. Update `MOD-04`'s row in the traceability table to match the checkbox.

Leave `AUD-01` and `ART-01` through `ART-04` at `[ ]` with their existing Phase 6 LEG-04 annotations. This run performed no human paraphrase read and claims none. If `03-07` Task 2's label fix warrants a sentence in `AUD-01`'s annotation, add only that sentence and do not move the checkbox.

Then run the checkbox-consistency scan in `<verify>` across the whole file. A checked box beside an UNVERIFIED annotation has appeared three separate times in this project; treat a hit as a defect in this task's own output, not as pre-existing noise, and fix it.

**`03-UAT.md` gap G-03-2.** Set `status` per the branch and rewrite `post_fix_evidence` to carry the new measurement alongside the prior ones in date order, so the file reads as a measurement history rather than a single overwritten claim. Do not delete the 5/6 or 14/16 figures; a residual that is dropped rather than disclosed is barred by this plan's prohibitions.

**Branch 3 only.** Before writing any disposition, run the paired baseline: materialise the pre-`03-07` `skills/proof-first` into a temp directory with `git show` against the commit recorded in `03-07-SUMMARY.md`, then re-run the identical matrix with `--skill-src` pointed at it. Record both arms in `RESULTS-mod04.md` under one heading, and only then write the "did not move it" finding. A same-instrument paired comparison is what makes that claim admissible; the cross-instrument version is not.

Finally, write the plan SUMMARY so it states the outcome in the first sentence, whatever it is. If the branch is 2, 3, 4 or 5, say plainly that `MOD-04` remains open and why. A SUMMARY that leads with the work done rather than the result is how a phase talks itself into a fourth false-Complete.
  </action>

  <acceptance_criteria>
    - Source assertion: the selected branch and its arithmetic are stated in the SUMMARY before any disposition edit is described.
    - Source assertion: the same `N` and `M` appear in `RESULTS-mod04.md`, the `MOD-04` annotation in `REQUIREMENTS.md`, `WINDOWS.md` entry 8 or its `fixed` state, and `03-UAT.md`'s G-03-2 evidence block.
    - Source assertion: `MOD-04`'s checkbox is `[x]` only if branch 1 was selected; in every other branch it is `[ ]`.
    - Source assertion: `MOD-04`'s annotation names the measured `SKILL.md` blob SHA and the reproduction command.
    - Source assertion: `AUD-01`, `ART-01`, `ART-02`, `ART-03` and `ART-04` are all still `[ ]`, and each annotation still names Phase 6 as its owner.
    - CLI assertion: no line in `.planning/REQUIREMENTS.md` contains a checked box and the string `UNVERIFIED`.
    - CLI assertion: `gsd-tools windows status` reports entry 8 in the state the selected branch requires, and entries 7 and 9 still `fixed` from `03-07`.
    - Source assertion: `03-UAT.md`'s G-03-2 evidence block still contains the prior 5/6 and 14/16 figures alongside the new one.
  </acceptance_criteria>

  <verify>
    <automated>grep -nE '^- \[x\].*UNVERIFIED' .planning/REQUIREMENTS.md | wc -l</automated>
    <fails_when>any number other than 0, meaning a checkbox was flipped beside an annotation that still says the requirement is unverified</fails_when>
    <automated>grep -cE '^- \[ \] \*\*(AUD-01|ART-01|ART-02|ART-03|ART-04)\*\*' .planning/REQUIREMENTS.md</automated>
    <fails_when>any number other than 5, meaning one of the five Phase-6-owned requirements was marked complete by this run</fails_when>
    <automated>grep -c 'Phase 6' .planning/REQUIREMENTS.md</automated>
    <fails_when>a count of 0, meaning the Phase 6 LEG-04 ownership annotations were removed</fails_when>
    <automated>grep -cE '5/6|14/16' .planning/phases/03-completeness-audit-artifact-patterns/03-UAT.md</automated>
    <fails_when>any count below 2, meaning a prior measurement was deleted rather than kept alongside the new one</fails_when>
    <automated>node "${CLAUDE_CONFIG_DIR:-$HOME/devoteam/.claude}/gsd-core/bin/gsd-tools.cjs" windows status</automated>
    <fails_when>non-zero exit, or entries 7 and 9 reporting anything other than fixed, or entry 8 reporting a state other than the one the selected branch requires</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any line other than "check_repo: 0 violations"</fails_when>
  </verify>

  <done>All four records that track `MOD-04` carry the same measured numerator and denominator, the checkbox matches the pre-committed rule rather than a post-hoc judgement, and the five Phase-6-owned requirements are untouched at `[ ]`.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols this plan creates or changes. Nothing new is invented here — this plan writes numbers into
existing records and appends to one file `03-06` created.

**Appended-to files (no new files)**
- `evals/conformance/RESULTS-mod04.md` — a second results block for the post-`03-07` matrix, plus
  the honest-caveats block, plus (branch 3 only) a paired-baseline arm

**Changed planning records**
- `.planning/WINDOWS.md` — entry 8 either moves to `fixed` or keeps `open` with a replaced
  description carrying the new measured rate
- `.planning/REQUIREMENTS.md` — the `MOD-04` checkbox and annotation, and `MOD-04`'s traceability
  row
- `.planning/phases/03-completeness-audit-artifact-patterns/03-UAT.md` — gap G-03-2's `status` and
  `post_fix_evidence`

**No new `check_repo.py` violation codes, no new SKILL.md headings, no new fixture files, no new
scripts.** The discrimination-proven count stays at 29 where `03-07` left it.
</artifacts_this_phase_produces>

<threat_model>
Security enforcement is active at ASVS L1, blocking on `high`. This plan runs a local CLI in a
loop and edits four Markdown records. It introduces no new code path — the runner and its threat
surface were assessed in `03-06-PLAN.md`, and `T-03-01` through `T-03-03` there still apply
unchanged to this plan's use of the same script. The honest register below adds only what is new
to this plan, and contains no high-severity threat.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live model output → planning records | A measured number produced by a non-deterministic process becomes the basis for a requirement's completion state. |
| measurement → four separate files | One number is transcribed by hand into four records that must agree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Spoofing | the `MOD-04` completion claim | medium | mitigate | A rate reported without its denominator, or with unscoreable sessions quietly folded into the conformant count, would spoof a requirement's completion state. Task 1 records every attempt including exclusions with reasons, the `<verify>` block asserts the exclusion count reconciles against the denominator, and the branch table fixes the checkbox rule before the number is known. |
| T-03-10 | Tampering | branch selection | medium | mitigate | Selecting a branch after seeing a number that nearly cleared the bar is the classic post-hoc move. `<pre_committed_decision_rule>` fixes the table before the run, and Task 2's first acceptance criterion requires the arithmetic to be stated in the SUMMARY before any edit is described. |
| T-03-11 | Repudiation | the four disagreeing records | medium | mitigate | Four records carrying different figures is how this phase's earlier false-Completes survived review. The same numerator and denominator is asserted across all four, and the prior 5/6 and 14/16 figures are retained rather than overwritten so the history stays auditable. |
| T-03-12 | Information disclosure | raw transcripts from 20-30 live sessions | low | mitigate | Transcripts stay in a `tempfile.mkdtemp()` directory and never enter git, per `T-03-02`. Only scored verdicts and evidence offsets reach the results file. |
| T-03-13 | Denial of service | the live matrix | low | accept | 20-30 sequential `claude -p` invocations can hit a usage limit, which is why the plan sizes the matrix at 20 for a 16-session bar and caps reruns at 30. A usage-limit refusal is recorded as unscoreable, never retried indefinitely, and branch 4 exists precisely so an under-sampled run is disclosed rather than padded. |

**Package legitimacy:** no `npm`, `pip`, or `cargo` install task exists in this plan. No `T-03-SC`
row, because there is no dependency to audit.

**No `high` or `critical` threat is present, so the `security_block_on: high` gate does not fire.**
</threat_model>

<verification>
Run after the last task commit:

- `python3 tools/check_repo.py --self-test` — 29 codes, exit 0.
- `python3 tools/check_repo.py --mutation-test` — 29 codes discrimination-proven, CONTROL 0 unexpected, exit 0.
- `python3 tools/check_repo.py` — `check_repo: 0 violations`, exit 0.
- `python3 evals/conformance/run_conformance.py --self-test` — exit 0.

Cross-record agreement, run by hand and recorded in the SUMMARY: the same `N` and `M` appear in
`RESULTS-mod04.md`, `REQUIREMENTS.md`'s `MOD-04` annotation, `WINDOWS.md` entry 8, and `03-UAT.md`'s
G-03-2 evidence block.

Checkbox scan: `grep -nE '^- \[x\].*UNVERIFIED' .planning/REQUIREMENTS.md` returns nothing.
</verification>

<success_criteria>
- A write-mode conformance rate for the post-`03-07` skill exists as a measured number over at least 16 scoreable sessions across two models and five fixtures — or an explicit, recorded statement that the run was under-sampled or the harness unavailable.
- The rate is reproducible: the runner, the fixtures, the command, the model ids and the measured `SKILL.md` blob SHA are all committed.
- The branch taken from `<pre_committed_decision_rule>` was selected by arithmetic, and the arithmetic is written down.
- `WINDOWS.md` entry 8, `REQUIREMENTS.md` `MOD-04`, and `03-UAT.md` G-03-2 all carry the same figures.
- `AUD-01` and `ART-01` through `ART-04` are still `[ ]` and still owned by Phase 6 LEG-04.
- No checked box sits beside an UNVERIFIED annotation anywhere in `REQUIREMENTS.md`.
</success_criteria>

<output>
Create `.planning/phases/03-completeness-audit-artifact-patterns/03-08-SUMMARY.md` when done.

Its first sentence states the measured rate and whether `MOD-04` closed — not what work was done.
Record also: the selected branch and the arithmetic that selected it; the exact headline line from
`RESULTS-mod04.md`; the measured `SKILL.md` blob SHA; and, if `MOD-04` did not close, one plain
sentence naming what would close it, given that instruction wording has now been measured twice.
