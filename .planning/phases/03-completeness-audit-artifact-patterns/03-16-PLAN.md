---
phase: 03-completeness-audit-artifact-patterns
plan: 16
type: execute
wave: 4
depends_on: ["03-13", "03-14", "03-15"]
gap_closure: true
autonomous: true
requirements: [MOD-04, AUD-02, MOD-05]
security_asvs_level: 1
security_block_on: high
files_modified:
  - evals/conformance/run_conformance.py
  - evals/conformance/RESULTS-mod04.md
  - tools/check_repo.py
  - README.md
  - .planning/WINDOWS.md

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Deleting `_write_result_line()`'s `handle.flush()` line makes `python3 evals/conformance/run_conformance.py --self-test` exit non-zero and print a `FAIL:` line naming behavior case 11. The regression guard discriminates the property it is named for, proven in both directions: silent against the real file, firing against a mutated sibling copy (03-VERIFICATION.md Truth 10, `missing` item 1)."
    - "Case 11's discrimination does not depend on which exception type interrupts the matrix, nor on any context manager's close()-on-exit: it asserts the recorded `write`/`flush` call sequence on a proxy handle, and reads the results file from its path while the underlying handle is still open (AUD-02 concurrency probe — 'If interrupted or run in parallel, what is guaranteed?' — interrupted half, explicit)."
    - "Exclusion lines (`verdict=unscoreable`, with their machine-produced `reason=` field) travel through the same `_write_result_line()` helper case 11 now guards, so MOD-05's exclusion accounting inherits the same durability guarantee and the same regression guard (MOD-05 concurrency probe, interrupted half, explicit)."
    - "`run_conformance.py`'s module docstring states what behavior case 11 actually asserts and names the evidence that it discriminates. No statement in the file claims a proof the test does not perform (MOD-04 instrument integrity)."
    - "`RESULTS-mod04.md`'s `## Instrument durability fix (03-13)` section no longer states that case 11 proved the flush call load-bearing, and the correction is recorded in a new dated section rather than silently edited away — the same annotate-don't-delete discipline 03-13 itself used for the enumeration correction (MOD-04)."
    - "`tools/check_repo.py`'s `_copy_repo_subset()` docstring is internally consistent: it names `results-breakdown-count-mismatch` as the one check that does read under `evals/`, instead of asserting in the same paragraph that nothing does (03-REVIEW.md IN-01)."
    - "`README.md` names the 10-point movement a decline, in the same term `RESULTS-mod04.md` and `WINDOWS.md` entry 8 already use — adding no claim and changing no figure (03-REVIEW.md IN-02)."
    - "`.planning/WINDOWS.md` carries a ledger entry for the self-test non-discrimination defect, recorded `fixed` by this round, and the file's JSON block still parses cleanly. The defect is tracked, not left untracked as the verifier found it."
    - "No published figure moves: `N_A = 3`, `M_A = 10` (30.0%), `N_B = 4`, `M_B = 10` (40.0%), the -10.0pp delta and the date 2026-09-16 are identical in all five records after this plan, and `MOD-04` stays unchecked. Closing this gap does NOT close MOD-04 (MOD-04)."
    - statement: "Two `run_conformance.py` invocations appending to the same results file at the same time are still NOT guaranteed non-interleaved output. The tool makes and documents no parallel-safety claim, and the project's operating pattern is one foreground invocation at a time. Carried forward verbatim from 03-13; this plan neither strengthens nor weakens it (AUD-02 and MOD-05 concurrency probe, parallel half)."
      verification: backstop
    - statement: "This plan adds no committed mutation harness for `run_conformance.py` itself, unlike `tools/check_repo.py --mutation-test`. Case 11's discrimination is proven once, at the commit that lands it, by a probe that is run and then deleted rather than committed. That narrower guarantee is stated in the module docstring, in the new results-file section, and in the WINDOWS.md entry, and routed to Phase 5's eval-harness work — it is disclosed, not claimed closed."
      verification: backstop
  prohibitions:
    - "`skills/proof-first/SKILL.md`, `skills/proof-first/references/completeness-audit.md` and `skills/proof-first/references/artifact-patterns.md` are not opened for edit by any task in this plan. All three are fully VERIFIED and were untouched by 03-13/14/15; nothing in this gap is about writer-facing artifacts."
    - "No published MOD-04 figure changes. `N_A = 3`, `M_A = 10` (30.0%), `N_B = 4`, `M_B = 10` (40.0%), the -10.0pp delta and the date 2026-09-16 appear identically in five records and were confirmed drift-free. No task may alter, re-derive, re-score or re-round any of them."
    - "MOD-04's v1 disposition (03-15's Option A accept-and-disclose decision) and its provenance are not reopened, re-litigated or superseded, and no fifth measurement round is scheduled. It is a recorded, accepted override on phase Truth 5."
    - "`_write_result_line()`'s production behavior stays exactly as shipped: it writes, then flushes. The `.flush()` call stays. This plan fixes the TEST that guards it, not the code it guards."
    - "`score_transcript()` (the 03-09 anchored scorer, `FAMILY_LINE_WINDOW_CHARS=400`), `FAMILY_PATTERNS`, and `run_session()`'s `TimeoutExpired` handling stay byte-for-byte untouched, as does `run_matrix()`'s per-session exception handling and its `reason=` strings."
    - "No checkbox in `.planning/REQUIREMENTS.md` is flipped by any task. Re-marking AUD-02, AUD-03, MOD-03 and MOD-05 is the verifier's task and only when the phase verifies clean. MOD-04 stays `[ ]` permanently for v1. AUD-01 and ART-01..04 stay `[ ]`, deferred to Phase 6 LEG-04."
    - "Scope is not widened to AUD-01 / ART-01..04's paraphrase-boundary reads. Those are Phase 6-owned and explicitly deferred; this plan performs no such read and claims none."
    - "No existing plan (03-01 through 03-15) is modified, superseded or renumbered, and no existing SUMMARY is rewritten."
    - "No dependency, no test runner, no build step, no package-manager install. Zero-dependency stdlib-only Python 3 is a documented project constraint; the only gate is the four commands in `.github/workflows/ci.yml`."
    - "The discrimination probe copy is never committed. It is created under `evals/conformance/`, run once, and deleted in the same task, and `git status --porcelain` must print nothing afterward."
  artifacts:
    - "evals/conformance/run_conformance.py — a `_FlushTrackingHandle` proxy class and a rewritten behavior case 11 that asserts strict write-then-flush call pairing and reads the results file before the underlying handle is closed, plus a module docstring that states what case 11 actually proves"
    - "evals/conformance/RESULTS-mod04.md — the `**The fix.**` paragraph of `## Instrument durability fix (03-13)` corrected, and a new `## Self-test discrimination correction (03-16)` section recording the defect, both independent reproductions, the fix, the both-directions probe and the disclosed residual"
    - "tools/check_repo.py — `_copy_repo_subset()`'s docstring naming `results-breakdown-count-mismatch` as the one check that reads under `evals/`"
    - "README.md — the Status paragraph naming the 10-point movement a decline, with every figure and the results-file pointer preserved"
    - ".planning/WINDOWS.md — a new entry for the self-test non-discrimination defect, recorded `fixed`, with the JSON block still parsing"
  key_links:
    - "`_FlushTrackingHandle`'s recorded call sequence to `_write_result_line()`'s `handle.flush()` call — the only thing that makes the guard discriminate, and the single point where a future silent deletion of that line becomes a red CI instead of a green one"
    - "the pre-close read of the results file path to the filesystem-level half of the property — call-order bookkeeping alone proves discipline but not that bytes left Python's buffer; the pre-close read proves the bytes did"
    - "the both-directions probe (silent against the real file, firing against the mutated copy) to `tools/check_repo.py --mutation-test`'s CONTROL/FIRE precedent — the in-repo standard this project already holds its own checker to"
    - "the corrected docstring and the corrected results-file sentence to this round's own honesty standard — a repaired guard still carrying the old overclaim would be the same defect relocated, not closed"
---

<objective>
Close `03-VERIFICATION.md`'s single failed truth (Truth 10). Self-test behavior case 11 in
`evals/conformance/run_conformance.py` was written to prove offline that `_write_result_line()`'s
`handle.flush()` call makes each scored session durable against a process interruption. It does not
prove it: the interrupting exception is caught inside the block whose own close-on-exit flushes the
file regardless, so deleting the flush call still yields a clean `self-test PASS`. Two committed
documents state that case 11 proves the property. Rewrite the test so it genuinely discriminates,
prove the discrimination in both directions, then correct the two statements — and close the two
lower-severity review findings and the untracked ledger entry that sit in the same files.

Purpose: the production code is correct today. What is false is the verification-integrity claim. A
future silent deletion of that one line would reintroduce the exact data-loss bug that has already
destroyed two real measurement runs, and it would ship with a green self-test and a green CI. This
repository's own mutation-testing preamble names that failure mode — "a dead check ship[ping] named
as covered" — as the thing it exists to prevent, and this project's governing constraint treats an
overstated claim as a first-class defect regardless of where it appears.

Output: a regression guard that fails when the property it guards is removed, two corrected
statements, two consistency fixes, and a ledger entry so the defect is tracked rather than untracked.
</objective>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-VERIFICATION.md
@.planning/phases/03-completeness-audit-artifact-patterns/03-REVIEW.md
@.planning/WINDOWS.md
@.claude/CLAUDE.md
</context>

<scope_note>
**This gap does not close MOD-04.** MOD-04 is an accepted, disclosed v1 residual whose checkbox stays
unchecked whatever this round achieves. `MOD-04` appears in this plan's `requirements` list because
the defect being closed is in the instrument that produced MOD-04's headline figure, not because this
plan satisfies the requirement. `AUD-02` and `MOD-05` appear because 03-13 mapped the durability and
exclusion-accounting properties to them and this plan repairs the guard on exactly those properties.
The other seven phase requirement IDs are genuinely untouched by this plan and are deliberately not
listed — the phase's requirements-coverage gate greps all sixteen plans collectively, and all ten IDs
are already claimed across 03-01..03-15.
</scope_note>

<prohibitions>
**The `.flush()` call stays.** This plan fixes the test that guards `_write_result_line()`, never the
code it guards. `_write_result_line()`'s two-line body is unchanged: write, then flush. If a task
finds itself editing that function's behavior, stop and report.

**`score_transcript()` and `run_session()` are byte-for-byte untouched.** The 03-09 anchored scorer
(`FAMILY_LINE_WINDOW_CHARS=400`), `FAMILY_PATTERNS`, and `run_session()`'s `TimeoutExpired`
catch-and-preserve are outside this plan entirely, as are `run_matrix()`'s four per-session exception
handlers and its `reason=` strings. 03-13 held this same line and the reviewer confirmed it with
`git diff`; hold it again.

**No published figure changes.** `N_A = 3`, `M_A = 10`, 30.0%, `N_B = 4`, `M_B = 10`, 40.0%, the
-10.0 percentage-point delta and the date `2026-09-16` appear identically in five records and were
confirmed drift-free. No task adjusts, re-rounds, re-derives or re-scores any of them. README's
decline clause states the arithmetic already implied by two figures it keeps verbatim; it adds no
number and no new claim.

**No checkbox in `.planning/REQUIREMENTS.md` moves.** Not AUD-02, not AUD-03, not MOD-03, not MOD-05,
not MOD-04, not AUD-01, not ART-01..04. A plan that flips a checkbox will be reverted.

**The three writer-facing skill files are not opened for edit.** `skills/proof-first/SKILL.md`,
`references/completeness-audit.md`, `references/artifact-patterns.md` are all fully VERIFIED. This
gap is about the measurement instrument and four documents, nothing else.

**MOD-04's v1 disposition is not reopened.** 03-15's Option A accept-and-disclose decision, its date,
its human attribution and its reopening condition stand. No fifth measurement round is scheduled and
no live `claude -p` session runs anywhere in this plan. Every proof here is offline; zero quota spend.

**No dependency, no test runner, no build step.** The durability proof stays inside
`run_conformance.py`'s existing `self_test()` function, which CI already runs. No `pytest`, no
`requirements.txt`, no lockfile.

**Zero package-manager installs.** No `pip`, `npm` or `cargo` install task exists in this plan, so
`03-RESEARCH.md`'s Package Legitimacy Audit gate is not engaged and no legitimacy checkpoint is
required.

**The probe copy is never committed.** It is created inside `evals/conformance/`, run once, and
deleted in the same task. `git status --porcelain` printing anything after Task 1 is a failure.
</prohibitions>

<assumption_delta_decision>
The deterministic assumption-delta detector returned `detected: true` on `pluralization` signals.

| Primary noun | Source phrase | Decision | Rationale |
|---|---|---|---|
| `fallback` | "the classification procedure and its no-family fallback" (ROADMAP, 03-03's plan line) | `no-change` | Pre-existing prose about shipped behavior this round does not touch. `artifact-patterns.md` still defines exactly one fallback value, `No family fits`, and no file in this plan is in that file's dependency chain. |
| `artifact families` | "the four artifact families" (ROADMAP, phase description) | `no-change` | Already plural, already four, frozen since 03-01's decision checkpoint. This plan introduces no new family, no new variant, no new platform and no new identity — it repairs a self-test and corrects four documents. |

Recorded as `no-change`, decided from this plan's actual scope (a test-hardening and
documentation-correction round) rather than from the detector's signal. Advisory and non-blocking;
revisit if a future plan adds a second fallback path or a fifth family.
</assumption_delta_decision>

<edge_coverage_assumptions>
No SPEC.md exists for this phase, so the deterministic edge probe ran over the ten phase requirement
IDs and returned 12 applicable edges, all 12 `unresolved`. Per §C of the spec-less probe fallback,
unresolved edges are explicit planner assumptions, surfaced rather than silently dropped.

**Edges this gap bears on — carried forward from 03-13's framing, not re-derived:**

| Requirement | Category | Probe | Assumption this plan makes |
|---|---|---|---|
| AUD-02 | concurrency | "If interrupted or run in parallel, what is guaranteed?" | **Interrupted half:** an interruption of a live invocation between sessions loses at most the one in-flight session. This plan does not change that guarantee; it repairs the guard that is supposed to hold it, so the guarantee becomes genuinely enforced rather than merely asserted. Promoted to an explicit `must_haves.truths` entry. **Parallel half:** unchanged — no parallel-safety claim is made, recorded as a `backstop` truth exactly as 03-13 recorded it. |
| MOD-05 | concurrency | "If interrupted or run in parallel, what is guaranteed?" | Exclusion lines travel through the same `_write_result_line()` helper, so exclusion accounting inherits both halves above identically. Promoted to an explicit truth. |

**Edges outside this gap's scope — stated assumptions, not resolutions:**

| Requirement | Category | Assumption |
|---|---|---|
| ART-03 | adjacency, empty, ordering | 03-14 authored truths against all three when it added `results-breakdown-count-mismatch` (`xN` multipliers count as N; enumeration sum is order-independent; absent file / zero count / single item return no violation). This plan changes no enumeration, adds no bullet with a stated count, and adds no violation code, so those three edges are assumed to hold unchanged and are re-asserted only by re-running `--mutation-test` at 32 codes. |
| AUD-01, AUD-03, ART-01, ART-02, ART-04, MOD-03, MOD-04 | unclassified | Each returned "unclassified — review manually". Reviewed: all seven bear on writer-facing artifact content or live-session behavior, none of which this plan touches. Assumed unchanged and out of scope. AUD-01 and ART-01..04 are additionally Phase 6 LEG-04-owned and explicitly deferred. |

No resolution is manufactured for any unresolved edge. Nothing above is claimed verified by this plan
except the two AUD-02/MOD-05 concurrency halves it genuinely exercises.
</edge_coverage_assumptions>

<artifacts_this_phase_produces>
Every symbol this plan creates. Nothing else in any of the five files gains or loses a public name.

| Symbol | Kind | File | Note |
|---|---|---|---|
| `_FlushTrackingHandle` | class | `evals/conformance/run_conformance.py` | Self-test-only proxy over a real writable handle. Records `write`/`flush` call order in a `calls` list so a test can assert the write-then-flush discipline independently of what any context manager's own close does on exit. Defines `write` and `flush` and nothing else — deliberately no `__getattr__` delegation. |
| `calls` | attribute | `evals/conformance/run_conformance.py` | The recorded call-order list on `_FlushTrackingHandle`, e.g. `['write', 'flush', 'write', 'flush']`. |
| behavior case 11 (rewritten) | self-test case | `evals/conformance/run_conformance.py` | Same case number, same stub session runner, new assertions. Replaces the non-discriminating on-disk-after-close check with a recorded-call-order assertion plus a pre-close read of the results file. |
| `## Self-test discrimination correction (03-16)` | section heading | `evals/conformance/RESULTS-mod04.md` | Records the defect, both independent reproductions, the fix, the both-directions probe, the disclosed residual, and that no figure changes. |
| WINDOWS.md entry 10 | ledger entry | `.planning/WINDOWS.md` | The self-test non-discrimination defect, recorded `fixed` by this round, created through the `gsd-tools windows` CLI. |

No new violation code, no new CLI flag, no new committed file. `tools/check_repo.py` stays at 32
discrimination-proven codes for the whole of this plan. `evals/conformance/_flush_probe_tmp.py` is
created and deleted inside Task 1 and is never committed.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: Make behavior case 11 fail when the flush call is removed, and prove it in both directions</name>

  <read_first>
    - `evals/conformance/run_conformance.py` lines 601-678 in full — behavior case 11 as it stands: the
      case comment, `fake_out_dir_11` / `fake_results_path_11` setup, `call_count_11`,
      `conformant_transcript_11`, `_stub_session_fn`, the `with`-statement that currently wraps the
      `run_matrix` call, the inner `try`/`except KeyboardInterrupt` that sets `interrupted_11`, the
      three assertion branches, and the `finally` cleanup. This is the exact region being rewritten.
    - `evals/conformance/run_conformance.py` lines 719-730 — `_write_result_line()`. Its body is two
      statements: write to the handle, then flush the handle. Confirm the flush statement's exact
      indentation and text before Task 1's probe depends on matching it.
    - `evals/conformance/run_conformance.py` lines 732-816 — `run_matrix()`. Confirm by reading that
      the ONLY methods it ever calls on `handle` are the two inside `_write_result_line()`, and that it
      writes exactly one result line per session iteration with no header line of its own. Both facts
      are what make a two-method proxy sufficient and make the expected call sequence exactly four
      entries for a three-session matrix interrupted on its third call.
    - `evals/conformance/run_conformance.py` lines 270-302 — `self_test()`'s preamble, the `all_ok`
      flag and the `verdicts_discriminated` set, so the rewritten case keeps the same reporting
      contract.
    - `evals/conformance/run_conformance.py` lines 537-600 — behavior case 10, the structural template:
      set up, act, assert, print a `FAIL:` line naming the case and set the all-ok flag false on
      mismatch, clean up in a `finally` block.
    - `.planning/phases/03-completeness-audit-artifact-patterns/03-REVIEW.md`, section `## Critical
      Issues` / `CR-01` — the finding this task closes, including its `**Fix:**` section's concrete
      `_FlushTrackingHandle` sketch. Treat that sketch as a starting point, not a paste-verbatim
      mandate: this task's assertion is stricter than the sketch's positional zip, which cannot see a
      trailing unpaired write.
    - `.planning/phases/03-completeness-audit-artifact-patterns/03-VERIFICATION.md` frontmatter
      `gaps[0]` — the failed truth, its two `artifacts` entries and its two `missing` items.
    - `tools/check_repo.py` lines 1854-1866 — the mutation-testing preamble. This is the in-repo
      precedent for what a discrimination proof looks like here, and the source of the standard this
      task is held to: a check that cannot fail is a dead check shipping named as covered.
    - `.github/workflows/ci.yml` — confirms `run_conformance.py --self-test` is the only place in this
      repository where a durability proof can be held, because there is no other test runner.
  </read_first>

  <files>evals/conformance/run_conformance.py</files>

  <action>
    Add a module-level class `_FlushTrackingHandle`, defined immediately above `def self_test():`. Its
    `__init__` takes one argument, the real writable handle, stores it on `self._real`, and initializes
    `self.calls` to an empty list. Its `write(self, text)` appends the string `'write'` to `self.calls`
    and returns `self._real.write(text)`. Its `flush(self)` appends the string `'flush'` to
    `self.calls` and returns `self._real.flush()`. Define no other method, and deliberately define no
    `__getattr__` delegation: a future code path that reaches the results handle by any route other
    than `_write_result_line()` must raise `AttributeError` loudly inside the self-test rather than be
    silently proxied through to the real handle. State that rationale in the class docstring, along
    with the fact that the class is self-test-only and is never constructed in live mode.

    Rewrite behavior case 11's body. Keep unchanged: the `fake_out_dir_11` temp directory and its
    `results.md` path, `call_count_11`, `conformant_transcript_11`, `_stub_session_fn` and its
    keyword signature, the `run_matrix` keyword arguments (`models=['claude-sonnet-5']`,
    `fixture_stems=['stub-fixture']`, `repeats=3`, `skill_src`, `transcript_dir=fake_out_dir_11`,
    `timeout_s=600`, `session_fn=_stub_session_fn`), the `verdicts_discriminated.add('conformant')` on
    success, and the `finally` block that removes the temp directory.

    Change the handle plumbing. Instead of binding the results file through a `with`-statement around
    the `run_matrix` call, open it with a plain `open(fake_results_path_11, 'a')` bound to
    `real_handle_11`, construct `tracked_handle_11 = _FlushTrackingHandle(real_handle_11)`, and pass
    `handle=tracked_handle_11` to `run_matrix`. Wrap the `run_matrix` call in a `try` whose
    `except KeyboardInterrupt:` sets `interrupted_11 = True`. Then, still BEFORE `real_handle_11` is
    closed, capture both pieces of evidence: snapshot the recorded call order with
    `recorded_calls_11 = list(tracked_handle_11.calls)`, and read the results file back from its own
    path into `pre_close_text_11` with `fake_results_path_11.read_text()`. Close `real_handle_11` only
    after both captures, in a nested `finally` so it closes on any path.

    Assert three things, each in its own branch, each printing a `FAIL:` line that names behavior case
    11 and sets `all_ok = False`, following case 10's exact reporting shape:

    First, `interrupted_11` is `True` — the stub's third call raised and nothing swallowed it.

    Second — this is the discriminating assertion — `recorded_calls_11` equals exactly
    `['write', 'flush', 'write', 'flush']`: two result lines written, each immediately followed by a
    flush, with no trailing unflushed write and no extra call. Print the actual `recorded_calls_11`
    value in the FAIL message so a failure is diagnosable from CI output alone. Assert the exact
    sequence rather than a positional pairing of alternating slices, because a positional pairing
    silently truncates an odd-length list and would therefore not see a final write that was never
    flushed. This assertion holds regardless of which exception type interrupts the matrix and
    regardless of what any context manager's close-on-exit would have done, which is precisely the
    property the old form of this case lacked.

    Third, `pre_close_text_11` — captured while `real_handle_11` was still open — already contains
    exactly two lines carrying the literal marker `| verdict=`, and both name `verdict=conformant`.
    This is the filesystem-level half: it proves the bytes actually left Python's buffer before the
    handle was closed, which recorded call bookkeeping alone cannot show.

    Rewrite the case comment above the setup to say what the case now proves and why the previous form
    did not. State: the discriminating assertion is the recorded write-then-flush call sequence; the
    pre-close read is its filesystem-level confirmation; the previous form caught the interrupting
    exception inside the same block whose own close-on-exit flushed the file regardless, so it passed
    identically with the flush call deleted; the finding was raised as `CR-01` in 03-REVIEW.md's round-5
    review and independently reproduced by 03-VERIFICATION.md's verifier; this case is the regression
    guard for `_write_result_line()`'s flush call and is expected to fail if that line is removed.

    Do not touch `_write_result_line()`, `run_matrix()`, `score_transcript()`, `run_session()`, or the
    module docstring — the docstring is Task 2's.

    Then run the discrimination probe, which is this task's real acceptance criterion and mirrors what
    `tools/check_repo.py --mutation-test` does for the checker. Produce a mutated sibling copy at
    `evals/conformance/_flush_probe_tmp.py` — inside `evals/conformance/` so the copy's
    `REPO_ROOT = pathlib.Path(__file__).resolve().parents[2]` still resolves to the repository root —
    by filtering out the single indented flush statement from `_write_result_line()`. Confirm the copy
    is exactly one line shorter than the original, then run the copy's `--self-test` and require both
    a non-zero exit and a printed `FAIL:` line naming behavior case 11. The copy never runs in live
    mode, so it never touches the real results file: `--self-test` exits before `main()` reaches
    argument handling for `--out`. Delete the copy immediately afterward and confirm the working tree
    is clean. Record the probe's exact stdout in the SUMMARY — that transcript is the evidence for this
    plan's headline claim, and the claim is only as good as the transcript.
  </action>

  <verify>
    <automated>python3 evals/conformance/run_conformance.py --self-test</automated>
    <fails_when>non-zero exit, or any line beginning `FAIL:` appears in the output, or the final line is not `self-test PASS - verdicts discriminated: conformant, no-family, rule-before-family, unscoreable`</fails_when>
    <automated>grep -c '^class _FlushTrackingHandle' evals/conformance/run_conformance.py</automated>
    <fails_when>prints anything other than `1`</fails_when>
    <automated>grep -c '^    handle\.flush()$' evals/conformance/run_conformance.py</automated>
    <fails_when>prints anything other than `1` — the production flush call must still be present and must still be the single unique line the probe below mutates</fails_when>
    <automated>grep -v '^[[:space:]]*#' evals/conformance/run_conformance.py | grep -c 'as fake_handle_11'</automated>
    <fails_when>prints anything other than `0` — the masking context-manager binding is still present in live code</fails_when>
    <automated>grep -v '^[[:space:]]*#' evals/conformance/run_conformance.py | grep -c 'tracked_handle_11'</automated>
    <fails_when>prints `0` or `1` — the proxy must be both constructed and passed to `run_matrix`, so at least 2 live references are expected</fails_when>
    <automated>python3 -c "import ast,pathlib;ast.parse(pathlib.Path('evals/conformance/run_conformance.py').read_text());print('parses')"</automated>
    <fails_when>a traceback is printed instead of the single word `parses`</fails_when>
    <automated>grep -v '^    handle\.flush()$' evals/conformance/run_conformance.py > evals/conformance/_flush_probe_tmp.py; python3 -c "a=len(open('evals/conformance/run_conformance.py').read().splitlines());b=len(open('evals/conformance/_flush_probe_tmp.py').read().splitlines());print('probe_delta=%d'%(a-b))"</automated>
    <fails_when>prints anything other than `probe_delta=1` — the mutation removed zero lines, or more than the one intended line</fails_when>
    <automated>python3 evals/conformance/_flush_probe_tmp.py --self-test; echo "probe_exit=$?"</automated>
    <fails_when>the output contains `probe_exit=0`, or the output contains no line beginning `FAIL: behavior case 11` — either means the guard still passes with the flush call deleted, which is the defect this plan exists to close</fails_when>
    <automated>rm -f evals/conformance/_flush_probe_tmp.py; git status --porcelain</automated>
    <fails_when>any line is printed — the probe copy or another stray file was left in the working tree</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or the output is anything other than `check_repo: 0 violations`</fails_when>
  </verify>

  <acceptance_criteria>
    - Source assertion: `_FlushTrackingHandle` is defined at module level, immediately above
      `def self_test():`, stores the real handle, exposes a `calls` list, defines exactly `write` and
      `flush`, and defines no `__getattr__`.
    - Source assertion: behavior case 11 constructs `_FlushTrackingHandle` over a plainly-opened
      append-mode handle and passes the proxy as `run_matrix`'s `handle` argument. The literal
      `as fake_handle_11` no longer appears in non-comment source.
    - Source assertion: behavior case 11 captures `recorded_calls_11` and `pre_close_text_11` before
      the real handle is closed, and the real handle is closed in a `finally`.
    - Behavior assertion: case 11 asserts `recorded_calls_11` equals exactly
      `['write', 'flush', 'write', 'flush']`, prints the actual list in its FAIL message, and does not
      use a positional pairing of alternating slices.
    - Behavior assertion: case 11 asserts `pre_close_text_11` contains exactly two lines carrying
      `| verdict=`, both naming `verdict=conformant`.
    - Behavior assertion (the plan's central criterion, both directions): running `--self-test` against
      the real `evals/conformance/run_conformance.py` exits `0` with no `FAIL:` line; running
      `--self-test` against a sibling copy inside `evals/conformance/` with the single indented flush
      statement removed from `_write_result_line()` exits non-zero AND prints a line beginning
      `FAIL: behavior case 11`. Both transcripts recorded in the SUMMARY.
    - Source assertion: `_write_result_line()`'s body is unchanged — `grep -c '^    handle\.flush()$'`
      prints `1`, and `git diff` shows no edit inside that function.
    - Source assertion: `score_transcript()`, `FAMILY_PATTERNS`, `FAMILY_LINE_WINDOW_CHARS`,
      `run_session()` and `run_matrix()` are untouched by this task's diff.
    - CLI output: `evals/conformance/_flush_probe_tmp.py` does not exist after the task, and
      `git status --porcelain` prints nothing.
    - CLI output: `python3 tools/check_repo.py` prints `check_repo: 0 violations` and exits `0`.
  </acceptance_criteria>

  <precondition>
    `git status --porcelain` prints nothing before this task starts, and
    `evals/conformance/_flush_probe_tmp.py` does not already exist. A dirty tree makes this task's
    probe-cleanup assertion unsatisfiable and its diff-scope assertions unreadable. If the only
    untracked entries are `.gsd/` or `__pycache__/`, confirm the repo-root `.gitignore` added in 03-12
    still lists them before proceeding.
  </precondition>

  <reversibility rating="reversible">
    A rewrite of one offline self-test case plus one added self-test-only class in one stdlib script.
    No production code path changes, no measurement depends on it, and reverting is a single
    `git revert`.
  </reversibility>

  <done>
    Deleting `_write_result_line()`'s flush call makes CI red instead of green, proven by running the
    deletion and observing the named failure, and the guard is silent against the real tree.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the two statements that claim case 11 proved a property it did not</name>

  <read_first>
    - `evals/conformance/run_conformance.py` lines 24-42 — the module docstring's live-mode `Usage:`
      entry, its `Durability guarantee:` sentence, the parenthetical at the end of that sentence which
      attributes the guarantee's offline proof to behavior case 11, and the following
      `What is still NOT guaranteed:` sentence about concurrent invocations. Only the parenthetical
      changes; the surrounding sentences stay.
    - `evals/conformance/run_conformance.py` behavior case 11 as rewritten by Task 1 — the corrected
      docstring must describe what the test now actually asserts, not a paraphrase written from this
      plan. Read the landed code, not this plan's description of it.
    - `evals/conformance/RESULTS-mod04.md` lines 840-890 — the whole `## Instrument durability fix
      (03-13)` section: its four bold lead-in paragraphs (`**The defect.**`, `**The fix.**`,
      `**What does not change.**`, `**The corrected enumeration.**`). Only one sentence inside
      `**The fix.**` changes.
    - `evals/conformance/RESULTS-mod04.md` lines 11-57 — the `## Scorer anchoring correction (CR-01)`
      banner. This is the register and shape template for the new section: state the defect, its
      direction, its consequence for the figures, and the fix, plainly and without softening.
    - `.planning/phases/03-completeness-audit-artifact-patterns/03-VERIFICATION.md` frontmatter
      `gaps[0].missing` item 2 — the exact correction being asked for, and its conditional: the
      statements may stand as strengthened claims only once the test genuinely discriminates.
    - `tools/check_repo.py` — `check_results_breakdown_count()`, locate it with
      `grep -n 'def check_results_breakdown_count' tools/check_repo.py`. Read enough of it to know
      which bullet shapes in `RESULTS-mod04.md` it parses, so the new prose section does not
      accidentally author one.
  </read_first>

  <files>evals/conformance/run_conformance.py, evals/conformance/RESULTS-mod04.md</files>

  <action>
    In `run_conformance.py`'s module docstring, replace the parenthetical at the end of the
    `Durability guarantee:` sentence — the one that credits behavior case 11 with the offline proof —
    with a precise statement of what case 11 now asserts. Name three things concretely: that case 11
    drives `run_matrix()` through a call-recording proxy handle and asserts the recorded call sequence
    is exactly write-then-flush for every result line with no trailing unflushed write; that it reads
    the results file from its own path before the underlying handle is closed, so the assertion is
    about bytes on disk rather than about buffered state; and that removing `_write_result_line()`'s
    flush call therefore makes case 11 fail rather than pass silently. Then state the evidentiary
    boundary plainly: that discrimination was verified once, by a one-time mutation probe at the commit
    that landed the rewrite, and this file ships no committed mutation harness of its own, unlike
    `tools/check_repo.py --mutation-test`. Keep the `03-REVIEW.md CR-01` citation. Keep the
    `What is still NOT guaranteed:` sentence about concurrent invocations byte-for-byte — that
    disclosure is carried forward from 03-13 unchanged. Change nothing else in the docstring.

    In `RESULTS-mod04.md`'s `## Instrument durability fix (03-13)` section, rewrite only the sentence
    inside `**The fix.**` that credits the new behavior case with proving the durability property.
    Replace it with what case 11 actually asserted as written in 03-13 — that it interrupted a matrix
    mid-run with `KeyboardInterrupt` and found the earlier session lines readable on disk after the
    interrupting exception unwound through the open file's own context manager — followed by a plain
    statement that this did not discriminate the explicit flush call from its absence, that the
    over-attribution was corrected in 03-16, and a pointer to the new section below. Change nothing
    else in that paragraph: the commit SHA `c99a848`, the date `2026-09-16`, the `run_matrix()` and
    `_write_result_line()` names, and the trailing parallel-safety residual sentence all stay. Do not
    touch `**The defect.**`, `**What does not change.**` or `**The corrected enumeration.**`, and do
    not touch any run block anywhere in the file.

    Append a new section `## Self-test discrimination correction (03-16)` at the end of the file, after
    `## v1 disposition decision (03-15)`. Write it as bold-lead-in prose paragraphs in the same
    register the 03-13 section uses, stating five things. First, the defect: behavior case 11 was
    written to prove offline that `_write_result_line()`'s flush call makes each scored session durable
    against a process interruption, and it did not — the interrupting exception was caught inside the
    same block whose own close-on-exit flushed the file regardless, so the case passed identically with
    the flush call deleted. Second, the provenance: found as `CR-01` in 03-REVIEW.md's round-5 review
    and independently reproduced by 03-VERIFICATION.md's verifier, who copied the script to a sibling
    path inside `evals/conformance/`, removed the flush call and observed a clean `self-test PASS`.
    Third, the fix: case 11 now asserts the recorded write-then-flush call sequence on a proxy handle
    and reads the results file before the underlying handle is closed, so it fails whenever the flush
    call is removed, regardless of which exception type interrupts the run. Fourth, the evidence: the
    both-directions probe result — silent against the real file, firing against the mutated copy with
    a named `FAIL:` line — with the date it was run and a pointer to `03-16-SUMMARY.md` for the exact
    transcripts. Fifth, the residual and what does not change: this instrument still ships no committed
    mutation harness of its own, the discrimination is proven once rather than continuously, that
    residual is tracked in `.planning/WINDOWS.md` and routed to Phase 5's eval-harness work; and no
    figure in this file moves — `N_A = 3`, `M_A = 10`, `N_B = 4`, `M_B = 10` and every committed run
    block stand exactly as recorded, `MOD-04` stays unchecked, and this correction repairs a
    verification claim, not a measurement.

    Write the new section as prose only. Do not author any bullet of the form `- <verdict>: N (...)` —
    that is the shape `results-breakdown-count-mismatch` parses, and a decorative example of it in a
    prose section would either trip the check or dilute it.
  </action>

  <verify>
    <automated>grep -c 'proved offline by' evals/conformance/run_conformance.py</automated>
    <fails_when>prints anything other than `0` — the docstring still attributes the proof in its pre-correction wording</fails_when>
    <automated>grep -c 'call-recording proxy' evals/conformance/run_conformance.py</automated>
    <fails_when>prints `0` — the corrected docstring does not describe the mechanism case 11 now uses</fails_when>
    <automated>grep -c 'parallel-safety claim' evals/conformance/run_conformance.py</automated>
    <fails_when>prints `0` — the carried-forward concurrency disclosure was dropped</fails_when>
    <automated>grep -c 'proves this' evals/conformance/RESULTS-mod04.md</automated>
    <fails_when>prints anything other than `0` — the results file still credits the behavior case with a proof it did not perform</fails_when>
    <automated>grep -c '^## Self-test discrimination correction (03-16)' evals/conformance/RESULTS-mod04.md</automated>
    <fails_when>prints anything other than `1`</fails_when>
    <automated>grep -c '^## Instrument durability fix (03-13)' evals/conformance/RESULTS-mod04.md</automated>
    <fails_when>prints anything other than `1` — the 03-13 section was deleted rather than corrected in place</fails_when>
    <automated>grep -c 'c99a848' evals/conformance/RESULTS-mod04.md</automated>
    <fails_when>prints `0` — the 03-13 fix commit citation was lost</fails_when>
    <automated>grep -c 'N_A = 3, M_A = 10' evals/conformance/RESULTS-mod04.md</automated>
    <fails_when>prints `0` — an Arm A figure was altered or deleted</fails_when>
    <automated>grep -c 'N_B = 4, M_B = 10' evals/conformance/RESULTS-mod04.md</automated>
    <fails_when>prints `0` — an Arm B figure was altered or deleted</fails_when>
    <automated>grep -c '^- 20..-..-.. | model=claude-sonnet-5' evals/conformance/RESULTS-mod04.md</automated>
    <fails_when>prints fewer than `58` — a committed run block line was deleted rather than annotated</fails_when>
    <automated>python3 evals/conformance/run_conformance.py --self-test</automated>
    <fails_when>non-zero exit, or any line beginning `FAIL:` appears, or the final line is not `self-test PASS - verdicts discriminated: conformant, no-family, rule-before-family, unscoreable`</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or the output is anything other than `check_repo: 0 violations`</fails_when>
  </verify>

  <acceptance_criteria>
    - Source assertion: the module docstring's live-mode `Usage:` entry states that case 11 asserts a
      recorded write-then-flush call sequence through a call-recording proxy handle, that it reads the
      results file before the underlying handle is closed, and that removing the flush call makes case
      11 fail.
    - Source assertion: the module docstring states that discrimination was verified by a one-time
      mutation probe and that this file ships no committed mutation harness, naming
      `tools/check_repo.py --mutation-test` as the contrast.
    - Source assertion: the docstring's concurrent-invocation non-guarantee sentence is byte-for-byte
      unchanged — `grep -c 'parallel-safety claim'` still prints at least `1`.
    - Source assertion: `grep -c 'proved offline by' evals/conformance/run_conformance.py` prints `0`
      and `grep -c 'proves this' evals/conformance/RESULTS-mod04.md` prints `0`.
    - Source assertion: `## Self-test discrimination correction (03-16)` appears exactly once, after
      `## v1 disposition decision (03-15)`, and covers all five required points: the defect, the
      provenance including both independent reproductions, the fix, the both-directions probe with its
      date, and the residual plus the no-figure-changes statement.
    - Source assertion: `## Instrument durability fix (03-13)` still exists, its commit citation
      `c99a848` is intact, and `**The defect.**`, `**What does not change.**` and
      `**The corrected enumeration.**` are unmodified.
    - Source assertion: the literals `N_A = 3, M_A = 10` and `N_B = 4, M_B = 10` are both still present
      and unmodified, and the run-block line count is still 58 or more.
    - Source assertion: the new section contains no bullet matching the `- <verdict>: N (...)` shape
      that `results-breakdown-count-mismatch` parses.
    - CLI output: `python3 evals/conformance/run_conformance.py --self-test` exits `0` with the exact
      PASS line, and `python3 tools/check_repo.py` prints `check_repo: 0 violations` and exits `0`.
  </acceptance_criteria>

  <precondition>
    Task 1 is committed and its both-directions probe transcript is recorded. The corrected statements
    are only true once the rewritten case genuinely discriminates; writing them before that would
    reproduce the exact defect this plan is closing. If Task 1's probe did not fire on the mutated
    copy, stop and report rather than softening this task's wording to fit.
  </precondition>

  <reversibility rating="reversible">
    A docstring correction and a documentation correction plus an appended prose section in an
    append-only results file. No figure, run block, code path or checkbox moves, so reverting restores
    the exact prior state.
  </reversibility>

  <done>
    Every statement in the repository about what behavior case 11 proves is true of the test that
    actually ships, and the correction is recorded with its provenance rather than quietly applied.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the two consistency findings and track the defect in the ledger</name>

  <read_first>
    - `tools/check_repo.py` lines 1866-1888 — `MUTATION_SOURCES` and `_copy_repo_subset()`'s docstring
      in full, including the clause about what reads under `evals/` and the clause enumerating which
      paths every glob and named-path scan targets. Both clauses change; the `MUTATION_SOURCES` tuple
      value does not.
    - `.planning/phases/03-completeness-audit-artifact-patterns/03-REVIEW.md`, sections `### IN-01` and
      `### IN-02` — both findings and both stated fixes, including IN-02's suggested clause.
    - `README.md` lines 50-70 — the Status paragraph disclosing the measured v1 limitation: the
      `3 of 10 scoreable sessions`, `(30.0%)`, `4 of 10`, `(40.0%)` figures, the date `2026-09-16`, the
      three caveats, and the literal `evals/conformance/RESULTS-mod04.md` pointer that
      `readme-results-pointer-missing` requires.
    - `.planning/WINDOWS.md` in full — both the Markdown table and the fenced JSON block, which must
      agree. Study entries 5, 7 and 9 as the shape for a `fixed` entry, and entry 9's closure-routing
      sentence as the precedent for naming where a residual goes next.
    - `.planning/phases/03-completeness-audit-artifact-patterns/03-VERIFICATION.md`, the
      `.planning/WINDOWS.md` row of the Required Artifacts table — the verifier's note that no entry
      exists yet for this defect and that it is untracked in the ledger.
  </read_first>

  <files>tools/check_repo.py, README.md, .planning/WINDOWS.md</files>

  <action>
    In `tools/check_repo.py`, rewrite the clause in `_copy_repo_subset()`'s docstring that asserts
    nothing under `evals/` is read by any check, so it names the exception instead. The replacement
    clause reads: `No check other than results-breakdown-count-mismatch itself reads anything under
    evals/`. Change the immediately following clause so it says `every other glob and named-path scan
    in this module targets NUMBERING.md, examples/, tools/, or skills/*/SKILL.md paths`. The trailing
    clause about widening the copy not changing what any other code fires against stays. Change nothing
    else in the docstring, nothing in the `MUTATION_SOURCES` tuple, and nothing in the function body.
    This is a comment-only edit and must add no violation code and change no check's behavior — the
    code count stays at 32.

    In `README.md`, amend one sentence in the Status paragraph so it names the movement a decline,
    exactly as `RESULTS-mod04.md` and `WINDOWS.md` entry 8 already do. The target state of that
    sentence is: `... claude-sonnet-5 conformed in 3 of 10 scoreable sessions (30.0%) — a 10-point
    decline against a paired same-instrument baseline of the immediately prior skill version at 4 of 10
    (40.0%).` Preserve every figure verbatim: `3 of 10 scoreable sessions`, `(30.0%)`, `4 of 10`,
    `(40.0%)`, and the date `2026-09-16` earlier in the paragraph. Add no number that is not already
    there — `10-point` is the arithmetic of the two rates the sentence already states, not a new
    measurement, and no significance is claimed for it. Change no other sentence in `README.md`, and
    leave the literal path `evals/conformance/RESULTS-mod04.md` intact so
    `readme-results-pointer-missing` stays silent.

    Record the defect in `.planning/WINDOWS.md` through the `gsd-tools windows` CLI, never by editing
    the file by hand — the Markdown table, the fenced JSON block and the frontmatter counters must stay
    in agreement, and the CLI is what keeps them so. Run
    `gsd-tools windows append --kind unmet-truth --phase 03 --file evals/conformance/run_conformance.py
    --description "<text>"`, note the entry id it returns, then run `gsd-tools windows fixed <id>`.
    Resolve `gsd-tools` the standard way, as `node <runtime>/gsd-core/bin/gsd-tools.cjs`.

    The description must state five things in one paragraph, following entry 9's precedent of naming
    its own closure route. First, the defect: self-test behavior case 11 in
    `evals/conformance/run_conformance.py`, added by 03-13 to prove that `_write_result_line()`'s flush
    call makes each scored session durable against a process interruption, did not discriminate the
    flush call's presence from its absence, because the interrupting exception was caught inside the
    same block whose own close-on-exit flushed the file regardless. Second, the stakes: a future silent
    deletion of that one line would have reintroduced the exact data-loss bug that has already
    destroyed two real measurement runs, shipping with a green self-test and a green CI — the failure
    mode this repository's own mutation-testing preamble exists to prevent. Third, the two independent
    reproductions: 03-REVIEW.md's round-5 `CR-01` and 03-VERIFICATION.md's verifier, each by copying
    the script to a sibling path inside `evals/conformance/`, removing the flush call and observing a
    clean `self-test PASS`. Fourth, the fix: 03-16 rewrote case 11 to assert the recorded
    write-then-flush call sequence through a proxy handle and to read the results file before the
    underlying handle is closed, proven in both directions by a one-time mutation probe. Fifth, the
    residual and its route: `run_conformance.py` still ships no committed mutation harness of its own,
    unlike `tools/check_repo.py --mutation-test`, so this discrimination is proven once rather than
    continuously; that narrower guarantee is disclosed in the module docstring, in
    `RESULTS-mod04.md`'s `## Self-test discrimination correction (03-16)` section and here, and is
    routed to Phase 5's eval-harness work rather than claimed closed.

    Record the entry as `fixed`, not `open`, and state that choice's reason in the SUMMARY: the defect
    this entry names is genuinely closed by this round and proven closed, and the narrower residual is
    disclosed inside this entry's own description rather than parked as a second open window, which
    would be a manufactured blocker on a guarantee nothing has ever depended on. Touch no other entry —
    entries 3, 4 and 6 stay `open`, entry 8 stays `waived` with its reason exactly as 03-15 left it,
    and entries 1, 2, 5, 7 and 9 stay `fixed`.

    Touch no checkbox in `.planning/REQUIREMENTS.md` and no gap status in `03-UAT.md`. Neither file is
    in this plan's `files_modified`.
  </action>

  <verify>
    <automated>grep -c 'No existing check reads anything under' tools/check_repo.py</automated>
    <fails_when>prints anything other than `0` — the self-contradictory clause is still present</fails_when>
    <automated>grep -c 'No check other than' tools/check_repo.py</automated>
    <fails_when>prints anything other than `1`</fails_when>
    <automated>grep -c 'decline' README.md</automated>
    <fails_when>prints `0` — the movement is still never named</fails_when>
    <automated>grep -c '3 of 10 scoreable sessions' README.md</automated>
    <fails_when>prints anything other than `1` — the Arm A figure was reworded or lost</fails_when>
    <automated>grep -c '(30.0%)' README.md</automated>
    <fails_when>prints anything other than `1` — the Arm A rate was altered</fails_when>
    <automated>grep -c '(40.0%)' README.md</automated>
    <fails_when>prints anything other than `1` — the Arm B baseline rate was altered</fails_when>
    <automated>grep -c 'evals/conformance/RESULTS-mod04.md' README.md</automated>
    <fails_when>prints `0` — the results-file pointer `readme-results-pointer-missing` requires is gone</fails_when>
    <automated>python3 -c "import json,pathlib,re;t=pathlib.Path('.planning/WINDOWS.md').read_text();m=re.search(r'json\n(\[.*?\])\n',t,re.S);d=json.loads(m.group(1));print('entries=%d fixed=%d open=%d waived=%d'%(len(d),sum(1 for e in d if e['status']=='fixed'),sum(1 for e in d if e['status']=='open'),sum(1 for e in d if e['status']=='waived')))"</automated>
    <fails_when>a traceback is printed instead of a single line, or the line is not `entries=10 fixed=6 open=3 waived=1` — the ledger's JSON block stopped parsing, the new entry is missing, it was not marked fixed, or another entry's status moved</fails_when>
    <automated>grep -c '^| 10 | 03 | unmet-truth | evals/conformance/run_conformance.py' .planning/WINDOWS.md</automated>
    <fails_when>prints anything other than `1` — the Markdown table row for the new entry is missing or disagrees with the JSON block</fails_when>
    <automated>grep -c "^- \[x\].*UNVERIFIED" .planning/REQUIREMENTS.md</automated>
    <fails_when>prints anything other than `0` — a requirement checkbox was flipped next to an UNVERIFIED marker</fails_when>
    <automated>git diff --name-only HEAD -- .planning/REQUIREMENTS.md skills/</automated>
    <fails_when>any line is printed — a prohibited file was modified</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or the output is anything other than `check_repo: 0 violations`</fails_when>
    <automated>python3 tools/check_repo.py --mutation-test</automated>
    <fails_when>non-zero exit, or the output is anything other than `mutation-test PASS: 32 codes discrimination-proven` — the comment-only edit changed a check's behavior, or the code count moved</fails_when>
  </verify>

  <acceptance_criteria>
    - Source assertion: `_copy_repo_subset()`'s docstring names `results-breakdown-count-mismatch` as
      the one check that reads under `evals/`, and its following clause reads `every other glob and
      named-path scan in this module targets ...`. `grep -c 'No check other than' tools/check_repo.py`
      prints `1`.
    - Source assertion: `MUTATION_SOURCES`'s tuple value is byte-for-byte unchanged and
      `_copy_repo_subset()`'s function body is unchanged — the edit is comment-only.
    - Source assertion: `README.md`'s Status sentence reads `... 3 of 10 scoreable sessions (30.0%) —
      a 10-point decline against a paired same-instrument baseline of the immediately prior skill
      version at 4 of 10 (40.0%).` and `grep -c 'decline' README.md` prints at least `1`.
    - Source assertion: every README figure is preserved — `3 of 10 scoreable sessions`, `(30.0%)` and
      `(40.0%)` each appear exactly once, and no number appears in that paragraph that was not there
      before apart from the `10-point` arithmetic of the two rates already stated.
    - Source assertion: the literal `evals/conformance/RESULTS-mod04.md` is still present in
      `README.md`.
    - Behavior assertion: `.planning/WINDOWS.md` holds exactly 10 entries, of which 6 are `fixed`, 3
      are `open` and 1 is `waived`; the new entry is id 10, phase `03`, kind `unmet-truth`, file
      `evals/conformance/run_conformance.py`, status `fixed`; and the fenced JSON block still parses.
    - Behavior assertion: the new entry's description covers all five required points — the defect,
      the stakes, both independent reproductions, the fix, and the residual with its Phase 5 route.
    - Behavior assertion: entries 3, 4 and 6 are still `open`; entry 8 is still `waived` with its
      03-15 reason text intact; entries 1, 2, 5, 7 and 9 are still `fixed`.
    - CLI output: `git diff --name-only HEAD -- .planning/REQUIREMENTS.md skills/` prints nothing, and
      `grep -c "^- \[x\].*UNVERIFIED" .planning/REQUIREMENTS.md` prints `0`.
    - CLI output: `python3 tools/check_repo.py` prints `check_repo: 0 violations`, and
      `python3 tools/check_repo.py --mutation-test` prints
      `mutation-test PASS: 32 codes discrimination-proven`. Both exit `0`.
  </acceptance_criteria>

  <precondition>
    The `gsd-tools` CLI is resolvable as `node <runtime>/gsd-core/bin/gsd-tools.cjs` and
    `gsd-tools windows status` exits `0` reporting `total_count: 9` before this task starts. The ledger
    must be appended through the CLI so its table, its JSON block and its frontmatter counters stay in
    agreement; a hand edit breaks the verifier's direct JSON parse.
  </precondition>

  <reversibility rating="reversible">
    One comment-only edit, one clause added to a README sentence, and one appended ledger entry. No
    code path, figure, checkbox or check behavior moves. Reverting the ledger entry is a one-line
    operation and the other two are a `git revert`.
  </reversibility>

  <done>
    The checker's own comment no longer contradicts itself, the README names the decline in the same
    term every other record uses, and the defect is in the ledger with its fix and its residual rather
    than untracked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a local, offline, stdlib-only measurement script plus four Markdown documents. The realistic
ASVS L1 surface is narrow, and the register below names only what genuinely exists. This repository
runs no network service, stores no user data, performs no authentication and exposes no endpoint; a
longer register here would be manufactured rather than honest.

| Boundary | Description |
|----------|-------------|
| `self_test()` to the filesystem under `tempfile.mkdtemp()` | Behavior case 11 creates a temp directory and a results file inside it, writes through the new proxy, reads the path back, and removes the directory in a `finally`. No path in this boundary is attacker-controlled: every component is generated by `tempfile`. |
| `_FlushTrackingHandle` to the real file handle it wraps | A self-test-only proxy over a handle the self-test itself opened. It is never constructed in live mode, and `run_matrix()`'s live callers pass a real handle. It forwards two methods and defines no `__getattr__`, so any other access raises rather than being silently proxied. |
| the discrimination probe to the repository working tree | Task 1 writes `evals/conformance/_flush_probe_tmp.py` inside the repository, runs it once with `--self-test`, and deletes it. It is a filtered copy of a committed file, not fetched or generated content. `--self-test` returns before `main()` handles `--out`, so the probe never opens the real results file. |
| `tools/check_repo.py --mutation-test` copying a repo subset | Pre-existing, unchanged by this plan. `_copy_repo_subset()` copies exactly `MUTATION_SOURCES` into a throwaway directory and never copies `.git` or `.planning`. This plan edits only that function's docstring. |
| `claude -p` stdout to `score_transcript()` | The one genuinely untrusted input this instrument handles: model-generated text matched with `re`, never evaluated, executed or interpolated into a command. Completely untouched by this plan — no live session runs here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-16-01 | Tampering | a future silent deletion of `_write_result_line()`'s flush call reintroducing the data-loss defect under a green CI | high | mitigate | Task 1 rewrites behavior case 11 to assert the recorded write-then-flush call sequence on a proxy handle and to read the results file before the underlying handle is closed, so the case fails whenever the flush call is removed, regardless of interrupting exception type. Proven in both directions by the mutation probe: silent against the real file, firing with a named `FAIL: behavior case 11` line against the mutated copy. This is the plan's central control and the finding it exists to close. |
| T-03-16-02 | Repudiation | shipped documents stating a verification claim the test does not perform | high | mitigate | Task 2 corrects both statements — the module docstring's attribution and `RESULTS-mod04.md`'s `**The fix.**` sentence — and records the correction with its provenance in a new dated section rather than editing it away silently. Task 2's `<precondition>` blocks the corrections from landing unless Task 1's probe actually fired. |
| T-03-16-03 | Tampering | an instrument edit that silently changes a measurement | high | mitigate | This plan changes only a self-test case and five documents. `score_transcript()`, `FAMILY_PATTERNS`, `FAMILY_LINE_WINDOW_CHARS`, `run_session()`, `run_matrix()` and `_write_result_line()` are all asserted untouched, the unchanged self-test verdict line is asserted verbatim, both arms' figures are asserted still present byte-for-byte, and the committed run-block line count is asserted at 58 or more. |
| T-03-16-04 | Tampering | the probe copy surviving into the tree or into a commit | medium | mitigate | The probe is created inside `evals/conformance/`, run once and deleted in the same task, and Task 1's verify asserts `git status --porcelain` prints nothing afterward. `--self-test` returns before `main()` reaches `--out` handling, so the copy cannot append to the real results file even if it were run in another mode by mistake. |
| T-03-16-05 | Tampering | concurrent `run_conformance.py` invocations interleaving lines in the same results file | medium | accept | Unchanged from 03-13 and disclosed, not prevented: the docstring's concurrency sentence is carried forward byte-for-byte and re-asserted by this plan's verify. File locking is a larger change than the defect warrants and is not a failure mode this project has ever hit. |
| T-03-16-06 | Tampering | the discrimination itself regressing, because no committed mutation harness holds it | medium | accept | Honestly disclosed rather than hidden. `run_conformance.py` ships no `--mutation-test` equivalent; case 11's discrimination is proven once, at the commit that lands it. The residual is stated in the module docstring, in `RESULTS-mod04.md`'s new section and in `WINDOWS.md` entry 10's description, and routed to Phase 5's eval-harness work. Building a committed mutation harness for this instrument is outside the verifier's two `missing` items and would widen a narrow gap-closure round. |
| T-03-16-07 | Information Disclosure | temp directories and probe files leaking repository content | low | accept | The self-test writes only generated placeholder text and stub transcripts under `tempfile.mkdtemp()` and removes the directory in a `finally`. The probe copy is a filtered copy of an already-public committed file. Nothing enters git history. |
| T-03-16-08 | Denial of Service | an open file handle held across a matrix, and a temp directory left behind on an abnormal exit | low | accept | One append-mode handle per matrix, closed in a `finally`; one temp directory per self-test case, removed in a `finally`. Both pre-existing shapes, neither worsened by this plan. |
| T-03-16-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan. Everything run is stdlib Python 3 against files already in the repository, so `03-RESEARCH.md`'s Package Legitimacy Audit gate is not engaged and no legitimacy checkpoint is required. |

Three threats are dispositioned `mitigate` at high severity, each with a specific asserted control in
the tasks above rather than generic advice. One is `mitigate` at medium. The four `accept` entries at
medium and low are each disclosed in shipped prose or in the ledger, not merely in this plan. Nothing
is left unmitigated, so nothing blocks execution at `security_block_on: high`.
</threat_model>

<verification>
Run all four project gate commands after all three tasks, from the repository root — these are the
exact four `.github/workflows/ci.yml` runs, and they are the only gate this repository has:

- `python3 tools/check_repo.py --self-test` — must exit `0` and begin
  `self-test PASS - verified violation codes:`. Do not assert a trailing `(N codes)` line; the tool
  does not print one (03-15-SUMMARY.md records that plan/tool discrepancy).
- `python3 tools/check_repo.py --mutation-test` — must print
  `mutation-test PASS: 32 codes discrimination-proven` and exit `0`. This plan adds no violation code;
  a different count means something unintended was touched.
- `python3 tools/check_repo.py` — must print `check_repo: 0 violations` and exit `0`.
- `python3 evals/conformance/run_conformance.py --self-test` — must print
  `self-test PASS - verdicts discriminated: conformant, no-family, rule-before-family, unscoreable`
  and exit `0`.

Then re-run the discrimination probe once more, after all three tasks have landed, and record the
transcript in the SUMMARY. The mutated sibling copy's `--self-test` must exit non-zero and print a
line beginning `FAIL: behavior case 11`; the real file's must exit `0` with no `FAIL:` line. A plan
that only asserts the real file still passes would reproduce the exact defect it is closing.

Then confirm the change is scoped exactly as claimed:

- `git diff --stat HEAD~3..HEAD` names only `evals/conformance/run_conformance.py`,
  `evals/conformance/RESULTS-mod04.md`, `tools/check_repo.py`, `README.md` and
  `.planning/WINDOWS.md` (adjust the range to the actual commit count if tasks were committed
  differently).
- `git diff HEAD~3..HEAD -- skills/ .planning/REQUIREMENTS.md .planning/phases/03-completeness-audit-artifact-patterns/03-UAT.md`
  produces no output.
- `git diff HEAD~3..HEAD -- evals/conformance/run_conformance.py` shows no edit inside
  `score_transcript()`, `run_session()`, `run_matrix()` or `_write_result_line()`.
- `git status --porcelain` prints nothing, and `evals/conformance/_flush_probe_tmp.py` does not exist.
</verification>

<success_criteria>
- Running `--self-test` against a copy of `run_conformance.py` with `_write_result_line()`'s flush
  call removed exits non-zero and prints a `FAIL:` line naming behavior case 11; running it against
  the real file exits `0` with no `FAIL:` line. Both transcripts are in `03-16-SUMMARY.md`.
- `_FlushTrackingHandle` exists at module level with a `calls` list and exactly two methods, and case
  11 asserts the recorded sequence is exactly `['write', 'flush', 'write', 'flush']` plus a pre-close
  read showing two `verdict=conformant` lines already on disk.
- `_write_result_line()`'s flush call is still present and unmodified, and `score_transcript()`,
  `run_session()` and `run_matrix()` are untouched.
- No statement in `run_conformance.py` or `RESULTS-mod04.md` credits case 11 with a proof it does not
  perform, and `## Self-test discrimination correction (03-16)` records the defect, both independent
  reproductions, the fix, the probe and the residual.
- `_copy_repo_subset()`'s docstring names `results-breakdown-count-mismatch` as the one check reading
  under `evals/`, and `README.md` names the 10-point movement a decline with every figure preserved.
- `.planning/WINDOWS.md` holds 10 entries — 6 `fixed`, 3 `open`, 1 `waived` — with entry 10 recording
  this defect as `fixed`, and its JSON block still parses.
- All four project gate commands exit `0`, with 32 codes reported by `--mutation-test`.
- No figure moves anywhere: `N_A = 3`, `M_A = 10`, 30.0%, `N_B = 4`, `M_B = 10`, 40.0%, -10.0pp and
  `2026-09-16` are identical across all five records.
- Every checkbox in `.planning/REQUIREMENTS.md` is exactly as this plan found it, `MOD-04` is still
  unchecked, and `grep -c "^- \[x\].*UNVERIFIED" .planning/REQUIREMENTS.md` returns `0`.
</success_criteria>

<output>
Create `.planning/phases/03-completeness-audit-artifact-patterns/03-16-SUMMARY.md` when done.

The SUMMARY must carry, verbatim, the stdout of both directions of the discrimination probe — the
real file's clean `--self-test` and the mutated copy's failing one. That transcript pair is the whole
evidence base for this plan's headline claim, and a SUMMARY that asserts the claim without it repeats
the defect the plan was written to close.
</output>