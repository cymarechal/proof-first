---
phase: 04-distribution-worked-examples
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
  - tools/check_repo.py
autonomous: false
requirements: [DIST-01, DIST-02]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A `.claude-plugin/plugin.json` and a `.claude-plugin/marketplace.json` exist at the repository root, both parse as JSON, and the marketplace entry points at the repository root itself so a harness auto-discovers `skills/proof-first/` with no component override."
    - "`skills/proof-first/SKILL.md`'s frontmatter `metadata.version`, `.claude-plugin/plugin.json`'s `version`, and `.claude-plugin/marketplace.json`'s single plugin entry's `version` are one value, and a CI-enforced code fails the build the moment any one of them moves alone — NUMBERING.md's already-stated versioning obligation becomes mechanical instead of conventional."
    - "A manifest that stops parsing as JSON, loses a required key, or whose `name` stops equalling the shipped skill folder name is a build failure, so DIST-02's install path cannot silently rot."
    - "Every place this repository states its own publish location — both manifests' `homepage` and `repository` values, the marketplace `owner.url`, and later the README install commands — states one identical `owner/repo` string, whether that string is the recorded placeholder or the real published location, so resolving it later is a single verified substitution."
    - "`python3 tools/check_repo.py --mutation-test` reports 35 codes discrimination-proven, up from 32, with the CONTROL line still reading 0 unexpected and no code reported fire-only."
  prohibitions:
    - "MUST NOT guess, infer, or invent this repository's GitHub `owner/repo` string — no git remote is configured, the value is unknown, and it is frozen as a visibly-marked placeholder until the decision checkpoint returns a real value."
    - "MUST NOT add any package-manager dependency, manifest, or lockfile to this repository; `tools/check_repo.py` stays standard-library only and `npx skills` is an end-user command this repository never runs."
    - "MUST NOT hand-roll a JSON reader for `.claude-plugin/*.json`; these are genuine JSON files and stdlib `json.loads` is the correct tool, unlike SKILL.md's frontmatter."
    - "MUST NOT make an unmeasured claim about this skill's own effect in either manifest's `description` field."
    - "MUST NOT use an emoji anywhere in shipped content."
    - "MUST NOT reproduce a contiguous run of any framework source's own wording, or a term coined by a source adopted here as this repository's own label, in either manifest."
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "The Claude Code plugin manifest DIST-02's install path resolves"
      contains: "\"name\": \"proof-first\""
      min_lines: 8
    - path: ".claude-plugin/marketplace.json"
      provides: "The marketplace manifest carrying one plugin entry rooted at the repository itself"
      contains: "\"source\": \"./\""
      min_lines: 10
    - path: "tools/check_repo.py"
      provides: "plugin-manifest-invalid, plugin-manifest-version-mismatch and publish-location-drift, each with firing and silent fixtures and a registered mutation"
      contains: "plugin-manifest-version-mismatch"
  key_links:
    - from: ".claude-plugin/plugin.json"
      to: "skills/proof-first/SKILL.md"
      via: "the manifest version equals the skill frontmatter's metadata.version"
      pattern: "\"version\": \"0\\.1\\.0\""
    - from: ".claude-plugin/marketplace.json"
      to: ".claude-plugin/plugin.json"
      via: "the single plugin entry restates the same name, version, license, homepage and repository"
      pattern: "\"name\": \"proof-first\""
    - from: "tools/check_repo.py"
      to: ".claude-plugin/plugin.json"
      via: "run_plugin_checks parses both manifests with stdlib json and compares them to the skill frontmatter"
      pattern: "\\.claude-plugin"
    - from: ".claude-plugin/marketplace.json"
      to: "skills/proof-first/"
      via: "source \"./\" makes the repository root the plugin root, so the harness auto-discovers the skill folder"
      pattern: "\"source\": \"\\./\""
---

<objective>
Prove Phase 4's whole distribution pipeline on one thin, real, shippable slice: the Claude Code
plugin channel, wired from `skills/proof-first/SKILL.md`'s frontmatter version through two new
root-level manifests to three CI-enforced violation codes, each discrimination-proven against a copy
of the real repository.

Purpose: the plugin channel is the only Phase 4 channel that touches every layer this phase will
modify — a new committed artifact outside `skills/`, a cross-file binding back into the frozen skill
content, three new `check_repo.py` codes with the full self-test-plus-mutation contract, and the
publish-location placeholder that README and the derivatives both inherit. Proving that whole path
green on one channel catches an architectural dead end after one commit instead of after four plans.
The placeholder mechanism this plan freezes is what makes the unknown `owner/repo` string a single
verified substitution later rather than a scatter of guesses, and `publish-location-drift` is
deliberately written over the full prospective file list so `04-03` and `04-04` inherit enforcement
with no further code change.

Output: `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json`; three new violation codes
(`plugin-manifest-invalid`, `plugin-manifest-version-mismatch`, `publish-location-drift`), each with
a firing fixture, a silent fixture and a registered mutation; the discrimination-proven total moved
from 32 to 35.
</objective>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-distribution-worked-examples/04-RESEARCH.md
@.planning/phases/04-distribution-worked-examples/04-PATTERNS.md
@.planning/phases/04-distribution-worked-examples/04-VALIDATION.md
@NUMBERING.md
@NOTICES.md
@README.md
@tools/check_repo.py
@skills/proof-first/SKILL.md
</context>

<interfaces>
<!-- Every line below was read from the named file during this planning session (2026-09-17). -->
<!-- Use these directly; no codebase exploration is needed to start.                          -->

**Measured baseline, re-verified this planning session — do not re-derive:**
- `python3 tools/check_repo.py` prints `check_repo: 0 violations`.
- `python3 tools/check_repo.py --self-test` prints `self-test PASS - verified violation codes:` followed by 32 code names.
- `python3 tools/check_repo.py --mutation-test` prints `mutation-test PASS: 32 codes discrimination-proven` and a CONTROL line reading `0 violations on the unmutated copy (0 known-open per KNOWN_OPEN_VIOLATIONS, 0 unexpected)`.
- `wc -l -w skills/proof-first/SKILL.md` prints `308 3710`; the estimated-token margin under the 5000 ceiling is 177. **This plan makes no edit to `SKILL.md`**, so that margin is untouched.
- `git remote -v` prints nothing. There is no configured remote and therefore no known publish location.

**The version source of truth (`skills/proof-first/SKILL.md` frontmatter, read this session):**
```yaml
name: proof-first
description: |
  Write or check RFP and RFI responses, solution proposals, executive
  summaries, and demo or discovery documents for technical presales and
  bid teams. ...
license: MIT
metadata:
  version: "0.1.0"
```
The version lives at the **nested** key `metadata.version`, not a column-zero key.
`parse_frontmatter(path)` (line 1091) returns `(keys, problems)` where a nested map's value is kept
as **opaque joined indented text** — so `keys['metadata']` is the string `version: "0.1.0"`. Extract
the version with one anchored regex over that string (`^version:\s*"?([^"\s]+)"?\s*$` with
`re.MULTILINE`); do not add a general YAML parser and do not change `parse_frontmatter`.

**`NUMBERING.md`'s already-frozen obligation (lines 143-149, quoted verbatim in `04-RESEARCH.md`):**
> "Semantic versioning is carried in the skill's frontmatter `metadata` and in the plugin manifest, matched by a git tag."

This sentence was written in Phase 1 before any manifest existed. `plugin-manifest-version-mismatch`
makes it mechanical. Do not edit `NUMBERING.md` in this plan.

**Existing helpers to reuse verbatim (`tools/check_repo.py`, line numbers read this session):**
- `SKILL_GLOB = 'skills/*/SKILL.md'` (line 385) — a fixed one-level pattern. Never widen it; `self_test()` writes a fixture one level shallower on purpose.
- `parse_frontmatter(path)` (line 1091) — described above.
- `_write(path, content)` (line 2420) — the self-test fixture writer.
- `_copy_repo_subset` (line 1869) / `MUTATION_SOURCES` (line 1866) — currently `('LICENSE', 'NUMBERING.md', 'NOTICES.md', 'README.md', 'examples', 'tools', 'skills', 'evals')`. **`.claude-plugin` is not in this tuple.** The mutation control copy will not contain the new manifests unless this plan adds `'.claude-plugin'` to `MUTATION_SOURCES` — exactly the widening `02-01` had to do for `skills` and `03-14` had to do for `evals`. Without it both new manifest mutations are inert.
- `KNOWN_OPEN_VIOLATIONS = frozenset()` (line 1851) — currently empty. Leave it empty.

**Contracts every new check must satisfy (unchanged from Phases 1-3):**
- A check function returns `list[(subject, "code-name message")]`. Both `mutation_test` and `self_test` recover the code via `line.split(' ', 1)[0]`, so the leading token of the message must be exactly the code string, unbroken by whitespace.
- New codes need a new `PLUGIN_CHECK_CODES` list and a `run_plugin_checks(repo_root)` aggregator, both concatenated into `ALL_CHECK_CODES` (line 1805) and called from `run_all_checks` (line 1816). Follow how `CATALOG_CHECK_CODES` (line 1775) and `run_catalog_checks` (line 1784) are wired.
- `MUTATIONS` (line 2296) is a list of `(code, description, mutate_fn)`. `mutation_test` (line 2332) fails any code in `ALL_CHECK_CODES` with no registered mutation. Each `_mutate_*` takes `root: Path` and mutates a **copy** of the real repository tree.
- `mutation_test` counts a code as discrimination-proven only when it is **silent on the unmutated control copy and fires on the mutated copy**. A code that already fires on the control is reported FIRE-ONLY and excluded from the total.
- A new bad fixture's codes must be unioned into `bad_codes` inside `self_test()` (line 3223 onward) or the generic coverage loop reports the new code as never observed firing.
- Every implemented code gets a bullet in the module's top-of-file docstring under "Violation codes implemented in this file:", stating what it catches and then plainly stating its **Declared ceiling**.

**Absence-is-not-failure convention.** `check_catalog_count`, `check_artifact_family_sections` and
`check_mc_catalog_id_drift` all return no violations when their subject file does not exist, checked
before any read. All three new codes in this plan follow it: a repository with no `.claude-plugin/`
directory is silent, and `publish-location-drift` skips any prospective carrier that does not exist.
This is what keeps `--self-test`'s many existing fixture roots green.

**Traps this repository has already been bitten by — two are live against this plan:**
1. A literal multi-word anchor an acceptance criterion greps for must sit entirely on one physical line. `02-04` lost two anchors to an authored line-wrap and fixed it by moving the wrap point, not the wording.
2. A mutation that operates on a file absent from `MUTATION_SOURCES`'s copied subset silently does nothing and the code is reported as never proven. See the `MUTATION_SOURCES` note above.

**Files this plan must NOT touch:** `skills/proof-first/SKILL.md` (its 177-token margin belongs to
`04-03`), `NUMBERING.md`, `NOTICES.md`, `README.md` (`04-04` owns it), and anything under `.gsd/`
(gitignored — never write a path under it into any plan, criterion, or commit).
</interfaces>

<decisions>
## Phase-wide interface choices this plan freezes (planner discretion — no `04-CONTEXT.md` exists, `/gsd-discuss-phase` did not run for this phase)

Every later Phase 4 plan reads this table and complies. Numbering continues the `P{phase}-{n}` scheme
Phase 3 used.

| ID | Choice | Reversibility |
|----|--------|---------------|
| P4-01 | **The plugin manifest name and the marketplace name are both `proof-first`, equal to the skill folder name `skills/proof-first/`.** A single identifier across the Agent Skills folder, the plugin manifest, the marketplace manifest and the install command `proof-first@proof-first` means a user types one word. `plugin-manifest-invalid` enforces the folder-name equality mechanically. | one-way |
| P4-02 | **The marketplace's single plugin entry uses `"source": "./"`** — the repository root is the plugin root, and the harness auto-discovers `skills/*/` and `output-styles/*.md` beneath it. No `skills` or `output-styles` component-override key is written into either manifest. This mirrors the real, working SimpleEnglish manifests field-for-field and keeps `04-03`'s new `output-styles/proof-first.md` discoverable with no manifest edit. | costly |
| P4-03 | **The unknown publish location is frozen as the literal three-token placeholder `<owner>/<repo>`**, written into `homepage` and `repository` as `https://github.com/<owner>/<repo>`, into `owner.url` as `https://github.com/<owner>`, and later into README's install commands as the bare `<owner>/<repo>`. Angle brackets are universally readable as a placeholder and cannot be mistaken for a real account. `publish-location-drift` asserts every carrier states **one** value — placeholder or real — so the eventual substitution is one verified diff. | reversible |
| P4-04 | **Three new codes in this plan, nine phase-wide, taking the discrimination-proven total 32 → 35 → 37 → 39 → 41.** This plan: `plugin-manifest-invalid`, `plugin-manifest-version-mismatch`, `publish-location-drift`. `04-02`: `before-after-family-missing`, `before-after-citation-missing`. `04-03`: `skill-derivative-stale`, `derivative-rule-coverage-incomplete`. `04-04`: `readme-install-path-missing`, `readme-before-after-order`. | reversible |
| P4-05 | **`publish-location-drift` is written once, here, over the full prospective carrier list** — `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md` — with absent files skipped. `04-04` inherits enforcement of README's install commands with no new code and no edit to this check. | reversible |
| P4-06 | **Both derivative artifacts carry the identical generated body**: `SKILL.md`'s post-frontmatter body plus the full text of `references/deletion-test.md`, `references/completeness-audit.md`, `references/artifact-patterns.md` and `references/checklist.md`, with `references/worked-examples.md` omitted and its omission stated in each derivative's own preamble. They differ only in wrapper — output-style YAML frontmatter versus a plain paste preamble. Rejected alternative, recorded: a short condensed output style in SimpleEnglish's shape, which would silently drop the completeness audit's eight dimensions and all four artifact-family conventions and could not honestly be offered as the same discipline. Implemented in `04-03`. | costly |
| P4-07 | **Phase 4's four plans execute in four sequential waves, with no parallelism.** All four modify `tools/check_repo.py`, and every plan's verify command is the whole-repository checker, which reads every Markdown file under `skills/` and `examples/` plus the root registries — disjoint file sets are therefore not sufficient isolation. Identical reasoning to Phase 3's P3-09. | reversible |
| P4-08 | **Threat IDs use `T-04-NN`, unique phase-wide, never restarting per plan.** This plan owns `T-04-01` through `T-04-05` and `T-04-SC1`; `04-02` continues at `T-04-06`, `04-03` at `T-04-11`, `04-04` at `T-04-16`. Phase 2's colliding per-plan schemes caused a real threat-register assembly incident; Phase 3 fixed it with this rule. | reversible |
| P4-09 | **`tools/generate_derivatives.py` emits no timestamp and no host-dependent value.** Its output is a pure function of the source bytes, so `--check` mode can assert byte equality against the committed files on any machine on any day. Implemented in `04-03`; recorded here because it is a phase-wide constraint on that file's design. | costly |

## Accepted gaps (recorded, not silently left)

- The manifests ship with the `<owner>/<repo>` placeholder unless the decision checkpoint returns a real value. This is a disclosed, ledgered state, not an oversight: the string is genuinely unknown (no git remote), and `publish-location-drift` guarantees the eventual swap is one consistent substitution the build verifies.
- `plugin-manifest-invalid` asserts key presence, JSON well-formedness and the folder-name equality. It does not and cannot assert that a live `claude plugin marketplace add` succeeds — that is a network-and-harness behaviour no file-reading checker observes, and it stays a manual smoke test recorded in `04-VALIDATION.md`'s Manual-Only table.
- `check_undefined_id`, `check_unlisted_figure` and the `pointer-*` checks do not scan `.claude-plugin/`, `output-styles/` or `prompts/`. Those roots are deliberately left outside the existing scanners this phase; widening them would change three proven checks' blast radius for no requirement. Recorded as deliberately out of scope.

## Flagged planner assumptions

- **A4-01 (unresolved, routed to the decision checkpoint in this plan).** This repository's public `owner/repo` is unknown — `git remote -v` printed nothing this planning session. Every install command DIST-01, DIST-02 and DIST-06 must state verbatim depends on it. It is not guessed anywhere; it is frozen as P4-03's placeholder and raised at the checkpoint.
- **A4-02.** Whether a Claude Code output style as long as P4-06 produces is a reasonable user experience is a judgment no file in this repository settles. The alternative silently under-delivers on DIST-03's own success criterion, which is the worse failure under this project's evidence discipline. Recorded, not resolved.
- **A4-03.** The mechanically concatenated derivatives will contain the attribution pointer line three times (once from `SKILL.md`, once from `completeness-audit.md`, once from `artifact-patterns.md`). Neither derivative is a `NOTICES.md` carrier, so `pointer-duplicated` does not fire and no violation results. Accepted as a cosmetic artifact of mechanical concatenation and stated in the generator's own docstring by `04-03`.

## Edge-probe accounting (spec-less fallback — 11 items in, 11 accounted for)

No SPEC file exists for this phase, so the deterministic edge probe's 11 applicable items are
resolved here. **6 resolved to `explicit`** and are authored as plain `must_haves.truths` strings in
their owning plans. **5 are `unclassified` and stay unresolved**, surfaced below as flagged
assumptions rather than auto-resolved or dropped.

| # | Requirement | Category | Resolution | Where |
|---|---|---|---|---|
| 1 | EX-02 | adjacency (a family heading present with no citation) | explicit | `04-02` truth: `before-after-citation-missing` fires for a family section whose body carries no allocated rule-ID token |
| 2 | EX-02 | empty (a family section with zero before/after rows) | explicit | `04-02` truth: `before-after-family-missing` fires for a family heading present without both a `✗` line and a `✓` line |
| 3 | EX-02 | ordering (is the four-family order fixed and enforced) | explicit | `04-02` truth: the four family headings appear in `ARTIFACT_FAMILY_SECTIONS` order, enforced by `before-after-family-missing` |
| 4 | DIST-01 | unclassified | **unresolved — flagged assumption A4-E1** | A real skills-CLI install cannot be exercised without a published repository; the structural half is `plugin-manifest-invalid` plus `publish-location-drift`, the live half is a manual smoke test |
| 5 | DIST-02 | unclassified | **unresolved — flagged assumption A4-E2** | A real marketplace add/install flow needs a live harness and a reachable source; structural half only in this plan |
| 6 | DIST-03 | unclassified | **unresolved — flagged assumption A4-E3** | Whether selecting the output style changes a live session's behaviour is model behaviour no checker observes |
| 7 | DIST-04 | unclassified | **unresolved — flagged assumption A4-E4** | Whether a pasted system prompt yields the same discipline is model behaviour; asserting it as measured is forbidden until Phase 5 |
| 8 | DIST-05 | unclassified | **unresolved — flagged assumption A4-E5** | Whether the generator's own derivation logic is semantically correct is not provable by a hash of its inputs; stated as a declared ceiling in `04-03` |
| 9 | DIST-06 | adjacency (README section presence) | explicit | `04-04` truth: `readme-install-path-missing` fires once per missing harness install anchor |
| 10 | DIST-06 | empty (an Install heading with no commands under it) | explicit | `04-04` truth: an `## Install` heading carrying none of the four anchors fires `readme-install-path-missing` four times |
| 11 | DIST-06 | ordering (before/after pairs precede install instructions) | explicit | `04-04` truth: `readme-before-after-order` fires unless the before/after heading precedes both `## Install` and `## Status` |

## Capability-gate outcomes (recorded so a later reader sees they were run, not skipped)

- **API coverage gate:** the detector was run this planning session against the phase scope and returned `{"detected":false}`. This phase integrates no external API, SDK or service — `npx skills` is a command an end user runs from README, never something this repository calls. A reasoned `COVERAGE.md` declaration is committed alongside these plans so the seal-time gate has an artifact to read.
- **Assumption-delta checkpoint:** run this planning session, returned `{"detected":false,"signals":[]}`. No singular-to-plural, required-to-optional or derived-to-chosen transition is in scope. Checkpoint skipped, not raised.
- **Schema push gate:** no ORM-shaped path exists anywhere in this repository. Skipped silently.
</decisions>

<source_audit>
## Multi-Source Coverage Audit (phase-wide)

| SOURCE | ID | Feature/Requirement | Plan | Status |
|---|---|---|---|---|
| GOAL | — | The finished rule catalog reaches a writer through every distribution channel the project promises, backed by real worked examples | 04-01..04-04 | COVERED |
| REQ | EX-02 | Before/after pairs covering each artifact family, after column citing real rule numbers | 04-02 | COVERED |
| REQ | DIST-01 | Install via the skills CLI with one command | 04-01 (manifest + publish-location mechanism), 04-04 (the stated command) | COVERED |
| REQ | DIST-02 | Install as a Claude Code plugin from a marketplace manifest in this repo | 04-01 | COVERED |
| REQ | DIST-03 | Turn the discipline on permanently as a Claude Code output style | 04-03 | COVERED |
| REQ | DIST-04 | Paste a system-prompt version in a harness with no skill support | 04-03 | COVERED |
| REQ | DIST-05 | Derivatives regenerated from SKILL.md whenever it changes, documented re-sync step | 04-03 (generator, staleness code, CI step, generator docstring), 04-04 (README re-sync section) | COVERED |
| REQ | DIST-06 | README leads with before/after pairs and states install paths per harness | 04-04 | COVERED |
| RESEARCH | Summary / Track B | Plugin scaffolding first — cheapest, fully precedented, mirrors the real working manifests field-for-field | 04-01 | COVERED |
| RESEARCH | Summary / Track A | `examples/before-after.md` with one family-level pair per family, gated by two new codes | 04-02 | COVERED |
| RESEARCH | Summary / Track C | A stdlib-only generator emitting both derivatives, hash-stamped, with a staleness code — never hand-maintained prose | 04-03 | COVERED |
| RESEARCH | Summary / Track D | README lead-in and Install sections only; Status and the measured-figure gate preserved | 04-04 | COVERED |
| RESEARCH | Pattern 1 | The real working manifest shapes, field-for-field, with `source: "./"` | 04-01 (P4-01, P4-02) | COVERED |
| RESEARCH | Pattern 2 | NUMBERING.md's existing version-match obligation made mechanical | 04-01 | COVERED |
| RESEARCH | Pattern 3 | Proof First's reference files carry instructional content a faithful derivative cannot omit | 04-01 (P4-06), 04-03 (implemented) | COVERED |
| RESEARCH | Pattern 4 | Generator plus hash-drift check, following the stated-value-vs-registry idiom, with its three-part declared ceiling | 04-03 | COVERED |
| RESEARCH | Pattern 5 | The four frozen family names and the closed 39-ID citable set | 04-02 | COVERED |
| RESEARCH | Don't Hand-Roll | stdlib `json` for the manifests, never a bespoke reader | 04-01 | COVERED |
| RESEARCH | Don't Hand-Roll | Reuse `ARTIFACT_FAMILY_SECTIONS`; never a third copy of the four strings | 04-02 | COVERED |
| RESEARCH | Don't Hand-Roll | No hand-maintained derivative kept in sync by contributor discipline | 04-03 | COVERED |
| RESEARCH | Pitfall 1 | Do not copy SimpleEnglish's derivative prose; generate mechanically | 04-03 | COVERED |
| RESEARCH | Pitfall 2 | Never assert sameness of outcome as fact in README or the derivatives | 04-03, 04-04 | COVERED |
| RESEARCH | Pitfall 3 | Build the version-match check as a deliverable, not an assumption | 04-01 | COVERED |
| RESEARCH | Pitfall 4 | Never invent the GitHub owner/repo; placeholder plus a human gate | 04-01 (P4-03, A4-01, the decision checkpoint) | COVERED |
| RESEARCH | Open Q1 | Output style content scope | 04-01 (P4-06 decides it), 04-03 (implements it) | COVERED |
| RESEARCH | Open Q2 | Whether worked-examples.md belongs in the derivatives | 04-01 (P4-06 omits it, states the omission) | COVERED |
| RESEARCH | Open Q3 | The actual GitHub owner/repo | 04-01 (decision checkpoint) | COVERED |
| RESEARCH | Validation | Every new code ships the three-part contract: wired check, firing and silent fixtures, registered mutation | 04-01..04-04 | COVERED |
| RESEARCH | Validation | Discrimination-proven count rises from 32, CONTROL still 0 unexpected | 04-01..04-04 | COVERED |
| PATTERNS | §plugin manifests | External SimpleEnglish manifests are a field-shape template only, not a repo path to modify | 04-01 | COVERED |
| PATTERNS | §check_repo Template A | Stated-value-vs-registry shape for the version and staleness checks | 04-01, 04-03 | COVERED |
| PATTERNS | §check_repo Template B | Section-presence shape reusing the frozen family tuple | 04-02 | COVERED |
| PATTERNS | §generate_derivatives | Docstring shape: one job, explicit stdlib import list, three-part declared ceiling | 04-03 | COVERED |
| PATTERNS | §before-after.md | Family granularity, every fact from the deal brief, citations from the closed 39-ID set | 04-02 | COVERED |
| PATTERNS | §README | The literal `evals/conformance/RESULTS-mod04.md` string must survive verbatim through the rewrite | 04-04 | COVERED |
| PATTERNS | §ci.yml | Append to the existing multi-line run block, never a new steps entry | 04-03 | COVERED |
| PATTERNS | Shared | Stdlib-only import discipline; declared-ceiling disclosure; frozen-interface reuse; three-part contract | 04-01..04-04 | COVERED |
| CONTEXT | — | No `04-CONTEXT.md` exists; `/gsd-discuss-phase` did not run for this phase. The research's three discretion points are decided in `<decisions>` above as P4-03, P4-06 and A4-02, and only the publish location is routed back to the user. | 04-01 | COVERED |

No row is MISSING.
</source_audit>

<artifacts_this_phase_produces>
Every symbol, code, path and identifier Phase 4 creates. Nothing below exists on disk before this
phase, so nothing below is drift from an existing source.

**New files (6):** `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`,
`examples/before-after.md`, `tools/generate_derivatives.py`, `output-styles/proof-first.md`,
`prompts/system-prompt.md`.

**New violation codes (9):** `plugin-manifest-invalid`, `plugin-manifest-version-mismatch`,
`publish-location-drift` (04-01); `before-after-family-missing`, `before-after-citation-missing`
(04-02); `skill-derivative-stale`, `derivative-rule-coverage-incomplete` (04-03);
`readme-install-path-missing`, `readme-before-after-order` (04-04).

**New `tools/check_repo.py` symbols:** `PLUGIN_MANIFEST_PATH`, `MARKETPLACE_MANIFEST_PATH`,
`PLUGIN_REQUIRED_KEYS`, `MARKETPLACE_REQUIRED_KEYS`, `PUBLISH_LOCATION_CARRIERS`,
`PUBLISH_LOCATION_RE`, `SKILLS_CLI_INSTALL_RE`, `MARKETPLACE_ADD_RE`, `DERIVATIVE_PATHS`,
`DERIVATIVE_SOURCE_NAMES`, `DERIVATIVE_STAMP_RE`, `BEFORE_AFTER_PATH`, `README_INSTALL_ANCHORS`,
`README_BEFORE_AFTER_HEADING`, `README_INSTALL_HEADING`, `_load_json_manifest`,
`_skill_metadata_version`, `_publish_locations_in`, `_derivative_stamp`, `_hash_sources`,
`check_plugin_manifest_invalid`, `check_plugin_manifest_version`, `check_publish_location_drift`,
`check_before_after_families`, `check_before_after_citations`, `check_skill_derivative_stale`,
`check_derivative_rule_coverage`, `check_readme_install_paths`, `check_readme_before_after_order`,
`PLUGIN_CHECK_CODES`, `run_plugin_checks`, `DERIVATIVE_CHECK_CODES`, `run_derivative_checks`,
`EXAMPLE_CHECK_CODES`, `run_example_checks`.

**New mutation functions and `MUTATIONS` entries (9):** `_mutate_plugin_manifest_invalid`,
`_mutate_plugin_manifest_version_mismatch`, `_mutate_publish_location_drift`,
`_mutate_before_after_family_missing`, `_mutate_before_after_citation_missing`,
`_mutate_skill_derivative_stale`, `_mutate_derivative_rule_coverage`,
`_mutate_readme_install_path_missing`, `_mutate_readme_before_after_order`.

**New self-test fixture helpers and scratch roots:** `_good_plugin_manifests`,
`_bad_plugin_manifests`, `_good_before_after`, `_bad_before_after`, `_good_derivative`,
`_stale_derivative`, `_good_readme_install`, `_bad_readme_install`, and their
`plugin_good_root` / `plugin_bad_root` / `beforeafter_good_root` / `beforeafter_bad_root` /
`derivative_good_root` / `derivative_bad_root` / `readme_install_good_root` /
`readme_install_bad_root` scratch roots inside `self_test()`.

**New `tools/generate_derivatives.py` symbols:** `SKILL_PATH`, `REFERENCE_SOURCES`,
`DERIVATIVE_SOURCE_NAMES`, `STAMP_TEMPLATE`, `read_sources`, `source_hash`, `build_body`,
`render_output_style`, `render_system_prompt`, `write_derivatives`, `check_derivatives`, `main`,
and the `--check` command-line flag.

**Modified existing symbols:** `MUTATION_SOURCES` (gains `'.claude-plugin'`, `'output-styles'`,
`'prompts'`), `ALL_CHECK_CODES`, `run_all_checks`, the module docstring's violation-code block,
`.github/workflows/ci.yml`'s existing multi-line run block, and `README.md`'s section set.

**New stable output vocabulary:** the placeholder token `<owner>/<repo>`; the derivative stamp
comment form `<!-- generated by tools/generate_derivatives.py from <paths> sha256:<64 hex> -->`; the
README headings `## Before and after` and `## Install`.

**No rule ID is allocated, deprecated, renumbered or retitled by this phase.** `NUMBERING.md`'s
Allocated IDs table is read, never written.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Freeze the two one-way distribution identifiers Phase 4 and every future release bind to — the plugin/marketplace name `proof-first` and the marketplace `source: "./"` rooting — and resolve this repository's publish location, or confirm it stays the recorded placeholder</decision>
  <context>
No `04-CONTEXT.md` exists — `/gsd-discuss-phase` did not run for this phase, so nothing here was
locked by a user decision and all of it is the planner's.

Two of these are published contracts the moment anyone installs the plugin. A user who has run
`claude plugin install proof-first@proof-first` has that identifier written into their local plugin
state; renaming it later is a migration for every installed user, not a refactor. The marketplace
`source: "./"` decision determines whether the repository root or a subdirectory is the plugin root,
and therefore whether `04-03`'s new `output-styles/proof-first.md` is auto-discovered at all — moving
it later relocates every component path.

The third is genuinely unknown rather than chosen. `git remote -v` printed nothing this planning
session: this repository has no configured remote, so its eventual GitHub `owner/repo` is a fact no
file in this tree carries. Three requirements state it verbatim in an install command a user will
copy and paste (DIST-01, DIST-02, DIST-06). `04-RESEARCH.md` Pitfall 4 names guessing it as the exact
failure this gate exists to prevent, and the Package Legitimacy Protocol's posture on unverifiable
external facts applies unchanged. This is raised as a decision rather than a verification because
`workflow.human_verify_mode` is `end-of-phase` in this project's config — content checks are harvested
into UAT, but this one blocks two files being written.
  </context>
  <options>
    <option id="adopt-with-placeholder">
      <name>Adopt the proposal verbatim and keep the publish-location placeholder</name>
      <pros>
`proof-first` as one identifier across the skill folder, the plugin manifest, the marketplace manifest
and the install command means a user types one word and never reconciles two names;
`plugin-manifest-invalid` then enforces the folder-name equality mechanically rather than by
convention. `source: "./"` mirrors the real, working SimpleEnglish marketplace field-for-field and
keeps every future component — the output style, any later command or agent — discoverable with no
manifest edit. The publish location stays the literal `<owner>/<repo>`, visibly a placeholder to any
reader, recorded as an open item in `.planning/WINDOWS.md`, and guarded by `publish-location-drift`
so that replacing it later is one substitution the build verifies rather than a scatter of strings to
find by hand. Phase 4 ships complete and honest with a disclosed unknown, which is this project's own
established posture for exactly this class of fact.
      </pros>
      <cons>
The committed manifests carry a non-resolving `https://github.com/&lt;owner&gt;/&lt;repo&gt;` URL until the
substitution happens, so the repository is not publishable as-is — Phase 6's launch gate must close
this or the README sends users nowhere. Nothing in the build fails while the placeholder is present,
so the open item depends on the ledger entry being read rather than on CI going red.
      </cons>
    </option>
    <option id="supply-the-location">
      <name>Adopt the identifiers and supply the real publish location now</name>
      <pros>
Every install command, both manifest `homepage`/`repository` values and the marketplace `owner.url`
carry the real string from the first commit, and `publish-location-drift` verifies they agree.
`04-04`'s README ships copy-pasteable commands. The `.planning/WINDOWS.md` open item never opens, and
Phase 6's launch gate has one less thing to close.
      </pros>
      <cons>
Requires the location to actually be decided — if the repository is later published somewhere else,
the substitution has to happen anyway, and a wrong-but-confident string is worse than a visible
placeholder under this project's own evidence discipline.
      </cons>
    </option>
    <option id="amend-an-identifier">
      <name>Amend the plugin name, the marketplace name, or the source rooting before anything is written</name>
      <pros>
Nothing has been written yet, so any of the following changes at zero cost right now: the plugin
manifest `name`, the marketplace `name` (they need not be equal — the install command is
`&lt;plugin&gt;@&lt;marketplace&gt;`), and whether `source` points at the repository root or at a subdirectory.
Name the field and its replacement value.
      </pros>
      <cons>
A plugin name differing from the skill folder name removes the mechanical folder-name equality
`plugin-manifest-invalid` would otherwise enforce, and leaves a user reconciling two identifiers. A
`source` other than `"./"` means `04-03`'s output style needs an explicit component override key in
the manifest and a different on-disk location.
      </cons>
    </option>
  </options>
  <resume-signal>Reply "adopt" to take the proposal verbatim with the placeholder, or give the real `owner/repo` string, or name the field and its replacement value</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a user can install this repository as a Claude Code plugin" — manifests plus the version binding, proven live</name>
  <reversibility rating="one-way">This task writes the plugin identifier a user's local plugin state records on install and the marketplace rooting every future component path depends on; both are published contracts once anyone installs, so the decision checkpoint above gates them.</reversibility>
  <precondition>`skills/proof-first/SKILL.md` exists with a frontmatter block whose `metadata:` key carries a `version:` sub-key (measured `"0.1.0"` at planning time), and `skills/` contains exactly one skill folder. Both were verified present this planning session; `plugin-manifest-version-mismatch` has no source of truth to compare against if either stops being true.</precondition>
  <files>.claude-plugin/plugin.json, .claude-plugin/marketplace.json, tools/check_repo.py</files>
  <read_first>
- `skills/proof-first/SKILL.md` lines 1-14 — the frontmatter block, especially the nested `metadata:` map and its `version:` sub-key. This is the single source of truth for the version string; never retype it from memory.
- `tools/check_repo.py`'s module docstring in full — note that every violation-code entry states what it catches and then plainly states its **Declared ceiling**; the new entry must match that rhetorical shape.
- `tools/check_repo.py` lines 1085-1160 — `parse_frontmatter` in full, including its docstring's statement that a nested map's value is kept as opaque indented text. This is why a targeted regex is needed and a YAML parser is not.
- `tools/check_repo.py` lines 1775-1830 — `CATALOG_CHECK_CODES`, `run_catalog_checks`, `ALL_CHECK_CODES`, `run_all_checks`. This is the wiring shape the new `PLUGIN_CHECK_CODES` / `run_plugin_checks` pair mirrors.
- `tools/check_repo.py` lines 1400-1428 — `check_catalog_count`, the stated-value-vs-registry template quoted verbatim in `04-PATTERNS.md`: parse a stated value, treat "unstated" as its own condition, compare, name both values in the message.
- `tools/check_repo.py` lines 1840-1900 — `KNOWN_OPEN_VIOLATIONS`, `MUTATION_SOURCES`, `_copy_repo_subset`. Read `MUTATION_SOURCES` twice: `.claude-plugin` is absent from it and the mutation is inert without the addition.
- `tools/check_repo.py` lines 2290-2400 — the `MUTATIONS` list's tuple shape and `mutation_test`'s CONTROL/discrimination logic, including the FIRE-ONLY reporting path.
- `tools/check_repo.py` lines 2410-2470 — `_write` and the fixture-helper style.
- `tools/check_repo.py` `self_test()` from line 3223 to the end of the function — the scratch-root declarations, the `bad_codes` union, the named per-code assertions, and the generic coverage loop.
- `04-RESEARCH.md` Pattern 1 — the two real, working SimpleEnglish manifests quoted verbatim. They live at `~/devoteam/.claude/plugins/marketplaces/simple-english/.claude-plugin/`, **outside this repository**; they are a field-shape template to read, never a path this repository modifies.
- `04-PATTERNS.md` sections "`.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json`" and "`tools/check_repo.py` — new codes" in full.
- This plan's `<interfaces>` block, twice.
  </read_first>
  <behavior>
    - `check_plugin_manifest_version` on a repository whose `.claude-plugin/` does not exist: returns an empty list, before any read.
    - On a repository whose `SKILL.md` states `metadata.version: "0.1.0"` and whose two manifests both state `"version": "0.1.0"`: returns an empty list.
    - On a repository whose `plugin.json` states `"version": "9.9.9"` while `SKILL.md` states `"0.1.0"`: returns exactly one violation whose message begins with the token `plugin-manifest-version-mismatch` and names both values and the offending file.
    - On a repository whose `marketplace.json`'s single plugin entry states a version disagreeing with `SKILL.md`: returns one violation naming `marketplace.json`.
    - On a repository whose `SKILL.md` frontmatter carries no `metadata.version` at all while a manifest exists: returns one violation per manifest, stating that the skill states no version to compare against.
  </behavior>
  <action>
Create both manifests and wire the first of this plan's three codes end to end, with the full
three-part contract. Do not create `output-styles/`, `prompts/`, `examples/before-after.md` or
`tools/generate_derivatives.py` — later plans own those and pre-creating them makes this tracer stop
being thin.

`.claude-plugin/plugin.json` (new file), mirroring the real working reference field-for-field with
this project's own values:
- `name`: `proof-first`, equal to the `skills/proof-first/` folder name (P4-01).
- `displayName`: `Proof First`.
- `description`: one sentence, derived from `SKILL.md`'s own frontmatter description and trimmed to
  manifest scope — what the skill writes and checks, for whom. It states no figure, no comparison and
  no claim about this skill's own effect.
- `version`: the exact string `SKILL.md`'s frontmatter `metadata.version` carries, read from the file
  rather than retyped.
- `author`: an object with a `name` key. Use `Proof First contributors` — the repository has no single
  named author and inventing one would be a fabricated fact.
- `homepage` and `repository`: `https://github.com/<owner>/<repo>` verbatim, the P4-03 placeholder, or
  the real location if the decision checkpoint supplied one.
- `license`: `MIT`, matching `LICENSE` and `SKILL.md`'s frontmatter.
- `keywords`: an array of `presales`, `rfp`, `proposal`, `technical-writing`.

`.claude-plugin/marketplace.json` (new file):
- `name`: `proof-first`.
- `owner`: an object with `name` set to `Proof First contributors` and `url` set to
  `https://github.com/<owner>` (or the real owner if supplied).
- `description`: one line, identical in substance to `plugin.json`'s.
- `plugins`: an array holding exactly one entry, carrying `name`, `source` set to the literal `./`
  (P4-02), `displayName`, `description`, `version`, `author`, `homepage`, `repository`, `license` and
  `keywords`, every one of them equal to `plugin.json`'s corresponding value.
- Write no `skills`, `output-styles`, `commands`, `agents`, `hooks` or `mcpServers` component key in
  either file — auto-discovery from the plugin root is the whole point of P4-02.

`tools/check_repo.py` — add `plugin-manifest-version-mismatch` with its full contract:
- Declare `PLUGIN_MANIFEST_PATH = '.claude-plugin/plugin.json'` and
  `MARKETPLACE_MANIFEST_PATH = '.claude-plugin/marketplace.json'` as module-level constants beside the
  other path constants. Every later reference uses them; do not retype either literal at a call site.
- Add `_load_json_manifest(repo_root, rel_path)` returning `(data, error)` — `json.loads` on the file's
  text, `(None, message)` on a `json.JSONDecodeError` or a non-object top level, and `(None, None)`
  when the file does not exist. Import `json` at the top of the module with the other stdlib imports;
  add nothing outside the standard library.
- Add `_skill_metadata_version(skill_path)` returning the version string or `None`: call the existing
  `parse_frontmatter`, take `keys.get('metadata', '')`, and apply one anchored multiline regex for a
  `version:` line with an optionally-quoted value. Do not modify `parse_frontmatter`.
- Add `check_plugin_manifest_version(repo_root)` implementing the `<behavior>` block above. Iterate
  `SKILL_GLOB` for the source of truth exactly as `check_catalog_count` does. Return an empty list
  before any read when neither manifest exists. Emit one violation per disagreeing manifest, message
  leading with the bare code token and naming the manifest path, the manifest's value and the skill's
  value.
- Create `PLUGIN_CHECK_CODES` as a new module-level list holding `'plugin-manifest-version-mismatch'`
  for now, and `run_plugin_checks(repo_root)` calling the new check. Concatenate `PLUGIN_CHECK_CODES`
  into `ALL_CHECK_CODES` and call `run_plugin_checks` from `run_all_checks`, following exactly how
  `CATALOG_CHECK_CODES` and `run_catalog_checks` are wired.
- Add the code's bullet to the module's top-of-file docstring, stating what it catches and then its
  **Declared ceiling**: it compares three declared strings for equality and says nothing about whether
  the version is semantically correct, whether a git tag exists for it, or whether the manifest
  installs.
- Add `'.claude-plugin'` to `MUTATION_SOURCES` so the control copy contains the new manifests. Without
  this the mutation registered below copies nothing and is silently inert.
- Add `_good_plugin_manifests(root)` and `_bad_plugin_manifests(root)` fixture helpers built on
  `_write`, plus `plugin_good_root` and `plugin_bad_root` scratch roots inside `self_test()`. The bad
  root's `plugin.json` states a version disagreeing with its own fixture `SKILL.md`. Assert by name
  that the bad root trips `plugin-manifest-version-mismatch` and that the good root does not, and
  union the bad root's codes into `bad_codes`.
- Register `('plugin-manifest-version-mismatch', "change the real .claude-plugin/plugin.json version so it no longer equals the skill frontmatter's metadata.version", _mutate_plugin_manifest_version_mismatch)` in `MUTATIONS`, with a mutate function that edits the copied tree's `plugin.json`, never the live repository.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, any line beginning "FAIL", or the verified-codes list not containing plugin-manifest-version-mismatch</fails_when>
    <automated>python3 tools/check_repo.py --mutation-test</automated>
    <fails_when>non-zero exit, a final line reporting fewer than 33 codes discrimination-proven, any FIRE-ONLY line, or a CONTROL line reporting any unexpected violation</fails_when>
    <automated>python3 -c "import json;p=json.load(open('.claude-plugin/plugin.json'));m=json.load(open('.claude-plugin/marketplace.json'));e=m['plugins'][0];print(p['name'],e['name'],e['source'],p['version'],e['version'],len(m['plugins']))"</automated>
    <fails_when>non-zero exit, or the printed line is not exactly "proof-first proof-first ./ 0.1.0 0.1.0 1"</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py` prints exactly `check_repo: 0 violations` and exits 0 with both manifests on disk.
- `python3 -c "import json,re,pathlib;v=re.search(r'^version:\s*\"?([^\"\s]+)\"?\s*$',pathlib.Path('skills/proof-first/SKILL.md').read_text().split('---')[1],re.M).group(1);p=json.load(open('.claude-plugin/plugin.json'));m=json.load(open('.claude-plugin/marketplace.json'));print(v==p['version']==m['plugins'][0]['version'])"` prints `True`.
- `python3 -c "import json;p=json.load(open('.claude-plugin/plugin.json'));print(sorted(p))"` prints a list containing every one of `author`, `description`, `displayName`, `homepage`, `keywords`, `license`, `name`, `repository`, `version`.
- `python3 -c "import json;m=json.load(open('.claude-plugin/marketplace.json'));print(sorted(set(m['plugins'][0]) & {'skills','output-styles','commands','agents','hooks','mcpServers'}), sorted(set(m) & {'skills','output-styles','commands','agents','hooks','mcpServers'}))"` prints `[] []` — no component-override key was written into either level.
- `python3 -c "import json;p=json.load(open('.claude-plugin/plugin.json'));m=json.load(open('.claude-plugin/marketplace.json'));e=m['plugins'][0];print([k for k in ('name','displayName','description','version','homepage','repository','license') if p[k]!=e[k]])"` prints `[]` — the marketplace entry restates the manifest exactly.
- `python3 -c "import re;s=open('tools/check_repo.py').read();print(\"'.claude-plugin'\" in re.search(r'^MUTATION_SOURCES = .*$',s,re.M).group(0))"` prints `True` — the control copy carries the manifests.
- `python3 tools/check_repo.py --mutation-test | tail -1` prints `mutation-test PASS: 33 codes discrimination-proven`.
- `python3 tools/check_repo.py --mutation-test | grep -c 'FIRE-ONLY'` prints `0`.
- `python3 -c "import ast,pathlib,sys;t=ast.parse(pathlib.Path('tools/check_repo.py').read_text());mods={n.split('.')[0] for x in ast.walk(t) if isinstance(x,ast.Import) for n in [a.name for a in x.names]}|{x.module.split('.')[0] for x in ast.walk(t) if isinstance(x,ast.ImportFrom) and x.module};extra=mods-{'argparse','datetime','difflib','hashlib','json','pathlib','re','shutil','subprocess','sys','tempfile','unicodedata','os','textwrap','collections','itertools','string'};print('EXTRA',sorted(extra));sys.exit(1 if extra else 0)"` prints `EXTRA []` and exits 0.
- `git check-ignore -v .claude-plugin/plugin.json .claude-plugin/marketplace.json; echo "ignored=$?"` prints `ignored=1` — both new files are tracked source, not ignored.
  </acceptance_criteria>
  <done>Both manifests exist at the repository root, parse as JSON, restate one identical set of facts, and state the same version the shipped skill's frontmatter states; a new CI-enforced code fails the build the moment any one of those three versions moves alone, and it is proven to fire against a mutated copy of the real repository while staying silent on the unmutated one.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the plugin channel's two remaining enforcement gaps — `plugin-manifest-invalid` and `publish-location-drift`, both proven live</name>
  <reversibility rating="reversible">Adding two violation codes over files this plan itself created is ordinary code change; the existing 33 self-test assertions and mutations all re-run unchanged in this task's own verify block, so a regression fails the task rather than shipping.</reversibility>
  <files>tools/check_repo.py</files>
  <read_first>
- `tools/check_repo.py` as left by Task 1 — the new `PLUGIN_CHECK_CODES`, `run_plugin_checks`, `_load_json_manifest`, `PLUGIN_MANIFEST_PATH` and `MARKETPLACE_MANIFEST_PATH`; both new codes reuse them rather than re-declaring anything.
- `tools/check_repo.py` lines 1506-1545 — `check_artifact_family_sections`, the per-item firing shape: one violation per missing element, each naming the file, the missing element and the requirement it serves. Both codes in this task fire per element, not once per file.
- `tools/check_repo.py`'s module docstring block for `pointer-unparseable` — the precedent for a single code covering two distinct structural conditions in one file family; `publish-location-drift` follows it.
- `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` as written by Task 1 — the exact key names and value shapes the new parsers read.
- `README.md` lines 1-40 — the current section set. This task must NOT edit README; it only needs to know that README is a prospective publish-location carrier that does not yet state one, so the check must skip a carrier that states none rather than firing on today's README.
- This plan's `<interfaces>` block and the P4-03 and P4-05 rows of `<decisions>`.
  </read_first>
  <behavior>
    - `check_plugin_manifest_invalid` on a repository with no `.claude-plugin/`: returns an empty list.
    - On the two manifests as Task 1 wrote them: returns an empty list.
    - On a `plugin.json` that is not parseable JSON: returns exactly one violation naming the file and the parse failure position.
    - On a `plugin.json` missing its `license` key: returns exactly one violation naming the file and the missing key.
    - On a `plugin.json` whose `name` is `proofirst` while the only skill folder is `proof-first`: returns exactly one violation naming both strings.
    - On a `marketplace.json` whose `plugins` array is empty, or whose single entry's `source` is not `./`: returns one violation per failed condition.
    - `check_publish_location_drift` on a repository where only `plugin.json` exists and states one location: returns an empty list — one carrier cannot disagree with itself.
    - Where `plugin.json` states `<owner>/<repo>` and `marketplace.json` states `someone/else`: returns one violation naming both distinct values and the files they came from.
    - Where a carrier that exists states no location at all: returns one violation naming that file. `README.md` before `04-04` runs states none and is therefore skipped as not-yet-a-carrier — the check reads only the structured fields and command forms listed in the action, so a README with no install command is silent, not violating.
  </behavior>
  <action>
Add the two remaining codes named in P4-04 for this plan, each with the full three-part contract:
the check wired into `run_plugin_checks` and `PLUGIN_CHECK_CODES`, a firing and a silent self-test
fixture with its own named assertion, and a `MUTATIONS` entry proving discrimination against a copy
of the real repository. Reuse Task 1's loader and constants; add no second JSON reader.

`plugin-manifest-invalid`:
- Declare `PLUGIN_REQUIRED_KEYS = ('name', 'displayName', 'description', 'version', 'author', 'homepage', 'repository', 'license', 'keywords')` and `MARKETPLACE_REQUIRED_KEYS = ('name', 'owner', 'description', 'plugins')` as module-level tuples.
- Add `check_plugin_manifest_invalid(repo_root)` implementing the `<behavior>` block. For each manifest that exists: report the parse failure and stop on that file if `_load_json_manifest` returned an error; otherwise report one violation per missing required key. For `plugin.json` additionally require that `name` equals the single `skills/*/` directory name discovered through `SKILL_GLOB`'s parent — report both strings when they differ, and stay silent when the repository has no skill folder at all. For `marketplace.json` additionally require `owner` to be an object carrying a non-empty `name`, `plugins` to be a list of exactly one object, and that entry to carry a `source` whose value is the literal `./`.
- Register the code in `PLUGIN_CHECK_CODES` and call the check from `run_plugin_checks`.
- Docstring bullet, with its **Declared ceiling**: it asserts JSON well-formedness, key presence and
  the folder-name equality only. It never validates a value's semantics, never reaches the network,
  and says nothing about whether a real `claude plugin marketplace add` succeeds — that is a manual
  smoke test recorded in `04-VALIDATION.md`.

`publish-location-drift`:
- Declare `PUBLISH_LOCATION_CARRIERS = (PLUGIN_MANIFEST_PATH, MARKETPLACE_MANIFEST_PATH, 'README.md')` — the full prospective list per P4-05, so `04-04` inherits enforcement with no edit here.
- Add `_publish_locations_in(repo_root, rel_path)` returning the set of distinct `owner/repo` strings a
  carrier states, read from **structured positions only**, never from a loose scan of every link in the
  file: for the two manifests, the `homepage` and `repository` values (and the marketplace entry's
  same two fields, plus `owner.url` contributing only its owner segment) with a leading
  `https://github.com/` stripped; for `README.md`, the argument that follows the literal command
  prefixes `npx skills add ` and `claude plugin marketplace add ` on a line. Anchoring on those exact
  command prefixes is what keeps an unrelated GitHub link elsewhere in README from being read as this
  repository's own location.
- Declare the two command-prefix patterns as module-level constants `SKILLS_CLI_INSTALL_RE` and
  `MARKETPLACE_ADD_RE` so `04-04`'s README check reuses them rather than declaring a third copy.
- Add `check_publish_location_drift(repo_root)` implementing the `<behavior>` block: collect each
  existing carrier's set; a carrier yielding an empty set while another carrier yields a non-empty one
  is one violation naming that file; a union with more than one distinct value is one violation naming
  every distinct value and the file each came from. A carrier that does not exist is skipped before any
  read. When no carrier states a location at all, return an empty list.
- Register the code and call the check from `run_plugin_checks`.
- Docstring bullet, with its **Declared ceiling**: it asserts that every carrier states one identical
  location; it does not and cannot assert that the location is correct, that it resolves, or that the
  repository is published there. Name the placeholder token as a deliberately non-resolving value that
  this check treats exactly like a real one, and point at `.planning/WINDOWS.md` for the open item.
- Add `_bad_plugin_manifests`-style fixtures: extend the existing `plugin_bad_root` or add
  `publish_bad_root` so one root trips `plugin-manifest-invalid` (a missing key and a name mismatch)
  and another trips `publish-location-drift` (two carriers stating different locations), each with its
  own named assertion, and union both roots' codes into `bad_codes`. The good roots must stay silent on
  both codes.
- Register two `MUTATIONS` entries: one deleting a required key from the copied tree's `plugin.json`,
  one rewriting the copied tree's `marketplace.json` `repository` value to a different owner/repo than
  `plugin.json` states. Both mutate the copy; neither touches the live repository.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, any line beginning "FAIL", or the verified-codes list not containing both plugin-manifest-invalid and publish-location-drift</fails_when>
    <automated>python3 tools/check_repo.py --mutation-test</automated>
    <fails_when>non-zero exit, a final line reporting fewer than 35 codes discrimination-proven, any FIRE-ONLY line, or a CONTROL line reporting any unexpected violation</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py --mutation-test | tail -1` prints `mutation-test PASS: 35 codes discrimination-proven`.
- `python3 tools/check_repo.py --mutation-test | grep -c 'FIRE-ONLY'` prints `0`.
- `python3 tools/check_repo.py --mutation-test | grep -c 'CONTROL: 0 violations on the unmutated copy (0 known-open per KNOWN_OPEN_VIOLATIONS, 0 unexpected)'` prints `1`.
- `python3 tools/check_repo.py --self-test | tr ',' '\n' | grep -cE 'plugin-manifest-invalid|plugin-manifest-version-mismatch|publish-location-drift'` prints `3`.
- Scratch-copy proof that both codes fire on real defects and the live tree is left untouched, run as one command: `T=$(mktemp -d); cp -R . "$T/r"; python3 -c "import json;p='$T/r/.claude-plugin/plugin.json';d=json.load(open(p));d.pop('license');json.dump(d,open(p,'w'));q='$T/r/.claude-plugin/marketplace.json';e=json.load(open(q));e['plugins'][0]['repository']='https://github.com/someone/else';json.dump(e,open(q,'w'))"; (cd "$T/r" && python3 tools/check_repo.py; true) | cut -d' ' -f1 | sort -u` prints a list containing both `plugin-manifest-invalid` and `publish-location-drift`.
- After that scratch-copy proof, `python3 tools/check_repo.py` in the real tree still prints `check_repo: 0 violations` — the mutation touched only the copy.
- `python3 -c "s=open('tools/check_repo.py').read();b=[l for l in s.splitlines() if l.strip().startswith('PUBLISH_LOCATION_CARRIERS')];print(b)"` prints a single line naming all three of the plugin manifest constant, the marketplace manifest constant and `README.md`.
- `python3 -c "s=open('tools/check_repo.py').read();d=s.split('\"\"\"')[1];print(sum(1 for c in ('plugin-manifest-invalid','plugin-manifest-version-mismatch','publish-location-drift') if c in d))"` prints `3` — all three codes are documented in the module docstring.
- `grep -c 'Declared ceiling' tools/check_repo.py` prints at least `32`; it printed `29` before this plan started, so each of the three new codes carries its own disclosure.
  </acceptance_criteria>
  <done>A malformed manifest, a missing required key, a plugin name that stops matching the shipped skill folder, and two files disagreeing about where this repository lives are each a build failure, each proven to fire against a mutated copy of the real repository and to stay silent on the unmutated one, with the discrimination-proven total at 35 and the CONTROL line still clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository JSON → `tools/check_repo.py` parsers | Locally-authored JSON crosses into a new loader with no schema library behind it; a malformed file must produce a named violation, never a traceback that kills the whole run. |
| `.claude-plugin/` manifests → an installing harness | Both manifests are read by Claude Code on a user's machine; a defect surfaces on their install, not as a local error here. |
| an unknown publish location → a committed, publicly readable install command | A guessed `owner/repo` would send a user to a repository this project does not control. |
| `skills/proof-first/SKILL.md`'s frozen frontmatter → a new consumer | A second file now depends on a version string Phases 1-3 owned alone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | a new check named as covered that cannot fire | high | mitigate | All three codes ship the full three-part contract: wired into `run_plugin_checks` and `PLUGIN_CHECK_CODES`, a firing and a silent self-test fixture with a distinct named assertion each way, and a `MUTATIONS` entry proving discrimination against a copy of the real repository. Acceptance criteria pin the discrimination-proven total at 33 then 35 so a silently-dropped mutation is visible, and a second criterion asserts no code is reported FIRE-ONLY. |
| T-04-02 | Tampering | `MUTATION_SOURCES` not covering `.claude-plugin` | high | mitigate | `_copy_repo_subset` copies only the named roots; a mutation against a file outside them edits nothing and the code is counted as unproven while looking green. The plan requires `'.claude-plugin'` to be added in Task 1 and pins the proven total at 33 in the same task's acceptance criteria, so the omission cannot pass. |
| T-04-03 | Information Disclosure | a guessed `owner/repo` shipped in a manifest a user installs from | high | mitigate | The value is genuinely unknown (`git remote -v` empty). P4-03 freezes a visibly non-real placeholder, the decision checkpoint raises it to a human before either file is written, `publish-location-drift` guarantees one value across every carrier, and the residual is recorded as a `.planning/WINDOWS.md` open item addressed to Phase 6's launch gate. |
| T-04-04 | Denial of Service | `self_test()`'s many existing fixture roots | medium | mitigate | Every existing fixture root lacks a `.claude-plugin/` directory. All three new checks return an empty list before any read when their subject file is absent, matching `check_catalog_count`'s and `check_mc_catalog_id_drift`'s stated absence-is-not-failure posture, and the good-root assertions prove the silence rather than assuming it. |
| T-04-05 | Tampering | the new JSON loader reading repository paths | low | mitigate | `_load_json_manifest` takes a module-level constant relative path, never a caller-supplied or file-derived one, and returns a named error rather than raising. Manifest values are compared as strings and never treated as paths, so no value read out of a manifest can direct a later read. |
| T-04-SC1 | Tampering | npm / pip / cargo installs | low | accept | This plan installs no package in any ecosystem. `04-RESEARCH.md`'s Package Legitimacy Audit records the one external name (`skills`, npm) as an end-user `npx` command documented in README by `04-04`, never a dependency of this repository; its registry existence, current version and source repository were verified against the live registry during research and its verdict is OK/Approved. `tools/check_repo.py` stays standard-library only, asserted by an acceptance criterion in Task 1. No install task exists, so no package-legitimacy checkpoint is required. |
</threat_model>

<verification>
- `python3 tools/check_repo.py --self-test && python3 tools/check_repo.py --mutation-test && python3 tools/check_repo.py` — the exact CI job order — runs green.
- Thirty-five violation codes are discrimination-proven, up from thirty-two, with the control copy still reporting 0 violations, 0 unexpected, and no code reported fire-only.
- `skills/proof-first/SKILL.md` is byte-identical to its pre-plan state: this plan reads its frontmatter and edits nothing, so the 177-token margin `04-03` needs is untouched.
- Neither `output-styles/`, `prompts/`, `examples/before-after.md` nor `tools/generate_derivatives.py` exists at the end of this plan — later plans own them and pre-creating them would make this tracer stop being thin.

**Honest verification statement.** Of this plan's two requirements:
- **DIST-02** is verified in full only on its structural half: both manifests exist, parse, agree with each other and with the shipped skill's version, and carry no component-override key. Whether `claude plugin marketplace add` followed by `claude plugin install proof-first@proof-first` actually installs is a live harness-and-network behaviour no file-reading checker observes; it stays a manual smoke test in `04-VALIDATION.md`'s Manual-Only table and is recorded as unresolved edge A4-E2.
- **DIST-01** is only partially addressed here. This plan builds the publish-location mechanism every skills-CLI install command depends on, but the command itself is authored in `04-04` and cannot be exercised without a published repository. Recorded as unresolved edge A4-E1.
The `<human-check>`-class content judgments this plan raises are carried by the decision checkpoint rather than by a verify block, matching `workflow.human_verify_mode: end-of-phase`.
</verification>

<success_criteria>
- A user with this repository available as a marketplace source has a valid, parseable plugin manifest and marketplace manifest to install from, rooted so the shipped skill folder is auto-discovered.
- The skill's frontmatter version, the plugin manifest version and the marketplace entry version are one value, and the build fails the moment any one of them moves alone — NUMBERING.md's Phase 1 versioning obligation is mechanical rather than conventional.
- A malformed manifest, a missing required key, or a plugin name that stops matching the shipped skill folder is a build failure.
- Every file that states where this repository lives states one identical value, so resolving the unknown publish location later is a single substitution the build verifies.
- The whole-repository checker, its self-test and its mutation test all run green, with three more codes discrimination-proven than before and the control copy still clean.
- The plugin identifier, the marketplace rooting and the publish location are settled by an explicit human decision before either manifest is written.
</success_criteria>

<output>
Create `.planning/phases/04-distribution-worked-examples/04-01-SUMMARY.md` when done.

In its frontmatter, `requirements-completed:` records **implementation, not verification** — the
field that once propagated a false "Complete" into `.planning/REQUIREMENTS.md`. List `[DIST-02]`
only. Do **not** list DIST-01: this plan builds the publish-location mechanism its install command
depends on and authors no command, and the SUMMARY must say so in prose rather than letting the
field imply otherwise.

State explicitly in the SUMMARY body that DIST-02's live install flow is unexercised in this
environment, that the publish location is a recorded placeholder unless the decision checkpoint
supplied a real value, and that both are provisional pending end-of-phase UAT and Phase 6's launch
gate.

Record as numbers rather than assertions: the `--mutation-test` discrimination-proven total, the
CONTROL line verbatim, and the resolved value (or confirmed placeholder) the decision checkpoint
returned.

If the checkpoint returned a real `owner/repo`, also record that `.planning/WINDOWS.md` needed no new
entry. If it confirmed the placeholder, add a `.planning/WINDOWS.md` open unrun-verify entry naming
the placeholder, the carriers holding it, and Phase 6 LEG-04 as the closing gate.
</output>
