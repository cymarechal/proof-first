---
phase: 04-distribution-worked-examples
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - examples/before-after.md
  - tools/check_repo.py
autonomous: true
requirements: [EX-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A reader opening `examples/before-after.md` finds one document-level before/after pair for each of the four frozen artifact families, in the frozen family order, each pair's after column citing at least one rule ID this repository actually ships."
    - "A family heading present in `examples/before-after.md` without both a `✗` line and a `✓` line beneath it is a build failure, so an empty or half-written family section cannot ship as if it were a pair."
    - "A family section whose body carries no allocated `PF-` or `MC-` token is a build failure, so EX-02's citation half is mechanical rather than conventional."
    - "The four family sections appear in the same order as the frozen `ARTIFACT_FAMILY_SECTIONS` tuple, and a reordering fails the build."
    - "Every currency amount, percentage and ISO date written anywhere in `examples/before-after.md` traces to a row of `examples/deal-brief.md`'s Canonical figures table, and every rule token it cites exists in `NUMBERING.md`'s Allocated IDs table — both already enforced by shipped codes with no new code needed."
    - statement: "Each pair's after column demonstrates the rewrite the cited rule asks for, rather than restating the rule's own wording back to the reader."
      verification: backstop
    - "`python3 tools/check_repo.py --mutation-test` reports 37 codes discrimination-proven, up from 35, with the CONTROL line still reading 0 unexpected and no code reported fire-only."
  prohibitions:
    - "MUST NOT cite any rule token under `examples/` that `NUMBERING.md`'s Allocated IDs table does not define — the citable set is closed at 31 `PF-` IDs and 8 `MC-` IDs, and a low-ID-hyphen-high-ID range expression on one line reads as two separate tokens, the second of which is not allocated."
    - "MUST NOT write a currency amount, a percentage, or an ISO date into `examples/before-after.md` that has no exact matching value in `examples/deal-brief.md`'s Canonical figures table — including inside the deliberately weak before column."
    - "MUST NOT redeclare the four artifact-family name strings; `ARTIFACT_FAMILY_SECTIONS` is a frozen interface already bound by SKILL.md's classification instruction and by Phase 5's linter, and a second typed copy is the exact drift its own source comment exists to prevent."
    - "MUST NOT name a real company, product, person or customer anywhere in the file; every fact traces to the invented deal in `examples/deal-brief.md`."
    - "MUST NOT make an unmeasured claim about this skill's own effect anywhere in the file."
    - "MUST NOT reproduce a contiguous run of any framework source's own wording, or a term coined by a source adopted here as this repository's own label."
    - "MUST NOT use an emoji anywhere in shipped content."
  artifacts:
    - path: "examples/before-after.md"
      provides: "One document-level before/after pair per artifact family, every after column citing a shipped rule ID"
      contains: "## RFP and RFI response"
      min_lines: 30
    - path: "tools/check_repo.py"
      provides: "before-after-family-missing and before-after-citation-missing, each with firing and silent fixtures and a registered mutation"
      contains: "before-after-citation-missing"
  key_links:
    - from: "examples/before-after.md"
      to: "tools/check_repo.py"
      via: "the four section headings are read from the frozen ARTIFACT_FAMILY_SECTIONS tuple, never a second copy"
      pattern: "ARTIFACT_FAMILY_SECTIONS"
    - from: "examples/before-after.md"
      to: "NUMBERING.md"
      via: "every cited rule token has an Allocated IDs row, already enforced by check_undefined_id"
      pattern: "(PF-\\d+\\.\\d+|MC-\\d+)"
    - from: "examples/before-after.md"
      to: "examples/deal-brief.md"
      via: "every figure traces to a Canonical figures row, already enforced by check_unlisted_figure"
      pattern: "Halverton"
    - from: "examples/before-after.md"
      to: "skills/proof-first/references/artifact-patterns.md"
      via: "each pair demonstrates the Order line that family's conventions state"
      pattern: "\\*\\*Order:\\*\\*"
---

<objective>
Ship EX-02's actual content: one document-level before/after pair for each of the four frozen
artifact families, in `examples/before-after.md`, with every after column citing rule numbers this
repository really ships — and turn all three halves of that sentence into build failures rather than
conventions.

Purpose: `references/worked-examples.md` already carries a per-rule `✗`/`✓` contrast for each catalog
rule. EX-02 asks for a different unit entirely — a whole document-level passage per artifact family,
which is what a reader evaluating this skill actually looks at first and what `04-04`'s README leads
with. Blending the two would violate `worked-examples.md`'s own stated scope. Two new codes make the
family coverage, the pair completeness, the family order and the citation presence mechanical, so a
later edit cannot quietly leave a family section empty or uncited.

Output: `examples/before-after.md` with four complete family pairs in the frozen order; two new
CI-enforced violation codes, each with firing and silent self-test fixtures and a registered
mutation; the discrimination-proven total moved from 35 to 37.
</objective>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-distribution-worked-examples/04-RESEARCH.md
@.planning/phases/04-distribution-worked-examples/04-PATTERNS.md
@.planning/phases/04-distribution-worked-examples/04-01-PLAN.md
@NUMBERING.md
@examples/deal-brief.md
@tools/check_repo.py
@skills/proof-first/references/artifact-patterns.md
@skills/proof-first/references/worked-examples.md
</context>

<interfaces>
<!-- Every line below was read from the named file during this planning session (2026-09-17). -->

**Baseline this plan starts from (as `04-01` leaves it):** `python3 tools/check_repo.py` prints
`check_repo: 0 violations`; `--mutation-test` prints `mutation-test PASS: 35 codes
discrimination-proven` with a CONTROL line reading `0 violations on the unmutated copy (0 known-open
per KNOWN_OPEN_VIOLATIONS, 0 unexpected)`. Re-run all three commands before starting and record the
numbers; if the total is not 35, stop and say so rather than proceeding.

**The frozen four-family interface (`tools/check_repo.py:1487-1496`, quoted verbatim):**
```python
ARTIFACT_FAMILY_SECTIONS = (
    # Frozen interface (P3-11) -- do not reword, re-case, pluralise, or
    # reorder these four strings. ...
    'RFP and RFI response',
    'Solution proposal',
    'Executive summary',
    'Demo and discovery material',
)
```
`ARTIFACT_FAMILY_REQUIREMENT` (line 1498) maps each of these to `ART-01`..`ART-04`. **This plan's two
codes serve EX-02, not ART-0N** — reuse `ARTIFACT_FAMILY_SECTIONS`, and name `EX-02` in the new
messages rather than borrowing that requirement map.

**The section-presence template to mirror (`check_artifact_family_sections`, line 1506):** iterate
`SKILL_GLOB`; return an empty list before any read when the subject file does not exist; run
`strip_fences` then `split_sections` on the text; emit one violation per missing heading, naming the
file, the missing heading and the requirement it serves. `split_sections(text)` (line 392) returns a
`{heading: body}` mapping split on `## ` headings — it does **not** preserve order, so the family
ordering check must read heading order from the raw line sequence, not from that mapping's keys.

**The closed citable set (`NUMBERING.md` Allocated IDs, read this session).** 31 `PF-` IDs:
`PF-0.1, PF-1.1, PF-1.2, PF-1.5, PF-1.9, PF-1.13, PF-1.14, PF-1.17, PF-1.21, PF-1.25, PF-2.1, PF-2.2,
PF-2.3, PF-2.4, PF-2.11, PF-2.12, PF-2.13, PF-2.14, PF-2.15, PF-2.16, PF-2.17, PF-3.1, PF-3.2, PF-3.3,
PF-4.1, PF-4.2, PF-4.3, PF-4.4, PF-5.1, PF-5.2, PF-5.3`. 8 `MC-` IDs: `MC-1, MC-6, MC-11, MC-16,
MC-21, MC-26, MC-31, MC-36`. Citing anything else trips the already-live `undefined-id`.

**The figure interface (`examples/deal-brief.md` Canonical figures, 18 rows, read this session).**
The only currency, percentage and ISO-date values that may appear anywhere under `examples/`:
`$2,300,000`, `$6,000,000`, `62%`, `25%`, `15%`, `30%`, `20%`, `55%`, `2026-10-30`. Bare counts
(`850`, `40`, `3`, `8`, `6`, `10`, `14`, `15`) are unconstrained — only currency, percent and
`YYYY-MM-DD` tokens are matched. `check_unlisted_figure` (line 640) scans `examples/` and `skills/`
recursively and applies `strip_fences` first, so an unfenced line carrying a stray amount fires.
**A currency token that absorbs a trailing sentence comma is reported as unmatched — reword the
sentence, never the checker** (02-02's recorded fix).

**The existing per-rule pair shape to echo at a different granularity
(`skills/proof-first/references/worked-examples.md:9-13`, quoted verbatim):**
```
## PF-0.1

✗ "Kestrel Systems Group offers a comprehensive, best-in-class cloud migration solution."
✓ "Halverton Mutual's 850-VM estate is at capacity, and its nightly settlement batch regularly
overruns its required window. This proposal describes an estate the team can govern, not one it
has to manage VM by VM."
```
The `✗` and `✓` lines are **unfenced**, so every figure inside them is scanned. Keep that shape: one
`✗` line and one `✓` line per family, each a whole paragraph rather than a single sentence, since
EX-02's unit is a document passage rather than a rule-level contrast.

**Each family's own frozen ordering rule (`skills/proof-first/references/artifact-patterns.md`,
read this session).** Each pair's `✓` half must visibly obey its family's `**Order:**` line:
- `RFP and RFI response` (line 41): the direct answer to the scored question comes first, before the context.
- `Solution proposal` (line 69): the architecture narrative first, then the capability mapping, then the risk treatment.
- `Executive summary` (line 95): the problem reframe first, then the business case, then the capability list.
- `Demo and discovery material` (line 115): discovery notes first, then the demo script they inform, then the follow-up.

**Grounding material available without inventing anything** (`examples/deal-brief.md`, read this
session): the 850-VM VMware vSphere estate and its 40 Oracle Database instances; the nightly
settlement batch job and its required 6-hour window; the recorded fact that the batch overrun has
never been instrumented, so no baseline exists; the 8-month regulatory examination window against the
vendor's own 14-month most-comparable prior programme; the buyer's scored questions Q1 through Q5 and
their weights; the technical evaluator's stated preference for the incumbent; the SOC 2 Type II gap.
The inconvenient facts are deliberately useful here — an honest `✓` half that names one is a stronger
example than one that does not.

**Contracts every new check must satisfy** — identical to `04-01`'s `<interfaces>` block, which this
plan does not restate: leading message token equals the code string; new codes registered in a code
list and called from an aggregator concatenated into `ALL_CHECK_CODES` and `run_all_checks`; a
`MUTATIONS` entry per code mutating a **copy** of the real tree; a bad/good fixture pair with named
assertions and the bad root's codes unioned into `bad_codes`; a module-docstring bullet with a
**Declared ceiling**. `MUTATION_SOURCES` already contains `'examples'`, so no widening is needed this
plan.

**Files this plan must NOT touch:** `skills/proof-first/SKILL.md` (its token margin belongs to
`04-03`), `skills/proof-first/references/worked-examples.md` (its per-rule scope is stated in its own
header and this plan's unit is different), `NUMBERING.md`, `README.md`, and anything under `.gsd/`
(gitignored).
</interfaces>

<plan_decisions>
This plan complies with the phase-wide decision table frozen in `04-01-PLAN.md`'s `<decisions>`
section — P4-04 (this plan owns `before-after-family-missing` and `before-after-citation-missing`,
taking the total 35 → 37), P4-07 (sequential waves) and P4-08 (threat IDs `T-04-06` onward). It adds
two plan-local choices:

| ID | Choice | Reversibility |
|----|--------|---------------|
| P4-10 | **`before-after-family-missing` covers three conditions over one parse**: a missing family heading, a family heading present without both a `✗` line and a `✓` line in its body, and the four headings appearing in an order other than `ARTIFACT_FAMILY_SECTIONS`'s. One code, one violation per failed element — the same per-element firing shape `artifact-family-section-missing` already uses, and the `pointer-unparseable` precedent for a single code covering more than one structural condition. Splitting these into three codes would triple the fixture and mutation surface for no additional discrimination. | reversible |
| P4-11 | **One pair per family, each half a paragraph rather than a sentence.** EX-02's unit is a document passage; a single-sentence contrast is already `worked-examples.md`'s job and duplicating it at this granularity would produce two files answering the same question. | reversible |

**Edge-probe items this plan discharges** (3 of the phase's 11, all resolved to `explicit` — see
`04-01-PLAN.md`'s edge-probe accounting table): EX-02 adjacency (a family heading present with no
citation), EX-02 empty (a family section with no rows), EX-02 ordering (the four-family order fixed
and enforced). Each is authored as a plain `must_haves.truths` string above.
</plan_decisions>

<artifacts_this_phase_produces>
The complete phase-wide list lives in `04-01-PLAN.md`'s `<artifacts_this_phase_produces>` section and
is not restated here in full. **This plan's own additions to it:**

**New file:** `examples/before-after.md`.

**New violation codes (2):** `before-after-family-missing`, `before-after-citation-missing`.

**New `tools/check_repo.py` symbols:** `BEFORE_AFTER_PATH`, `check_before_after_families`,
`check_before_after_citations`, `EXAMPLE_CHECK_CODES`, `run_example_checks`.

**New mutation functions and `MUTATIONS` entries (2):** `_mutate_before_after_family_missing`,
`_mutate_before_after_citation_missing`.

**New self-test fixture helpers and scratch roots:** `_good_before_after`, `_bad_before_after`, and
the `beforeafter_good_root` / `beforeafter_bad_root` scratch roots inside `self_test()`.

**Modified existing symbols:** `ALL_CHECK_CODES`, `run_all_checks`, the module docstring's
violation-code block.

**No rule ID is allocated, deprecated, renumbered or retitled.** `NUMBERING.md` is read, never
written.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author `examples/before-after.md` — four document-level pairs, four families, real citations, every fact from the deal brief</name>
  <reversibility rating="reversible">A committed example file is prose; it carries no published identifier and no rule allocation, and rewriting a pair later costs one commit.</reversibility>
  <precondition>`examples/deal-brief.md` exists with an 18-row Canonical figures table, and `skills/proof-first/references/artifact-patterns.md` exists with all four family sections and their `**Order:**` lines. Both are prior-phase artifacts this task writes against and both were verified present this planning session; without them every figure and every ordering claim in the new file would be ungrounded.</precondition>
  <files>examples/before-after.md</files>
  <read_first>
- `examples/deal-brief.md` in full — the deal spine, `## People and roles`, `## Pain points`, `## Inconvenient facts`, `## Customer source material` (the five scored questions and their weights, the discovery quotes, the economic buyer's stated priorities, the decision criteria and the paper process), and the 18-row `## Canonical figures` table. Every fact in the new file comes from here and nowhere else.
- `skills/proof-first/references/worked-examples.md` in full — the visual grammar to echo: a heading, a blank line, one unfenced `✗` line, one unfenced `✓` line, and the file-header paragraph stating the file's own scope. Note its scope sentence explicitly; the new file's header must state a different, non-overlapping scope.
- `skills/proof-first/references/artifact-patterns.md` in full — the classification procedure, each family's conventions, and each family's `**Order:**` line. Each `✓` half must visibly obey its family's order.
- `skills/proof-first/SKILL.md` — the rule statements and their `**Replace with:**` lines for whichever rules the pairs cite, so a citation points at a rule that actually asks for the rewrite shown.
- `NUMBERING.md`'s `## Allocated IDs` table — the closed 39-ID citable set. Do not cite from memory.
- This plan's `<interfaces>` block, twice — particularly the figure interface and the trailing-comma trap.
  </read_first>
  <action>
Create `examples/before-after.md` with four document-level before/after pairs, one per artifact
family, in the frozen family order. Do not touch `references/worked-examples.md`; its per-rule scope
is stated in its own header and this file's unit is deliberately different.

File header (three or four sentences, before the first family heading):
- State this file's scope: one document-level before/after pair per artifact family this skill
  classifies, at passage granularity rather than the per-rule contrast `references/worked-examples.md`
  already carries, and name that file so a reader is never left wondering which is which.
- State that every fact in every example traces to `examples/deal-brief.md`, and that the deal, the
  parties and the figures in it are invented.
- State that every after column cites at least one rule ID allocated in `NUMBERING.md`.
- Do not carry the attribution pointer line: `NOTICES.md`'s carrier list does not name this path, and
  adding it would put an unrequired copy outside the list the checker reads.

Four sections, level-two headings, using the four frozen family strings byte-for-byte as they appear
in `ARTIFACT_FAMILY_SECTIONS`, in that order: `RFP and RFI response`, `Solution proposal`,
`Executive summary`, `Demo and discovery material`.

Each section holds, in this order:
- One unfenced line beginning with `✗` carrying a whole paragraph of the non-compliant draft, in
  double quotes, in the register a real vendor draft is written in: adjectives with no evidence
  behind them, the scored answer buried under context, a claim that reads as a commitment, a
  comparison against the incumbent with nothing behind it, or a metric with no stated source —
  whichever failures that family's conventions are actually about. It must be genuinely bad in a way
  a presales reader recognises, not a caricature.
- One unfenced line beginning with `✓` carrying the compliant rewrite of the same passage, obeying
  that family's `**Order:**` line visibly, attaching the buyer's own figures and stated words from the
  deal brief where the before column had adjectives, and using the existing marker vocabulary where
  a fact is genuinely not available rather than inventing one.
- One short line naming the rule IDs the rewrite applies, drawn only from the closed 39-ID set, each
  written as a bare token so `check_undefined_id` sees it. Never write a range expression — a low ID,
  a hyphen and a high ID on one line reads as two tokens and the second is not allocated.

Concrete grounding per family, so nothing is invented:
- `RFP and RFI response`: the buyer's highest-weighted scored question and its weight, answered
  first; the answer-first ordering versus a context-first draft.
- `Solution proposal`: the 850-VM VMware vSphere estate and its 40 Oracle Database instances, the
  architecture narrative before the capability mapping, and the risk treatment naming the
  examination window against the vendor's own most-comparable prior programme duration.
- `Executive summary`: the problem reframe built on the nightly settlement batch overrunning its
  required window and the recorded fact that the overrun has never been instrumented, then the
  business case against the current annual run rate, then the capability list.
- `Demo and discovery material`: the discovery quote the buyer actually used in their own words,
  the demo script it informs, and the follow-up, with the buyer's verbatim term retained and marked
  rather than replaced.

Figure discipline, checked by a shipped code: the only currency, percentage and ISO-date tokens
permitted anywhere in this file are the Canonical figures values listed in this plan's
`<interfaces>` block. Bare counts are unconstrained. Write no amount immediately followed by a comma
— the currency pattern absorbs it and the token stops matching.

Claim discipline: nothing in this file asserts anything about this skill's own effect. The pairs show
the rewrite; they do not claim a result from it.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 -c "import re,pathlib;t=pathlib.Path('examples/before-after.md').read_text();print([l[3:].strip() for l in t.splitlines() if l.startswith('## ')])"</automated>
    <fails_when>the printed list is not exactly ['RFP and RFI response', 'Solution proposal', 'Executive summary', 'Demo and discovery material'] in that order</fails_when>
    <automated>python3 -c "import re,pathlib;t=pathlib.Path('examples/before-after.md').read_text();print(len([l for l in t.splitlines() if l.startswith('✗')]), len([l for l in t.splitlines() if l.startswith('✓')]))"</automated>
    <fails_when>the printed pair is not "4 4"</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py` prints exactly `check_repo: 0 violations` and exits 0 with the new file on disk — proving `undefined-id` and `unlisted-figure` both accept it with no new code.
- `python3 -c "import re,pathlib;t=pathlib.Path('examples/before-after.md').read_text();print([l[3:].strip() for l in t.splitlines() if l.startswith('## ')])"` prints exactly `['RFP and RFI response', 'Solution proposal', 'Executive summary', 'Demo and discovery material']`.
- `python3 -c "import pathlib;t=pathlib.Path('examples/before-after.md').read_text().splitlines();print(len([l for l in t if l.startswith('✗')]), len([l for l in t if l.startswith('✓')]))"` prints `4 4`.
- `python3 -c "import re,pathlib;t=pathlib.Path('examples/before-after.md').read_text();secs=re.split(r'^## ',t,flags=re.M)[1:];print([bool(re.search(r'PF-\d+\.\d+|MC-\d+',s)) for s in secs])"` prints `[True, True, True, True]` — every family section cites at least one rule token.
- `python3 -c "import re,pathlib;a=set(re.findall(r'PF-\d+\.\d+|MC-\d+',pathlib.Path('examples/before-after.md').read_text()));n=set(re.findall(r'\|\s*(PF-\d+\.\d+|MC-\d+)\s*\|',pathlib.Path('NUMBERING.md').read_text()));print(sorted(a-n))"` prints `[]` — no cited token falls outside the Allocated IDs table.
- `python3 -c "import re,pathlib;t=pathlib.Path('examples/before-after.md').read_text();print(sorted(set(re.findall(r'\\\$[\d,]+|\b\d+%|\b\d{4}-\d{2}-\d{2}\b',t))))"` prints a list every member of which appears in `examples/deal-brief.md`'s Canonical figures Value column.
- `python3 -c "import pathlib;t=pathlib.Path('examples/before-after.md').read_text();print(sum(1 for m in ('Halverton','Kestrel') if m in t))"` prints `2` — both invented parties are used and no other party name is introduced.
- `grep -cF 'references/worked-examples.md' examples/before-after.md` prints `1` — the header names the sibling file whose scope this one does not overlap.
- `git check-ignore -v examples/before-after.md; echo "ignored=$?"` prints `ignored=1`.
  </acceptance_criteria>
  <done>`examples/before-after.md` exists with four document-level before/after pairs under the four frozen family headings in the frozen order, each after column citing at least one allocated rule ID, every figure traceable to the Canonical figures table, and the whole-repository checker running clean with no new code yet written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make EX-02 mechanical — `before-after-family-missing` and `before-after-citation-missing`, both proven live</name>
  <reversibility rating="reversible">Adding two violation codes over a file this plan itself created is ordinary code change; all 35 existing self-test assertions and mutations re-run unchanged in this task's own verify block, so a regression fails the task rather than shipping.</reversibility>
  <files>tools/check_repo.py</files>
  <read_first>
- `tools/check_repo.py` lines 1487-1545 — `ARTIFACT_FAMILY_SECTIONS`, its frozen-interface comment, `ARTIFACT_FAMILY_REQUIREMENT`, and `check_artifact_family_sections` in full. This is the exact shape both new checks mirror, and the tuple both must import rather than redeclare.
- `tools/check_repo.py` lines 385-430 — `SKILL_GLOB`, `strip_fences`, `split_sections`, `table_rows`. Note that `split_sections` returns a mapping and therefore loses heading order; the ordering condition must read the raw line sequence.
- `tools/check_repo.py` lines 550-585 — `check_undefined_id` and its ID-token regexes. Reuse the same token shapes for the citation check rather than declaring a third copy.
- `tools/check_repo.py`'s module docstring in full — the **Declared ceiling** rhetorical convention every code bullet follows.
- `tools/check_repo.py` lines 1775-1830 — the code-list and aggregator wiring shape, as extended by `04-01`.
- `tools/check_repo.py` lines 2290-2400 — the `MUTATIONS` tuple shape and `mutation_test`'s CONTROL, discrimination and FIRE-ONLY logic.
- `tools/check_repo.py` `self_test()` from line 3223 to the end — scratch-root declarations, `_write` usage, the `bad_codes` union, named per-code assertions, and the generic coverage loop.
- `examples/before-after.md` as written by Task 1 — the exact heading and row shapes both parsers read.
- This plan's `<interfaces>` block and `04-01-PLAN.md`'s `<interfaces>` block for the shared new-check contracts.
  </read_first>
  <behavior>
    - `check_before_after_families` on a repository with no `examples/before-after.md`: returns an empty list, checked before any read.
    - On the file as Task 1 wrote it: returns an empty list.
    - On a file whose `## Solution proposal` heading has been deleted, body left in place: returns exactly one violation whose message begins with `before-after-family-missing` and names the file, the missing heading and `EX-02`.
    - On a file whose `## Executive summary` section body has had its `✓` line deleted: returns exactly one violation naming that heading and the missing half.
    - On a file whose `## Executive summary` and `## Solution proposal` headings have been swapped: returns exactly one violation naming the expected order and the order found.
    - `check_before_after_citations` on a repository with no `examples/before-after.md`: returns an empty list.
    - On the file as Task 1 wrote it: returns an empty list.
    - On a file whose `## Demo and discovery material` section has had every `PF-` and `MC-` token removed: returns exactly one violation whose message begins with `before-after-citation-missing` and names the file, the heading and `EX-02`.
    - On a family section that exists but is completely empty: both codes fire — the family code for the two missing halves, the citation code for the absent token.
  </behavior>
  <action>
Add the two codes named in P4-04 for this plan, each with the full three-part contract: the check
wired into a new aggregator and registered in a new code list, a firing and a silent self-test fixture
with its own named assertion, and a `MUTATIONS` entry proving discrimination against a copy of the
real repository.

Shared setup:
- Declare `BEFORE_AFTER_PATH = 'examples/before-after.md'` as a module-level constant beside the other
  path constants. Both checks and both mutations use it; do not retype the literal at a call site.
- Both checks read the file once through `strip_fences` and reuse `split_sections` for bodies. The
  ordering condition reads the raw stripped text's `## ` lines in file order, because `split_sections`
  returns an unordered mapping.
- **Import `ARTIFACT_FAMILY_SECTIONS`; never declare a second copy of the four strings.** Its own
  source comment names this file as one of the bindings the freeze exists to protect.

`before-after-family-missing` (P4-10, three conditions over one parse):
- Add `check_before_after_families(repo_root)` implementing the `<behavior>` block. Return an empty
  list before any read when the file does not exist. For each string in `ARTIFACT_FAMILY_SECTIONS` not
  present as a `## ` heading, emit one violation naming the file, the missing heading and `EX-02`. For
  each heading that is present but whose body contains no line beginning with the ballot-cross
  character, or no line beginning with the check character, emit one violation naming the heading and
  which half is absent. Finally, compare the ordered list of those four headings as they appear in the
  file against the tuple's own order and, if they differ while all four are present, emit one
  violation naming the expected sequence and the sequence found.
- Docstring bullet, with its **Declared ceiling**: this is heading presence, row presence and order
  only. It says nothing about whether a pair's before half is genuinely non-compliant, whether its
  after half is genuinely a rewrite rather than a restatement of the rule's own wording, or whether
  the rewrite obeys its family's stated ordering — those are semantic judgments this repository's
  stack does not perform, and they stay with end-of-phase UAT.

`before-after-citation-missing`:
- Add `check_before_after_citations(repo_root)` implementing the `<behavior>` block. Return an empty
  list before any read when the file does not exist. For each family section present, search the
  section body for a `PF-` or `MC-` token using the same token shapes `check_undefined_id` already
  uses, and emit one violation naming the file, the heading and `EX-02` when none is found. Validity
  of the token is already `undefined-id`'s job; this check asserts presence only.
- Docstring bullet, with its **Declared ceiling**: it asserts that a citation exists somewhere in the
  section, not that the citation is textually adjacent to the sentence it justifies, nor that the
  cited rule is the correct one for that rewrite. State plainly that `undefined-id` owns whether the
  cited ID exists and that correctness of choice is a manual judgment.

Wiring and proof:
- Create `EXAMPLE_CHECK_CODES` holding both code strings and `run_example_checks(repo_root)` calling
  both checks; concatenate the list into `ALL_CHECK_CODES` and call the aggregator from
  `run_all_checks`, following exactly how `04-01` wired `PLUGIN_CHECK_CODES` and `run_plugin_checks`.
- Add `_good_before_after(root)` and `_bad_before_after(root)` fixture helpers built on `_write`, plus
  `beforeafter_good_root` and `beforeafter_bad_root` scratch roots inside `self_test()`. The bad root's
  file is missing one family heading, has one family section without its check-character line, has two
  headings in the wrong relative order, and has one family section with no rule token — so both codes
  fire with distinct named assertions. The good root must be silent on both. Union the bad root's codes
  into `bad_codes`.
- Register two `MUTATIONS` entries against the real `examples/before-after.md` in the copied tree: one
  deleting a family heading line while leaving its body, one stripping every rule token from a single
  family section. `MUTATION_SOURCES` already contains `'examples'`, so no widening is needed.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, any line beginning "FAIL", or the verified-codes list not containing both before-after-family-missing and before-after-citation-missing</fails_when>
    <automated>python3 tools/check_repo.py --mutation-test</automated>
    <fails_when>non-zero exit, a final line reporting fewer than 37 codes discrimination-proven, any FIRE-ONLY line, or a CONTROL line reporting any unexpected violation</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py --mutation-test | tail -1` prints `mutation-test PASS: 37 codes discrimination-proven`.
- `python3 tools/check_repo.py --mutation-test | grep -c 'FIRE-ONLY'` prints `0`.
- `python3 tools/check_repo.py --mutation-test | grep -c 'CONTROL: 0 violations on the unmutated copy (0 known-open per KNOWN_OPEN_VIOLATIONS, 0 unexpected)'` prints `1`.
- `python3 tools/check_repo.py --self-test | tr ',' '\n' | grep -cE 'before-after-family-missing|before-after-citation-missing'` prints `2`.
- `python3 -c "s=open('tools/check_repo.py').read();body=s[s.index('def check_before_after_families'):s.index('def check_before_after_citations')];print('ARTIFACT_FAMILY_SECTIONS' in body)"` prints `True` — the new check reads the frozen tuple by name.
- `python3 -c "import re;s=open('tools/check_repo.py').read();print(len(re.findall(r'ARTIFACT_FAMILY_SECTIONS = \(',s)))"` prints `1` — the tuple is declared exactly once in the whole file, so no second typed copy of the four strings was introduced.
- Scratch-copy proof of both codes, run as one command: `T=$(mktemp -d); cp -R . "$T/r"; python3 -c "import pathlib,re,sys;p=pathlib.Path(sys.argv[1]);t=p.read_text();t='\n'.join(l for l in t.splitlines() if l.strip()!='## Solution proposal');parts=re.split(r'(?m)^## ',t);parts=[re.sub(r'PF-\d+\.\d+|MC-\d+','',s) if s.startswith('Executive summary') else s for s in parts];p.write_text('## '.join(parts))" "$T/r/examples/before-after.md"; (cd "$T/r" && python3 tools/check_repo.py; true) | cut -d' ' -f1 | sort -u` prints a list containing both `before-after-family-missing` and `before-after-citation-missing`.
- After that scratch-copy proof, `python3 tools/check_repo.py` in the real tree still prints `check_repo: 0 violations` — the mutation touched only the copy.
- `python3 -c "s=open('tools/check_repo.py').read();d=s.split('\"\"\"')[1];print(sum(1 for c in ('before-after-family-missing','before-after-citation-missing') if c in d))"` prints `2` — both codes are documented in the module docstring.
  </acceptance_criteria>
  <done>A missing artifact family, a family section missing either half of its pair, a reordered family sequence, and a family section with no rule citation are each a build failure, each proven to fire against a mutated copy of the real repository and to stay silent on the unmutated one, with the discrimination-proven total at 37 and the CONTROL line still clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locally-authored example Markdown → two new `tools/check_repo.py` parsers | Arbitrarily-shaped Markdown crosses into new section and token parsers with no schema library behind them. |
| a committed example file → a public, MIT-licensed repository | The prose is the first thing an evaluating reader sees, and `04-04`'s README leads with it. |
| paraphrased presales conventions → published example prose | The before/after pairs sit next to content derived from three trademarked, proprietary frameworks. |
| the frozen four-family tuple → a third consumer | A list already bound by SKILL.md's classification instruction and Phase 5's linter gains another reader. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Tampering | a new check named as covered that cannot fire | high | mitigate | Both codes ship the full three-part contract: wired into `run_example_checks` and `EXAMPLE_CHECK_CODES`, a firing and a silent self-test fixture with a distinct named assertion each way, and a `MUTATIONS` entry proving discrimination against a copy of the real repository. An acceptance criterion pins the discrimination-proven total at 37 so a silently-dropped mutation is visible, and a second asserts no code is reported FIRE-ONLY. |
| T-04-07 | Tampering | a second typed copy of the four family strings | high | mitigate | `ARTIFACT_FAMILY_SECTIONS` carries its own frozen-interface comment naming this phase's committed examples as one of the bindings it protects. The plan forbids a redeclaration and an acceptance criterion greps the new check's own function body to prove the strings are not retyped inside it. |
| T-04-08 | Information Disclosure | figures and party names in the new example file | medium | mitigate | Every party, person and figure in `examples/deal-brief.md` is invented and that file says so. `check_unlisted_figure` already scans `examples/` recursively, so any currency amount, percentage or ISO date written into the new file must trace to a Canonical figures row; an acceptance criterion additionally enumerates the tokens found and requires each to appear in that table, and a further criterion requires exactly the two invented party names and no third. |
| T-04-09 | Tampering | example prose crossing the framework reproduction boundary | medium | mitigate | The pairs are written from `examples/deal-brief.md`'s own facts and from each family's already-shipped `**Order:**` line, never from a framework source's wording. The reproduction boundary is a semantic judgment `SOURCES.md` states no tool in this stack performs; it is harvested into end-of-phase UAT and its residual formal clearance belongs to Phase 6's LEG-04 gate. |
| T-04-10 | Tampering | the new parsers reading repository paths | low | mitigate | Both checks read one module-level constant relative path and never treat any extracted value as a path. Tokens are matched by anchored regexes reused from `check_undefined_id`; nothing read out of the file can direct a later read. |
| T-04-SC2 | Tampering | npm / pip / cargo installs | low | accept | This plan installs no package in any ecosystem. `tools/check_repo.py` stays standard-library only and `examples/before-after.md` is prose. No install task exists, so no package-legitimacy checkpoint is required. |
</threat_model>

<verification>
- `python3 tools/check_repo.py --self-test && python3 tools/check_repo.py --mutation-test && python3 tools/check_repo.py` — the exact CI job order — runs green.
- Thirty-seven violation codes are discrimination-proven, up from thirty-five, with the control copy still reporting 0 violations, 0 unexpected, and no code reported fire-only.
- `examples/` holds exactly `deal-brief.md` and `before-after.md`.
- `skills/proof-first/SKILL.md` and `skills/proof-first/references/worked-examples.md` are byte-identical to their pre-plan state.

**Honest verification statement.** EX-02 is verified in full only on its structural half: four family
sections exist in the frozen order, each carrying both halves of a pair and at least one allocated
rule citation, with every figure traceable to the Canonical figures table. Its content-quality half —
whether each after column demonstrates the rewrite the cited rule asks for rather than restating the
rule's own wording, and whether each before column is a recognisable real-draft failure rather than a
caricature — is a semantic judgment no file-reading checker performs. It is authored above as a
`verification: backstop` truth so the verifier abstains rather than silently passing, and it is
harvested into end-of-phase UAT for the developer's batch review, matching
`workflow.human_verify_mode: end-of-phase`.
</verification>

<success_criteria>
- A reader opening `examples/before-after.md` sees one document-level before/after pair for each of the four artifact families, in the frozen order, with every after column citing rule numbers this repository really ships.
- A missing family, a half-written pair, a reordered family sequence and an uncited family section are each a build failure rather than a convention.
- Every figure in the file traces to the shared canonical deal brief and every cited rule token exists in the registry, both proven by already-shipped codes with no new code needed.
- The whole-repository checker, its self-test and its mutation test all run green, with two more codes discrimination-proven than before and the control copy still clean.
</success_criteria>

<output>
Create `.planning/phases/04-distribution-worked-examples/04-02-SUMMARY.md` when done.

In its frontmatter, `requirements-completed:` records **implementation, not verification**. List
`[EX-02]`, and state explicitly in the SUMMARY body that only EX-02's structural half is
mechanically verified — the content-quality half (is each after column a demonstrated rewrite rather
than a restatement of the rule's wording) remains unverified by any check in this repository and is
provisional pending end-of-phase UAT.

Record as numbers rather than assertions: the `--mutation-test` discrimination-proven total, the
CONTROL line verbatim, the count of `✗` and `✓` lines in the new file, and the sorted list of rule
IDs the file cites.
</output>
