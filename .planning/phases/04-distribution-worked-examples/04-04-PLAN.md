---
phase: 04-distribution-worked-examples
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - README.md
  - tools/check_repo.py
autonomous: true
requirements: [DIST-01, DIST-06, DIST-05]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A reader opening README.md meets a before/after pair before anything else about the project's status or measurements, and reaches an install path for every supported harness without scrolling past a caveat block to find it."
    - "README states four install paths — the skills CLI one-liner, the Claude Code marketplace add-and-install pair, selecting the output style, and pasting the system prompt — and a missing one is a build failure, so the four-harness promise cannot silently decay to three."
    - "An `## Install` heading present with none of the four commands beneath it is a build failure, because the check asserts each anchor's presence rather than the heading's."
    - "The before/after section precedes both the install section and the status section, and a reordering that buries the pairs is a build failure."
    - "README's install commands, both plugin manifests' repository fields and the marketplace owner URL all state one identical publish location, enforced by `publish-location-drift` with no new code — README simply becomes its third carrier."
    - "README's existing measured-figure disclosure survives the rewrite intact: the literal `evals/conformance/RESULTS-mod04.md` path is still present, `readme-results-pointer-missing` still passes, and no performance figure is added, moved or restated."
    - "README documents the derivative re-sync step in the place a contributor actually reads, naming the one command and the two checks that fail the build when it is skipped."
    - "`python3 tools/check_repo.py --mutation-test` reports 41 codes discrimination-proven, up from 39, with the CONTROL line still reading 0 unexpected and no code reported fire-only."
  prohibitions:
    - "MUST NOT delete, paraphrase away, or move into a fenced block the literal string `evals/conformance/RESULTS-mod04.md`; `readme-results-pointer-missing` greps README for that path and the mutation that proves it deletes every line containing it."
    - "MUST NOT assert sameness of outcome, of result, or of model behaviour between the output style, the system prompt and the installed skill — this project publishes measured claims or no claims, and no benchmark has run."
    - "MUST NOT add, restate, round, or re-caveat any performance figure; the one measured figure README already carries stays exactly as written, and no new number about this skill's effect is introduced."
    - "MUST NOT add a second copy of the attribution pointer line; README is a required carrier and must contain it exactly once."
    - "MUST NOT cite an unallocated rule token in README's unfenced prose; `check_undefined_id` scans README with fenced blocks stripped, so a token in a lead-in paragraph is checked while one inside a command block is not."
    - "MUST NOT guess this repository's publish location; use whatever value `04-01`'s decision checkpoint settled, identical to the string both manifests already carry."
    - "MUST NOT delete or renumber the existing `## What this is`, `## Status`, `## Repository layout`, `## Rule numbering`, `## Versioning` or `## License and notices` sections; this rewrite is additive plus one tree update."
    - "MUST NOT use an emoji anywhere in shipped content."
  artifacts:
    - path: "README.md"
      provides: "A reader-facing entry point leading with before/after pairs, stating an install path per harness, and documenting the derivative re-sync step"
      contains: "## Install"
      min_lines: 180
    - path: "tools/check_repo.py"
      provides: "readme-install-path-missing and readme-before-after-order, each with firing and silent fixtures and a registered mutation"
      contains: "readme-install-path-missing"
  key_links:
    - from: "README.md"
      to: "examples/before-after.md"
      via: "the lead-in section shows one pair and links to the full file"
      pattern: "examples/before-after\\.md"
    - from: "README.md"
      to: ".claude-plugin/marketplace.json"
      via: "the marketplace add command names the same publish location the manifest carries, enforced by publish-location-drift"
      pattern: "claude plugin marketplace add "
    - from: "README.md"
      to: "tools/generate_derivatives.py"
      via: "the re-sync section names the one command a contributor runs after editing the skill"
      pattern: "generate_derivatives\\.py"
    - from: "README.md"
      to: "evals/conformance/RESULTS-mod04.md"
      via: "the existing measured-figure pointer readme-results-pointer-missing guards, preserved verbatim"
      pattern: "evals/conformance/RESULTS-mod04\\.md"
---

<objective>
Close Phase 4 at the front door: rewrite README so a reader meets a before/after pair first and an
install path for every supported harness second, document the derivative re-sync step where a
contributor will actually read it, and make both halves of that promise mechanical rather than
editorial.

Purpose: everything the previous three plans built is invisible until README points at it. DIST-06 is
also the one requirement whose failure mode is silent decay — a README that loses one of four install
paths, or that grows a caveat block above the examples, still reads fine to whoever made the edit.
Two new codes turn the ordering and the four-anchor coverage into build failures, and `04-01`'s
`publish-location-drift` gains README as its third carrier with no new code, so the install commands
cannot disagree with the manifests they install from.

Output: a rewritten README lead-in, a new `## Install` section, a new re-sync section, an updated
layout tree; two new CI-enforced violation codes with firing and silent fixtures and registered
mutations; the discrimination-proven total moved from 39 to 41, closing Phase 4 at nine new codes.
</objective>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-distribution-worked-examples/04-RESEARCH.md
@.planning/phases/04-distribution-worked-examples/04-PATTERNS.md
@.planning/phases/04-distribution-worked-examples/04-01-PLAN.md
@README.md
@NUMBERING.md
@tools/check_repo.py
@examples/before-after.md
</context>

<interfaces>
<!-- Every line below was read from the named file during this planning session (2026-09-17). -->

**Baseline this plan starts from (as `04-03` leaves it):** `python3 tools/check_repo.py` prints
`check_repo: 0 violations`; `--mutation-test` prints `mutation-test PASS: 39 codes
discrimination-proven` with a CONTROL line reading `0 violations on the unmutated copy (0 known-open
per KNOWN_OPEN_VIOLATIONS, 0 unexpected)`. Re-run all three commands plus
`python3 tools/generate_derivatives.py --check` before starting; if the total is not 39, stop and say
so rather than proceeding.

**README's current section order (full 146-line file read this session):**
1. `# Proof First` — title plus a one-line description (line 3).
2. `## What this is` (line 5).
3. `## Status` (line 14) — the load-bearing measured-figure disclosure, running to line 73. Contains "What exists today" / "What does not exist yet" lists, the MOD-04 conformance figure with its caveats block, and the paragraph distinguishing it from the unrun persuasion benchmark.
4. `## Repository layout` (line 75) — a fenced target tree with `(planned)` / `(exists)` annotations.
5. `## Rule numbering` (line 124).
6. `## Versioning` (line 131).
7. `## License and notices` (line 136) — ends with the attribution pointer inside a fenced block (line 145).

**The literal string `readme-results-pointer-missing` guards**, appearing at README lines 35 and 60
and again in the layout tree at line 104: `evals/conformance/RESULTS-mod04.md`. The registered
mutation (`tools/check_repo.py:2308`) is `"delete every line of README.md containing the
results-pointer path"`, so the check greps the file's text for that literal path. **It must survive
this rewrite verbatim.** Do not paraphrase it, do not turn it into a bare link title, and do not
reduce its occurrences to zero anywhere.

**Two README-scanning behaviours that differ and both matter:**
- `check_pointer_missing` / `check_pointer_duplicated` (line ~750) read README's **raw** lines and count those whose stripped form equals the attribution pointer exactly. README currently contains it once, inside a fenced block at line 145. Adding a second copy anywhere fires `pointer-duplicated`.
- `check_undefined_id` (line 550) applies `strip_fences` to README before scanning for `PF-\d+\.\d+` and `MC-\d+` tokens. A rule token in an unfenced lead-in paragraph **is** checked against the Allocated IDs table; one inside a fenced command block is not. Cite only from the closed 39-ID set.
- `check_unlisted_figure` scans `examples/` and `skills/` only — README is not scanned for figures, which is why its existing percentage figures are legal.

**The five layout-tree entries this phase turns from `(planned)` into `(exists)`** (README lines
91-97 and 106): `output-styles/proof-first.md`, `prompts/system-prompt.md`,
`examples/before-after.md`, `.claude-plugin/`, and `tools/generate_derivatives.py` (a new row beside
`tools/check_repo.py`). The tree is inside a fenced block, so nothing in it is scanned for rule tokens
or figures — but it is read by humans and stating `(planned)` for a file that now exists is the same
class of self-contradiction `02-09` and `03-10` both had to fix.

**The exact install command forms, verified against the real working reference implementation's own
README this planning session:**
```
npx skills add <owner>/<repo>

claude plugin marketplace add <owner>/<repo> && claude plugin install proof-first@proof-first

# Or inside Claude Code:
/plugin marketplace add <owner>/<repo>
/plugin install proof-first@proof-first
```
`proof-first@proof-first` is `<plugin-name>@<marketplace-name>`, both frozen as `proof-first` by
`04-01`'s P4-01. `<owner>/<repo>` is P4-03's placeholder, or whatever real value `04-01`'s decision
checkpoint returned — read it out of `.claude-plugin/plugin.json`'s `repository` field rather than
retyping it, because `publish-location-drift` compares them.

**Constants `04-01` already declared that this plan's README check reuses rather than redeclares:**
`SKILLS_CLI_INSTALL_RE` and `MARKETPLACE_ADD_RE`, the two anchored command-prefix patterns
`_publish_locations_in` uses to read README's stated location. `readme-install-path-missing` reuses
both for its first two anchors.

**Contracts every new check must satisfy** — as stated in `04-01-PLAN.md`'s `<interfaces>` block,
which this plan does not restate. `MUTATION_SOURCES` already contains `'README.md'`, so no widening is
needed this plan.

**Files this plan must NOT touch:** `skills/proof-first/SKILL.md`, anything under
`skills/proof-first/references/`, `examples/`, `output-styles/`, `prompts/`,
`tools/generate_derivatives.py`, `NUMBERING.md`, `NOTICES.md`, and anything under `.gsd/`
(gitignored). If README's rewrite would require a derivative to change, stop — the derivatives are
generator output and `04-03` owns the generator.
</interfaces>

<plan_decisions>
This plan complies with the phase-wide decision table frozen in `04-01-PLAN.md`'s `<decisions>`
section — P4-03 (the publish-location placeholder or its resolved value), P4-04 (this plan owns
`readme-install-path-missing` and `readme-before-after-order`, taking the total 39 → 41), P4-05
(README becomes `publish-location-drift`'s third carrier with no new code), P4-07 (sequential waves)
and P4-08 (threat IDs `T-04-16` onward). It adds two plan-local choices:

| ID | Choice | Reversibility |
|----|--------|---------------|
| P4-15 | **"Leads with before/after pairs" is operationalised as: the `## Before and after` heading precedes both `## Install` and `## Status`.** DIST-06's wording is editorial; a check needs a predicate. Requiring the pairs above the status-and-caveats block is the reading that matches the requirement's intent — a reader should see what the skill does before they see what has and has not been measured about it — and it leaves `## What this is` as the only section above it. | reversible |
| P4-16 | **Four install anchors, each its own violation.** The skills-CLI command prefix, the marketplace add command prefix, the output-style path and the system-prompt path. Each is a distinct harness route DIST-06 promises; firing per missing anchor rather than once per file names exactly which route decayed, following `artifact-family-section-missing`'s per-element shape. | reversible |

**Edge-probe items this plan discharges** (3 of the phase's 11, all resolved to `explicit` — see
`04-01-PLAN.md`'s edge-probe accounting table): DIST-06 adjacency (README section presence), DIST-06
empty (an install heading with no commands beneath it), DIST-06 ordering (before/after pairs precede
install instructions). Each is authored as a plain `must_haves.truths` string above.
</plan_decisions>

<artifacts_this_phase_produces>
The complete phase-wide list lives in `04-01-PLAN.md`'s `<artifacts_this_phase_produces>` section and
is not restated here in full. **This plan's own additions to it:**

**New violation codes (2):** `readme-install-path-missing`, `readme-before-after-order`.

**New `tools/check_repo.py` symbols:** `README_INSTALL_ANCHORS`, `README_BEFORE_AFTER_HEADING`,
`README_INSTALL_HEADING`, `README_STATUS_HEADING`, `check_readme_install_paths`,
`check_readme_before_after_order`.

**New mutation functions and `MUTATIONS` entries (2):** `_mutate_readme_install_path_missing`,
`_mutate_readme_before_after_order`.

**New self-test fixture helpers and scratch roots:** `_good_readme_install`, `_bad_readme_install`,
and the `readme_install_good_root` / `readme_install_bad_root` scratch roots inside `self_test()`.

**New README sections:** `## Before and after`, `## Install`, `## Keeping derivatives in sync`.

**Modified existing symbols:** `README_CHECK_CODES` and the existing README aggregator,
`ALL_CHECK_CODES`, the module docstring's violation-code block, and README's `## Repository layout`
tree annotations.

**No rule ID is allocated, deprecated, renumbered or retitled.** `NUMBERING.md` is read, never
written.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README's front half — lead with a before/after pair, state an install path per harness, document the re-sync step, and leave the measured-figure disclosure untouched</name>
  <reversibility rating="reversible">README prose carries no published identifier and no rule allocation; the one irreversible-adjacent element is the publish location, which `04-01`'s checkpoint already settled and which this file only restates.</reversibility>
  <precondition>`examples/before-after.md`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `output-styles/proof-first.md` and `prompts/system-prompt.md` all exist on disk; `README.md` still contains the literal string `evals/conformance/RESULTS-mod04.md`. The first five are the previous three plans' outputs that this README points at, and the last is the guarded string a shipped code greps for — if any is untrue, the rewrite would either point at nothing or turn CI red.</precondition>
  <files>README.md</files>
  <read_first>
- `README.md` in full, all 146 lines — every existing section, exactly where `## Status`'s caveats block begins and ends, both occurrences of the results-pointer path in prose plus the one in the tree, and the fenced attribution pointer at the end.
- `examples/before-after.md` in full — the four pairs, so the README lead-in quotes a real one rather than inventing a fifth.
- `.claude-plugin/plugin.json` — the `repository` value, which is the publish location string README must restate identically.
- `tools/generate_derivatives.py`'s module docstring — the re-sync sentence to restate in README's own words without contradicting it.
- `output-styles/proof-first.md` and `prompts/system-prompt.md` — their generated preambles, so README's description of each artifact agrees with what the artifact says about itself.
- `NUMBERING.md`'s `## Allocated IDs` table — the closed 39-ID citable set, for any rule token the lead-in mentions in unfenced prose.
- `04-RESEARCH.md` Pitfall 2 in full — why no statement about sameness of outcome may appear here.
- This plan's `<interfaces>` block, twice, especially the two differing README-scanning behaviours.
  </read_first>
  <action>
Rewrite README additively. Delete no existing section and renumber nothing; the existing `## Status`,
`## Repository layout`, `## Rule numbering`, `## Versioning` and `## License and notices` sections keep
their content, their order relative to each other and their wording except for the tree annotations
named below.

Insert `## Before and after` immediately after `## What this is` and before `## Status` (P4-15):
- Two or three sentences framing what the pairs show: a real document passage as a vendor commonly
  drafts it, and the same passage rewritten under the numbered rules, with the rules that produced each
  change named.
- One complete pair reproduced from `examples/before-after.md` — copy it, do not write a fifth one —
  including its rule citations. Any rule token that lands in unfenced prose is checked against the
  Allocated IDs table, so reproduce the citations exactly as the source file states them.
- One line linking to `examples/before-after.md` for the remaining three families, naming the four
  families so a reader knows the coverage without opening the file.
- No figure, no percentage and no claim about this skill's effect anywhere in this section.

Insert `## Install` immediately after `## Before and after`:
- Four labelled routes, one short paragraph plus one fenced command block each, in this order.
  1. **Skills CLI** — the one-line `npx skills add ` command carrying the publish location, stating in
     prose that this is the one-command route for any harness the Agent Skills standard covers.
  2. **Claude Code plugin** — the `claude plugin marketplace add ` command chained with
     `claude plugin install proof-first@proof-first`, plus the in-session `/plugin` equivalents, and one
     sentence noting the marketplace manifest lives in this repository at `.claude-plugin/`.
  3. **Output style** — name `output-styles/proof-first.md`, state that it is selected through `/config`
     and stays on for the whole session, and state in one clause that it is generated from the skill
     content rather than written separately.
  4. **System prompt** — name `prompts/system-prompt.md`, state that it is pasted whole into a
     system-prompt field, an `AGENTS.md` or an equivalent for a harness with no skill support.
- Close the section with one honest sentence about what the last two routes are and are not: they carry
  the same rule text, the same completeness audit and the same artifact-family conventions as the skill,
  proven by a check in this repository; whether a session driven by either reaches the same conclusions
  as one with the skill folder installed has not been measured, and this repository publishes measured
  claims or none. Do not phrase this as a promise of matching results in either direction.
- Read the publish-location string out of `.claude-plugin/plugin.json`'s `repository` value rather than
  retyping it. `publish-location-drift` compares README's two command arguments against the manifests,
  and a typo there fails the build.

Insert `## Keeping derivatives in sync` after `## Install`:
- State the one command a contributor runs after editing `SKILL.md` or any of its reference files, name
  `tools/generate_derivatives.py` explicitly, and state that CI runs the generator's own `--check` mode
  and the checker's staleness code, either of which fails the build when the step is skipped. This is
  DIST-05's documented re-sync step in the place a contributor actually reads.

Update `## Repository layout`'s fenced tree only where it is now wrong:
- `output-styles/proof-first.md`, `prompts/system-prompt.md`, `examples/before-after.md` and
  `.claude-plugin/` change from `(planned)` to `(exists)`; expand `.claude-plugin/` into its two manifest
  rows; add a `generate_derivatives.py   (exists)` row beside `check_repo.py` under `tools/`.
- Change nothing else in the tree, and change nothing in the paragraph above or below it except any
  sentence that is now false about what has not been created.

Leave `## Status` alone except where its "What does not exist yet" list names something this phase just
created. Move such an entry into "What exists today" with no editorialising. Restate, round, re-caveat
and re-explain no figure. The literal `evals/conformance/RESULTS-mod04.md` path stays in prose exactly
where it is, in both places it currently appears, plus the tree.

Add no second copy of the attribution pointer line; README already carries it once at the end of
`## License and notices` and a duplicate fires a shipped code.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 -c "import pathlib;t=pathlib.Path('README.md').read_text();hs=[l[3:].strip() for l in t.splitlines() if l.startswith('## ')];print(hs.index('Before and after')<hs.index('Install')<hs.index('Status'))"</automated>
    <fails_when>non-zero exit (a heading is absent), or the printed value is False</fails_when>
    <automated>python3 -c "import pathlib;t=pathlib.Path('README.md').read_text();print([a for a in ('npx skills add ','claude plugin marketplace add ','claude plugin install proof-first@proof-first','output-styles/proof-first.md','prompts/system-prompt.md','tools/generate_derivatives.py','evals/conformance/RESULTS-mod04.md') if a not in t])"</automated>
    <fails_when>non-zero exit, or the printed list is anything other than []</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py` prints exactly `check_repo: 0 violations` and exits 0 — proving `readme-results-pointer-missing`, `pointer-missing`, `pointer-duplicated`, `undefined-id` and `publish-location-drift` all still pass against the rewritten file.
- `python3 -c "import pathlib;t=pathlib.Path('README.md').read_text();hs=[l[3:].strip() for l in t.splitlines() if l.startswith('## ')];print(hs)"` prints a list whose first four entries are `What this is`, `Before and after`, `Install`, `Keeping derivatives in sync`, followed by `Status`, `Repository layout`, `Rule numbering`, `Versioning`, `License and notices`.
- `python3 -c "import pathlib;t=pathlib.Path('README.md').read_text();print([a for a in ('npx skills add ','claude plugin marketplace add ','claude plugin install proof-first@proof-first','output-styles/proof-first.md','prompts/system-prompt.md') if a not in t])"` prints `[]` — all four install routes and both derivative paths are named.
- `grep -cF 'evals/conformance/RESULTS-mod04.md' README.md` prints `3` — the guarded literal survives in both prose positions and in the tree, exactly as it did before the rewrite.
- `python3 -c "import pathlib;p='Concepts here are paraphrased from publicly described sales frameworks. Not affiliated with or endorsed by any framework rights-holder. See NOTICES.md.';print(sum(1 for l in pathlib.Path('README.md').read_text().splitlines() if l.strip()==p))"` prints `1`.
- `python3 -c "import json,re,pathlib;loc=json.load(open('.claude-plugin/plugin.json'))['repository'].rsplit('github.com/',1)[-1];t=pathlib.Path('README.md').read_text();print(sorted(set(re.findall(r'(?:npx skills add|claude plugin marketplace add|/plugin marketplace add) (\S+)',t))), loc)"` prints a single-element list whose one member equals the printed location.
- `grep -c '(planned)' README.md` prints `0` if every remaining tree entry now exists, or otherwise prints only the count of entries that genuinely do not exist yet; in either case `python3 -c "import pathlib,re;t=pathlib.Path('README.md').read_text();print([l.strip() for l in t.splitlines() if '(planned)' in l])"` lists no line naming `output-styles/proof-first.md`, `prompts/system-prompt.md`, `examples/before-after.md`, `.claude-plugin` or `generate_derivatives.py`.
- `python3 -c "import pathlib;t=pathlib.Path('README.md').read_text().lower();print([p for p in ('equivalent behavior','equivalent behaviour','same behavior','same behaviour','identical behavior','identical behaviour','same result','same output') if p in t])"` prints `[]`.
- `python3 -c "import re,pathlib;a=set(re.findall(r'PF-\d+\.\d+|MC-\d+',re.sub(r'(?s)```.*?```','',pathlib.Path('README.md').read_text())));n=set(re.findall(r'\|\s*(PF-\d+\.\d+|MC-\d+)\s*\|',pathlib.Path('NUMBERING.md').read_text()));print(sorted(a-n))"` prints `[]` — every rule token in unfenced README prose is allocated.
- `python3 -c "import re,pathlib;t=pathlib.Path('README.md').read_text();print(sorted(set(re.findall(r'\b\d+(?:\.\d+)?%',t))))"` prints the identical list it printed before this task started — no percentage figure was added, removed or altered.
  </acceptance_criteria>
  <done>README leads with a real before/after pair copied from `examples/before-after.md`, states all four install routes with commands carrying the same publish location the manifests carry, documents the derivative re-sync step, shows a layout tree that matches what is actually on disk, and still carries its measured-figure disclosure and its single attribution pointer untouched — with the whole-repository checker running clean and no new code written yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make DIST-06 mechanical — `readme-install-path-missing` and `readme-before-after-order`, both proven live</name>
  <reversibility rating="reversible">Adding two violation codes over a file this plan itself rewrote is ordinary code change; all 39 existing self-test assertions and mutations re-run unchanged in this task's own verify block, so a regression fails the task rather than shipping.</reversibility>
  <files>tools/check_repo.py</files>
  <read_first>
- `tools/check_repo.py`'s existing README check block around line 811 — `check_readme_results_pointer`, `README_CHECK_CODES` and the README aggregator. Both new codes join that aggregator rather than creating a fourth one.
- `tools/check_repo.py` as left by `04-01` — `SKILLS_CLI_INSTALL_RE` and `MARKETPLACE_ADD_RE`, the two anchored command-prefix patterns. Reuse both; do not declare a third copy of either command prefix.
- `tools/check_repo.py` lines 385-400 — `strip_fences`. **Neither new check may use it**: README's install commands live inside fenced blocks and stripping them would erase every anchor. Read the raw text for both checks and state that reason in each docstring.
- `tools/check_repo.py` lines 1506-1545 — `check_artifact_family_sections`, the per-element firing shape both codes follow.
- `tools/check_repo.py`'s module docstring in full — the **Declared ceiling** rhetorical convention.
- `tools/check_repo.py` lines 2290-2400 and `self_test()` from line 3223 — the mutation tuple shape and the fixture/assertion conventions.
- `README.md` as rewritten by Task 1 — the exact heading text and anchor strings both parsers read.
- `04-01-PLAN.md`'s `<interfaces>` block for the shared new-check contracts.
  </read_first>
  <behavior>
    - `check_readme_install_paths` on a repository with no `README.md`: returns an empty list, checked before any read.
    - On README as Task 1 rewrote it: returns an empty list.
    - On a README with every `npx skills add ` line deleted: returns exactly one violation whose message begins with `readme-install-path-missing`, names the missing route and names `DIST-06`.
    - On a README carrying an `## Install` heading with no command and no derivative path beneath it anywhere in the file: returns four violations, one per missing anchor.
    - On a README with the `prompts/system-prompt.md` mention deleted: returns exactly one violation naming that route.
    - `check_readme_before_after_order` on a repository with no `README.md`: returns an empty list.
    - On README as Task 1 rewrote it: returns an empty list.
    - On a README with no `## Before and after` heading: returns exactly one violation naming the absent heading and `DIST-06`.
    - On a README whose `## Before and after` section has been moved below `## Status`: returns exactly one violation naming both headings and the order found.
    - On a README with no `## Install` heading: returns exactly one violation naming the absent heading.
  </behavior>
  <action>
Add the two codes named in P4-04 for this plan, each with the full three-part contract: the check wired
into the existing README aggregator and registered in `README_CHECK_CODES`, a firing and a silent
self-test fixture with named assertions, and a `MUTATIONS` entry proving discrimination against a copy
of the real repository. `MUTATION_SOURCES` already contains `'README.md'`, so no widening is needed.

`readme-install-path-missing` (P4-16, four anchors, one violation each):
- Declare `README_INSTALL_ANCHORS` as a module-level tuple of `(route_label, matcher)` pairs covering
  the four routes: the skills-CLI command prefix (reuse `SKILLS_CLI_INSTALL_RE`), the marketplace add
  command prefix (reuse `MARKETPLACE_ADD_RE`), the literal output-style path, and the literal
  system-prompt path. Reusing `04-01`'s two patterns is what keeps README's install commands and
  `publish-location-drift`'s reader anchored on the same text.
- Add `check_readme_install_paths(repo_root)` implementing the `<behavior>` block. Read README's **raw**
  text, without `strip_fences`, because the commands live inside fenced blocks; state that reason in the
  function's docstring so a future reader does not "fix" it. Emit one violation per anchor not found,
  naming the route label, the file and `DIST-06`.
- Docstring bullet, with its **Declared ceiling**: it asserts that each route's identifying text appears
  somewhere in the file. It does not assert that the command is correct, that it resolves, that the
  argument is a real repository, or that the four routes appear under the install heading rather than
  scattered — `publish-location-drift` owns the argument's consistency and nothing owns its correctness
  until the repository is published.

`readme-before-after-order` (P4-15):
- Declare `README_BEFORE_AFTER_HEADING`, `README_INSTALL_HEADING` and `README_STATUS_HEADING` as
  module-level constants holding the three level-two heading strings.
- Add `check_readme_before_after_order(repo_root)` implementing the `<behavior>` block. Read README's raw
  lines, collect the index of each of the three headings, and emit one violation naming any that is
  absent; when all three are present, emit one violation if the before/after index is not lower than both
  the install index and the status index, naming the order found and the order required.
- Docstring bullet, with its **Declared ceiling**: it asserts heading presence and relative position
  only. It says nothing about whether the section actually contains a pair, whether the pair is any good,
  or whether a reader experiences the file as leading with examples — the first of those is
  `before-after-family-missing`'s job over a different file, and the last two are manual judgments.
- Register both codes in `README_CHECK_CODES` and call both checks from the existing README aggregator.
- Add `_good_readme_install(root)` and `_bad_readme_install(root)` fixture helpers built on `_write`, plus
  `readme_install_good_root` and `readme_install_bad_root` scratch roots inside `self_test()`. The bad
  root's README is missing two of the four anchors and has its before/after heading below its status
  heading, so both codes fire with distinct named assertions. The good root must be silent on both. Union
  the bad root's codes into `bad_codes`.
- Register two `MUTATIONS` entries against the real README in the copied tree: one deleting every line
  containing the skills-CLI command prefix, one moving the `## Before and after` heading line to below the
  `## Status` heading line. Both mutate the copy only.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, any line beginning "FAIL", or the verified-codes list not containing both readme-install-path-missing and readme-before-after-order</fails_when>
    <automated>python3 tools/check_repo.py --mutation-test</automated>
    <fails_when>non-zero exit, a final line reporting fewer than 41 codes discrimination-proven, any FIRE-ONLY line, or a CONTROL line reporting any unexpected violation</fails_when>
    <automated>python3 tools/check_repo.py --self-test && python3 tools/check_repo.py --mutation-test && python3 tools/check_repo.py && python3 evals/conformance/run_conformance.py --self-test && python3 tools/generate_derivatives.py --check</automated>
    <fails_when>non-zero exit at any stage — this is the exact command sequence the CI job runs</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py --mutation-test | tail -1` prints `mutation-test PASS: 41 codes discrimination-proven`.
- `python3 tools/check_repo.py --mutation-test | grep -c 'FIRE-ONLY'` prints `0`.
- `python3 tools/check_repo.py --mutation-test | grep -c 'CONTROL: 0 violations on the unmutated copy (0 known-open per KNOWN_OPEN_VIOLATIONS, 0 unexpected)'` prints `1`.
- `python3 tools/check_repo.py --self-test | tr ',' '\n' | grep -cE 'readme-install-path-missing|readme-before-after-order'` prints `2`.
- `python3 -c "import re;s=open('tools/check_repo.py').read();b=s[s.index('def check_readme_install_paths'):];b=b[:b.index('\ndef ',10)];code=re.sub(r'(?s)\"\"\".*?\"\"\"','',b);print('strip_fences(' not in code)"` prints `True` — the install check's executable body never calls the fence stripper, so a fenced command block is still visible to it. The docstring is removed before the check so the function may name the stripper while explaining why it does not use it.
- `python3 -c "s=open('tools/check_repo.py').read();b=s[s.index('def check_readme_install_paths'):];b=b[:b.index('\ndef ',10)];print('SKILLS_CLI_INSTALL_RE' in b and 'MARKETPLACE_ADD_RE' in b)"` prints `True` — both command patterns are reused from `04-01` rather than redeclared.
- Scratch-copy proof both codes fire, run as one command: `T=$(mktemp -d); cp -R . "$T/r"; python3 -c "import pathlib,sys;p=pathlib.Path(sys.argv[1]);ls=[l for l in p.read_text().splitlines() if 'npx skills add ' not in l];i=ls.index('## Before and after');j=ls.index('## Status');ls.insert(j+1,ls.pop(i));p.write_text('\n'.join(ls))" "$T/r/README.md"; (cd "$T/r" && python3 tools/check_repo.py; true) | cut -d' ' -f1 | sort -u` prints a list containing both `readme-install-path-missing` and `readme-before-after-order`.
- After that scratch-copy proof, `python3 tools/check_repo.py` in the real tree still prints `check_repo: 0 violations`.
- `python3 -c "s=open('tools/check_repo.py').read();d=s.split('\"\"\"')[1];print(sum(1 for c in ('readme-install-path-missing','readme-before-after-order') if c in d))"` prints `2`.
- `grep -c 'Declared ceiling' tools/check_repo.py` prints at least `38` — each of Phase 4's nine new codes carries its own disclosure, on top of the 29 present before the phase began.
  </acceptance_criteria>
  <done>A README that loses one of its four install routes, or that buries the before/after pairs below the install or status sections, is a build failure — proven to fire against a mutated copy of the real repository and to stay silent on the unmutated one — with the discrimination-proven total at 41, the CONTROL line still clean, and the full CI command sequence green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → the first reader who has never seen this project | The file is the only artifact a prospective user reads before deciding to install, and its claims are load-bearing for this project's whole evidence posture. |
| README's install commands → a user's shell | Each command is copied and pasted verbatim; a wrong argument sends a user to a repository this project does not control. |
| a rewrite → an already-guarded literal string | `readme-results-pointer-missing` greps README for one exact path, and the rewrite touches the sections around it. |
| locally-authored README Markdown → two new parsers | Arbitrarily-shaped Markdown crosses into new heading and anchor parsers with no schema library behind them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-16 | Repudiation | README asserting sameness of outcome between a derivative and the installed skill | high | mitigate | This project publishes measured claims or none, and no benchmark has run. The prohibition list forbids such a statement, the install section's closing sentence states the structural claim and the absence of measurement positively, and an acceptance criterion scans the whole file for a list of sameness phrasings and requires an empty result. |
| T-04-17 | Tampering | the guarded results-pointer literal lost in the rewrite | high | mitigate | `readme-results-pointer-missing` is already live and its registered mutation deletes every line containing the path, so a rewrite that removes it turns CI red. The plan additionally pins the literal's occurrence count at 3 as an acceptance criterion, so a reduction from three to one is caught even though the shipped code would still pass at one. |
| T-04-18 | Tampering | a new check named as covered that cannot fire | high | mitigate | Both codes ship the full three-part contract: wired into the existing README aggregator and `README_CHECK_CODES`, a firing and a silent self-test fixture with a distinct named assertion each way, and a `MUTATIONS` entry proving discrimination against a copy of the real repository. Acceptance criteria pin the discrimination-proven total at 41 and assert no code is reported FIRE-ONLY. |
| T-04-19 | Tampering | an install command whose argument disagrees with the manifests it installs from | high | mitigate | `publish-location-drift`, built in `04-01` over the full prospective carrier list, gains README as its third carrier with no code change; the plan requires the string to be read out of `.claude-plugin/plugin.json` rather than retyped, and an acceptance criterion compares README's command arguments against that manifest value directly. |
| T-04-20 | Tampering | a new check silently blinded by `strip_fences` | medium | mitigate | README's install commands live inside fenced blocks, so a check that strips fences would find no anchor and pass on an empty README. Both new checks read raw text, each docstring states why, and an acceptance criterion greps the install check's own function body to prove `strip_fences` is not called there. |
| T-04-21 | Information Disclosure | a second copy of the attribution pointer introduced by the rewrite | low | mitigate | README is a required carrier and `pointer-duplicated` fires on a second copy; the prohibition list forbids adding one and an acceptance criterion counts the occurrences at exactly 1. |
| T-04-SC4 | Tampering | npm / pip / cargo installs | low | accept | This plan installs no package in any ecosystem. README documents `npx skills add` as a command an end user runs; the `skills` package's registry existence, current version and source repository were verified against the live registry during research and its verdict is OK/Approved. Nothing is added to this repository's own dependency tree, which has no manifest at all. No install task exists, so no package-legitimacy checkpoint is required. |
</threat_model>

<verification>
- `python3 tools/check_repo.py --self-test && python3 tools/check_repo.py --mutation-test && python3 tools/check_repo.py && python3 evals/conformance/run_conformance.py --self-test && python3 tools/generate_derivatives.py --check` — the exact CI job order — runs green.
- Forty-one violation codes are discrimination-proven, up from thirty-two at the start of Phase 4, with the control copy still reporting 0 violations, 0 unexpected, and no code reported fire-only.
- `skills/proof-first/SKILL.md`, everything under `skills/proof-first/references/`, `examples/`, `output-styles/`, `prompts/` and `tools/generate_derivatives.py` are all byte-identical to their pre-plan state.
- `README.md` still contains the literal `evals/conformance/RESULTS-mod04.md` three times and the attribution pointer exactly once.

**Honest verification statement.** Of this plan's three requirements:
- **DIST-06** is verified in full on its structural half: the four install routes are present, the before/after section precedes both the install and status sections, and both facts are CI-enforced. Its prose-quality half — whether the lead-in actually reads as leading with examples, and whether the install section is clear to a first-time reader — is a judgment no file-reading checker performs and is harvested into end-of-phase UAT, matching `workflow.human_verify_mode: end-of-phase`.
- **DIST-01** is implemented here and verified only as far as this environment allows: the command is stated, its argument agrees with both manifests, and the package it invokes was verified against the live npm registry during research. Whether `npx skills add` resolves and installs cannot be exercised until the repository is published; it stays a manual smoke test in `04-VALIDATION.md`'s Manual-Only table and is recorded as unresolved edge A4-E1.
- **DIST-05**'s README half is verified: the re-sync command and the two checks that enforce it are named in the file. The mechanism itself was verified by `04-03`.
</verification>

<success_criteria>
- A reader opening README sees what the skill does to a real document before they see anything about what has and has not been measured about it.
- Every harness this project promises support for has a stated install path in README, and losing one is a build failure.
- README's install commands, the plugin manifest and the marketplace manifest all name one identical publish location, enforced with no new code.
- A contributor who edits the skill finds the re-sync step documented in README, naming the one command and the two checks that fail the build when it is skipped.
- README's existing measured-figure disclosure and its single attribution pointer survive the rewrite untouched.
- The whole-repository checker, its self-test, its mutation test, the conformance self-test and the generator's own check all run green, closing Phase 4 at forty-one codes discrimination-proven with the control copy still clean.
</success_criteria>

<output>
Create `.planning/phases/04-distribution-worked-examples/04-04-SUMMARY.md` when done.

In its frontmatter, `requirements-completed:` records **implementation, not verification**. List
`[DIST-01, DIST-06]`. Do **not** re-list DIST-05: `04-03` owns its mechanism and this plan adds only
its README restatement, which the SUMMARY body should say in prose.

State explicitly in the SUMMARY body that DIST-01's live install flow is unexercised in this
environment and remains a manual smoke test until the repository is published, that DIST-06's
prose-quality half is unverified by any check in this repository, and that both are provisional
pending end-of-phase UAT.

Record as numbers rather than assertions: the `--mutation-test` discrimination-proven total, the
CONTROL line verbatim, the occurrence count of the guarded results-pointer literal in README, the
attribution-pointer occurrence count, and the publish-location string README and both manifests now
share.

Because this plan closes the phase, also state in the SUMMARY: the nine violation codes Phase 4 added
by name, the five files it created, and the two requirement halves that end the phase
`nyquist_compliant: false` by design — the live-session behaviour of DIST-03 and DIST-04 — so
`/gsd-verify-work` reads the ceiling from the artifact rather than inferring it.
</output>
