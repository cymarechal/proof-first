---
phase: 04-distribution-worked-examples
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - examples/before-after.md
  - README.md
autonomous: true
requirements: [EX-02, DIST-06]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every sentence inside a ✓ column of `examples/before-after.md` runs at or under PF-4.1's stated 25-word ceiling, counted as whitespace-delimited words with bracketed marker spans removed — the writing this repository presents as correct now obeys the catalog's one countable rule."
    - "No ✓ column narrates the rule it cites. The rationale for each rewrite lives only in the `Rules applied:` footer that already sits outside the quotation, which is the home SKILL.md's Write-mode section reserves for it."
    - "No ✗ or ✓ line in `examples/before-after.md` states a word-spelled count between two and twelve, so no example asserts a quantity that `examples/deal-brief.md` never states and that the digit-shaped figure interface cannot see."
    - "The ✓ column of `## Solution proposal` opens on the capability Halverton Mutual must be able to exercise and names AWS Control Tower afterwards as the means — the shape PF-1.9 requires, not a product as the sentence subject with the capability trailing as an apposition."
    - "The PF-3.3 retention marker in `## Demo and discovery material` is attached to a retained term inside the vendor's own sentence, so the deletion test has a term to fire against."
    - "README's reproduced RFP pair is byte-identical, line for line, to the repaired pair in `examples/before-after.md`, so README's own claim that it is reproduced from that file stays true."
    - "`python3 tools/check_repo.py --mutation-test` still reports 41 codes discrimination-proven, unchanged by this plan, with the CONTROL line reading 0 unexpected and no FIRE-ONLY line. This plan repairs prose and registers no violation code."
    - statement: "Each ✓ column reads as an applied rewrite a technical evaluator would accept rather than a paraphrase of the rule's own wording, and the PF-1.9 recast genuinely leads with the capability rather than merely reordering the clause."
      verification: backstop
    - statement: "The PF-3.3 marker is attached to a term whose deletion would change technical meaning, not to a phrase that survives deletion intact."
      verification: backstop
  prohibitions:
    - "MUST NOT edit any rule body in `skills/proof-first/SKILL.md` or in any file under `skills/proof-first/references/`. The examples are wrong; the rules are not. A rule that genuinely looks wrong is reported as a flagged assumption in the SUMMARY, never silently rewritten."
    - "MUST NOT add a row to `examples/deal-brief.md`'s Canonical figures table, and MUST NOT edit `examples/deal-brief.md` at all. The invented counts are removed from the examples rather than legitimised into the brief; see the reversibility note on Task 1."
    - "MUST NOT introduce any currency amount, percentage or ISO date that has no matching Canonical figures row; `unlisted-figure` scans this file and fires on the first unmatched token."
    - "MUST NOT cite a PF- or MC- token that `NUMBERING.md`'s Allocated IDs table does not define; `check_undefined_id` scans `examples/` and `README.md` with fences stripped."
    - "MUST NOT remove a family heading, a ✗ line, a ✓ line or a `Rules applied:` footer, and MUST NOT reorder the four family headings; `before-after-family-missing` and `before-after-citation-missing` fire on any of those."
    - "MUST NOT edit any ✗ column. Each one is the exhibit of bad writing this file exists to contrast against, and its rule breaches are part of what it exhibits."
    - "MUST NOT name a real company, product vendor relationship or person beyond the platform nouns `examples/deal-brief.md` already licenses: VMware vSphere, Oracle Database, Amazon EC2, Amazon Aurora PostgreSQL, AWS Control Tower."
    - "MUST NOT assert anything about this skill's effect, or about equivalence of behaviour between the installed skill, the output style and the system prompt. No benchmark has run; Phase 5 owns that."
    - "MUST NOT use an emoji anywhere in shipped content."
    - "MUST NOT hand-edit `output-styles/proof-first.md` or `prompts/system-prompt.md`, and MUST NOT edit any section of README.md other than the three reproduced example lines; `04-08` owns README's own prose."
  artifacts:
    - path: "examples/before-after.md"
      provides: "Four document-level before/after pairs whose ✓ columns obey PF-4.1, carry no rule narration, invent no count, and demonstrate PF-1.9 and PF-3.3 in the shape those rules require"
      contains: "## Solution proposal"
      min_lines: 30
    - path: "README.md"
      provides: "A reproduced RFP pair held line-for-line identical to the repaired source pair"
      contains: "Rules applied: PF-2.1, MC-11."
      min_lines: 200
  key_links:
    - from: "README.md"
      to: "examples/before-after.md"
      via: "README's ✗ and ✓ lines and its Rules-applied footer are reproduced verbatim from the RFP pair in that file"
      pattern: "examples/before-after\\.md"
    - from: "examples/before-after.md"
      to: "examples/deal-brief.md"
      via: "every fact and every figure in every pair traces to the brief, which states that anything asserted beyond it is an invented fact and a defect"
      pattern: "examples/deal-brief\\.md"
    - from: "examples/before-after.md"
      to: "skills/proof-first/SKILL.md"
      via: "each ✓ column demonstrates the rule its footer cites, and the footer is the only place the rationale appears"
      pattern: "Rules applied:"
---

<objective>
Repair the prose of `examples/before-after.md` so the writing this repository presents as correct
actually obeys the catalog it teaches, and keep README's reproduced copy of the RFP pair identical to
the repaired source.

Purpose: the phase's four executed plans shipped a 41-code green gate over four ✓ columns that break
PF-4.1 in 10 of 11 sentences (longest 88 words, mean 45), while the ✗ columns they are contrasted
against break it once in 12 (mean 17). On the one countable rule in the catalog, the writing labelled
correct scores worse than the writing labelled wrong. The same columns narrate their own rules inside
the quoted prose, invert PF-1.9, and state two counts `examples/deal-brief.md` never states — which
that file's own closing section calls an invented fact and a defect. This is the file README uses to
sell the project. It has to be right before any checker is written around it.

Output: four repaired ✓ columns in `examples/before-after.md`, a README whose reproduced pair matches
them line for line, and a gate that still reports 41 codes discrimination-proven. This plan adds no
violation code; `04-07` mechanises the three defect classes that have a signature, on top of a file
this plan has already made clean.
</objective>

<gap_coverage>
Gaps from `04-UAT.md` this plan closes, each traceable to a task:

| Gap | Where | How |
|---|---|---|
| G-04-1 | Tasks 1, 2, 3 | Rule narration deleted from all four ✓ columns; the rationale stays in the `Rules applied:` footers, the home SKILL.md line 261 reserves for it. Mechanised in `04-07` as `example-rule-narration`. |
| G-04-2 | Tasks 1, 2, 3 | Every ✓ sentence brought to 25 words or fewer, from 10 of 11 over and a maximum of 88. Mechanised in `04-07` as `example-sentence-length`. |
| G-04-3 | Task 2 | The Solution proposal ✓ column recast capability-first with AWS Control Tower named afterwards as the means. Not mechanised; carried as a `verification: backstop` truth with a first-sentence product-absence probe as its mechanical half. |
| G-04-4 | Tasks 1, 3 | Both invented counts removed rather than legitimised into the Canonical figures table; see Task 1's reversibility note. Mechanised in `04-07` as `before-after-spelled-count`. |
| G-04-5 | Task 3 | The PF-3.3 marker re-attached to a retained term inside the vendor's own sentence. Not mechanised; carried as a `verification: backstop` truth with a preceding-character probe as its mechanical half. |
| G-04-6 | Task 1, partially | Only the README half that inherits from this file — the three reproduced lines are kept identical to the repaired source. The other six README defects are `04-08`'s. |

G-04-7 is out of this plan's scope and is owned by `04-10`.
</gap_coverage>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-distribution-worked-examples/04-UAT.md
@.planning/phases/04-distribution-worked-examples/04-PATTERNS.md
@.planning/phases/04-distribution-worked-examples/04-02-SUMMARY.md
@examples/before-after.md
@examples/deal-brief.md
@README.md
@skills/proof-first/SKILL.md
</context>

<interfaces>
<!-- Every line below was read from the named file during this planning session (2026-09-17). -->

**Baseline this plan starts from.** All four gate commands pass today:
`python3 tools/check_repo.py` prints `check_repo: 0 violations`;
`python3 tools/check_repo.py --self-test` passes;
`python3 tools/check_repo.py --mutation-test` prints `mutation-test PASS: 41 codes
discrimination-proven` with a CONTROL line reading
`0 violations on the unmutated copy (0 known-open per KNOWN_OPEN_VIOLATIONS, 0 unexpected)`;
`python3 tools/generate_derivatives.py --check` exits 0. If the total is not 41, stop and say so
rather than proceeding.

**`examples/before-after.md` as it stands (36 lines, read in full this session).** Line 1 title,
lines 3-8 a preamble paragraph, then four `## ` family sections in the frozen
`ARTIFACT_FAMILY_SECTIONS` order. Each section is a ✗ line, a ✓ line, a blank line and a
`Rules applied: ...` footer. The ✗/✓ lines are single physical lines carrying the whole passage
inside one pair of ASCII double quotes.

| Section | ✗ line | ✓ line | Footer |
|---|---|---|---|
| RFP and RFI response | 12 | 13 | 15 — `Rules applied: PF-2.1, MC-11.` |
| Solution proposal | 19 | 20 | 22 — `Rules applied: PF-1.9, PF-2.14, PF-2.17.` |
| Executive summary | 26 | 27 | 29 — `Rules applied: PF-0.1, PF-1.25, PF-2.11.` |
| Demo and discovery material | 33 | 34 | 36 — `Rules applied: PF-3.3, PF-2.14, MC-31.` |

**Measured word counts per sentence, this session**, bracketed marker spans removed, sentences split
on `.`/`?`/`!` followed by whitespace. ✓ columns in file order:
`[88, 16]`, `[43, 32, 87]`, `[33, 45, 30]`, `[34, 55, 34]` — 10 of 11 over 25, max 88, mean 45.
✗ columns: `[18, 18, 28, 18]`, `[14, 13, 18]`, `[20, 13, 11]`, `[13, 22]` — 1 of 12 over 25, mean 17.

**Word-spelled cardinals measured in ✗/✓ lines, this session: 5 occurrences.** One in the RFP ✓ line
naming a number of sequenced cut-over waves; one in the Solution proposal ✓ line counting the risks;
one in the Executive summary ✓ line counting the economic buyer's stated terms; two in the Demo ✓ line
counting the governed accounts. Zero occurrences in any ✗ line.

**The rules under repair, quoted from `skills/proof-first/SKILL.md`.**
- `PF-4.1` (line 213): "No sentence runs longer than 25 words, counted as words delimited by
  whitespace — one stated definition of length, not an implied one." Replace-with: "split at 25 words
  into two sentences, one claim per sentence, evidence adjacent to its claim."
- `PF-1.9` (line 83): "State the capability as what the buyer must be able to do. A product name
  enters only afterward, as the means of delivering it — never as the sentence's subject."
- `PF-3.3` (line 203): a term appearing verbatim in the customer's own supplied source material "is
  retained and marked, not deleted". Replace-with: "the retention marker naming its source:
  `[PF-3.3: customer's term, retained — source]`."
- Write mode (line 261), the sentence G-04-1 turns on: "No list of applied rules follows the prose."

**Facts the brief licenses** (`examples/deal-brief.md`, read in full): 850 VMware vSphere virtual
machines; 40 Oracle Database instances; Amazon EC2, Amazon Aurora PostgreSQL and AWS Control Tower as
targets; Q1 weighted 30% and asking for "the migration approach and cut-over plan"; the 6-hour
settlement-batch window with no measured baseline; the $2,300,000 current annual run rate; the 8-month
examination window against Kestrel's own 14-month comparable programme; SOC 2 Type I held against a
Type II requirement; Marcus Feld's discovery quote "We need a landing zone we can actually govern —
right now every VM is a snowflake"; Diane Osoria's "I'm measured on run-rate reduction and a clean
exam, not on architecture elegance." The brief states no number of migration waves and no number of
demo accounts. Its closing section: "Anything a later example asserts beyond the facts stated here is
an invented fact and a defect."

**Checks that read this file today and re-run against the repair.**
- `before-after-family-missing` — all four frozen headings present, in order, each with a ✗ and a ✓.
- `before-after-citation-missing` — each section body carries at least one `PF-#.#` or `MC-#` token.
- `check_undefined_id` — every PF-/MC- token in `examples/` and `README.md`, fences stripped, must
  exist in `NUMBERING.md`'s Allocated IDs table.
- `unlisted-figure` — every currency amount, percentage and ISO date in `examples/**/*.md` must equal a
  Canonical figures row value. Its declared ceiling states plainly that it does not catch bare counts;
  that hole is what let the two invented counts ship, and `04-07` closes the word-spelled half of it.

**README's reproduced pair** (verified this session): README lines 23, 24 and 26 are byte-identical to
`examples/before-after.md` lines 12, 13 and 15. README line 19 states the pair is "reproduced from
`examples/before-after.md`". Nothing in the repository enforces that today; `04-08` adds the code that
does.

**`examples/before-after.md` is not a derivative source.** `DERIVATIVE_SOURCE_NAMES` in both
`tools/check_repo.py` and `tools/generate_derivatives.py` lists `SKILL.md` plus `deletion-test.md`,
`completeness-audit.md`, `artifact-patterns.md` and `checklist.md` only. Editing this file does not
stale the derivative stamp and requires no regeneration.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: Repair the RFP and RFI response pair end to end, and propagate it into README</name>
  <reversibility rating="reversible">The scope decision this task settles — remove the invented counts from the example prose rather than add rows to the Canonical figures table — is reversible in either direction. Removal is chosen because a migration wave count is the vendor's own solution-design choice rather than a fact about the buyer's deal, because the Canonical figures table is an interface Phase 5 binds to and widening it for an example's convenience inverts the dependency, and because the brief's own rule is that the example bends to the brief.</reversibility>
  <precondition>All four gate commands pass and `python3 tools/check_repo.py --mutation-test` reports 41 codes discrimination-proven with 0 unexpected CONTROL violations. Every verify step in this plan compares against that baseline; starting from a different one makes each failing-direction statement meaningless.</precondition>
  <files>examples/before-after.md, README.md</files>
  <read_first>
- `examples/before-after.md` in full, all 36 lines — the preamble, all four sections, and the exact punctuation of the ✗/✓ quoting convention this repair must preserve.
- `examples/deal-brief.md` in full, especially `## Estate and target platforms`, `### Scored RFP questions`, `## Pain points` and `## Canonical figures`, so every retained figure traces to a row and no new fact enters.
- `skills/proof-first/SKILL.md` lines 123-130 (PF-2.1) and 213-218 (PF-4.1), plus line 261's Write-mode sentence — the three rules this pair is judged against.
- `skills/proof-first/references/completeness-audit.md`, the MC-11 entry — the second rule this pair's footer cites, so the repaired column still demonstrates it.
- `README.md` lines 14-30 — the before/after lead-in, the three reproduced lines, and the sentence at line 19 claiming they are reproduced from the source file.
- This plan's `<interfaces>` block in full, especially the measured per-sentence word counts and the list of checks that re-run against the repair.
  </read_first>
  <action>
Rewrite line 13's ✓ column of `## RFP and RFI response`. Leave line 12's ✗ column untouched.

Three defects come out of the ✓ column, and nothing else changes.

First, sentence length. The column is currently two sentences of 88 and 16 words. Rewrite it as four
or more sentences, none over 25 whitespace-delimited words. Keep one claim per sentence with its
evidence adjacent, which is what PF-4.1's replace-with instruction asks for and what makes the split
nearly free once the narration is gone. Every figure that stays must still trace to a Canonical
figures row: the 30% weight on Q1, the 850-VM VMware vSphere estate, the 40 Oracle Database instances,
the required 6-hour settlement-batch window. Amazon EC2 and Amazon Aurora PostgreSQL stay as the named
targets.

Second, rule narration. Two spans restate PF-2.1 and MC-11 in the vendor's own voice inside the
quotation. The first is the trailing clause after the em dash that contrasts checkable evidence
against an unbacked claim. The second is the whole final sentence, which announces that the answer has
been placed first rather than simply placing it first. Delete both. The rationale belongs in the
`Rules applied: PF-2.1, MC-11.` footer at line 15, which already sits outside the quotation and is the
home SKILL.md line 261 reserves for it. Do not extend the footer into a paragraph; its existing
one-line form stays exactly as it is.

Third, an invented count. The clause describing how the estate moves states a number of sequenced
cut-over waves. The brief states no such number. Remove the number. Wave-by-wave sequencing may stay
as a shape — cutting over inside a scheduled maintenance window, validating settlement-batch
completion against the required 6-hour window, then proceeding to the next cut-over — as long as no
quantity is asserted and no word-spelled cardinal between two and twelve survives anywhere in the
line. Refer to the next cut-over rather than counting the remaining ones.

What must survive: Q1 identified as the highest-weighted scored question at 30%; the answer to the
question standing at the front of the column rather than a vendor-background preamble; the estate
figures; the 6-hour validation gate. The rewritten column answers the question first because it is
written that way, not because it says it is.

Then propagate. README lines 23, 24 and 26 are a verbatim reproduction of lines 12, 13 and 15 of this
file. Replace README's three lines with the repaired ones so all three match character for character.
Change nothing else in README in this task: the lead-in paragraph at lines 16-19, the Install section
and everything below stay exactly as they are, because `04-08` owns README's own prose defects and
touching them here would collide with that plan.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 -c "import re,pathlib,sys;L=pathlib.Path('examples/before-after.md').read_text().splitlines();i=L.index('## RFP and RFI response');j=next(k for k in range(i+1,len(L)) if L[k].startswith('## '));c=[l for l in L[i:j] if l[:1]==chr(10003)][0];b=re.sub(r'\[[^\]]*\]','',c[1:].strip().strip(chr(34)));s=[x for x in re.split(r'(?<=[.!?])\s+',b) if x.strip()];print(len(s),max(len(x.split()) for x in s))"</automated>
    <fails_when>non-zero exit, or the first printed integer is below 4, or the second printed integer is above 25</fails_when>
    <automated>python3 -c "import re,pathlib;L=[l for l in pathlib.Path('examples/before-after.md').read_text().splitlines() if l[:1] in (chr(10007),chr(10003))];print(sum(len(re.findall(r'\b(two|three|four|five|six|seven|eight|nine|ten|eleven|twelve)\b',l,re.I)) for l in L))"</automated>
    <fails_when>non-zero exit, or the printed integer is 5 or above (the measured baseline is 5; this task removes one, so 4 or below is required here and Task 3 drives it to 0)</fails_when>
    <automated>python3 -c "import pathlib;B=set(pathlib.Path('examples/before-after.md').read_text().splitlines());R=[l for l in pathlib.Path('README.md').read_text().splitlines() if l[:1] in (chr(10007),chr(10003)) or l.startswith('Rules applied:')];print([l[:40] for l in R if l not in B])"</automated>
    <fails_when>non-zero exit, or the printed list is anything other than []</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py` prints exactly `check_repo: 0 violations` and exits 0.
- The RFP ✓ column splits into at least 4 sentences, up from the baseline 2, and its longest sentence is at most 25 words, down from the baseline 88.
- The file-wide count of word-spelled cardinals between `two` and `twelve` across all ✗ and ✓ lines is at most 4, down from the measured baseline of 5.
- Every one of README's ✗, ✓ and `Rules applied:` lines appears verbatim as a line in `examples/before-after.md`: the printed drift list is `[]`.
- `grep -c '^Rules applied: PF-2.1, MC-11\.$' examples/before-after.md` prints `1` — the footer is unchanged in content and still a single line.
- `git diff -- examples/before-after.md` shows no change to the ✗ line of the RFP section.
- `git diff --stat -- README.md` reports at most 3 changed lines, proving no other README section moved.
  </acceptance_criteria>
  <done>The RFP ✓ column obeys PF-4.1, narrates no rule, states no invented count, and README reproduces it verbatim. `check_repo.py` reports 0 violations.</done>
</task>

<task type="auto">
  <name>Task 2: Recast the Solution proposal ✓ column so PF-1.9 is demonstrated rather than inverted</name>
  <files>examples/before-after.md</files>
  <read_first>
- `examples/before-after.md` lines 17-22 as Task 1 leaves them — the Solution proposal section in full.
- `skills/proof-first/SKILL.md` lines 83-87 (PF-1.9), 165-172 (PF-2.14) and 183-190 (PF-2.17) — the three rules this pair's footer cites and must demonstrate.
- `skills/proof-first/SKILL.md` lines 265-271 — the Write-mode paragraph on an inline marker taking the exact place a claim would have occupied, and the register convention, so both REVIEW markers keep their shipped form.
- `examples/deal-brief.md` `## Inconvenient facts` and `## Timeline` — the SOC 2 Type I against Type II gap and the 14-month comparable programme against the 8-month examination window, the two risks this column names.
- This plan's `<interfaces>` block, especially the PF-1.9 quotation and the measured counts `[43, 32, 87]` for this column.
  </read_first>
  <action>
Rewrite line 20's ✓ column of `## Solution proposal`. Leave line 19's ✗ column untouched.

Four defects come out, and nothing else changes.

First, the PF-1.9 inversion, which is the headline defect. The column currently makes the target
architecture the sentence subject and delivers the capability as a post-colon apposition, then
announces the mapping in a following sentence because the sentence shape does not show it. Recast so
the first sentence states, as its subject and verb, what Halverton Mutual must be able to do — govern
every account in the new estate from one place, under guardrails the team can see and enforce. Name
AWS Control Tower only in a later sentence, as the means that delivers it. The rule's own words: a
product name enters only afterward, as the means of delivering it, never as the sentence's subject.
The estate detail — the 850-VM VMware vSphere estate, the 40 Oracle Database instances, Amazon EC2 for
compute, Amazon Aurora PostgreSQL for the migrated data layer — stays, but arrives after the
capability rather than before it.

Second, rule narration. Delete the sentence explaining that the architecture is what delivers the
capability the buyer asked for, including its trailing contrast between a mapped product and an
asserted generic strength. Delete the clause introducing the risks that explains they are named
instead of omitted. Both restate the rule instead of applying it. The footer at line 22 already
carries PF-1.9, PF-2.14 and PF-2.17 and is where that rationale belongs.

Third, a word-spelled count. The risk-introducing sentence counts the risks. Remove the count: name
each risk in its own sentence and let the reader see how many there are without being told. This also
serves the fourth defect.

Fourth, sentence length. Measured `[43, 32, 87]`; every sentence must come in at 25 words or fewer.
The 87-word sentence carries both risks and both markers at once — split it so each risk is its own
sentence with its own marker adjacent, which is what PF-2.14 and PF-2.17 ask for anyway.

Both inline markers keep their shipped bracket form exactly: the compliance REVIEW marker naming the
SOC 2 Type I against Type II gap, and the commitment REVIEW marker naming the 14-month comparable
programme against the 8-month window. Keep each marker's text inside a single pair of square brackets
on one line, because the word-count measurement removes bracketed spans before counting and a marker
split across two bracket pairs would be counted as prose.

Do not weaken either risk statement. Both facts sit in the brief's inconvenient-facts section
precisely so the integrity rules have real material to fire on; softening them would remove the only
reason this pair cites PF-2.14 and PF-2.17 at all.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 -c "import re,pathlib;L=pathlib.Path('examples/before-after.md').read_text().splitlines();i=L.index('## Solution proposal');j=next(k for k in range(i+1,len(L)) if L[k].startswith('## '));c=[l for l in L[i:j] if l[:1]==chr(10003)][0];b=re.sub(r'\[[^\]]*\]','',c[1:].strip().strip(chr(34)));s=[x for x in re.split(r'(?<=[.!?])\s+',b) if x.strip()];print(len(s),max(len(x.split()) for x in s),len(re.findall(r'\b(two|three|four|five|six|seven|eight|nine|ten|eleven|twelve)\b',c,re.I)))"</automated>
    <fails_when>non-zero exit, or the first printed integer is below 4, or the second printed integer is above 25, or the third printed integer is anything other than 0</fails_when>
    <automated>python3 -c "import re,pathlib;L=pathlib.Path('examples/before-after.md').read_text().splitlines();i=L.index('## Solution proposal');j=next(k for k in range(i+1,len(L)) if L[k].startswith('## '));c=[l for l in L[i:j] if l[:1]==chr(10003)][0];b=re.sub(r'\[[^\]]*\]','',c[1:].strip().strip(chr(34)));first=re.split(r'(?<=[.!?])\s+',b)[0];print([p for p in ('VMware vSphere','Oracle Database','Amazon EC2','Amazon Aurora PostgreSQL','AWS Control Tower') if p in first])"</automated>
    <fails_when>non-zero exit, or the printed list is anything other than []</fails_when>
    <automated>python3 -c "import pathlib;t=pathlib.Path('examples/before-after.md').read_text();print(t.count('[PF-2.17 REVIEW'),t.count('[PF-2.14 REVIEW'))"</automated>
    <fails_when>non-zero exit, or the first printed integer is not 1, or the second printed integer is below 1</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py` prints exactly `check_repo: 0 violations` and exits 0.
- The Solution proposal ✓ column splits into at least 4 sentences and its longest is at most 25 words, down from a baseline maximum of 87.
- That column's first sentence names none of the five licensed platform nouns: the printed product list is `[]`, which is the mechanical half of PF-1.9's "never as the sentence's subject".
- That column contains zero word-spelled cardinals between `two` and `twelve`.
- Exactly one `[PF-2.17 REVIEW` marker and at least one `[PF-2.14 REVIEW` marker remain in the file, each inside a single pair of square brackets on one line.
- `grep -c '^Rules applied: PF-1.9, PF-2.14, PF-2.17\.$' examples/before-after.md` prints `1`.
- `git diff -- examples/before-after.md` shows no change to the ✗ line of the Solution proposal section.
  </acceptance_criteria>
  <done>The Solution proposal ✓ column leads with the capability and names the product as the means, splits into sentences at or under 25 words, carries both integrity markers intact, and narrates no rule.</done>
</task>

<task type="auto">
  <name>Task 3: Repair the Executive summary and Demo pairs, re-attach the PF-3.3 marker to a term, and drive the file-wide measurements to zero</name>
  <files>examples/before-after.md</files>
  <read_first>
- `examples/before-after.md` lines 24-36 as Tasks 1 and 2 leave them — both remaining sections in full.
- `skills/proof-first/SKILL.md` lines 55-62 (PF-0.1), 113-118 (PF-1.25), 147-154 (PF-2.11) and 203-208 (PF-3.3) — the four rules these two pairs cite.
- `skills/proof-first/references/deletion-test.md` in full — the edge cases governing what counts as a retained term, so the PF-3.3 marker lands on something whose deletion would change technical meaning.
- `skills/proof-first/references/completeness-audit.md`, the MC-31 entry — the follow-up-owner obligation the demo pair's footer cites.
- `examples/deal-brief.md` `### Discovery call quotes` and `### Economic buyer stated priorities` — the exact wording of Marcus Feld's and Diane Osoria's supplied source material, which is what PF-3.3's override fires against.
- This plan's `<interfaces>` block, especially the measured counts `[33, 45, 30]` and `[34, 55, 34]` for these two columns and the file-wide cardinal baseline of 5.
  </read_first>
  <action>
Rewrite line 27's ✓ column of `## Executive summary` and line 34's ✓ column of
`## Demo and discovery material`. Leave both ✗ columns untouched.

Executive summary carries three defects.

Delete the rule narration: the trailing clause explaining that the business case is built from the
economic buyer's stated terms instead of from a capability list, and the final sentence's opening,
which announces that the summary turns to the migration's deliverables only after the reframe and the
case. Write the reframe, then the case, then the deliverables, in that order, without narrating the
order. The footer PF-0.1, PF-1.25, PF-2.11 at line 29 is where the rationale belongs.

Remove the word-spelled count. The same narration clause counts Diane Osoria's stated terms; it goes
with the clause, and no count is reintroduced elsewhere.

Fix the sentence length. Measured `[33, 45, 30]`; all three are over the ceiling. Split at clause
boundaries into sentences of at most 25 words each.

What must survive in the executive summary column: the `[PF-2.11 GAP: no measured baseline for the
settlement batch overrun]` marker in the exact position the claim would have occupied; Diane Osoria
named with her role and her two stated priorities, the $2,300,000 current annual run rate and the
clean regulatory examination; the 6-hour settlement-batch window; and the closing list of what the
migration delivers.

Demo and discovery material carries four defects.

Fix the PF-3.3 marker placement, which is G-04-5. The marker currently trails a whole attributed
quotation, so there is no term for the deletion test to fire against and the rule is demonstrated
where it is not needed. Re-attach it: put Marcus Feld's own phrase — a landing zone the team can
actually govern — inside the vendor's own sentence as a retained term, and place the marker
immediately after that phrase in its shipped form, naming Marcus Feld and discovery as the source. The
full quotation may stay as its own short sentence with its attribution, but the marker moves off the
quotation and onto the term.

Remove the invented count. The demo-script sentence counts the accounts AWS Control Tower governs and
the success clause refers back to that count. The brief states no number of demo accounts. Remove
both: name the accounts without counting them, and have the success clause turn on any account in the
demo rather than on a numbered set. No word-spelled cardinal between two and twelve may survive
anywhere in the line.

Delete the rule narration: the contrast between the scenario Marcus Feld described and a standard
product tour. Showing his scenario is the demonstration; saying it is not a product tour is the
narration. The footer PF-3.3, PF-2.14, MC-31 at line 36 is where that belongs.

Fix the sentence length. Measured `[34, 55, 34]`; all three are over. Split at clause boundaries.

What must survive in the demo column: Marcus Feld named with his role; his verbatim discovery phrase
retained and marked; AWS Control Tower and the 850-VM estate; the success criterion stated as a
checkable condition on an out-of-band change being blocked, agreed with his team before the session
runs; and the MC-31 follow-up owner carrying the commitment REVIEW marker on the date.

This is the last prose task, so the verify block here asserts the file-wide zeros across all four
sections rather than one section's numbers.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py --self-test &amp;&amp; python3 tools/check_repo.py --mutation-test &amp;&amp; python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit at any stage, or the mutation summary line reports a number other than 41 codes discrimination-proven, or any FIRE-ONLY line appears, or the CONTROL line reports more than 0 unexpected</fails_when>
    <automated>python3 tools/generate_derivatives.py --check</automated>
    <fails_when>non-zero exit, or any printed line names a derivative as differing from freshly rendered output or as missing</fails_when>
    <automated>python3 -c "import re,pathlib;C=[l for l in pathlib.Path('examples/before-after.md').read_text().splitlines() if l[:1]==chr(10003)];w=[];[w.extend(len(x.split()) for x in re.split(r'(?<=[.!?])\s+',re.sub(r'\[[^\]]*\]','',l[1:].strip().strip(chr(34)))) if x.strip()) for l in C];print(len(C),len(w),max(w),sum(1 for v in w if v>25))"</automated>
    <fails_when>non-zero exit, or the first printed integer is not 4, or the third printed integer is above 25, or the fourth printed integer is anything other than 0</fails_when>
    <automated>python3 -c "import re,pathlib;L=[l for l in pathlib.Path('examples/before-after.md').read_text().splitlines() if l[:1] in (chr(10007),chr(10003))];print(sum(len(re.findall(r'\b(two|three|four|five|six|seven|eight|nine|ten|eleven|twelve)\b',l,re.I)) for l in L))"</automated>
    <fails_when>non-zero exit, or the printed integer is anything other than 0</fails_when>
    <automated>python3 -c "import re,pathlib;C=[l for l in pathlib.Path('examples/before-after.md').read_text().splitlines() if l[:1]==chr(10003)];M=('asserted','a claim','a capability list','a generic strength','a standard product tour','a feature list','a paraphrase','an assertion','boilerplate','marketing');h=[];[h.append(l[:30]) for l in C for m in re.finditer(r'rather than|,\s+not\s+|instead of',l) if any(t in l[m.end():m.end()+60] for t in M)];print(h)"</automated>
    <fails_when>non-zero exit, or the printed list is anything other than []</fails_when>
    <automated>python3 -c "import pathlib;L=pathlib.Path('examples/before-after.md').read_text().splitlines();i=L.index('## Demo and discovery material');c=[l for l in L[i:] if l[:1]==chr(10003)][0];k=c.find('[PF-3.3');print(k>0, c[max(0,k-2):k])"</automated>
    <fails_when>non-zero exit, or the printed boolean is False, or the printed two-character context ends with a straight or curly closing quotation mark, which would mean the marker is still attached to a whole quotation rather than to a term</fails_when>
  </verify>
  <acceptance_criteria>
- All four gate commands pass: `check_repo.py --self-test`, `check_repo.py --mutation-test` reporting exactly `41 codes discrimination-proven` with `0 unexpected` on CONTROL and no FIRE-ONLY line, `check_repo.py` printing `check_repo: 0 violations`, and `generate_derivatives.py --check` exiting 0.
- Across all four ✓ columns: 4 columns measured, longest sentence at most 25 words, and exactly 0 sentences over 25 — down from 10 of 11 over, max 88.
- The file-wide count of word-spelled cardinals between `two` and `twelve` in ✗/✓ lines is exactly 0, down from the measured baseline of 5.
- The narration probe — a contrastive connective followed within 60 characters by one of the ten frozen meta terms — prints `[]` across all ✓ columns, down from 4 of 4 columns hit at baseline.
- In the Demo ✓ column the `[PF-3.3` marker is present and the two characters immediately preceding it are not a closing quotation mark, proving the marker is attached to a term rather than trailing a quotation.
- `git diff -- examples/before-after.md` shows no change to any ✗ line in the file.
  </acceptance_criteria>
  <done>All four ✓ columns obey PF-4.1, narrate no rule, invent no count, and the PF-3.3 marker sits on a term. All four gate commands pass at 41 codes.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates no new symbol. It changes content only:

- `examples/before-after.md` — four rewritten ✓ columns (RFP and RFI response, Solution proposal, Executive summary, Demo and discovery material). No heading, no ✗ column, no `Rules applied:` footer and no section order changes.
- `README.md` — three replaced lines (the reproduced ✗ line, ✓ line and `Rules applied:` footer of the RFP pair). No other line changes.

New violation codes: none. New functions: none. New fixture roots: none. New MUTATIONS entries: none.
Expected discrimination-proven count after this plan: **41**, unchanged.

Residuals this plan leaves open, for the phase's verification step to record in `.planning/WINDOWS.md`:
- G-04-3's PF-1.9 judgment and G-04-5's PF-3.3 term-attachment judgment are both semantic. Neither is
  mechanised here or in `04-07`; both are carried as `verification: backstop` truths above. A
  product-name-absence probe on the first sentence and a preceding-character probe on the marker are
  the mechanical halves this plan does assert, and they are proxies, not verdicts.
- This plan closes or reclassifies no existing WINDOWS entry. Entries 11, 12 and 13 stay open and
  untouched.
</artifacts_this_phase_produces>

<threat_model>
Security enforcement is active with no `workflow.security_asvs_level` and no `workflow.security_block_on`
configured, so this block uses the defaults: **ASVS Level 1**, blocking threshold **high**.

This is a documentation-and-stdlib-scripts repository. It has no network surface, no authentication,
no session state, no database, no deserialisation of untrusted input, and no user input at all beyond
files a contributor commits. This plan touches two Markdown files and adds no code. A thin register is
the correct outcome; it is stated rather than omitted so the reasoning is auditable.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor commit → shipped example content | Untrusted prose crosses into files an installed user and a public reader both consume. This is the only boundary this plan touches. |
| shipped example content → installed user's agent session | `examples/before-after.md` is read by a human, not executed; the skill folder that ships to a user does not include `examples/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05-01 | Tampering | `examples/before-after.md` ✓ columns | medium | mitigate | The repair is asserted by measurement, not by reading: four gate commands plus four inline probes (sentence length, cardinal count, narration signature, marker attachment) run in Task 3's verify block. `04-07` turns three of those probes into registered violation codes so a later edit cannot silently undo this plan. |
| T-04-05-02 | Information disclosure | example prose naming a real party | high | mitigate | Every fact must trace to `examples/deal-brief.md`, which is wholly invented and says so; the prohibition list forbids naming any real company, person or vendor relationship beyond the five platform nouns the brief licenses. `unlisted-figure` mechanically blocks any currency, percentage or ISO date with no canonical row. |
| T-04-05-03 | Repudiation | README drifting from the source it claims to reproduce | medium | mitigate | Task 1's drift probe asserts every README ✗/✓/footer line appears verbatim in the source file; `04-08` registers `readme-example-drift` so the claim is enforced continuously rather than once. |
| T-04-05-04 | Spoofing | a rewritten ✓ column asserting an unmeasured effect claim | medium | mitigate | Prohibition list forbids any statement about the skill's effect or about equivalence between the skill, output style and system prompt; the Task 3 verify block runs the full gate, and `check_undefined_id` blocks citation of an unallocated rule token. |
| T-04-05-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan, and the repository's zero-dependency constraint forbids adding one: `tools/check_repo.py` and `tools/generate_derivatives.py` import only the standard library and there is no package manifest or lockfile anywhere in the tree. A Package Legitimacy Audit is therefore not required. Accepted on the basis that the absence of any install surface is itself mechanically checkable — a new import would have to pass review against the stated stdlib-only discipline. |
</threat_model>

<verification>
After all three tasks, from the repository root:

1. `python3 tools/check_repo.py --self-test` — exits 0.
2. `python3 tools/check_repo.py --mutation-test` — prints `mutation-test PASS: 41 codes
   discrimination-proven`, a CONTROL line reading `0 unexpected`, and no FIRE-ONLY line.
3. `python3 tools/check_repo.py` — prints `check_repo: 0 violations`.
4. `python3 tools/generate_derivatives.py --check` — exits 0.
5. The four inline probes in Task 3's verify block report: 4 ✓ columns, 0 sentences over 25 words,
   0 word-spelled cardinals in ✗/✓ lines, and `[]` from the narration probe.
6. `git diff -- examples/before-after.md` shows changes on ✓ lines only.
</verification>

<success_criteria>
- All four ✓ columns of `examples/before-after.md` have every sentence at or under 25 words.
- Zero narration-signature hits and zero word-spelled cardinals across the file's quoted lines.
- The Solution proposal ✓ column's first sentence names no product.
- The Demo ✓ column's PF-3.3 marker is attached to a term, not to a quotation.
- README's three reproduced lines are verbatim lines of `examples/before-after.md`.
- The gate still reports 41 codes discrimination-proven, 0 violations, 0 unexpected CONTROL.
</success_criteria>

<output>
Create `.planning/phases/04-distribution-worked-examples/04-05-SUMMARY.md` when done. Record the
per-column before and after sentence-count measurements, the exact wording removed for each narration
span, and both `verification: backstop` residuals verbatim so the phase's verification step can file
them in `.planning/WINDOWS.md`.
</output>
