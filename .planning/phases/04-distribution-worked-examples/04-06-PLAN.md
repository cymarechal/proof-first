---
phase: 04-distribution-worked-examples
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - skills/proof-first/references/worked-examples.md
autonomous: true
requirements: [EX-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every sentence inside a ✓ column of `skills/proof-first/references/worked-examples.md` runs at or under PF-4.1's stated 25-word ceiling, counted as whitespace-delimited words with bracketed marker spans removed — measured 8 of 31 sentences over the ceiling before this plan, maximum 37 words."
    - "The repository's other example file now clears the same bar `04-05` applied to `examples/before-after.md`, so the sentence-length code `04-07` registers can scan both files and stay silent on the control copy rather than being scoped narrowly to hide a known breach."
    - "Every figure, every rule citation, every retained-term marker and every integrity marker in the file survives the split unchanged: `unlisted-figure` and `check_undefined_id` both scan `skills/**/*.md` and would fire on a figure or token introduced or mangled by a rewrite."
    - "Each of the 28 ✗/✓ pairs still demonstrates the rule its own `## PF-` or `## MC-` heading names, and the ✗ columns are untouched — the contrast each pair exists to show is preserved exactly."
    - "`python3 tools/check_repo.py --mutation-test` still reports 41 codes discrimination-proven, unchanged by this plan, with the CONTROL line reading 0 unexpected and no FIRE-ONLY line. This plan repairs prose and registers no violation code."
    - statement: "Each split ✓ column still reads as a single coherent rewrite rather than as two stapled fragments, and no split has moved a piece of evidence away from the claim it backs."
      verification: backstop
  prohibitions:
    - "MUST NOT edit any rule body in `skills/proof-first/SKILL.md`, `references/checklist.md`, `references/completeness-audit.md`, `references/artifact-patterns.md` or `references/deletion-test.md`. Those four reference files plus SKILL.md are the derivative sources; `worked-examples.md` is deliberately not one of them, which is why this plan can edit it without regenerating anything."
    - "MUST NOT edit any ✗ column. Each is the exhibit of non-compliant writing its pair exists to contrast against."
    - "MUST NOT add, remove, round, reformat or relocate any currency amount, percentage, ISO date or count; `unlisted-figure` scans `skills/**/*.md` and fires on the first token with no matching Canonical figures row."
    - "MUST NOT add or remove a `PF-` or `MC-` citation token, and MUST NOT change any `## PF-` or `## MC-` heading; `check_undefined_id`, `catalog-id-drift` and `mc-catalog-id-drift` all read tokens and headings from this tree."
    - "MUST NOT change the number of ✗/✓ pairs, which is 28, or the order in which they appear."
    - "MUST NOT split a sentence by moving a bracketed marker out of the position the claim would have occupied; a marker stays adjacent to the claim it replaces, per SKILL.md's Write-mode paragraph."
    - "MUST NOT introduce a word-spelled count that was not already present; this file keeps its existing spelled counts because `04-07`'s spelled-count code is deliberately scoped to `examples/before-after.md` only, and widening it here is out of this plan's scope."
    - "MUST NOT use an emoji anywhere in shipped content."
    - "MUST NOT name a real company, product vendor relationship or person beyond the nouns `examples/deal-brief.md` already licenses."
  artifacts:
    - path: "skills/proof-first/references/worked-examples.md"
      provides: "28 per-rule ✗/✓ pairs whose ✓ columns all obey PF-4.1's 25-word sentence ceiling"
      contains: "## MC-36"
      min_lines: 140
  key_links:
    - from: "skills/proof-first/references/worked-examples.md"
      to: "skills/proof-first/SKILL.md"
      via: "each pair's heading names the rule the ✓ column demonstrates, and PF-4.1 is the rule this repair applies to the file itself"
      pattern: "## PF-"
    - from: "skills/proof-first/references/worked-examples.md"
      to: "examples/deal-brief.md"
      via: "every figure in every pair traces to a Canonical figures row, enforced by unlisted-figure across skills/"
      pattern: "Halverton Mutual"
---

<objective>
Bring `skills/proof-first/references/worked-examples.md`'s ✓ columns under PF-4.1's 25-word sentence
ceiling, so the shipped file that teaches 28 rules one at a time stops breaking the one rule in the
catalog a machine can count.

Purpose: this is a finding this gap-closure planning session measured rather than one the UAT listed.
`04-UAT.md` found 10 of 11 ✓ sentences over the ceiling in `examples/before-after.md`; the same
measurement run against `worked-examples.md` this session found 8 of 31 over, maximum 37 words. That
file ships inside the skill folder a user installs. Leaving it breached while `04-07` adds a
sentence-length code carefully scoped around it would be exactly the move this repository exists to
argue against: narrowing the instrument until the claim survives. Repairing it first lets the code
scan both example files honestly.

Output: eight split sentences across eight pairs, every figure and marker intact, and a gate that
still reports 41 codes discrimination-proven. This plan adds no violation code. It runs in parallel
with `04-05`, which touches a disjoint set of files.
</objective>

<gap_coverage>
This plan closes no numbered gap from `04-UAT.md`. It closes a finding this gap-closure planning
session measured directly: `skills/proof-first/references/worked-examples.md` breaks PF-4.1 in 8 of 31
✓ sentences, maximum 37 words — the same defect class as G-04-2, in the other example file, which the
UAT did not measure.

It is planned rather than disclosed-and-deferred for one reason: `04-07`'s `example-sentence-length`
scans both example files, and a code scoped around a known breach would be the instrument-narrowing
move this repository exists to argue against. Owner of the finding: this plan. Owner of the
enforcement: `04-07`.
</gap_coverage>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-distribution-worked-examples/04-UAT.md
@.planning/phases/04-distribution-worked-examples/04-PATTERNS.md
@skills/proof-first/references/worked-examples.md
@skills/proof-first/SKILL.md
@examples/deal-brief.md
</context>

<interfaces>
<!-- Every line below was read from the named file during this planning session (2026-09-17). -->

**Baseline this plan starts from.** All four gate commands pass:
`python3 tools/check_repo.py` prints `check_repo: 0 violations`; `--self-test` passes;
`--mutation-test` prints `mutation-test PASS: 41 codes discrimination-proven` with a CONTROL line
reading `0 unexpected`; `python3 tools/generate_derivatives.py --check` exits 0.

**The rule this repair applies**, quoted from `skills/proof-first/SKILL.md` line 213 (PF-4.1): "No
sentence runs longer than 25 words, counted as words delimited by whitespace — one stated definition
of length, not an implied one." Replace-with: "split at 25 words into two sentences, one claim per
sentence, evidence adjacent to its claim."

**Measured this session across the file's 28 ✓ columns: 31 sentences, 8 over the ceiling, maximum 37
words.** Counting convention: bracketed marker spans removed first, then split on `.`/`?`/`!` followed
by whitespace, then whitespace-delimited word count. The eight, by line number and by the rule heading
the pair sits under:

| Line | Heading | Words | Opening of the sentence |
|---|---|---|---|
| 57 | `## PF-1.25` | 30 | "For Diane Osoria, Halverton Mutual's CFO: this migration is measured on the run rate ..." |
| 92 | `## PF-2.17` | 31 | "Kestrel Systems Group holds a SOC 2 Type I report today; Halverton Mutual's RFP requires Type II ..." |
| 117 | `## MC-6` | 28 | "Diane Osoria, Halverton Mutual's Chief Financial Officer, states her own priority directly ..." |
| 122 | `## MC-11` | 29 | "Halverton Mutual scores bidders 55% on technical approach, 25% on commercial model ..." |
| 127 | `## MC-16` | 28 | "Three bidders are shortlisted; proposals are due 2026-10-30, and every bidder is scored ..." |
| 132 | `## MC-21` | 30 | "The proposal clears a security review (15 business days), a procurement review ..." |
| 142 | `## MC-31` | 28 | "Marcus Feld, Halverton Mutual's Vice President of Infrastructure, said in discovery ..." |
| 147 | `## MC-36` | 37 | "Halverton Mutual is weighing three bidders, including Ardent Digital, the incumbent managing 62% ..." |

Lines 92 and 132 end in a bracketed marker. With the marker span removed the residue is a
sentence-final em dash, which is why the measured counts are 31 and 30 rather than higher; the marker
itself is not counted and must not be moved.

**This file is not a derivative source.** `DERIVATIVE_SOURCE_NAMES`, held identically in
`tools/check_repo.py` and `tools/generate_derivatives.py`, lists `skills/proof-first/SKILL.md`,
`references/deletion-test.md`, `references/completeness-audit.md`, `references/artifact-patterns.md`
and `references/checklist.md`. `worked-examples.md` is absent from both copies. Editing it does not
stale the derivative hash stamp and requires no regeneration — confirmed by reading both tuples.

**Checks that read this file and re-run against the repair.**
- `unlisted-figure` scans `examples/` and `skills/` for currency amounts, percentages and ISO dates
  and fires on any with no matching Canonical figures row. The figures in the eight sentences above —
  $2,300,000, 55%, 25%, 20%, 15%, 30%, 62%, 2026-10-30, 15, 10, 8 — all have rows today.
- `check_undefined_id` scans `skills/` for `PF-#.#` and `MC-#` tokens against the Allocated IDs table.
- `skill-too-long` and `skill-token-budget-exceeded` read `skills/*/SKILL.md` only, so growing this
  reference file by a handful of words touches neither ceiling.

**Nothing in `tools/check_repo.py` reads this file by name.** Confirmed by grepping the module for
`worked-examples`: zero hits. Its only coverage today is the two path-glob checks named above. That is
precisely the hole `04-07` closes.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: Split the two PF-section sentences over the ceiling and prove the whole-file measurement moves</name>
  <precondition>All four gate commands pass and `python3 tools/check_repo.py --mutation-test` reports 41 codes discrimination-proven with 0 unexpected CONTROL violations. This plan's verify steps compare against that baseline.</precondition>
  <files>skills/proof-first/references/worked-examples.md</files>
  <read_first>
- `skills/proof-first/references/worked-examples.md` in full — all 28 pairs, so the two edits match the file's own ✗/✓ quoting convention and heading rhythm rather than inventing a second style.
- `skills/proof-first/SKILL.md` lines 113-118 (PF-1.25) and 183-190 (PF-2.17) — the two rules whose pairs this task edits, so each split ✓ column still demonstrates its own rule.
- `skills/proof-first/SKILL.md` lines 213-218 (PF-4.1) — the rule being applied, including its stated counting definition.
- `skills/proof-first/SKILL.md` lines 265-271 — the Write-mode paragraph on a marker taking the exact place a claim would have occupied, which governs where the PF-2.17 marker may sit after the split.
- `examples/deal-brief.md` `## Inconvenient facts`, `## Timeline` and `### Economic buyer stated priorities` — the source facts behind both sentences, so nothing new enters and nothing true is lost.
- This plan's `<interfaces>` block, especially the eight-row measurement table and the counting convention.
  </read_first>
  <action>
Split two ✓ columns, and change nothing else in the file.

At line 57, under `## PF-1.25`, the ✓ column is one 30-word sentence carrying two claims: that the
migration is measured on the run rate Diane Osoria needs brought down from $2,300,000, and that it is
measured on a clean regulatory examination rather than on architecture elegance. Split it at the
boundary between those two claims so each is its own sentence of at most 25 words, with its evidence
adjacent. The outcome-to-stated-priority tie PF-1.25 asks for must survive in the first sentence:
Diane Osoria named, her role named, and her own stated figure attached.

At line 92, under `## PF-2.17`, the ✓ column is a 31-word sentence carrying three facts — the SOC 2
Type I report held, the Type II the RFP requires, and the observation window closing after the
2026-10-30 submission date — followed by a bracketed compliance marker. Split it so the held
certification and the required one are one sentence and the window-versus-date fact is another, each
at most 25 words. The bracketed marker stays adjacent to the claim it replaces and keeps its exact
shipped text inside one pair of square brackets on one line; do not relocate it to the end of the
column or split its text across two bracket pairs.

Preserve every figure exactly as written, including the `$2,300,000` amount and the `2026-10-30` date.
Do not reword either fact, do not soften the compliance gap, and do not add a transition phrase that
narrates the split. Both ✗ columns stay byte-identical.

This is the tracer: it proves the whole repair path — edit a ✓ column, keep every figure and marker
intact, re-run the full gate, and watch the whole-file over-ceiling count fall — before the remaining
six sentences follow the same path in Task 2.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 -c "import re,pathlib;C=[l for l in pathlib.Path('skills/proof-first/references/worked-examples.md').read_text().splitlines() if l[:1]==chr(10003)];w=[];[w.extend(len(x.split()) for x in re.split(r'(?<=[.!?])\s+',re.sub(r'\[[^\]]*\]','',l[1:].strip().strip(chr(34)))) if x.strip()) for l in C];print(len(C),max(w),sum(1 for v in w if v>25))"</automated>
    <fails_when>non-zero exit, or the first printed integer is not 28, or the third printed integer is above 6 (the measured baseline is 8; this task removes two)</fails_when>
    <automated>python3 -c "import pathlib;t=pathlib.Path('skills/proof-first/references/worked-examples.md').read_text();print(t.count('$2,300,000'),t.count('2026-10-30'),t.count('[PF-2.17'))"</automated>
    <fails_when>non-zero exit, or any printed integer is lower than its pre-task value recorded before the edit</fails_when>
    <automated>python3 tools/generate_derivatives.py --check</automated>
    <fails_when>non-zero exit, or any printed line names a derivative as differing from freshly rendered output or as missing, which would mean this file was wrongly treated as a derivative source</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py` prints exactly `check_repo: 0 violations` and exits 0.
- `python3 tools/generate_derivatives.py --check` exits 0 with no differing-file line, confirming this file is outside the derivative source set and no regeneration was needed.
- The whole-file measurement prints 28 ✓ columns and at most 6 sentences over 25 words, down from the measured baseline of 8.
- The ✓ column under `## PF-1.25` splits into at least 2 sentences, each at most 25 words, and still names Diane Osoria, her role, and the `$2,300,000` figure.
- The ✓ column under `## PF-2.17` splits into at least 2 sentences, each at most 25 words, retains the `2026-10-30` date, and carries exactly one `[PF-2.17` marker inside a single pair of square brackets on one line.
- `git diff -- skills/proof-first/references/worked-examples.md` shows changes on ✓ lines only, and on exactly 2 of them.
  </acceptance_criteria>
  <done>Both PF-section ✓ columns are at or under 25 words per sentence with every figure and marker intact, and the whole-file over-ceiling count has fallen from 8 to at most 6.</done>
</task>

<task type="auto">
  <name>Task 2: Split the remaining six MC-section sentences and drive the whole-file over-ceiling count to zero</name>
  <files>skills/proof-first/references/worked-examples.md</files>
  <read_first>
- `skills/proof-first/references/worked-examples.md` as Task 1 leaves it — specifically the pairs under `## MC-6`, `## MC-11`, `## MC-16`, `## MC-21`, `## MC-31` and `## MC-36`.
- `skills/proof-first/references/completeness-audit.md` in full — the six MC entries these pairs demonstrate, so each split column still satisfies the audit item its heading names.
- `examples/deal-brief.md` `### Decision criteria`, `### Scored RFP questions`, `## Timeline`, `## Paper process`, `### Discovery call quotes` and `## Parties` — the source facts behind all six sentences.
- `skills/proof-first/SKILL.md` lines 213-218 (PF-4.1) and 265-271 (marker placement), again, before editing the two columns that carry markers.
- This plan's `<interfaces>` block, especially the six remaining rows of the measurement table and the note that lines 92 and 132 end in a bracketed marker.
  </read_first>
  <action>
Split six ✓ columns. Change nothing else.

Under `## MC-6` at line 117, the 28-word sentence names Diane Osoria with her full role and then quotes
her own stated priority. Split the attribution from the quotation so each is at most 25 words, keeping
the quotation verbatim and keeping the `$2,300,000` figure inside it exactly as the brief states it.

Under `## MC-11` at line 122, the 29-word sentence carries two distinct scoring facts: the three
top-level rubric weights (55%, 25%, 20%) and the separate per-question weighting range (15% to 30%).
Split at that boundary. All five percentages stay exactly as written; every one has a Canonical
figures row and `unlisted-figure` will fire on any that is altered.

Under `## MC-16` at line 127, the 28-word sentence carries the shortlist, the 2026-10-30 submission
date, the common five-question scoring and the 8-month examination window. Split it into two sentences
at a clause boundary that keeps each date or count adjacent to the claim it evidences.

Under `## MC-21` at line 132, the 30-word sentence enumerates three review steps with their durations
and ends in a bracketed marker. Split the enumeration so no sentence exceeds 25 words, keep the
15-business-day security review and the 10-business-day legal review durations exactly, keep the
plainly-stated absence of a procurement duration rather than inventing one, and keep the marker
adjacent to the claim it replaces inside one pair of square brackets on one line.

Under `## MC-31` at line 142, the 28-word sentence names Marcus Feld with his full role and then
carries his verbatim discovery quotation. Split the attribution from the quotation. The quotation's
wording is supplied customer source material and must stay character-for-character as the brief states
it, including the em dash and the snowflake clause.

Under `## MC-36` at line 147, the 37-word sentence — the file's longest — carries the shortlist, the
incumbent's 62% share, a third bidder, and Priya Raghunathan's stated preference for extending the
incumbent's contract. Split it into three sentences at most, none over 25 words. The 62% figure and
Priya Raghunathan's stated position both stay; that position is an inconvenient fact the brief carries
on purpose and softening it would remove the reason this pair cites MC-36.

Across all six: change no figure, add no figure, move no marker away from its claim, add no transition
phrase that narrates the split, and leave every ✗ column byte-identical. This task ends with the
whole-file over-ceiling count at zero, which is the precondition `04-07`'s sentence-length code needs
to be silent on the control copy.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py --self-test &amp;&amp; python3 tools/check_repo.py --mutation-test &amp;&amp; python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit at any stage, or the mutation summary line reports a number other than 41 codes discrimination-proven, or any FIRE-ONLY line appears, or the CONTROL line reports more than 0 unexpected</fails_when>
    <automated>python3 tools/generate_derivatives.py --check</automated>
    <fails_when>non-zero exit, or any printed line names a derivative as differing from freshly rendered output or as missing</fails_when>
    <automated>python3 -c "import re,pathlib;C=[l for l in pathlib.Path('skills/proof-first/references/worked-examples.md').read_text().splitlines() if l[:1]==chr(10003)];w=[];[w.extend(len(x.split()) for x in re.split(r'(?<=[.!?])\s+',re.sub(r'\[[^\]]*\]','',l[1:].strip().strip(chr(34)))) if x.strip()) for l in C];print(len(C),len(w),max(w),sum(1 for v in w if v>25))"</automated>
    <fails_when>non-zero exit, or the first printed integer is not 28, or the third printed integer is above 25, or the fourth printed integer is anything other than 0</fails_when>
    <automated>python3 -c "import re,pathlib;t=pathlib.Path('skills/proof-first/references/worked-examples.md').read_text();print(len([l for l in t.splitlines() if l[:1]==chr(10007)]),len(re.findall(r'PF-\d+\.\d+|MC-\d+',t)))"</automated>
    <fails_when>non-zero exit, or the first printed integer is not 28, or the second printed integer is lower than its pre-plan value recorded before Task 1</fails_when>
  </verify>
  <acceptance_criteria>
- All four gate commands pass: `--self-test` exits 0, `--mutation-test` reports exactly `41 codes discrimination-proven` with `0 unexpected` on CONTROL and no FIRE-ONLY line, `check_repo.py` prints `check_repo: 0 violations`, and `generate_derivatives.py --check` exits 0.
- The whole-file measurement prints 28 ✓ columns, a maximum sentence length of at most 25 words, and exactly 0 sentences over 25 — down from 8 over and a maximum of 37.
- The file still carries exactly 28 ✗ lines and 28 ✓ lines, and its total count of `PF-`/`MC-` tokens is not lower than before the plan began.
- `grep -c '2026-10-30' skills/proof-first/references/worked-examples.md` and `grep -c '62%' skills/proof-first/references/worked-examples.md` each print the same value they printed before this plan began.
- `git diff -- skills/proof-first/references/worked-examples.md` shows changes on ✓ lines only, on exactly 8 of them across both tasks.
  </acceptance_criteria>
  <done>All 28 ✓ columns obey PF-4.1, every figure and marker is intact, the ✗ columns are untouched, and all four gate commands pass at 41 codes.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates no new symbol. It changes content only:

- `skills/proof-first/references/worked-examples.md` — 8 rewritten ✓ columns, under the headings
  `## PF-1.25`, `## PF-2.17`, `## MC-6`, `## MC-11`, `## MC-16`, `## MC-21`, `## MC-31` and
  `## MC-36`. No heading, no ✗ column, no citation token and no pair ordering changes.

New violation codes: none. New functions: none. New fixture roots: none. New MUTATIONS entries: none.
Expected discrimination-proven count after this plan: **41**, unchanged.

What this plan unlocks: `04-07`'s `example-sentence-length` and `example-rule-narration` codes scan
both `examples/before-after.md` and this file. Without this repair, either the codes would fire on the
mutation harness's control copy (reported FIRE-ONLY and never discrimination-proven) or their scope
would have to be narrowed around a known breach. Neither is acceptable in this repository.
</artifacts_this_phase_produces>

<threat_model>
Security enforcement is active with no `workflow.security_asvs_level` and no `workflow.security_block_on`
configured, so this block uses the defaults: **ASVS Level 1**, blocking threshold **high**.

This is a documentation-and-stdlib-scripts repository with no network surface, no authentication, no
session state and no user input beyond files a contributor commits. This plan edits one Markdown file
and adds no code. A thin register is the correct outcome; the reasoning is stated rather than the block
omitted.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor commit → shipped skill content | This file ships inside `skills/proof-first/`, the folder a user installs. Untrusted prose crosses directly into an installed user's context window. |
| shipped reference file → installed agent session | The file is read as instruction content by an agent. It carries no executable content and no tool invocation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06-01 | Tampering | ✓ columns of `worked-examples.md` | medium | mitigate | The repair is asserted by whole-file measurement in both tasks' verify blocks, not by reading. `04-07` then registers `example-sentence-length` over this exact file so a later regression fails CI. |
| T-04-06-02 | Tampering | figures and rule tokens inside the split sentences | high | mitigate | `unlisted-figure` scans `skills/**/*.md` against the Canonical figures table and `check_undefined_id` scans the same tree for unallocated `PF-`/`MC-` tokens; both run in every verify step. Task 2 additionally asserts the token count has not fallen and two specific figure counts are unchanged. |
| T-04-06-03 | Information disclosure | a split introducing a real person, company or unlicensed product noun | high | mitigate | The prohibition list forbids it; every fact must trace to `examples/deal-brief.md`, which is wholly invented and states so in its first paragraph. Splitting a sentence adds no fact by construction, and the verify steps assert no figure count moved. |
| T-04-06-04 | Tampering | derivative artifacts going stale unnoticed | medium | mitigate | Both tasks run `python3 tools/generate_derivatives.py --check`. That command exiting 0 is the mechanical proof that this file is genuinely outside `DERIVATIVE_SOURCE_NAMES` and that no hand-regeneration was silently required. |
| T-04-06-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan, and the repository's zero-dependency constraint forbids adding one: no package manifest or lockfile exists anywhere in the tree and both tools import only the standard library. A Package Legitimacy Audit is therefore not required. Accepted because the absence of any install surface is itself checkable by inspection of the two tools' import blocks. |
</threat_model>

<verification>
After both tasks, from the repository root:

1. `python3 tools/check_repo.py --self-test` — exits 0.
2. `python3 tools/check_repo.py --mutation-test` — prints `mutation-test PASS: 41 codes
   discrimination-proven`, a CONTROL line reading `0 unexpected`, and no FIRE-ONLY line.
3. `python3 tools/check_repo.py` — prints `check_repo: 0 violations`.
4. `python3 tools/generate_derivatives.py --check` — exits 0.
5. The whole-file probe prints `28 <n> <max<=25> 0`.
6. `git diff --stat -- skills/proof-first/references/worked-examples.md` reports 8 changed ✓ lines and
   no changed ✗ line.
</verification>

<success_criteria>
- All 28 ✓ columns have every sentence at or under 25 words; the over-ceiling count is 0, down from 8.
- All 28 ✗ columns are byte-identical to their pre-plan content.
- Every figure, every `PF-`/`MC-` token and every bracketed marker survives unchanged.
- The gate still reports 41 codes discrimination-proven, 0 violations, 0 unexpected CONTROL.
- `generate_derivatives.py --check` exits 0 with no regeneration performed.
</success_criteria>

<output>
Create `.planning/phases/04-distribution-worked-examples/04-06-SUMMARY.md` when done. Record the
before and after whole-file measurements (sentence count, maximum, over-ceiling count), the eight
headings edited, and the `verification: backstop` residual verbatim so the phase's verification step
can file it in `.planning/WINDOWS.md`.
</output>
