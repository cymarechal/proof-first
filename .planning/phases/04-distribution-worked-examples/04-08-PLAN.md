---
phase: 04-distribution-worked-examples
plan: 08
type: execute
wave: 3
depends_on: ["04-07"]
gap_closure: true
files_modified:
  - README.md
autonomous: true
requirements: [DIST-06]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A reader meets the first ✗ line of a before/after pair inside the first screen: measured at line 23 before this plan, behind twelve lines of project description, and at line 20 or lower after it."
    - "Every install route README prints is identified as runnable today or as blocked on publication. Routes 3 and 4 work from a local clone and README says so; routes 1 and 2 name the disclosed `<owner>/<repo>` placeholder and README says plainly that they cannot resolve until this repository is published."
    - "No maintainer instruction sits between the Install section and the Status section. `## Keeping derivatives in sync` moves below `## Status`, out of a first-time reader's install path, with its content and its named command unchanged."
    - "README's repository-layout legend describes only markers the tree actually uses. The `planned` marker it explained appeared zero times while nineteen entries carried an `(exists)` marker and the rest — including `SKILL.md`, the product itself — carried nothing, implying it did not exist."
    - "README's caveat sentence names exactly the model identifiers `evals/conformance/RESULTS-mod04.md` records, rather than asserting a count of measured models it does not name."
    - "README makes no claim about its own rule catalog that the catalog does not support. The lead-in no longer asserts that every rule exists to turn the left paragraph into the right one — false for the review-marker rules and the check-mode rules — in a repository that bans unmeasured claims."
    - "`python3 tools/check_repo.py --mutation-test` still reports 44 codes discrimination-proven, unchanged by this plan, with the CONTROL line reading 0 unexpected and no FIRE-ONLY line. This plan repairs prose and registers no violation code."
    - statement: "A prospective evaluator opening this README understands what the skill does and how to install it within the first screen or two, and the Install section is clear enough to act on without opening another file."
      verification: backstop
  prohibitions:
    - "MUST NOT delete, paraphrase away, reformat or move into a fenced block the literal string `evals/conformance/RESULTS-mod04.md`. `readme-results-pointer-missing` greps README for it, and its registered mutation deletes every line containing it. It appears exactly twice today and must still appear exactly twice."
    - "MUST NOT add, restate, round, re-caveat or relocate any performance figure. The MOD-04 conformance figures, the 30.0% and 40.0% values, the 10-point decline and the 2026-09-16 measurement date all stay exactly as written."
    - "MUST NOT assert sameness of outcome, of result or of model behaviour between the output style, the system prompt and the installed skill. No benchmark has run; Phase 5 owns that."
    - "MUST NOT resolve, guess or substitute the `<owner>/<repo>` publish-location placeholder. It is a deliberate, disclosed placeholder held identical across both manifests and README by `publish-location-drift`, recorded as WINDOWS.md entry 11 and routed to Phase 6's LEG-04 gate."
    - "MUST NOT change any ✗ line, ✓ line or `Rules applied:` footer in README. `04-05` set those to match `examples/before-after.md` verbatim and `04-09` registers the code that enforces it."
    - "MUST NOT add a second copy of the attribution pointer line; README is a required carrier and `pointer-duplicated` fires on a second occurrence."
    - "MUST NOT move `## Before and after` below `## Install` or `## Status`; `readme-before-after-order` fires on that."
    - "MUST NOT remove any of the four install anchors — the skills-CLI command prefix, the marketplace add command prefix, the output-style path and the system-prompt path; `readme-install-path-missing` fires once per missing anchor."
    - "MUST NOT cite a PF- or MC- token in unfenced prose that `NUMBERING.md`'s Allocated IDs table does not define."
    - "MUST NOT use an emoji anywhere in shipped content."
  artifacts:
    - path: "README.md"
      provides: "A reader-facing entry point that leads with a worked example inside the first screen, states which install routes are runnable today, keeps maintainer instruction out of the install path, and makes no claim its own catalog or its own measurements do not support"
      contains: "## Before and after"
      min_lines: 190
  key_links:
    - from: "README.md"
      to: "examples/before-after.md"
      via: "the lead-in reproduces one pair verbatim and links to the full file for the other three families"
      pattern: "examples/before-after\\.md"
    - from: "README.md"
      to: "evals/conformance/RESULTS-mod04.md"
      via: "the measured-figure disclosure points at the one committed measurement, guarded by readme-results-pointer-missing"
      pattern: "evals/conformance/RESULTS-mod04\\.md"
    - from: "README.md"
      to: ".claude-plugin/plugin.json"
      via: "the install commands restate the same publish location both manifests carry, held identical by publish-location-drift"
      pattern: "claude plugin marketplace add "
---

<objective>
Repair the six concrete defects `04-UAT.md` test 4 found in README, so the file that decides whether a
prospective evaluator installs this skill stops undercutting the honesty posture that is its strongest
asset.

Purpose: test 4 returned PASS-WITH-ISSUES, not FAIL. The macro-order is right and the 30% conformance
disclosure and the explicit refusal to claim behavioural equivalence should both survive verbatim —
they are the best writing in the repository. But no install command as printed is identified as
runnable; the layout legend explains a marker that appears zero times while half the tree carries a
marker and the other half, including the product itself, carries none; maintainer instruction sits
inside a first-time reader's install path; one sentence claims two measured models and names one; and
the lead-in makes an unmeasured claim about the repository's own rules, in a repository that bans them.
Twelve lines of project description stand between the reader and the first example.

Output: a restructured README whose first example is inside the first screen, whose install routes each
state their own runnability, whose layout legend describes only markers the tree uses, and whose two
false or over-broad sentences are narrowed to what the evidence supports. No figure moves; no new
claim is added. `04-09` then mechanises the two defect classes that have a signature.
</objective>

<gap_coverage>
This plan closes G-04-6 from `04-UAT.md` — all six README defects test 4 recorded — in prose.

| Defect | Where |
|---|---|
| (a) no install command identified as runnable | Task 1 |
| (c) maintainer instruction inside the install path | Task 1 |
| (f) twelve lines of project description before the first ✗ | Task 1 |
| (e) unmeasured claim about the repository's own rules | Task 2 |
| (d) a caveat counting two measured models and naming one | Task 2 |
| (b) a layout legend explaining a marker the tree never uses | Task 2 |

`04-09` then mechanises defects (b) and (f), plus the cross-file identity behind the reproduced pair
that `04-05` had to repair by hand in two places. G-04-1 through G-04-5 are `04-05` and `04-07`'s;
G-04-7 is `04-10`'s.
</gap_coverage>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-distribution-worked-examples/04-UAT.md
@.planning/phases/04-distribution-worked-examples/04-04-PLAN.md
@.planning/phases/04-distribution-worked-examples/04-04-SUMMARY.md
@README.md
@examples/before-after.md
@evals/conformance/RESULTS-mod04.md
@tools/check_repo.py
</context>

<interfaces>
<!-- Every line and line number below was read from the named file during this planning session (2026-09-17). -->

**Baseline this plan starts from, after `04-05`, `04-06` and `04-07` land.** `python3
tools/check_repo.py` prints `check_repo: 0 violations`; `--self-test` passes; `--mutation-test` prints
`mutation-test PASS: 44 codes discrimination-proven` with `0 unexpected` on CONTROL and no FIRE-ONLY
line; `python3 tools/generate_derivatives.py --check` exits 0. If the total is not 44, stop and say so.

**README's section order today, 219 lines, read in full this session.**

| Line | Heading | Note |
|---|---|---|
| 1 | `# Proof First` | title; line 3 is a one-line description |
| 5 | `## What this is` | six lines of project description |
| 14 | `## Before and after` | lead-in at 16-19, the pair at 21-26, the link line at 28-30 |
| 32 | `## Install` | four routes at 38-64, the honesty paragraph at 66-69 |
| 71 | `## Keeping derivatives in sync` | maintainer instruction, sitting between Install and Status |
| 84 | `## Status` | the measured-figure disclosure, running to line 143 |
| 145 | `## Repository layout` | legend at 147-148, fenced tree at 150-189, prose at 191-195 |
| 197 | `## Rule numbering` | |
| 204 | `## Versioning` | |
| 209 | `## License and notices` | ends with the fenced attribution pointer at 217-219 |

**The six defects, by line, as `04-UAT.md` test 4 states them.**
- (a) Lines 38-64: no install command as printed is identified as runnable. Routes 1 and 2 name the
  `<owner>/<repo>` placeholder and cannot resolve; routes 3 and 4 work from a local clone today but
  README never says so. One sentence fixes it.
- (b) Lines 147-148: the legend explains a `planned` marker. Measured this session: `planned` appears
  once in the whole file, in that legend sentence, and zero times inside the fenced tree. Nineteen tree
  entries carry `(exists)`; the rest, including `skills/proof-first/SKILL.md`, carry nothing.
- (c) Line 71: `## Keeping derivatives in sync` is maintainer instruction sitting between Install and
  Status, inside a reader's install path.
- (d) Line 133: "both measured models are Anthropic-hosted", while only `claude-sonnet-5` is named in
  the figures above it. `evals/conformance/RESULTS-mod04.md` records sessions for two model
  identifiers, `claude-sonnet-5` and `claude-opus-5`; the second is named in that file's superseded
  block, not in README.
- (e) Lines 16-17: "Every rule in this skill exists to turn a paragraph like the one on the left into
  the one on the right." False for PF-5.1 through PF-5.3 and for PF-2.14 through PF-2.17 — an
  unmeasured claim about the repository's own rules, in a repository that bans them.
- (f) Twelve lines of project description precede the first ✗, which sits at line 23.

**Measured counts this session, to be preserved or changed deliberately.**
- `grep -cF 'evals/conformance/RESULTS-mod04.md' README.md` prints `2`. This matches WINDOWS.md entry
  13, which records that `04-04-PLAN.md` expected 3 and that 2 was always the true count. Keep it at 2;
  do not fabricate a third occurrence.
- `grep -c '(exists)' README.md` prints `19`.
- `grep -c 'planned' README.md` prints `1`, in the legend sentence at line 147.
- The first `✗` line is at line 23.

**Checks that read README and re-run against the repair**, from `tools/check_repo.py`:
- `readme-before-after-order` (line 2452): `## Before and after`, `## Install` and `## Status` must all
  be present as exact heading lines, and the before/after heading's line index must be lower than both
  others'. Moving Before-and-after earlier keeps this satisfied; moving it later fails.
- `readme-install-path-missing` (line 2417): four anchors must appear somewhere in the file — the
  `npx skills add ` prefix, the `claude plugin marketplace add ` prefix, the literal
  `output-styles/proof-first.md` and the literal `prompts/system-prompt.md`. Its declared ceiling says
  plainly it does not assert the routes appear under the Install heading, only that each anchor is
  present. It reads raw text and never strips fences.
- `readme-results-pointer-missing` — the results path literal must be present.
- `pointer-missing` and `pointer-duplicated` — the attribution pointer line must appear exactly once.
- `publish-location-drift` (line 2334) — the argument following each of the two install command
  prefixes must agree with both manifests' owner segment. Read the value out of
  `.claude-plugin/plugin.json` rather than retyping it.
- `check_undefined_id` — every `PF-`/`MC-` token in README's unfenced prose must be an allocated ID.

**The publish-location placeholder is deliberate.** WINDOWS.md entry 11 records it as a disclosed,
non-resolving placeholder pending the real GitHub owner and repository, with no git remote configured,
routed to Phase 6's LEG-04 launch gate. This plan does not resolve it and does not guess it; it only
makes README state plainly what that means for a reader trying routes 1 and 2 today.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: Restructure — lead with the example, state each route's runnability, move maintainer instruction out of the install path</name>
  <reversibility rating="reversible">Section order in a README is reversible at any time and binds no published identifier. The one adjacent irreversible element, the publish location, is untouched: this task only describes what the existing placeholder means for a reader today.</reversibility>
  <precondition>`04-07` has landed and `python3 tools/check_repo.py --mutation-test` reports 44 codes discrimination-proven with 0 unexpected CONTROL violations. README's ✗/✓ lines already match `examples/before-after.md` verbatim from `04-05`; if they do not, stop, because this task moves those lines and a pre-existing mismatch would be carried forward invisibly.</precondition>
  <files>README.md</files>
  <read_first>
- `README.md` in full, all 219 lines — every section, both occurrences of the results-pointer literal, the fenced attribution pointer at the end, and the exact text of the four install routes.
- `tools/check_repo.py` lines 2405-2500 — `README_INSTALL_ANCHORS`, `check_readme_install_paths` and `check_readme_before_after_order`, including both declared ceilings, so the reorder is checked against what the code actually asserts rather than what it appears to assert.
- `tools/check_repo.py` lines 2260-2330 — `_owner_segment`, `SKILLS_CLI_INSTALL_RE`, `MARKETPLACE_ADD_RE` and `_publish_locations_in`, so the two command arguments stay byte-identical to the manifests' owner segment.
- `.claude-plugin/plugin.json` — the `repository` value, the publish-location string README restates.
- `.planning/WINDOWS.md` entry 11 — the disclosed placeholder and its Phase 6 closure route, which this task describes rather than resolves.
- `examples/before-after.md` as `04-05` leaves it — the repaired RFP pair whose three lines README reproduces and which this task relocates unchanged.
- This plan's `<interfaces>` block in full, especially the section-order table and the list of checks that re-run.
  </read_first>
  <action>
Restructure README. Move sections whole; do not rewrite their bodies except where this task names a
specific sentence.

Move `## Before and after` so it is the first `## ` heading in the file, immediately after the title
and the one-line description. `## What this is` follows it. This is defect (f): the pair now arrives
inside the first screen instead of behind twelve lines of project description. Carry the section's
body with it unchanged — the lead-in paragraph, the family label, the ✗ line, the ✓ line, the
`Rules applied:` footer and the link line. Change no character of any of those three reproduced lines;
`04-05` set them to match `examples/before-after.md` verbatim and `04-09` registers the code that
enforces it. Trim the lead-in only by shortening it if needed to keep the first ✗ at line 20 or lower;
the sentence-level accuracy fix to that lead-in is Task 2's job, not this one's.

Move `## Keeping derivatives in sync` so it sits after `## Status` rather than between `## Install` and
`## Status`. This is defect (c). Carry its body unchanged, including the named command
`python3 tools/generate_derivatives.py` and the sentence stating that CI runs the generator's own
`--check` mode and the checker's staleness code. Do not retitle it and do not fold it into another
section; DIST-05 requires a documented re-sync step and this is it.

Add the runnability statement to `## Install`. This is defect (a). One short paragraph, placed
immediately after the existing publish-location sentence at the top of the section, stating three
facts and no more. Routes 3 and 4 work today from a local clone of this repository, because
`output-styles/proof-first.md` and `prompts/system-prompt.md` are committed files a reader already has
after cloning. Routes 1 and 2 name a publish-location placeholder and will not resolve until this
repository is published. The placeholder is deliberate and disclosed, not an oversight, and every
command and manifest that states it is held identical by a check in this repository. Do not name a
date, do not promise a publication, and do not substitute a guessed owner or repository name. Keep the
existing sentence that introduces the placeholder rather than duplicating its content.

Everything else keeps its current order: `## Status`, then `## Keeping derivatives in sync`, then
`## Repository layout`, `## Rule numbering`, `## Versioning`, `## License and notices`. Change no
figure. Change neither occurrence of the results-pointer literal. Leave the fenced attribution pointer
exactly where it is and exactly once.

This is the tracer: it proves the whole path — move a section, keep every guarded literal, keep every
install anchor, keep the publish location identical to both manifests, and re-run the full gate —
before Task 2 makes the four sentence-level accuracy fixes.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 -c "import pathlib;L=pathlib.Path('README.md').read_text().splitlines();H=[l[3:].strip() for l in L if l.startswith('## ')];print(H)"</automated>
    <fails_when>non-zero exit, or the first entry is not "Before and after", or "Keeping derivatives in sync" appears at an index lower than "Status", or any of the nine existing headings is absent from the list</fails_when>
    <automated>python3 -c "import pathlib;L=pathlib.Path('README.md').read_text().splitlines();print(next(i for i,l in enumerate(L,1) if l[:1]==chr(10007)))"</automated>
    <fails_when>non-zero exit (no ✗ line exists at all), or the printed line number is above 20</fails_when>
    <automated>python3 -c "import pathlib;t=pathlib.Path('README.md').read_text();print(t.count('evals/conformance/RESULTS-mod04.md'), sum(1 for l in t.splitlines() if l.strip().startswith('Concepts here are paraphrased')), [a for a in ('npx skills add ','claude plugin marketplace add ','output-styles/proof-first.md','prompts/system-prompt.md','tools/generate_derivatives.py') if a not in t])"</automated>
    <fails_when>non-zero exit, or the first printed integer is not 2, or the second printed integer is not 1, or the printed list is anything other than []</fails_when>
    <automated>python3 -c "import pathlib;B=set(pathlib.Path('examples/before-after.md').read_text().splitlines());R=[l for l in pathlib.Path('README.md').read_text().splitlines() if l[:1] in (chr(10007),chr(10003)) or l.startswith('Rules applied:')];print([l[:40] for l in R if l not in B])"</automated>
    <fails_when>non-zero exit, or the printed list is anything other than []</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py` prints exactly `check_repo: 0 violations` and exits 0, proving `readme-before-after-order`, `readme-install-path-missing`, `readme-results-pointer-missing`, `pointer-missing`, `pointer-duplicated`, `publish-location-drift` and `check_undefined_id` all still pass.
- The printed heading list begins with `Before and after`, and `Keeping derivatives in sync` appears after `Status`.
- The first `✗` line is at line 20 or lower, down from the measured baseline of 23.
- The results-pointer literal still appears exactly 2 times and the attribution pointer line exactly once.
- All four install anchors plus the generator path are still present: the printed missing-anchor list is `[]`.
- Every README ✗/✓/`Rules applied:` line still appears verbatim in `examples/before-after.md`.
- The Install section contains a statement distinguishing routes runnable from a local clone today from routes blocked on publication, and `grep -c '<owner>/<repo>' README.md` prints the same value it printed before this task.
  </acceptance_criteria>
  <done>The example leads the file inside the first screen, each install route states its own runnability, maintainer instruction sits below Status, and every guarded literal and install anchor survives.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow the two unsupported claims and repair the self-contradicting layout legend</name>
  <files>README.md</files>
  <read_first>
- `README.md` as Task 1 leaves it — in particular the relocated before/after lead-in, the caveat paragraph inside `## Status`, and the `## Repository layout` legend and fenced tree.
- `skills/proof-first/SKILL.md` in full, or at minimum every `### PF-` heading — so the narrowed lead-in claim is checked against what the catalog's rules actually do. PF-5.1 through PF-5.3 govern check mode and hedging; PF-2.14 through PF-2.17 emit review markers. Neither family turns a left paragraph into a right one.
- `evals/conformance/RESULTS-mod04.md` — every `model=` value the file records, so the caveat names exactly the identifiers that file carries and asserts no count it does not name. Both `claude-sonnet-5` and `claude-opus-5` appear; the second only inside the file's superseded block, which the caveat must not misrepresent as an anchored figure.
- `.planning/WINDOWS.md` entry 13 — the recorded fact that the results-pointer literal has always appeared twice, not three times, so no third occurrence is fabricated.
- `tools/check_repo.py` lines 970-1010 — `README_RESULTS_POINTER` and `check_readme_results_pointer`, the check guarding that literal.
- This plan's `<interfaces>` block, especially the measured counts for `(exists)`, `planned` and the results-pointer literal.
  </read_first>
  <action>
Make four sentence-level repairs. Change nothing structural; Task 1 owns the order.

Defect (e), the before/after lead-in. The opening sentence claims every rule in the skill exists to
turn a paragraph like the one on the left into the one on the right. That is false for the check-mode
rules and for the review-marker rules, neither of which rewrites a paragraph at all, and it is an
unmeasured claim about the repository's own catalog in a repository that bans unmeasured claims.
Narrow it to what is true and checkable: the rules this pair cites turned the left paragraph into the
right one, and each change is named alongside it. Do not replace one over-broad claim with another; do
not state a count of rules; do not characterise the catalog's effect.

Defect (d), the measured-models caveat inside `## Status`. The sentence asserts that both measured
models are Anthropic-hosted while the figures above it name one. Read `evals/conformance/RESULTS-mod04.md`
and name the model identifiers it actually records, stating which figures are anchored and which sit in
that file's superseded block, in the file's own terms. If naming the second identifier would imply an
anchored figure the file does not support, restate the caveat to cover only the model the anchored
figures name and drop the count. Either repair is acceptable; asserting a number of models without
naming them is not. Change no figure, no percentage, no date and no count of sessions.

Defect (b), the repository-layout legend and its markers. The legend sentence explains a `planned`
marker that appears zero times inside the tree, while nineteen entries carry an `(exists)` marker and
every other entry — including `skills/proof-first/SKILL.md`, the actual product — carries none,
implying it does not exist. Every path the tree names exists on disk today. Remove all nineteen
`(exists)` markers from the fenced tree, and replace the legend sentence with one stating plainly that
every path shown exists in this repository today. Do not introduce a third marker vocabulary. Do not
add or remove a path from the tree; if a path in the tree does not exist on disk, stop and report it
rather than deleting the line, because that would be a real finding and not a legend defect.

Then re-read the paragraph below the tree, which explains where the skill lives and which directories
never ship to an installed user. If any sentence there is now false because it referred to a marker
this task removed, repair that sentence too; otherwise leave it alone.

Change no figure anywhere in the file. Keep both occurrences of the results-pointer literal. Keep the
attribution pointer line exactly once.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py --self-test &amp;&amp; python3 tools/check_repo.py --mutation-test &amp;&amp; python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit at any stage, or the mutation summary line reports a number other than 44 codes discrimination-proven, or any FIRE-ONLY line appears, or the CONTROL line reports more than 0 unexpected</fails_when>
    <automated>python3 tools/generate_derivatives.py --check</automated>
    <fails_when>non-zero exit, or any printed line names a derivative as differing from freshly rendered output or as missing</fails_when>
    <automated>python3 -c "import pathlib;t=pathlib.Path('README.md').read_text();print(t.count('(exists)'), t.count('planned'))"</automated>
    <fails_when>non-zero exit, or either printed integer is anything other than 0</fails_when>
    <automated>python3 -c "import re,pathlib;t=pathlib.Path('README.md').read_text();f=re.findall(r'```\n(proof-first/.*?)```',t,re.S)[0];import os;names=[n for n in re.findall(r'([A-Za-z0-9_.-]+\.(?:md|py|json|yml))',f)];missing=[n for n in names if not any(p.name==n for p in pathlib.Path('.').rglob(n))];print(missing)"</automated>
    <fails_when>non-zero exit, or the printed list is anything other than [] — a filename shown in the layout tree that exists nowhere in the repository</fails_when>
    <automated>python3 -c "import pathlib;t=pathlib.Path('README.md').read_text();print(t.count('evals/conformance/RESULTS-mod04.md'), t.count('30.0%'), t.count('40.0%'), t.count('2026-09-16'))"</automated>
    <fails_when>non-zero exit, or the first printed integer is not 2, or any of the other three printed integers differs from the value it printed before this task</fails_when>
    <automated>python3 -c "import pathlib,re;t=pathlib.Path('README.md').read_text();print('both measured models' in t, len(re.findall(r'claude-(?:sonnet|opus)-5',t)))"</automated>
    <fails_when>non-zero exit, or the printed boolean is True, or the printed integer is 0</fails_when>
  </verify>
  <acceptance_criteria>
- All four gate commands pass: `--self-test` exits 0, `--mutation-test` reports exactly `44 codes discrimination-proven` with `0 unexpected` on CONTROL and no FIRE-ONLY line, `check_repo.py` prints `check_repo: 0 violations`, and `generate_derivatives.py --check` exits 0.
- `grep -c '(exists)' README.md` prints `0`, down from 19, and `grep -c 'planned' README.md` prints `0`, down from 1 — the legend and the tree now describe the same thing.
- Every filename shown in the layout tree resolves to a file that exists in the repository: the printed missing list is `[]`.
- The literal phrase asserting a count of measured models without naming them is absent, and at least one `claude-sonnet-5` or `claude-opus-5` identifier is present in the caveat.
- `grep -cF 'evals/conformance/RESULTS-mod04.md' README.md` prints `2`, and the counts of `30.0%`, `40.0%` and `2026-09-16` are unchanged from before this task.
- The before/after lead-in makes no claim about what every rule in the catalog does; `grep -c 'Every rule' README.md` prints `0`.
- `git diff -- README.md` for this task touches no ✗ line, no ✓ line and no `Rules applied:` line.
  </acceptance_criteria>
  <done>The lead-in claims only what the cited rules did, the caveat names the models it counts, the layout legend and the tree agree, and every figure and guarded literal is unchanged.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan creates no new symbol. It changes content only:

- `README.md` — `## Before and after` relocated to the first section; `## Keeping derivatives in sync`
  relocated below `## Status`; a runnability paragraph added to `## Install`; the before/after lead-in
  sentence narrowed; the measured-models caveat sentence corrected; nineteen `(exists)` markers removed
  from the layout tree and the legend sentence rewritten.

New violation codes: none. New functions: none. New fixture roots: none. New MUTATIONS entries: none.
Expected discrimination-proven count after this plan: **44**, unchanged.

What this plan unlocks: `04-09` registers `readme-example-drift`, `readme-example-lead-distance` and
`readme-layout-legend-drift` against exactly this repaired structure. Two of those three would be
reported FIRE-ONLY today — the first ✗ sits at line 23 and the legend names a marker the tree never
uses — so the prose must land first.

Residual this plan leaves open, for the phase's verification step to record in `.planning/WINDOWS.md`:
whether README genuinely reads well to a first-time reader stays a semantic judgment no code in this
repository performs. It is carried above as a `verification: backstop` truth. WINDOWS entry 12 already
records the prose-quality half of DIST-06 as unverified; this plan does not close or reclassify it, and
its closure condition should be re-evaluated by the phase's verification step once `04-09` lands.
</artifacts_this_phase_produces>

<threat_model>
Security enforcement is active with no `workflow.security_asvs_level` and no `workflow.security_block_on`
configured, so this block uses the defaults: **ASVS Level 1**, blocking threshold **high**.

This is a documentation-and-stdlib-scripts repository. This plan edits one Markdown file and adds no
code. There is no network surface, no authentication and no user input. A thin register is the correct
outcome; the reasoning is stated rather than the block omitted.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository README → prospective installer | README prints commands a reader will paste into a shell. This is the one place in the repository where prose becomes an executed instruction. |
| repository README → public reader evaluating the project's claims | The measured-figure disclosure is the project's credibility surface; a figure misstated here is a claim the repository cannot support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08-01 | Spoofing | the two install commands naming a publish location | high | mitigate | The prohibition list forbids resolving or guessing the placeholder, and `publish-location-drift` holds README's two command arguments identical to both manifests' owner segment. Task 1 additionally makes README state plainly that routes 1 and 2 do not resolve today, so a reader cannot be led to install from a repository this project does not control. |
| T-04-08-02 | Repudiation | the measured-figure disclosure | high | mitigate | Every figure is frozen by prohibition and asserted unchanged by Task 2's count probe; `readme-results-pointer-missing` keeps the pointer to the source measurement present, and the probe asserts it still appears exactly twice, matching WINDOWS entry 13. |
| T-04-08-03 | Tampering | the attribution pointer line | medium | mitigate | `pointer-missing` and `pointer-duplicated` both fire on a move that drops or duplicates it; Task 1's probe asserts exactly one occurrence after the reorder. |
| T-04-08-04 | Information disclosure | the layout tree naming a path that does not exist | medium | mitigate | Task 2's probe resolves every filename in the fenced tree against the working tree and requires an empty missing list, and the action instructs stopping and reporting rather than deleting a line if a path is genuinely absent. `04-09` then registers the code that keeps the legend and the markers in agreement. |
| T-04-08-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; the zero-dependency constraint forbids one and no package manifest or lockfile exists in the tree. The `npx skills add` command README prints is an install instruction for a reader, not a dependency of this repository, and it resolves to a placeholder that cannot install anything today. A Package Legitimacy Audit is therefore not required. |
</threat_model>

<verification>
After both tasks, from the repository root:

1. `python3 tools/check_repo.py --self-test` — exits 0.
2. `python3 tools/check_repo.py --mutation-test` — prints `mutation-test PASS: 44 codes
   discrimination-proven`, `0 unexpected` on CONTROL, no FIRE-ONLY line.
3. `python3 tools/check_repo.py` — prints `check_repo: 0 violations`.
4. `python3 tools/generate_derivatives.py --check` — exits 0.
5. The heading list begins with `Before and after`; `Keeping derivatives in sync` follows `Status`.
6. The first `✗` line is at line 20 or lower; `(exists)` and `planned` both count 0; the results-pointer
   literal counts 2; the attribution pointer counts 1.
</verification>

<success_criteria>
- README leads with a worked example inside the first screen.
- Every install route states whether it is runnable today.
- No maintainer instruction sits between Install and Status.
- The layout legend and the layout tree describe the same marker vocabulary, and every path shown exists.
- No sentence claims more than the catalog or the committed measurements support.
- The gate still reports 44 codes discrimination-proven, 0 violations, 0 unexpected CONTROL.
</success_criteria>

<output>
Create `.planning/phases/04-distribution-worked-examples/04-08-SUMMARY.md` when done. Record the
before and after heading order, the first-✗ line number before and after, the marker counts before and
after, the exact replacement wording for defects (d) and (e), and the `verification: backstop` residual
so the phase's verification step can file it in `.planning/WINDOWS.md`.
</output>
