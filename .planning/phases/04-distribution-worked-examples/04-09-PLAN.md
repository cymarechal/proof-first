---
phase: 04-distribution-worked-examples
plan: 09
type: execute
wave: 4
depends_on: ["04-08"]
gap_closure: true
files_modified:
  - tools/check_repo.py
autonomous: true
requirements: [DIST-06]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "README's reproduced example lines are held identical to `examples/before-after.md` by a build failure, so README's own claim that the pair is reproduced from that file cannot quietly stop being true — the defect class that let `04-05` have to repair the same prose in two places."
    - "A README whose first ✗ line sits beyond the frozen ceiling, or which carries no ✗ line at all, fails the build. DIST-06's promise that README leads with before/after pairs stops being an editorial judgment made once and becomes a measurement made on every push."
    - "A repository-layout legend explaining a marker the tree never uses, or a tree marker no legend explains, fails the build — the exact self-contradiction 04-UAT.md test 4 item (b) found, where a `planned` marker was explained zero times over a tree carrying nineteen `(exists)` markers and nothing else."
    - "`python3 tools/check_repo.py --mutation-test` reports exactly 47 codes discrimination-proven, up from 44, with the CONTROL line reading 0 unexpected and no FIRE-ONLY line. A code registered in ALL_CHECK_CODES with no MUTATIONS entry fails the run outright."
    - "Each of the three codes fires on its own known-bad fixture, stays silent on a known-good fixture, and stays silent on a fixture root shipping no README.md — the three-part contract every one of this module's 44 existing codes already carries."
    - "Each new check's docstring states its own declared ceiling in the rhetorical shape the module already uses: what it catches, then plainly what it does not."
  prohibitions:
    - "MUST NOT import anything outside the Python standard library, and MUST NOT add a package manifest, a lockfile or a pip install step anywhere, including under `evals/`."
    - "MUST NOT redeclare `README_BEFORE_AFTER_HEADING`, `README_INSTALL_HEADING`, `README_STATUS_HEADING`, `README_INSTALL_ANCHORS`, `BEFORE_AFTER_PATH`, `strip_fences` or `split_sections`. Reuse the existing module-level definitions."
    - "MUST NOT register a code in `ALL_CHECK_CODES` without a matching `MUTATIONS` entry."
    - "MUST NOT weaken, narrow or delete any existing check, fixture, assertion or `MUTATIONS` entry to make room for the new ones."
    - "MUST NOT add an entry to `KNOWN_OPEN_VIOLATIONS`; it is `frozenset()` today and the expected outcome is that it stays that way."
    - "MUST NOT edit `README.md`, `examples/before-after.md` or any file under `skills/` in this plan. `04-05` and `04-08` already made the content clean; if a new code fires on the real repository, the code's threshold or scope is wrong, or one of those plans is incomplete — report it, do not edit content to suit the checker."
    - "MUST NOT set `README_FIRST_EXAMPLE_MAX_LINE` to whatever the repaired README happens to measure. Freeze it at 20, stated as a ceiling with margin, so the code has room to be a real constraint rather than a transcription of today's file."
    - "MUST NOT make `readme-example-drift` compare anything other than whole lines. A substring or normalised comparison would silently accept a reproduction that differs in whitespace or punctuation, which is exactly the drift it exists to catch."
    - "MUST NOT use an emoji anywhere in shipped content."
  artifacts:
    - path: "tools/check_repo.py"
      provides: "Three new violation codes — readme-example-drift, readme-example-lead-distance, readme-layout-legend-drift — each with a check function, a declared-ceiling docstring, a module-docstring bullet, firing and silent self-test fixtures, and a registered MUTATIONS entry"
      contains: "readme-example-drift"
      min_lines: 5600
  key_links:
    - from: "tools/check_repo.py"
      to: "README.md"
      via: "all three codes read README; the existing README_BEFORE_AFTER_HEADING and README_STATUS_HEADING constants are reused rather than new literals"
      pattern: "README_BEFORE_AFTER_HEADING"
    - from: "tools/check_repo.py"
      to: "examples/before-after.md"
      via: "readme-example-drift compares README's quoted lines against this file's lines, reusing BEFORE_AFTER_PATH"
      pattern: "BEFORE_AFTER_PATH"
---

<objective>
Register the two README defect classes that have a mechanical signature, plus the cross-file identity
README itself asserts, so DIST-06's promise stops being an editorial judgment made once per phase.

Purpose: `04-UAT.md` test 4 found six README defects behind a green 41-code gate. Two of them are
cleanly countable — the distance from the top of the file to the first example, and a layout legend
describing a marker vocabulary the tree does not use — and a third is a cross-file identity README
states in its own prose ("reproduced from `examples/before-after.md`") that nothing enforces. `04-05`
had to repair the same three lines in two files by hand precisely because no code held them together.
Where a gap has a mechanizable signature, the code ships alongside the prose fix.

Output: `readme-example-drift`, `readme-example-lead-distance` and `readme-layout-legend-drift`, each
with a firing fixture, a silent fixture and a registered `MUTATIONS` entry. The discrimination-proven
total moves from 44 to 47.
</objective>

<gap_coverage>
This plan mechanises the half of G-04-6 that has a signature, on top of the prose `04-08` repaired.

| Gap | Where | Code |
|---|---|---|
| G-04-6, the reproduced-pair identity behind defect 3a's two-file repair | Task 1 | `readme-example-drift` |
| G-04-6 (f), twelve lines before the first example | Task 2 | `readme-example-lead-distance` |
| G-04-6 (b), a legend explaining a marker the tree never uses | Task 3 | `readme-layout-legend-drift` |

G-04-6's remaining three defects — (a) runnability, (d) the model-count caveat, (e) the over-broad
claim about the catalog — are deliberately not mechanised. Each is a statement about whether prose is
true, not about whether two artifacts agree, and this repository's own position is that a regex proxy
is disclosed as a proxy rather than dressed as a verdict. They are repaired in prose by `04-08` and
carried there as a `verification: backstop` truth. G-04-1 through G-04-5 are `04-05` and `04-07`'s;
G-04-7 is `04-10`'s.
</gap_coverage>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-distribution-worked-examples/04-UAT.md
@.planning/phases/04-distribution-worked-examples/04-PATTERNS.md
@.planning/phases/04-distribution-worked-examples/04-04-PLAN.md
@tools/check_repo.py
@README.md
@examples/before-after.md
</context>

<interfaces>
<!-- Every line and line number below was read from tools/check_repo.py during this planning session (2026-09-17). -->

**Baseline this plan starts from, after `04-08` lands.** `python3 tools/check_repo.py` prints
`check_repo: 0 violations`; `--self-test` passes; `--mutation-test` prints `mutation-test PASS: 44
codes discrimination-proven` with `0 unexpected` on CONTROL and no FIRE-ONLY line;
`python3 tools/generate_derivatives.py --check` exits 0. README's first ✗ line sits at line 20 or
lower, its layout tree carries zero `(exists)` markers and its prose carries zero `planned` mentions.
If any of that is untrue, stop — a code added on top of unclean content is reported FIRE-ONLY and never
counts as discrimination-proven.

**The README section of `tools/check_repo.py`, lines 966-1030 and 2405-2500.**
- `README_RESULTS_POINTER = 'evals/conformance/RESULTS-mod04.md'` at line 970.
- `check_readme_results_pointer(repo_root)` at 973.
- `README_CHECK_CODES` at 1005, currently listing `readme-results-pointer-missing`,
  `readme-install-path-missing` and `readme-before-after-order`.
- `run_readme_checks(repo_root)` at 1010, which calls `check_readme_results_pointer`,
  `check_readme_install_paths` and `check_readme_before_after_order`.
- `README_INSTALL_ANCHORS` at 2405, `README_BEFORE_AFTER_HEADING = '## Before and after'` at 2412,
  `README_INSTALL_HEADING = '## Install'` at 2413, `README_STATUS_HEADING = '## Status'` at 2414.
- `check_readme_install_paths(repo_root)` at 2417 — reads raw text, never calls `strip_fences`, with a
  stated reason: every anchor lives inside a fenced block or a code span, and stripping fences would
  find none of them and pass silently on an empty README.
- `check_readme_before_after_order(repo_root)` at 2452 — reads the three heading line indices.
Both README checks return `[]` before any read when `README.md` does not exist, and both push
`('README.md', message)` tuples whose message begins with the code name.

**The three-part contract every existing code carries**, unchanged from `04-07`:
1. A check function called from `run_all_checks()` through `run_readme_checks`, with the code name in
   `README_CHECK_CODES`, which feeds `ALL_CHECK_CODES` at line 2680.
2. A `--self-test` fixture pair, with the new roots' code sets folded into the `bad_codes |= (...)`
   union block at lines 3955-3967, so the coverage loop at 5230-5240 needs no edit.
3. A `MUTATIONS` entry appended to the list at line 3291. `mutation_test()` fails the run for any
   `ALL_CHECK_CODES` member with no entry.

**Existing README fixture builders and roots**, lines 4222-4260 and 4869-4880:
- `_good_readme_install()` — all three ordering headings in the required order and all four install
  anchors present, so both existing README codes stay silent. It carries no ✗ line today.
- `_bad_readme_install()` — two of four anchors missing and `## Before and after` placed after
  `## Status`.
- Roots `readme_install_good_root` and `readme_install_bad_root`, written at 4878-4879, code sets
  computed at 4950-4951.
- Separately, `readme_good_root` and `readme_bad_root` at 4625 exercise the results-pointer code only.

**Existing README mutation functions**, lines 3266-3288: `_mutate_readme_install_path_missing` deletes
every line containing the skills-CLI command prefix from the copied real README;
`_mutate_readme_before_after_order` moves the `## Before and after` heading line to immediately after
the `## Status` heading line. Both take `root: Path` and mutate the copy in place.

**Measured on the pre-`04-08` README, this session**, which is what each threshold is calibrated
against and what each mutation reproduces:
- The first `✗` line sits at line 23, behind twelve lines of project description.
- `grep -c '(exists)' README.md` prints `19`; `grep -c 'planned' README.md` prints `1`, in the legend
  sentence alone and never inside the fenced tree.
- README lines 23, 24 and 26 are byte-identical to `examples/before-after.md` lines 12, 13 and 15,
  verified by set membership over whole lines. Nothing in the repository enforces that identity today.

**`README.md` is not scanned by `unlisted-figure`.** `UNLISTED_FIGURE_SCAN_ROOTS` is
`('examples', 'skills')`, so filler lines inserted into README by a mutation cannot trip that code.
`check_undefined_id` does scan README with fences stripped, so a mutation's filler text must carry no
`PF-`/`MC-`-shaped token.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: Register readme-example-drift end to end — check, fixtures, mutation, 44 to 45 codes</name>
  <precondition>`04-08` has landed and `python3 tools/check_repo.py --mutation-test` reports 44 codes discrimination-proven with 0 unexpected CONTROL violations; every README line beginning with a ballot-cross or check character, and every line beginning with the applied-rules prefix, already appears verbatim as a line in `examples/before-after.md`. A code added while those differ is reported FIRE-ONLY and never counts.</precondition>
  <files>tools/check_repo.py</files>
  <read_first>
- `tools/check_repo.py` lines 966-1030 — the README section's constants, `README_CHECK_CODES` and `run_readme_checks`, which this task extends.
- `tools/check_repo.py` lines 2405-2500 — both existing README checks, their declared-ceiling docstrings, and in particular `check_readme_install_paths`'s stated reason for reading raw text rather than stripping fences.
- `tools/check_repo.py` lines 23-120 — the module docstring's code list, especially the `unlisted-figure` bullet at 54-71 and the `pointer-missing` bullet at 72-82, whose "code-point equality, with no Unicode normalisation and no case folding" wording is the exact convention this code's comparison must state.
- `tools/check_repo.py` lines 4222-4260 — `_good_readme_install` and `_bad_readme_install`, the fixture builders this task extends.
- `tools/check_repo.py` lines 4869-4880 and 4950-4951 — where the two README install roots are written and where their code sets are computed.
- `tools/check_repo.py` lines 3955-3967 — the `bad_codes |= (...)` union block.
- `tools/check_repo.py` lines 4992-5040 — the existing per-code assertion blocks for the two README codes and the two example codes, the exact shape the new assertions must mirror.
- `tools/check_repo.py` lines 3266-3288 and 3291-3340 — the two existing README mutation functions and the `MUTATIONS` list.
- `README.md` as `04-08` leaves it, and `examples/before-after.md` as `04-05` leaves it — the two files whose identity this code enforces.
  </read_first>
  <action>
Add one violation code, `readme-example-drift`.

Declare one module-level constant beside `README_BEFORE_AFTER_HEADING` at line 2412: the applied-rules
line prefix, the literal string that opens each footer line in both files. Do not retype the ballot-cross
or check characters as new constants if the module already names them; if it does not, declare them once
beside this constant and reuse them, rather than embedding the raw characters at several call sites.

Write `check_readme_example_drift(repo_root)`. Return an empty list before any read when either
`README.md` or `BEFORE_AFTER_PATH` does not exist — both files are required for the comparison to mean
anything, and a fixture root shipping only one of them must stay silent. Read both files raw, without
stripping fences, for the same reason `check_readme_install_paths` gives: the reproduced lines are
plain prose lines and fence-stripping would change what is compared. Build the set of
`examples/before-after.md`'s lines. Then, for every README line whose first character is the
ballot-cross or the check character, and for every README line beginning with the applied-rules prefix,
assert that the whole line is a member of that set. Fire once per README line with no match, subject
`README.md`, message beginning with the code name and naming the README line number and a short prefix
of the line's text.

The docstring states the declared ceiling in the module's established shape. What it catches: a README
line reproducing an example that is not, character for character, a line of the file README says it is
reproduced from. What it does not: comparison is whole-line code-point equality with no Unicode
normalisation, no case folding and no whitespace collapsing — the same convention `pointer-missing`
already states — so a reproduction differing only in an invisible code point is reported as drift, which
is the intended direction. It asserts membership, not position or completeness: README may reproduce
one pair, four pairs or none at all, and may reproduce a ✓ line without its ✗ line; this check says
nothing about which pair README chose or whether the pair is whole. It does not compare the surrounding
prose, the family label or the link line. And it asserts nothing in the opposite direction: a line
present in `examples/before-after.md` and absent from README is not a violation, because README is a
lead-in and is not required to reproduce everything.

Add the code name to `README_CHECK_CODES` and call the function from `run_readme_checks`. Add a bullet
to the module docstring's code list following the established pattern.

Fixtures. Extend `_good_readme_install()` so it also carries a ✗ line, a ✓ line and an applied-rules
footer line, placed immediately after the before/after heading — keeping every existing property that
fixture already has, because the existing assertions on `readme_install_good_root` for
`readme-install-path-missing` and `readme-before-after-order` must continue to hold unchanged. Add a
matching fixture builder producing a minimal `examples/before-after.md` whose lines include those exact
three lines, so the pair is consistent. Add `readme_drift_good_root`, writing both files under it, and
`readme_drift_bad_root`, writing the same README but a before-after file in which one of the three
lines differs by a single word. Compute both roots' code sets beside the existing
`readme_install_*_codes` lines and fold both into the `bad_codes` union.

Add the assertion block: the code must not fire on `readme_drift_good_root`; it must fire on
`readme_drift_bad_root`; it must not fire on `readme_install_good_root`, which ships a README carrying
reproduced lines but no `examples/before-after.md` at all, proving the both-files-required precondition
is a tested behaviour rather than a docstring claim; and it must not fire on `good_root`, which ships
no README.

Mutation. Add `_mutate_readme_example_drift(root)`, taking `root: Path`, locating the first line of the
copied real `README.md` whose first character is the ballot-cross character, and replacing one word of
it with a different word — mutating README rather than `examples/before-after.md`, so the mutation
reproduces the real failure mode, which is README drifting away from the source it names. Register it
in `MUTATIONS` with an imperative description.

This is the tracer. It proves the whole path — constant, check, docstring bullet, extended fixture,
new root pair, isolation assertions, mutation registration — before two more codes follow it.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, or any line beginning with "FAIL:", or the closing "self-test PASS - verified violation codes:" line does not contain "readme-example-drift"</fails_when>
    <automated>python3 tools/check_repo.py --mutation-test</automated>
    <fails_when>non-zero exit, or the summary line reports a number other than 45 codes discrimination-proven, or any FIRE-ONLY line appears, or the CONTROL line reports more than 0 unexpected, or any line reads "mutation-test FAIL"</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 tools/check_repo.py --mutation-test | grep -c '^mutation-test OK: readme-example-drift '</automated>
    <fails_when>the printed count is anything other than 1</fails_when>
    <automated>python3 -c "import subprocess,sys;out=subprocess.run([sys.executable,'tools/check_repo.py','--self-test'],capture_output=True,text=True).stdout;line=[l for l in out.splitlines() if l.startswith('self-test PASS')][0];print(len(line.split(': ',1)[1].split(', ')))"</automated>
    <fails_when>non-zero exit, or the printed integer is anything other than 45</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py --self-test` exits 0 with no `FAIL:` line and a verified-codes list of exactly 45 entries containing `readme-example-drift`.
- `python3 tools/check_repo.py --mutation-test` prints `mutation-test PASS: 45 codes discrimination-proven`, `0 unexpected` on CONTROL, and no FIRE-ONLY line.
- `python3 tools/check_repo.py --mutation-test | grep -c '^mutation-test OK: readme-example-drift '` prints `1`.
- `python3 tools/check_repo.py` prints exactly `check_repo: 0 violations` against the real repository, proving README and `examples/before-after.md` agree line for line after `04-05` and `04-08`.
- The self-test asserts silence on `readme_install_good_root`, so the both-files-required behaviour is discrimination-tested.
- The existing assertions on `readme_install_good_root` for `readme-install-path-missing` and `readme-before-after-order` still pass unchanged after `_good_readme_install()` was extended.
- `python3 tools/check_repo.py --mutation-test | grep -c '^mutation-test OK: '` prints `45` — no pre-existing code lost its mutation.
  </acceptance_criteria>
  <done>`readme-example-drift` fires on a drifted reproduction, stays silent when either file is absent and on the good fixture, has a registered mutation, and the gate reports 45 codes.</done>
</task>

<task type="auto">
  <name>Task 2: Register readme-example-lead-distance — 45 to 46 codes</name>
  <files>tools/check_repo.py</files>
  <read_first>
- `tools/check_repo.py` lines 966-1030 and 2405-2500 as Task 1 leaves them — the README section, now carrying four codes.
- `tools/check_repo.py` lines 2452-2500 — `check_readme_before_after_order`'s docstring, which states what heading ordering does and does not prove; this code's ceiling must say plainly where the two divide.
- `.planning/REQUIREMENTS.md`, the DIST-06 entry — "Reader gets a README that leads with before/after pairs and states install paths for each harness", including its recorded note that the structural half is CI-enforced and the prose half is not.
- `tools/check_repo.py` lines 4222-4260 as Task 1 leaves them — the extended README fixture builders.
- This plan's `<interfaces>` block, especially the measured pre-`04-08` first-✗ line number of 23.
  </read_first>
  <action>
Add one violation code, `readme-example-lead-distance`.

Declare one module-level constant beside `README_STATUS_HEADING`: `README_FIRST_EXAMPLE_MAX_LINE`, the
integer 20. Comment it as a frozen ceiling with deliberate margin, not a transcription of the repaired
file: the measured pre-repair value was 23 and the repaired file sits at or below 20, so the constant
constrains rather than merely records. State in the comment that raising this number is a deliberate
weakening of DIST-06 and should be argued for, not typed.

Write `check_readme_example_lead_distance(repo_root)`. Return an empty list before any read when
`README.md` does not exist. Read the file raw, without stripping fences, and find the one-based line
number of the first line whose first character is the ballot-cross character. Fire in two cases, each
with its own message: when no such line exists anywhere in the file, naming that README shows no
before/after example at all; and when the first one's line number exceeds
`README_FIRST_EXAMPLE_MAX_LINE`, naming the measured line number and the ceiling. Subject is
`README.md` in both cases.

The docstring states the ceiling. What it catches: a README where a reader must scroll past more than
the frozen number of lines before meeting the first example, and a README with no example at all. What
it does not: it counts physical lines from the top of the raw file, so a long paragraph written as one
physical line counts once while the same prose hard-wrapped counts many times — a stated definition
rather than an implied one, and the reason the ceiling carries margin. It asserts the example arrives
early, never that the example is good, that the prose above it is necessary, or that the pair below it
is complete; `readme-example-drift` owns the reproduction's fidelity and
`readme-before-after-order` owns the section ordering. It reads only the ballot-cross character, so a
README leading with a ✓ line and no ✗ line is reported as having no example, which is intended: the
contrast is the point.

Add the code name to `README_CHECK_CODES` and call it from `run_readme_checks`. Add its module
docstring bullet.

Fixtures. `_good_readme_install()`, as Task 1 extended it, already carries a ✗ line near the top; add
an assertion-supporting root only where needed. Add `_late_example_readme()`, a fixture carrying the
same headings and anchors but with enough filler prose lines above the before/after section to push the
first ✗ line past the ceiling, and `_no_example_readme()`, a fixture carrying the headings and anchors
and no ✗ line at all. Add `readme_lead_late_root` and `readme_lead_none_root`, write one fixture to
each root's `README.md`, compute their code sets and fold both into the `bad_codes` union.

Add the assertion block: the code must fire on `readme_lead_late_root`; it must fire on
`readme_lead_none_root`, so both firing conditions are separately proven rather than one standing in
for the other; it must not fire on `readme_install_good_root`; and it must not fire on `good_root`,
which ships no README.

Mutation. Add `_mutate_readme_example_lead_distance(root)`, inserting enough plain filler lines
immediately after the copied real README's title line to push the first ✗ line past the ceiling. The
filler text must contain no `PF-`- or `MC-`-shaped token, because `check_undefined_id` scans README with
fences stripped and an unallocated token there would make the mutation fire a second, unrelated code.
Register it in `MUTATIONS` with an imperative description naming the insertion point and the effect.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py --self-test</automated>
    <fails_when>non-zero exit, or any line beginning with "FAIL:", or the closing verified-codes line does not contain "readme-example-lead-distance"</fails_when>
    <automated>python3 tools/check_repo.py --mutation-test</automated>
    <fails_when>non-zero exit, or the summary line reports a number other than 46 codes discrimination-proven, or any FIRE-ONLY line appears, or the CONTROL line reports more than 0 unexpected, or any line reads "mutation-test FAIL"</fails_when>
    <automated>python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit, or any output line other than "check_repo: 0 violations"</fails_when>
    <automated>python3 -c "import pathlib;L=pathlib.Path('README.md').read_text().splitlines();print(next(i for i,l in enumerate(L,1) if l[:1]==chr(10007)))"</automated>
    <fails_when>non-zero exit, or the printed line number is above 20, which would mean the real README does not satisfy the ceiling the new code freezes</fails_when>
    <automated>python3 -c "import re,pathlib;t=pathlib.Path('tools/check_repo.py').read_text();m=re.search(r'README_FIRST_EXAMPLE_MAX_LINE\s*=\s*(\d+)',t);print(m.group(1) if m else 'ABSENT')"</automated>
    <fails_when>non-zero exit, or the printed value is "ABSENT", or the printed integer is anything other than 20</fails_when>
  </verify>
  <acceptance_criteria>
- `python3 tools/check_repo.py --self-test` exits 0 with no `FAIL:` line and a verified-codes list of exactly 46 entries containing `readme-example-lead-distance`.
- `python3 tools/check_repo.py --mutation-test` prints `mutation-test PASS: 46 codes discrimination-proven`, `0 unexpected` on CONTROL, no FIRE-ONLY line.
- `README_FIRST_EXAMPLE_MAX_LINE` is present in `tools/check_repo.py` with the value `20`.
- The real README's first `✗` line is at line 20 or lower, and `python3 tools/check_repo.py` prints exactly `check_repo: 0 violations`.
- The self-test proves both firing conditions separately: the code fires on `readme_lead_late_root` and on `readme_lead_none_root`.
- The mutation's filler text contains no `PF-`/`MC-`-shaped token, so the mutation isolates one code.
  </acceptance_criteria>
  <done>`readme-example-lead-distance` fires on a late example and on a missing example, stays silent on the good fixture and on a root with no README, has a registered mutation, and the gate reports 46 codes.</done>
</task>

<task type="auto">
  <name>Task 3: Register readme-layout-legend-drift — 46 to 47 codes</name>
  <files>tools/check_repo.py</files>
  <read_first>
- `tools/check_repo.py` lines 966-1030 and 2405-2500 as Tasks 1 and 2 leave them — the README section, now carrying five codes.
- `tools/check_repo.py` lines 550-600 — `strip_fences`, `split_sections` and `table_rows`, and the `FENCE_RE`/`FENCE_CONTENT_RE` patterns the module already uses to isolate fenced content, so this check reuses one of them rather than declaring a third fence pattern.
- `tools/check_repo.py` lines 900-925 — `check_pointer`, whose declared ceiling on code-point equality is the module's canonical wording for a comparison-based check.
- `README.md`'s `## Repository layout` section as `04-08` leaves it — the rewritten legend sentence and the fenced tree with no markers.
- `.planning/phases/04-distribution-worked-examples/04-UAT.md` test 4 item (b) — the exact defect: a legend explaining a marker that appears zero times over a tree where nineteen entries carried a different marker and the rest carried none.
- This plan's `<interfaces>` block, especially the measured marker counts on the pre-`04-08` README.
  </read_first>
  <action>
Add one violation code, `readme-layout-legend-drift`.

Declare one module-level constant naming the layout heading, `'## Repository layout'`, beside the three
existing README heading constants. Declare two compiled regexes: one matching a marker written in the
legend prose as a short lowercase word or hyphenated phrase inside double quotes, and one matching a
marker written in the tree as the same shape inside parentheses. Bound both to a short maximum length
so an ordinary quoted phrase or a parenthesised sentence is not mistaken for a marker, and state that
bound in a comment.

Write `check_readme_layout_legend_drift(repo_root)`. Return an empty list before any read when
`README.md` does not exist, and return an empty list when the layout heading is absent — a README with
no layout section is silent, not violating, matching `publish-location-drift`'s stated decision that a
carrier with nothing to say is silent. Isolate the layout section's text by heading, then split it into
the fenced tree and the prose outside the fence, reusing the module's existing fence pattern. Collect
the set of markers the prose explains and the set of markers the tree uses. Fire once per marker the
prose explains that the tree never uses, and once per marker the tree uses that the prose never
mentions. Subject is `README.md`; each message begins with the code name and names the marker and which
direction the mismatch runs.

The docstring states the ceiling plainly. What it catches: a legend and a tree that disagree about which
markers exist — either direction. What it does not: it compares marker vocabulary only. It says nothing
about whether a marker is applied to the right entries, whether the tree matches the filesystem, whether
the legend's explanation of a marker is accurate, or whether an entry that carries no marker should. Its
marker shape is a short lowercase token inside double quotes in prose and inside parentheses in the
tree; a marker written in any other shape — bracketed, uppercase, or longer than the stated bound — is
invisible to this check. And it reads only the layout section, so a marker vocabulary introduced
elsewhere in README is out of scope.

Add the code name to `README_CHECK_CODES` and call it from `run_readme_checks`. Add its module
docstring bullet.

Fixtures. Add `_good_readme_layout()`, a minimal README carrying a layout heading, a prose sentence
explaining one marker in the quoted shape, and a fenced tree using that same marker on at least one
entry — silent. Add `_bad_readme_layout()`, carrying a prose sentence explaining one marker the tree
never uses and a tree using a second marker the prose never mentions, so both firing directions are
exercised in one fixture, following `_bad_before_after`'s documented practice of combining conditions.
Add `readme_layout_good_root` and `readme_layout_bad_root`, write one fixture to each, compute their
code sets and fold the bad one into the `bad_codes` union.

Add the assertion block: the code must not fire on `readme_layout_good_root`; it must fire on
`readme_layout_bad_root`; it must not fire on `readme_install_good_root`, which ships a README with no
layout heading at all, proving the silent-when-absent behaviour; and it must not fire on `good_root`.

Mutation. Add `_mutate_readme_layout_legend_drift(root)`, inserting one sentence into the copied real
README's layout section prose that explains a quoted marker the tree does not use. Register it in
`MUTATIONS` with an imperative description.

Finally, re-read `README_CHECK_CODES` and `run_readme_checks` and confirm all six README codes are
listed and called. This task closes the plan, so its verify block asserts the final 47 and re-runs all
four gate commands.
  </action>
  <verify>
    <automated>python3 tools/check_repo.py --self-test &amp;&amp; python3 tools/check_repo.py --mutation-test &amp;&amp; python3 tools/check_repo.py</automated>
    <fails_when>non-zero exit at any stage, or any line beginning with "FAIL:", or the mutation summary line reports a number other than 47 codes discrimination-proven, or any FIRE-ONLY line appears, or the CONTROL line reports more than 0 unexpected</fails_when>
    <automated>python3 tools/generate_derivatives.py --check</automated>
    <fails_when>non-zero exit, or any printed line names a derivative as differing from freshly rendered output or as missing</fails_when>
    <automated>python3 tools/check_repo.py --mutation-test | grep -c '^mutation-test OK: '</automated>
    <fails_when>the printed count is anything other than 47</fails_when>
    <automated>python3 -c "import subprocess,sys;out=subprocess.run([sys.executable,'tools/check_repo.py','--self-test'],capture_output=True,text=True).stdout;line=[l for l in out.splitlines() if l.startswith('self-test PASS')][0];c=line.split(': ',1)[1].split(', ');print(len(c),sorted(x for x in c if x.startswith('readme-')))"</automated>
    <fails_when>non-zero exit, or the printed integer is anything other than 47, or the printed list has fewer than 6 entries</fails_when>
    <automated>python3 -c "import pathlib;t=pathlib.Path('tools/check_repo.py').read_text();d=t.split('\"\"\"',2)[1];print(sorted(c for c in ('readme-example-drift','readme-example-lead-distance','readme-layout-legend-drift') if c in d))"</automated>
    <fails_when>non-zero exit, or the printed list has fewer than 3 entries, which would mean a code shipped without its module-docstring bullet and its declared ceiling</fails_when>
    <automated>python3 -c "import ast,pathlib,re;t=pathlib.Path('tools/check_repo.py').read_text();ast.parse(t);print(sorted(set(re.findall(r'^\s*(?:import|from)\s+([a-zA-Z_][\w.]*)',t,re.M))))"</automated>
    <fails_when>non-zero exit, or the printed list contains any name outside {argparse, hashlib, json, pathlib, re, shutil, sys, tempfile}</fails_when>
  </verify>
  <acceptance_criteria>
- All four gate commands pass: `--self-test` exits 0 with no `FAIL:` line, `--mutation-test` prints `mutation-test PASS: 47 codes discrimination-proven` with `0 unexpected` on CONTROL and no FIRE-ONLY line, `check_repo.py` prints `check_repo: 0 violations`, and `generate_derivatives.py --check` exits 0.
- `python3 tools/check_repo.py --mutation-test | grep -c '^mutation-test OK: '` prints `47`.
- The self-test verified-codes list has exactly 47 entries and contains at least 6 codes whose names begin with `readme-`.
- All three new code names appear in the module docstring's first triple-quoted block, each with its own declared-ceiling text.
- The bad layout fixture exercises both mismatch directions — a legend marker the tree never uses and a tree marker the legend never mentions — in one fixture.
- `tools/check_repo.py` parses under `ast.parse` and imports only standard-library modules; `ls package.json pyproject.toml requirements.txt Makefile 2>/dev/null | wc -l` prints `0`.
  </acceptance_criteria>
  <done>All three README codes are registered, discrimination-proven and documented with declared ceilings; the gate reports 47 codes with a clean control and no fire-only line.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New violation codes (3): `readme-example-drift`, `readme-example-lead-distance`,
`readme-layout-legend-drift`.

New constants in `tools/check_repo.py`: the applied-rules line prefix, `README_FIRST_EXAMPLE_MAX_LINE`
fixed at 20, the repository-layout heading constant, a legend-marker regex and a tree-marker regex.

New check functions: `check_readme_example_drift`, `check_readme_example_lead_distance`,
`check_readme_layout_legend_drift` — all three called from the existing `run_readme_checks` and listed
in the existing `README_CHECK_CODES`.

New fixture builders: a minimal before-after companion for the drift roots, `_late_example_readme`,
`_no_example_readme`, `_good_readme_layout`, `_bad_readme_layout`, plus an extension to the existing
`_good_readme_install` adding a ✗ line, a ✓ line and an applied-rules footer.

New fixture roots (6): `readme_drift_good_root`, `readme_drift_bad_root`, `readme_lead_late_root`,
`readme_lead_none_root`, `readme_layout_good_root`, `readme_layout_bad_root`.

New mutation functions and MUTATIONS entries (3): `_mutate_readme_example_drift`,
`_mutate_readme_example_lead_distance`, `_mutate_readme_layout_legend_drift`.

New module-docstring bullets (3), one per code, each carrying its declared ceiling.

Expected discrimination-proven count after this plan: **47** (44 + 3).

This plan closes or reclassifies no WINDOWS entry. Entry 12's prose-quality half of DIST-06 is narrowed
by these three codes but not closed: whether README reads well to a first-time reader remains a semantic
judgment, carried as `04-08`'s `verification: backstop` truth. The phase's verification step should
re-evaluate entry 12's description against what became mechanical here rather than marking it resolved.
</artifacts_this_phase_produces>

<threat_model>
Security enforcement is active with no `workflow.security_asvs_level` and no `workflow.security_block_on`
configured, so this block uses the defaults: **ASVS Level 1**, blocking threshold **high**.

This is a documentation-and-stdlib-scripts repository. The code this plan touches runs only in CI and on
a contributor's machine, reads only files inside the repository, opens no socket, and writes only into a
`tempfile.TemporaryDirectory`. A thin register is the correct outcome; the reasoning is stated rather
than the block omitted.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository files → `check_repo.py` parsing | The checker reads contributor-authored Markdown. A pathological file could in principle drive regex backtracking or an unhandled exception that converts a real violation into a crash. |
| `mutation_test()` → filesystem | The harness copies a subset of the tree into a temporary directory and mutates the copy; a path defect there could write outside the sandbox. |
| CI runner → repository | The GitHub Actions job runs these commands with no secret and no network dependency beyond checkout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-09-01 | Denial of service | the two new marker regexes | medium | mitigate | Both are bounded-length character classes inside a fixed delimiter pair, with an explicit maximum repetition, so neither nests a quantifier inside a quantified group and neither can backtrack catastrophically. The layout section is bounded by the committed README's size. |
| T-04-09-02 | Tampering | `_mutate_*` functions writing outside the scratch root | high | mitigate | Every new mutation function takes `root: Path` and joins only repository-relative constants onto it, mirroring the 44 existing functions; `mutation_test()` wraps each in its own `tempfile.TemporaryDirectory`. The live check runs after the mutation suite, so an escape would surface as a dirty working tree. |
| T-04-09-03 | Repudiation | a code registered but never proven live | high | mitigate | `mutation_test()` fails the run for any `ALL_CHECK_CODES` member with no `MUTATIONS` entry and counts a code as discrimination-proven only when it was silent on the control and fired on its mutation. Task 3's verify asserts exactly 47 `mutation-test OK:` lines. |
| T-04-09-04 | Spoofing | README reproducing an example that no longer matches its stated source | high | mitigate | This is the threat `readme-example-drift` exists for: README asserts in its own prose that the pair is reproduced from `examples/before-after.md`, and until this plan nothing held that true. Whole-line code-point equality with no normalisation makes a silently-altered reproduction a build failure. |
| T-04-09-05 | Tampering | a future contributor raising the lead-distance ceiling to pass | medium | mitigate | The constant is frozen at 20 with a comment stating that raising it is a deliberate weakening of DIST-06, and Task 2's verify asserts the literal value is 20, so a quiet increase fails the plan's own acceptance criteria and is visible in review. |
| T-04-09-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan; the zero-dependency constraint forbids one. Task 3's verify asserts mechanically that the import set stays inside the standard library and that no package manifest or lockfile exists in the tree. A Package Legitimacy Audit is therefore not required. |
</threat_model>

<verification>
After all three tasks, from the repository root:

1. `python3 tools/check_repo.py --self-test` — exits 0, no `FAIL:` line, 47 verified codes.
2. `python3 tools/check_repo.py --mutation-test` — prints `mutation-test PASS: 47 codes
   discrimination-proven`, `0 unexpected` on CONTROL, no FIRE-ONLY line, 47 `mutation-test OK:` lines.
3. `python3 tools/check_repo.py` — prints `check_repo: 0 violations`.
4. `python3 tools/generate_derivatives.py --check` — exits 0.
5. `README_FIRST_EXAMPLE_MAX_LINE` equals 20 and the real README's first ✗ line is at or below it.
6. All three new code names appear in the module docstring's code list with declared ceilings.
</verification>

<success_criteria>
- Three new codes registered, each with a check function, a declared-ceiling docstring, a module
  docstring bullet, a firing fixture, a silent fixture and a `MUTATIONS` entry.
- The gate reports exactly 47 codes discrimination-proven, 0 violations, 0 unexpected CONTROL, no
  FIRE-ONLY line.
- `readme-example-drift` is proven silent when either file is absent; `readme-example-lead-distance`
  proves both of its firing conditions separately; `readme-layout-legend-drift` proves both mismatch
  directions.
- Zero third-party imports; no package manifest, lockfile or install step anywhere in the tree.
</success_criteria>

<output>
Create `.planning/phases/04-distribution-worked-examples/04-09-SUMMARY.md` when done. Record the exact
`mutation-test PASS` line, the 47-entry verified-codes list, each new code's declared ceiling verbatim,
and a note on what WINDOWS entry 12 now covers mechanically versus what remains a semantic judgment, so
the phase's verification step can re-evaluate that entry rather than silently closing it.
</output>
