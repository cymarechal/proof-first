---
phase: 04-distribution-worked-examples
plan: 14
type: execute
wave: 1
depends_on: []
gap_closure: true
gap_ids: [G-04-8]
files_modified:
  - README.md
  - .planning/WINDOWS.md
  - tools/check_repo.py
  - examples/before-after.md
autonomous: true
requirements: [DIST-06, EX-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`README.md` states nothing as fact about this repository's own behaviour that the same `README.md` disproves elsewhere on the page. Specifically: the sentence disclosing that the `/config` listing is unobserved scopes itself to the interactive case and names the headless mechanism this repository does drive, so it agrees with lines describing `evals/conformance/run_conformance.py` and with the conformance figure under `## Status`."
    - "The same false universal negative is removed from `.planning/WINDOWS.md` entry 16, which carries a verbatim copy of it. The ledger's rendered table row and its fenced JSON entry state the identical corrected text; `gsd-tools windows append` succeeding is the proof they agree, because it refuses to write while any row disagrees with its own JSON source of truth."
    - "`README.md` no longer describes its four install routes as one per harness class. Two of the four are Claude Code's own, and the sentence introducing the list says so."
    - "`README.md` states how a reader obtains the local clone that routes 3 and 4 need, or states plainly that it cannot yet — the clone URL is the same unpublished `<owner>/<repo>` as routes 1 and 2. A reader finishes the Install section knowing which routes are blocked and on what, with no route silently assuming an artifact the page never tells them how to get."
    - "`## Status` opens no paragraph with a bare fragment. The lead-in to the caveat list is a grammatical clause whose antecedent is on the page."
    - "Every word of `## Status` other than that one lead-in fragment is unchanged. `git diff -- README.md` shows no edit to any figure, any caveat, any qualifier or any pointer inside that section."
    - "`examples/before-after.md`'s Executive summary ✓ column asserts no deliverable the canonical deal brief does not support. The sentence that claimed the migration delivers automated failover now states scope the brief's own pain points carry, and the pair still demonstrates PF-0.1, PF-1.25 and PF-2.11."
    - "This plan registers no new violation code. `python3 tools/check_repo.py --mutation-test` reports 48 codes discrimination-proven, the same 48 as before this plan. The refusal to register a 49th is written down in `tools/check_repo.py`'s module docstring and carried as a tracked ledger entry, so a later reader finds a decision with reasoning rather than an absence."
    - "The four CI gate commands are green after every task: `python3 tools/check_repo.py` exits 0 printing `check_repo: 0 violations`, `python3 tools/check_repo.py --self-test` prints PASS, `python3 tools/generate_derivatives.py --check` exits 0, and `python3 evals/conformance/run_conformance.py --self-test` prints PASS."
    - statement: "A cold reader of `README.md` finds no two sentences on the page that contradict each other. No code in this repository performs that judgement, and Task 2 records why rather than pretending otherwise."
      verification: backstop
    - statement: "A technical evaluator reading the Executive summary ✓ column finds no claim about what the migration delivers that `examples/deal-brief.md` does not support. Evidential sufficiency of a prose claim is semantic; no code in this repository judges it."
      verification: backstop
  prohibitions:
    - "MUST NOT register a new violation code in `tools/check_repo.py`. The mechanisability assessment in Task 2 is a deliberate refusal with stated reasoning; reversing it silently, or adding a code to look thorough, is the outcome this plan exists to avoid. `--mutation-test` must report exactly 48."
    - "MUST NOT edit `skills/proof-first/SKILL.md` or any file under `skills/proof-first/references/`, and MUST NOT hand-edit `output-styles/proof-first.md` or `prompts/system-prompt.md`. No skill content changes here, so no derivative regeneration is required or permitted. `git diff --stat -- skills output-styles prompts` must print nothing."
    - "MUST NOT edit the `.claude-plugin/` manifests."
    - "MUST NOT edit any `README.md` line beginning with `✗`, `✓` or `Rules applied:`. `readme-example-drift` compares each of them character for character against `examples/before-after.md` and fires on any difference, including an invisible code point. The `examples/before-after.md` edit in Task 3 is in the Executive summary pair, which `README.md` does not reproduce — `README.md` reproduces the RFP and RFI pair only."
    - "MUST NOT insert any line above `README.md`'s current first ✗ line. `readme-example-lead-distance` enforces a 20-line ceiling from the title to that line and it is already close. Every edit in Task 1 sits below it."
    - "MUST NOT change the arguments of `npx skills add <owner>/<repo>` (line 53) or `claude plugin marketplace add <owner>/<repo>` (line 60). `publish-location-drift` reads README only at those two command prefixes and holds them in agreement with both plugin manifests. The placeholder may be named in prose freely — prose positions are outside that code's scan."
    - "MUST NOT remove either output-style destination directory from `README.md`. `readme-output-style-destination-missing` fires if neither `~/.claude/output-styles/` nor `.claude/output-styles/` is named."
    - "MUST NOT introduce a line break inside the literal `evals/conformance/RESULTS-mod04.md` anywhere it appears in `README.md`. `readme-results-pointer-missing` matches the whole path string and a wrapped copy is invisible to it. Measured now: 2 lines carry it."
    - "MUST NOT change any figure, caveat, qualifier or pointer inside `## Status`. The measured-figure disclosure was negotiated across 03-10, 03-15 and 04-08 and is currently correct. The one permitted edit in that section is replacing the orphaned lead-in fragment at line 149, which is itself an artifact of 04-13's whitespace-only paragraph-break insertion."
    - "MUST NOT restructure, shorten or relocate `## Status`. That deferral was decided in a prior round on the ground that it would edit a negotiated disclosure, and the reasoning stands. It is carried forward in this plan's `<deferred_items>` table, not re-litigated."
    - "MUST NOT add a spelled cardinal (two, three, four, five, six, seven, eight, nine, ten, eleven, twelve) to `examples/before-after.md`. `before-after-spelled-count` scans that file and fires on them."
    - "MUST NOT add a currency amount, a percentage or an ISO date to `examples/before-after.md` that has no row in `examples/deal-brief.md`'s Canonical figures table. `unlisted-figure` scans `examples/` and `skills/`."
    - "MUST NOT pair a contrastive connective (`rather than`, `, not`, `instead of`) with a rule-meta term inside 60 characters on any ✓ line. `example-rule-narration` fires on that shape."
    - "MUST NOT exceed 25 words in any sentence of any ✓ line in `examples/before-after.md`. `example-sentence-length` enforces PF-4.1's ceiling."
    - "MUST NOT check any `- [ ]` requirement checkbox in `.planning/REQUIREMENTS.md`. EX-02 and DIST-06 both carry standing UNVERIFIED notes and both stay unchecked. After the last task, `grep -n '^- \\[x\\]' .planning/REQUIREMENTS.md` must return nothing it did not return before."
    - "MUST NOT wrap any command in `timeout`. That binary is not installed on this machine."
    - "MUST NOT use an emoji anywhere in shipped content."
    - "MUST NOT edit `.planning/phases/04-distribution-worked-examples/04-UAT.md`, `04-13-PLAN.md` or `04-13-SUMMARY.md`. Those carry the false sentence as historical record of the defect and must keep carrying it."
  artifacts:
    - path: "README.md"
      provides: "An Install section and a Status section that assert nothing the same page disproves, describe the four routes as they actually divide, and state what a reader still cannot obtain"
      contains: "claude -p"
      min_lines: 240
    - path: ".planning/WINDOWS.md"
      provides: "Entry 16 corrected to the interactive-only scope, plus a new entry recording that no code compares two assertions in one document"
      contains: "output-styles"
      min_lines: 200
    - path: "tools/check_repo.py"
      provides: "A module docstring that states, alongside the existing paraphrase-judgement limit, that this checker does not compare two factual assertions in the same document for consistency"
      contains: "same document"
      min_lines: 6699
    - path: "examples/before-after.md"
      provides: "An Executive summary ✓ column whose every deliverable claim traces to examples/deal-brief.md"
      contains: "Oracle Database licensing"
      min_lines: 30
  key_links:
    - from: "README.md"
      to: "evals/conformance/run_conformance.py"
      via: "README's disclosure of what is unobserved names the headless mechanism this repository actually drives, so the disclosure and the script description describe the same world"
      pattern: "claude -p"
    - from: "README.md"
      to: ".planning/WINDOWS.md"
      via: "entry 16 carries a verbatim copy of README's false sentence; correcting one without the other leaves the defect alive in the ledger that tracks it"
      pattern: "output-styles"
    - from: "tools/check_repo.py"
      to: ".planning/WINDOWS.md"
      via: "the refusal to mechanise self-contradiction is stated in the checker's own docstring and tracked as an open ledger entry, so the non-coverage is discoverable from either side"
      pattern: "same document"
    - from: "examples/before-after.md"
      to: "examples/deal-brief.md"
      via: "every deliverable the Executive summary ✓ column names traces to a stated pain point in the canonical brief"
      pattern: "failover"
---

<objective>
Close G-04-8: one false sentence in `README.md`, plus the verbatim copy of it living in the
ledger entry that tracks the same disclosure.

`README.md:83` asserts "this repository's own environment drives no live harness session." The
same page disproves it at lines 111-113 and 134-137, and `evals/conformance/run_conformance.py:216`
builds a real `claude -p` invocation. The true claim is narrower: no *interactive* session runs
here, so the `/config` picker specifically is unobserved. Over-claiming a limitation is still
over-claiming, and the page's own stated standard is measured claims or none.

Then decide, honestly and in writing, whether any code in this stack can catch a self-contradicting
factual claim of this shape. The expected answer is no. A written refusal with reasoning is the
deliverable; a 49th code that cannot fire on the defect it names would be the CR-01 overstatement a
third time.

Then fold in three cheap README credibility defects from the same cold read, and one unevidenced
deliverable claim in `examples/before-after.md`. Everything not folded in is deferred explicitly in
the `<deferred_items>` table with a named owner.

Purpose: this is the third consecutive round in which a green gate coexisted with a false statement
a cold reader caught on first pass, and the second consecutive round in which the fix introduced the
next defect. The repair must not repeat the pattern, which means it must be small, must be verified
against the exact gates listed here, and must not reach for a stronger claim than the evidence
supports in either direction.

Output: a corrected `README.md`, a corrected and extended `.planning/WINDOWS.md`, one paragraph of
recorded reasoning in `tools/check_repo.py`'s module docstring, a corrected
`examples/before-after.md`, and `04-14-SUMMARY.md`.
</objective>

<execution_context>
@~/devoteam/.claude/gsd-core/workflows/execute-plan.md
@~/devoteam/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-distribution-worked-examples/04-UAT.md

Read only the `## Gaps` entry for `G-04-8` and test 4's `retest_detail` in the UAT. `G-04-3` and
`G-04-4` are `resolved`; leave them alone.

@README.md
@.planning/WINDOWS.md
@examples/deal-brief.md
@examples/before-after.md
</context>

<measurements>
Measured in the working tree at plan time. The executor should expect these exact values before
editing, and treat any disagreement as a signal that the tree moved.

**Gate baseline — all four green:**

| Command | Result |
|---|---|
| `python3 tools/check_repo.py` | `check_repo: 0 violations`, exit 0 |
| `python3 tools/check_repo.py --self-test` | `self-test PASS`, 48 codes listed |
| `python3 tools/generate_derivatives.py --check` | exit 0 |
| `python3 evals/conformance/run_conformance.py --self-test` | `self-test PASS - verdicts discriminated: conformant, no-family, rule-before-family, unscoreable` |
| `python3 tools/check_repo.py --mutation-test` | `mutation-test PASS: 48 codes discrimination-proven` |

**`grep -cF` line counts in `README.md`:**

| Literal | Lines now | Lines required after |
|---|---|---|
| `drives no live harness session` | 1 | 0 |
| `one per harness class` | 1 | 0 |
| `claude -p` | 0 | at least 1 |
| `/config` | 3 | at least 3 |
| `~/.claude/output-styles/` | 2 | at least 1 |
| `.claude/output-styles/` | 2 | at least 1 |
| `evals/conformance/RESULTS-mod04.md` | 2 | exactly 2 |

`grep -n '^At minimum:' README.md` prints `149:At minimum:` now and must print nothing after.

**Facts verified in source, not inferred from prose:**

- `evals/conformance/run_conformance.py:216` builds `['claude', '-p', prompt, '--model', model, '--disallowedTools'] + DISALLOWED_TOOLS`.
- `evals/conformance/run_conformance.py:205` copies the skill into a temp `.claude/skills/proof-first`.
- `.planning/WINDOWS.md` entry 16 carries the identical false clause, at table line 33 and JSON line 223.
- `_publish_locations_in` reads `README.md` only at the literal prefixes `npx skills add ` and `claude plugin marketplace add `. Prose occurrences of `<owner>/<repo>` are outside its scan.
- `EXAMPLE_PROSE_PATHS` is `examples/before-after.md` and `skills/proof-first/references/worked-examples.md`; `UNLISTED_FIGURE_SCAN_ROOTS` is `examples` and `skills`. `README.md` is outside both, so no sentence-length or figure gate applies to README prose.
- `NARRATION_CONNECTIVE_RE` is `rather than|,\s*not\b|instead of`, windowed at 60 characters.
- `gsd-tools windows append` writes immediately with no dry-run mode, and refuses to write while the rendered table disagrees with the fenced JSON.
- `.planning/REQUIREMENTS.md` currently has 17 lines matching `^- \[x\]`; EX-02 and DIST-06 are not among them.
</measurements>

<tasks>

<task type="tracer">
  <name>Task 1: Narrow README's false universal negative, and fold in three prose defects</name>
  <files>README.md</files>
  <action>
Four edits to one file. Make them in one pass, then run the whole gate set once.

**Edit A — the gap itself, line 83.** The paragraph at lines 80-83 currently ends: `That a Claude
Code session then lists it in /config has not been observed here:` followed by a universal negative
about the whole repository. Keep the first half — the `/config` listing genuinely is unobserved and
the ledger tracks it. Replace the clause after the colon so it scopes the limitation to the
interactive case and names the headless mechanism this repository does drive.

The replacement must do three things and nothing more: (1) keep asserting that the `/config` listing
has not been observed here; (2) state that this repository does drive live sessions, naming
`evals/conformance/run_conformance.py` and the headless `claude -p` mechanism; (3) state that a
headless session has no `/config` picker, which is why that one link is unobserved rather than
tested.

Candidate wording, to be checked against the literal counts in `<measurements>` before it is kept:

> That a Claude Code session then lists it in `/config` has not been observed here. This repository
> does drive live sessions — `evals/conformance/run_conformance.py` runs headless `claude -p`, and
> the conformance figure under `## Status` comes from those sessions — but a headless session has no
> `/config` picker, so the picker is the one link in this route nothing here exercises.

Do not soften this into an implication that the route works. Do not delete the limitation. The
limitation is real; only its scope was wrong.

**Edit B — line 36.** The sentence introducing the four routes describes them as one per harness
class. Routes 2 and 3 are both Claude Code, so the sentence mis-describes its own list. Rewrite it
to say how the four actually divide: one for every harness the Agent Skills standard reaches, two
that are Claude Code's own, one for a harness with no skill support. Keep it to one sentence.

**Edit C — the clone gap, in the paragraph at lines 43-47.** Routes 3 and 4 are described as running
from a local clone, but no route on the page says how to obtain the clone, and the only repository
URL on the page is the unresolved placeholder. Read strictly, a reader with only this README can
execute none of the four. State that plainly, in one sentence appended to that paragraph: both
routes need a local clone, the clone URL is the same unpublished `<owner>/<repo>` as routes 1 and 2,
and until this repository is published there is no URL to clone from.

Add prose only. Do not add a `git clone` command carrying the placeholder — a fourth non-resolving
command on the page trades one credibility defect for another. The honest move is to name the
dependency, not to dress it up as runnable.

**Edit D — line 149.** `## Status` opens a paragraph with a two-word fragment that has no antecedent.
Git shows this is an artifact of 04-13's permitted whitespace-only paragraph-break insertion: before
that break the fragment continued the preceding sentence. Restore its antecedent with the smallest
possible change — a lead-in clause referring back to the caveats the previous paragraph already
names, introducing the same three caveats that follow. Add no claim, remove no claim, change no
figure and no qualifier. This is the only edit permitted anywhere in `## Status`.
  </action>
  <verify>
    <automated>
python3 tools/check_repo.py
python3 tools/check_repo.py --self-test 2>&1 | tail -1
python3 tools/generate_derivatives.py --check && echo "generator-check exit 0"
python3 evals/conformance/run_conformance.py --self-test 2>&1 | tail -1
for p in 'drives no live harness session' 'one per harness class'; do printf '%-40s %s\n' "$p" "$(grep -cF "$p" README.md)"; done
for p in 'claude -p' '/config' '~/.claude/output-styles/' '.claude/output-styles/' 'evals/conformance/RESULTS-mod04.md'; do printf '%-40s %s\n' "$p" "$(grep -cF "$p" README.md)"; done
grep -n '^At minimum:' README.md; echo "orphan-fragment-grep-exit=$?"
git diff -U0 -- README.md | grep -c '^[+-]' 
git diff --stat -- skills output-styles prompts .claude-plugin examples
    </automated>
    <human-check>
Re-read the Install section and the first two paragraphs of `## Status` cold, as an evaluator who has
never seen the repository. Confirm no sentence on the page contradicts another, and that the
`## Status` edit changed a connective and nothing else.
    </human-check>
  </verify>
  <done>
The first two literal counts print `0`; `claude -p` prints at least `1`; `/config` prints at least
`3`; both output-style destinations print at least `1`; the results pointer prints exactly `2`. The
orphan-fragment grep matches nothing. All four gate commands are green. `git diff --stat` over
`skills`, `output-styles`, `prompts`, `.claude-plugin` and `examples` prints nothing — Task 1 touches
`README.md` only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the ledger's copy of the false sentence, and write down the mechanisability refusal</name>
  <files>.planning/WINDOWS.md, tools/check_repo.py</files>
  <action>
**Part 1 — fix entry 16.** `.planning/WINDOWS.md` entry 16 ends with a verbatim copy of the sentence
Task 1 just corrected in `README.md`. Fixing the README and leaving the ledger untouched leaves the
defect alive in the exact record that tracks the disclosure.

Entry 16's text exists twice: in the rendered table row at line 33 and in the fenced JSON entry at
line 223. Hand-edit both to the identical corrected text. Narrow the clause the same way Task 1 did
— the unobserved thing is a live Claude Code session listing the style in `/config`, and the reason
is that this repository's live sessions are headless `claude -p` runs with no picker, not that it
drives none. Change nothing else in the entry: its closure condition, its provenance note and its
two destination paths are all correct and stay.

There is no `edit` verb on the ledger tool — only `status`, `append`, `waive` and `fixed` — so this
is a hand-edit, and the two copies drifting apart is the failure mode. Part 2's `append` call is the
proof they did not: `gsd-tools windows append` refuses to write while any table row disagrees with
its own fenced JSON. If it refuses with a table-drift error naming row 16, the two copies disagree
and the edit is wrong.

**Part 2 — append the non-coverage entry.** Record that no code in this repository compares two
factual assertions in the same document for consistency, and that this is a decision rather than an
oversight.

```
node "$GSD_TOOLS" windows append --kind unrun-verify --phase 04 --file README.md --description "<text>"
```

`append` writes immediately and has no dry-run, so compose the description before calling it. The
description must carry: the defect class (a claim stated as fact that another passage in the same
file disproves); the measured evidence that no gate sees it (three consecutive rounds — G-04-3,
G-04-4 and G-04-8 — where `check_repo.py` was green and a cold human read found a false sentence on
first pass); the reason no code is registered (see Part 3); and the closure condition (a cold
human read per round, which is what caught all three, routed to end-of-phase UAT rather than to CI).

**Part 3 — the mechanisability assessment, recorded in the checker itself.** G-04-8's `missing` list
asks for this decision honestly made and written down. Make it, and record it where a future
contributor reaching for a new code will find it: `tools/check_repo.py`'s module docstring already
opens with a limits statement naming the paraphrase judgement it cannot perform (Phase 6's LEG-04).
Extend that paragraph with the same shape of disclosure for this class.

The assessment to record, and the reasoning that must survive into the docstring in condensed form:

- The defect is a contradiction between two assertions about fifty lines apart. Detecting it requires
  deciding that two different phrasings refer to the same thing and conflict. That is entailment, not
  pattern matching, and nothing in this stdlib-only stack performs it.
- A blocklist of the exact phrasings this round removes would prove only that these sentences did not
  come back. That is the instance, not the class — the same overstatement CR-01 closed once already.
- A broad negative-existential regex over README prose would be a hard build gate resting on a fuzzy
  proxy. Every code in this file fails the build; a false-positive-prone gate on legitimate future
  disclosure prose trades one credibility failure for another.
- A narrow presence code was considered and measured, not waved away. Requiring `README.md` to name
  the headless mechanism would be red before this plan and green after, which meets this
  repository's red-then-green evidence bar. It is still refused, on the ground that separates it from
  the code 04-13 did register: that code's string carried independent reader value, because a route
  missing its destination directory is unexecutable. A token naming the headless mechanism carries no
  such value. Its only function would be to gesture at this specific correction, and its presence
  cannot distinguish a correctly scoped disclosure from an over-broad one that happens to mention the
  mechanism elsewhere on the page.

Register no code. Keep the docstring addition to the limits paragraph short — two or three sentences
naming what is not checked and pointing at the ledger entry for the full reasoning. Do not add a
contract entry to the violation-code list, because there is no code to list.
  </action>
  <verify>
    <automated>
python3 tools/check_repo.py
python3 tools/check_repo.py --self-test 2>&1 | tail -1
python3 tools/check_repo.py --mutation-test 2>&1 | tail -1
python3 evals/conformance/run_conformance.py --self-test 2>&1 | tail -1
printf 'windows-false-clause-lines %s\n' "$(grep -cF 'drives no live harness session' .planning/WINDOWS.md)"
node "$GSD_TOOLS" windows status --pick ledger.total_count
node "$GSD_TOOLS" windows status --pick ledger.open_count
python3 -c "import re,sys; t=open('tools/check_repo.py',encoding='utf-8').read(); d=t.split('\"\"\"')[1]; print('docstring-mentions-same-document:', 'same document' in d)"
git diff --stat -- README.md examples skills output-styles prompts .claude-plugin
    </automated>
  </verify>
  <done>
`grep -cF` over `.planning/WINDOWS.md` for the false clause prints `0`. `windows status` reports
`total_count` 17 and `open_count` 10, and the `append` that produced them did not raise a table-drift
error, which is the proof that entry 16's table row and JSON entry agree. The checker's module
docstring names the unchecked class. `--mutation-test` prints `mutation-test PASS: 48 codes
discrimination-proven` — the same 48, no new code. All gates green. `git diff --stat` shows no change
to `README.md` (Task 1 owns it and is already committed), `examples`, `skills`, `output-styles`,
`prompts` or `.claude-plugin`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove the unevidenced deliverable claim from the Executive summary example</name>
  <files>examples/before-after.md</files>
  <action>
This task is not part of G-04-8. It is an owner-decision fold-in from the same cold read, recorded
under test 3's `retest_detail` in `04-UAT.md` as `UNSOURCED DELIVERABLE, orchestrator-verified`.
Test 3 passed with it present, because it violates none of the three rules that pair cites. It is
folded in anyway because an unevidenced deliverable claim in the flagship example file is the same
defect class as the two just closed in this phase.

Note before starting: this reopens a file whose current state is cold-read-verified as of round 2,
and the last two rounds each saw a careful fix introduce the next defect. Keep the edit to one
sentence and verify against every gate listed below.

`examples/before-after.md:27`, the Executive summary ✓ column, ends with a sentence asserting that
the migration will hand Halverton Mutual three outcomes: automated failover, relief on Oracle
Database licensing, and a governable landing zone. `examples/deal-brief.md` supports none of those
three as delivered outcomes. It records
them as pain points and as things named parties want: `:37` that failover across the estate is a
manual process, `:35` that Oracle Database licensing costs are an increasing share of the run rate,
and `:27` plus the Marcus Feld quote at `:70` that a governable landing zone is what he is asking
for. The claim carries no evidence and no marker.

Replace that one sentence so it states scope the brief supports — what the proposed migration
targets — instead of outcomes it delivers. Leave the first two sentences of the ✓ column untouched:
sentence 1 carries the PF-2.11 GAP marker and the PF-0.1 buyer-first opening, sentence 2 carries
PF-1.25's named economic buyer with her own stated measures. Leave the ✗ line and the
`Rules applied:` footer untouched.

Candidate wording, 24 words, to be re-counted after any change:

> The proposed migration targets the pains Halverton Mutual named: manual failover across the estate,
> rising Oracle Database licensing cost, and no governable landing zone.

Any alternative wording must clear all of these, which is why the candidate is shaped as it is:
25 words maximum per sentence; no spelled cardinal; no currency, percentage or ISO date absent from
the Canonical figures table; no contrastive connective within 60 characters of a rule-meta term; no
`PF-` or `MC-` token that is not in the Allocated IDs table; and every named item traceable to a
stated pain point in `examples/deal-brief.md`.
  </action>
  <verify>
    <automated>
python3 tools/check_repo.py
python3 tools/check_repo.py --self-test 2>&1 | tail -1
python3 tools/check_repo.py --mutation-test 2>&1 | tail -1
python3 tools/generate_derivatives.py --check && echo "generator-check exit 0"
printf 'delivers-automated-failover-lines %s\n' "$(grep -cF 'delivers automated failover' examples/before-after.md)"
sed -n '27p' examples/before-after.md | python3 -c "import sys,re; l=sys.stdin.read().strip(); body=re.sub(r'\[[^]]*\]','',l.lstrip('✓').strip()); [print(len(s.split()), s.strip()[:70]) for s in re.split(r'(?<=[.!?])\s+', body) if s.strip()]"
grep -n 'Rules applied' examples/before-after.md | sed -n '3p'
git diff -U0 -- examples/before-after.md | grep -c '^[+-]'
git diff --stat -- README.md skills output-styles prompts .claude-plugin examples/deal-brief.md
    </automated>
    <human-check>
Read the Executive summary pair against `examples/deal-brief.md` and confirm every item the ✓ column
now names is supported by a stated pain point, and that the pair still demonstrates PF-0.1, PF-1.25
and PF-2.11.
    </human-check>
  </verify>
  <done>
`grep -cF 'delivers automated failover' examples/before-after.md` prints `0`. Every sentence printed
by the word-count probe is 25 words or fewer. All gate commands green, `--mutation-test` still
`48 codes discrimination-proven`. `git diff -U0` over the file shows a two-line change (one removed,
one added). No other shipped file changed.
  </done>
</task>

</tasks>

<deferred_items>

Every observation from the round-2 cold read that this plan does not fix, with the reason and a named
owner. Nothing is dropped silently.

| Item | Source | Reason deferred | Owner |
|---|---|---|---|
| A resolving clone URL for routes 3 and 4 | UAT test 4 `retest_detail`, third secondary observation | Genuinely unfixable here: no git remote is configured and the repository is not published, so no URL exists to state. Task 1 Edit C closes the half that is fixable — the page will say a clone is needed and that the clone URL is the same unresolved placeholder. Substituting the real owner and repo is the remaining half. | `.planning/WINDOWS.md` entry 11, routed to Phase 6 LEG-04 |
| Restructuring `## Status` so it is not ~40% of the page, and so "30.0%" is not the document's only on-screen number | UAT test 4 `retest_detail`, fourth secondary observation | A prior round already deferred this on the ground that it would edit a measured-figure disclosure negotiated across 03-10, 03-15 and 04-08. That reasoning stands and is carried forward, not re-litigated. Task 1 Edit D fixes only the orphaned lead-in fragment, which is a grammar artifact of 04-13's whitespace insertion, not part of the disclosure. | `.planning/WINDOWS.md` entry 12, and Phase 5 once a persuasion figure exists to sit beside the conformance figure |
| A CI code that catches a self-contradicting factual claim | G-04-8 `missing` item 2 | Refused, not deferred. Task 2 Part 3 records the assessment and the reasoning in `tools/check_repo.py`'s module docstring and in a new ledger entry. Re-opening it requires new evidence that the class is checkable, not a fresh attempt at the same proxies. | Task 2, recorded as a decision |
| A live Claude Code session listing the copied output style in `/config` | UAT test 4, and `.planning/WINDOWS.md` entry 16 | No interactive harness runs in this environment. Task 1 Edit A and Task 2 Part 1 correct the *scope* of the disclosure; the observation itself still requires a human on a real machine. | `.planning/WINDOWS.md` entry 16, routed to Phase 6 LEG-04 |
| The PF-3.3 marked span at `examples/before-after.md:34` being slightly wider than the verbatim customer term | UAT test 3 `retest_detail`, noted weakness (a) | Explicitly recorded by the cold reader as correct behavior with an imprecise span, failing no test. Narrowing it means re-touching a marker that 04-12 deliberately placed, for no correctness gain. | Phase 5's prose-quality work, if it is worth doing at all |
| PF-4.1's ceiling forcing the product into a standalone sentence at `examples/before-after.md:20`, where the rule's own exhibit keeps it in a post-semicolon clause | UAT test 3 `retest_detail`, noted weakness | A tension between two rules, not a violation of either, as the reader itself recorded. Resolving it is a rule-catalog question, and this plan may not touch `skills/`. | Phase 6 catalog review |

</deferred_items>

<threat_model>
`workflow.security_enforcement` is `true`, `workflow.security_asvs_level` is `1`, and
`workflow.security_block_on` is `high`.

This is a documentation-and-stdlib-scripts repository with no network surface, no authentication, no
session state, no database, and no untrusted input beyond files a contributor commits. This plan
edits four files and registers no code. A thin register is the correct outcome; it is stated rather
than omitted so the reasoning is auditable.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository README → prospective evaluator | Every factual claim on the page is something a reader acts on or trusts. A false one spends trust the rest of the document needs. This is the boundary the whole plan sits on. |
| repository README → reader's shell | The page instructs a reader to run `mkdir` and `cp`. This plan adds no command and changes none. |
| CI checker → build verdict | A code named for coverage it lacks turns a red state green. This plan's central decision is to refuse such a code. |
| contributor commit → shipped example prose | An unevidenced claim in the flagship example is the exact failure the skill exists to prevent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-14-01 | Spoofing | `README.md:83`'s claim about this repository's own behaviour | high | mitigate | The threat is realised in the shipped file. Task 1 Edit A narrows the claim to the interactive case and names the headless mechanism the page elsewhere describes, so the two passages describe one world. Verified by a literal count going to zero and by a positive count on the mechanism name. |
| T-04-14-02 | Repudiation | `.planning/WINDOWS.md` entry 16 carrying a verbatim copy of the false claim | high | mitigate | Task 2 Part 1 corrects both the rendered row and the fenced JSON. The `append` in Part 2 is the agreement proof: it refuses to write while any row disagrees with its own JSON. |
| T-04-14-03 | Repudiation | a check named for coverage it does not have | high | mitigate | No code is registered. The refusal, the two rejected proxy designs and the measured reason the narrow presence code is not equivalent to 04-13's are written into the checker's own docstring and the ledger, so a later reader finds a decision rather than an absence. `--mutation-test` must report 48, unchanged. |
| T-04-14-04 | Tampering | the negotiated measured-figure disclosure in `## Status` | high | mitigate | One edit permitted in that section, to a two-word grammar fragment that 04-13's whitespace insertion orphaned. Every figure, caveat, qualifier and pointer is frozen by an explicit prohibition and observable in `git diff`. |
| T-04-14-05 | Tampering | reopening a cold-read-verified example file | medium | mitigate | Task 3 is scoped to one sentence, with the full gate list stated inline (word ceiling, spelled cardinals, figure table, narration proxy, rule-ID registry, brief traceability) precisely because the last two rounds saw a careful fix introduce the next defect. A two-line diff is an acceptance criterion. |
| T-04-14-06 | Information disclosure | citing an unverifiable source for a harness behaviour | medium | mitigate | No URL, documentation page or version number is cited. The `claude -p` claim is sourced from this repository's own committed script at a named line, which the executor can read. |
| T-04-14-07 | Elevation of privilege | requirement checkboxes flipping to complete without verification | medium | mitigate | An explicit prohibition, plus a count check: `.planning/REQUIREMENTS.md` has 17 lines matching `^- \[x\]` now and must have 17 after. EX-02 and DIST-06 stay unchecked. |
| T-04-14-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan, and the repository's zero-dependency constraint forbids adding one: both `tools/` scripts and the conformance runner import only the standard library, and there is no package manifest or lockfile in the tree. A Package Legitimacy Audit is therefore not required. Accepted because the absence of any install surface is itself observable. |
</threat_model>

<verification>
After the last task, from the repository root:

```
python3 tools/check_repo.py
python3 tools/check_repo.py --self-test 2>&1 | tail -1
python3 tools/check_repo.py --mutation-test 2>&1 | tail -1
python3 tools/generate_derivatives.py --check && echo "generator-check exit 0"
python3 evals/conformance/run_conformance.py --self-test 2>&1 | tail -1
printf 'false-clause-README %s\n'  "$(grep -cF 'drives no live harness session' README.md)"
printf 'false-clause-WINDOWS %s\n' "$(grep -cF 'drives no live harness session' .planning/WINDOWS.md)"
printf 'harness-class-claim %s\n'  "$(grep -cF 'one per harness class' README.md)"
printf 'headless-mechanism %s\n'   "$(grep -cF 'claude -p' README.md)"
printf 'unsourced-deliverable %s\n' "$(grep -cF 'delivers automated failover' examples/before-after.md)"
printf 'results-pointer %s\n'      "$(grep -cF 'evals/conformance/RESULTS-mod04.md' README.md)"
printf 'checked-requirements %s\n' "$(grep -c '^- \[x\]' .planning/REQUIREMENTS.md)"
git diff --stat -- skills output-styles prompts .claude-plugin examples/deal-brief.md
```

Expected: `check_repo: 0 violations`; `self-test PASS`; `mutation-test PASS: 48 codes
discrimination-proven`; `generator-check exit 0`; conformance `self-test PASS`;
`false-clause-README 0`; `false-clause-WINDOWS 0`; `harness-class-claim 0`; `headless-mechanism` at
least 1; `unsourced-deliverable 0`; `results-pointer 2`; `checked-requirements 17`; and an empty
`git diff --stat`.

Note on `grep -cF`: it prints `0` and exits 1 when nothing matches. The count inside the command
substitution is the assertion; the exit status is not.
</verification>

<success_criteria>
1. `README.md` asserts nothing about this repository's own behaviour that the same page disproves.
   The `/config` limitation survives, correctly scoped to the interactive case.
2. `.planning/WINDOWS.md` entry 16 carries the same corrected scope, in both its rendered row and its
   fenced JSON, proven in agreement by a subsequent `append` that did not raise table drift.
3. No new violation code exists. The refusal is recorded with its reasoning in
   `tools/check_repo.py`'s module docstring and as a tracked ledger entry, and `--mutation-test`
   still reports 48.
4. The three folded-in README defects are fixed, and `## Status` is otherwise byte-unchanged.
5. `examples/before-after.md`'s Executive summary ✓ column names no deliverable the canonical brief
   does not support, in a two-line diff.
6. Every unfixed observation from the round-2 cold read appears in `<deferred_items>` with a reason
   and a named owner.
7. All four CI gate commands are green.
</success_criteria>

<output>
Create `.planning/phases/04-distribution-worked-examples/04-14-SUMMARY.md` when done.

The summary must carry, verbatim enough to be auditable:

- The mechanisability assessment from Task 2 Part 3, including the two rejected proxy designs and the
  measured reason the narrow presence code was refused where 04-13's was registered. This is the part
  a future round will want to read before reaching for the same code.
- The before-and-after text of `README.md:83` and of `.planning/WINDOWS.md` entry 16's final clause.
- The before-and-after text of `examples/before-after.md:27`'s third sentence, with each named item
  mapped to the `examples/deal-brief.md` line that supports it.
- Both `verification: backstop` truths restated verbatim for the phase's verification step.
- The `<deferred_items>` table, carried forward unchanged.
</output>
