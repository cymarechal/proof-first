---
phase: 06-legal-review-gate-launch
plan: 06-06
title: Close the round-2 UAT gaps — the PF-1 counterweight, the /config contradiction, and the claim the fix reintroduced
gap_closure: true
gap_ids: [G-06-7, G-06-9]
created: 2026-09-21
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05]
---

# 06-06 — Gap closure for Phase 6 UAT, round 2

## Why this plan exists

`/gsd-verify-work 06` round 2 re-asked round 1's tests 2, 3 and 6 of the files 06-05 corrected,
using five independent headless readers who had not written the corrections. Test 8, the opinion
register, passed. Tests 7 and 9 returned fourteen checkably-false statements, every one re-verified
against a committed file before being recorded.

Three things about this round are worth naming before the tasks, because they change what "done"
has to mean.

**06-05 created one of the defects it was sent to fix.** Commit 36fbf6c wrote the `/config`
observation into `LEGAL-REVIEW.md`:536-541 and left `README.md`:86-91 asserting the picker "has not
been observed here". Two committed files now say opposite things about the same observation, and
the round that produced the contradiction is the round whose whole subject was reconciling
statements against outcomes.

**One defect is the round-1 defect, reintroduced in the paragraph rewritten to remove it.** G-06-6's
first finding was README claiming enforcement machinery it does not have. Task 1 of 06-05 rewrote
that paragraph and, in the replacement, wrote `README.md`:264-265 — "Different codes check those —
`catalog-count-mismatch`, `readme-layout-tree-stale` and their neighbours read the counted artefact
and compare." `catalog-count-mismatch` reads `SKILL.md`, not README. A mutation probe against an
unmutated sibling control settles it: mutating README's 31 and 28 to 37 and 44 leaves
`check_repo.py` at 0 violations, exactly as the control does.

**All ten CI commands were green through both rounds.** This is the fifth consecutive round matching
`WINDOWS.md` id 17. The conclusion 06-05 drew — that the pattern argues against building a
fuzzy-proxy gate for this class — still holds for the semantic half. It does not hold for
Task 3 below, where a guard for the exact regression already exists and simply matches the wrong
literal.

## Task 1 — Reconcile README with the `/config` observation this project already recorded

**Gap:** G-06-9, finding 1. **Files:** `README.md`:84-91, and check `LEGAL-REVIEW.md`:536-541 for
what was actually observed.

README says the picker listing "has not been observed here" and calls it "the one link in this
route nothing here exercises". `LEGAL-REVIEW.md`:536-541 records the observation on 2026-09-21 with
platform, harness version, entry position, persistence to `.claude/settings.local.json`, and two
controls; ledger row 16 repeats it.

Rewrite README's passage to state what was observed and the two limits `LEGAL-REVIEW.md` itself
states: the observation was made by automation reading a terminal rather than by a human eye, on one
platform, against a project-scoped `.claude/output-styles/` rather than `~/.claude/output-styles/`.

Do not overclaim in the other direction. `WINDOWS.md` id 16 stays open; whoever owns that entry
decides whether a captured render satisfies a condition written as a human observation. README
should say the picker was observed and that the entry is still open, not that the entry is closed.

**Verify:** no sentence in README asserts the picker is unobserved. The passage's limits match
`LEGAL-REVIEW.md`:536-541 rather than softening or hardening them.

## Task 2 — Correct "three runs against the shipped skill description"

**Gap:** G-06-9, finding 3. **File:** `README.md`:134.

`evals/trigger/RESULTS-trigger.md` holds three run blocks. Two carry head-14 sha256 `d5dd651a…`,
the shipped description. Arm A (`:101`) carries `9049c7d8…`, a 551-character description recorded at
`:152-156` as tested and then reverted. README's own `:139-140` says "Arm A describes nothing this
repository ships" five lines after the bullet claims all three.

State two runs against the shipped description plus one against a reverted treatment, or drop the
count. The rest of the bullet is accurate and was verified figure by figure this round — do not
disturb it.

**Verify:** no sentence in README states a count of runs against the shipped description that
`RESULTS-trigger.md`'s three run headings contradict.

## Task 3 — Correct "no benchmark has run" in the shipped skill, and widen the guard that should have caught it

**Gap:** G-06-9, finding 2. **Files:** `skills/proof-first/references/artifact-patterns.md`:144,
then `tools/generate_derivatives.py`, then `tools/check_repo.py`:4264.

This is the only finding of the round that ships to an installed user. The sentence reads "It is not
evidence that a convention wins deals or improves scores; no benchmark has run, and this repository
makes measured claims or none." It is carried verbatim into `output-styles/proof-first.md`:695 and
`prompts/system-prompt.md`:683. The benchmark ran on 2026-09-18 and recorded 96 generations
(`evals/benchmark/RESULTS.md`:1).

Correct the source file, then run `python3 tools/generate_derivatives.py` so both derivatives
follow. The point the sentence is making is still true and still worth making — the artifact-family
table is not evidence that a convention wins deals. Keep that half; replace the false premise with
what the benchmark actually establishes and does not.

Then widen the guard. `tools/check_repo.py`:4264 defines
`STALE_COMPARISON_CLAIM = 'No benchmark has compared'` and fails the build on it. The machinery for
this exact regression already exists and matches a sibling literal. Add this literal to the same
check and prove the addition discriminates via `--mutation-test`.

This is the one place in this round where a mechanical gate is the right answer rather than a proxy
for a semantic judgement: the target is a fixed string, not a meaning.

**Verify:** the string "no benchmark has run" appears in no file under `skills/`, `output-styles/`
or `prompts/`. `python3 tools/generate_derivatives.py --check` passes. `check_repo.py
--mutation-test` reports the widened code as discrimination-proven, and its code count rises.

## Task 4 — Bound the `publish-location-drift` claim to what the checker compares

**Gap:** G-06-9, finding 4. **File:** `README.md`:43-44.

README says the code "fails the build if any command or manifest carrying it stops agreeing with the
others". `tools/check_repo.py`:724-726 declares the exclusion in terms: "it also compares owner
segments only, so a repository-name-only drift under an unchanged owner is not detected", and
`_publish_locations_in` (`:3620`) returns owner segments.

Bound README's sentence to the owner-segment comparison. The checker's own docstring already states
the honest form; reuse it rather than inventing new wording.

**Verify:** README's description of the code's scope matches `check_repo.py`'s declared ceiling.

## Task 5 — Correct what `bench-deal-brief.md` is for

**Gap:** G-06-9, finding 5. **Files:** `README.md`:9-10 and `:171-172`.

Both passages say the benchmark's sessions are prompted with `evals/benchmark/bench-deal-brief.md`.
`run_benchmark.py`:295 sends `scenario['prompt']` and nothing else; the brief is read exactly once,
inside `--self-test` at `:1646`, to assert it shares no named entity with `examples/deal-brief.md`.
"Thornfield", the brief's buyer, appears zero times in `scenarios.json` and zero times across the 96
committed raw records.

The brief's own `:9` states the accurate relation — "it grounds Phase 5's benchmark scenarios". Use
that. The separation half of README's claim is true and verified this round; keep it.

**Verify:** no sentence in README says a session is prompted with the brief. The relation stated
matches `bench-deal-brief.md`:9 and `run_benchmark.py`:295.

## Task 6 — Correct the enforcement claim 06-05 reintroduced

**Gap:** G-06-9, finding 6. **File:** `README.md`:264-265.

The sentence names `catalog-count-mismatch` and `readme-layout-tree-stale` as the codes that check
README's inventory counts. `catalog-count-mismatch` (`check_repo.py`:198-202) compares a
`skills/*/SKILL.md` stated count against `NUMBERING.md`'s Allocated IDs table and never opens
README. `README_CHECK_CODES` (`:1468-1473`) contains no code that reads a count out of README.

Name the codes that actually read README, or say plainly that README's inventory counts are not
mechanically enforced. The third-case sentence two lines later — "What no code checks is the third
case: figures quoted from a results file in prose outside the claim region" — is already the honest
form; this sentence should match its register.

Do not close this by adding a new README-count code. That would be a real option, but it is a
different decision with its own scope, and this task is to stop the document claiming enforcement it
does not have. If the code is wanted, open it as its own item.

**Verify:** re-run the round-2 mutation probe against an unmutated sibling control. Mutating
README's 31 and 28 must either fail the build, or leave a README sentence that does not claim
otherwise. Record which of the two outcomes was chosen.

## Task 7 — Decide the activation contrast

**Gap:** G-06-9, finding 7. **File:** `README.md`:111-112.

"The installed skill has to be triggered and was not in 3 of its 12 sessions, while the output style
and the pasted prompt are unconditionally on once selected." The 3-of-12 figure comes from
`RESULTS-routes.md`:15's Activation column, whose metric is `run_routes.py`:487,
`MARKER_RE.search(text)`. On that same metric `prompt-on` scores 8 of 12 (`:17`) — worse than
skill-on's 9 of 12 — in the row directly below.

The sentence is defensible on a loading-versus-emitting distinction, and round 1 recorded it as a
placement judgement. Round 2's brief named "a cold reader deciding otherwise" as the finding, and a
reader who had not seen round 1's adjudication decided otherwise. Two rounds is enough evidence that
the distinction does not survive contact with the table it cites.

Either carry prompt-on's 8 of 12 beside the skill-on figure, or drop the measured figure from the
contrast and make the point about loading mechanics without borrowing the activation column's
number. Do not keep a measured figure whose neighbouring row undercuts the sentence it supports.

**Verify:** no sentence in README draws a contrast from the Activation column without carrying every
arm's figure from that column.

## Task 8 — Reconcile `bench-deal-brief.md` with the ledger

**Gap:** G-06-9, finding 8. **File:** `evals/benchmark/bench-deal-brief.md`:10-12.

The brief still states "This environment has no live network access, so the name-collision web
search ... could not be repeated here for this brief's names; that is an open, disclosed
unrun-verify item, not a completed check." `LEGAL-REVIEW.md`:617, ledger row 18, books that same
search **Fixed**: "Closed by 06-02's searches; the premise that this environment has no network had
expired."

Update the brief to record that the search ran, when, and what it returned, pointing at whichever
section of `LEGAL-REVIEW.md` holds the result. This is the third artefact in this project found
carrying the expired no-network premise; if a fourth is found, the premise itself should be swept
for repo-wide rather than fixed one file at a time.

**Verify:** no committed file states the no-network premise as current. `grep -rn "no live network
access"` over the tracked tree returns only historical records that say so in the past tense.

## Task 9 — Correct the PF-1 counterweight and everything downstream of it

**Gap:** G-06-7, finding (a). **Files:** `LEGAL-REVIEW.md`:289-291, `:299-301`, `:383-384`, `:628`.

This is the most serious item in the round. `LEGAL-REVIEW.md`:289-291 says "the list appears in an
internal ID registry rather than in `SKILL.md`, the output style or the system prompt — nothing a
reader of the shipped skill sees names these seven elements as a set in this order." All three named
files carry the identical sentence naming all seven in the table's order:

    skills/proof-first/SKILL.md:63
    output-styles/proof-first.md:87
    prompts/system-prompt.md:75

`:299-301` inherits the error ("confined to `NUMBERING.md` ... since no shipped file cites them"),
and so do `## What remains open` item 5 and ledger row 29.

Correct all four places. Then re-read the entry's disposition in light of the corrected facts: the
false statement was the counterweight that made leaving id 29 open look conservative, and the remedy
it priced at one file actually touches `NUMBERING.md` and `SKILL.md` plus a
`generate_derivatives.py` run. Whether the entry still stays open is a judgement for this plan's
executor to make and write down — but it must be made against the true facts, and the change in
facts must be visible in the entry rather than absorbed silently.

**Verify:** no sentence in `LEGAL-REVIEW.md` says the seven elements appear only in `NUMBERING.md`.
The ledger row and the `What remains open` item carry the corrected scope. The disposition states
whether it changed and why.

## Task 10 — Correct the four remaining falsifiable statements in the reproduction-boundary sections

**Gap:** G-06-7, findings (b) through (e). **File:** `LEGAL-REVIEW.md`.

Four separate corrections, each checkable against a named file:

1. `:204-207` says "four of the eight positions could be swapped with no change to any spelling".
   `NUMBERING.md`:77-78 is `Paper Process` then `Pain` — a third same-initial pair. The count is
   six. The same paragraph spells M-E-D-D-P-P-C-C at `:203` and then omits the P pair from the list
   it is enumerating. Correct it to six. Commit 0b8a865's message carries the same undercount; note
   the correction in this round's commit rather than trying to rewrite history.
2. `:236-237` says "Economic Buyer" and "Paper Process" are "block headings in
   `completeness-audit.md`". Neither string occurs in that file; its headings are MC ids plus rule
   titles. Repo-wide the two appear only in `NUMBERING.md`, `check_repo.py` fixtures and
   `LEGAL-REVIEW.md`. Correct it — and note the true fact is narrower and better for the entry than
   the false one, so the correction strengthens the disposition rather than weakening it.
3. `:268` cites `NUMBERING.md`:26-40 for the PF-1 carve-up. The heading is at `:31`, the prose at
   `:33-35`, the table at `:37-45`; line 26 is a PF-2 row of a different table, and the cited span
   excludes five of the seven elements the same sentence enumerates. Correct to `:31-45`. The same
   wrong range appears in round 1's own UAT evidence — the error travelled from the finding into the
   fix, which is worth one sentence in the commit message.
4. `:296` says PF-1's two deciding questions "are the same two live questions id 6 ends on".
   `## What remains open` lists them as separate items 5 (`:383-387`) and 6 (`:388-390`) with
   different content. Either correct the claim of equivalence, or state why id 29 stays open without
   leaning on it.

**Verify:** each corrected sentence is true against the file named beside it. `python3
tools/check_repo.py` stays green.

## Task 11 — Reconcile the append-only rule with what 06-05 did

**Gap:** G-06-7, finding (f). **File:** `LEGAL-REVIEW.md`:16.

The file states "It is append-only — later reviews add sections, they do not rewrite earlier ones."
Commit 0b8a865 removed 32 lines and added 64, rewriting the id-6 section in place. The falsified
acronym reasoning is gone from the file; its substance survives only in the "Correcting the earlier
reading first" paragraph.

Two honest resolutions, and this plan does not pick between them:

- Restate the rule so it permits correcting a premise that is false against the repository's own
  registry, provided the correction states what was corrected — which is what 06-05 in fact did.
- Or keep the rule as written and restore the superseded text under a dated heading, the way
  `## Human observations` sections 1 to 3 were handled.

The second is more consistent with how the same round treated the same problem forty lines away.
Whichever is chosen, the rule and the file's actual history must agree afterwards.

**Verify:** `LEGAL-REVIEW.md`:16's rule is true of the file's own history, or the history has been
restored to satisfy it.

## Task 12 — Re-run the checks, update the ledger, and record the round

**Gap:** both. **Files:** `LEGAL-REVIEW.md` `## Human observations` and `## Ledger disposition`,
`.planning/WINDOWS.md`.

Run all ten commands from `.github/workflows/ci.yml` and record the result. Then append a
`## Human observations` section 5 recording round 2: what the five readers were asked, what they
returned, what was re-verified, and the one reader claim this round refuted (the
`**Result: no checkably-false statement found.**` staleness, which its own heading already
discloses as superseded).

Update the ledger. Id 17's reason currently reads "Fourth round of the pattern"; it is now the fifth,
and the fifth round is the one where a gap-closure round introduced a contradiction with a file it
wrote in the same round. Id 12 stays open and its closure condition should name this round's
findings rather than only G-06-6's. Id 29's row needs Task 9's corrected scope. Keep the counts at
the foot of the section consistent with the rows, and keep `WINDOWS.md`'s JSON fence as the source
of truth — render the table from it, never the reverse.

Record the two backlog items this round produced rather than dropping them: `README.md`:188's
"drafted twice" omitting the 3 repeats, which two rounds of readers have now tripped on; and the
reasoning critiques of the id-6 prongs recorded under G-06-7's `missing` list, which are judgements
about argument quality rather than checkable falsehoods.

**Verify:** all ten CI commands green. Ledger counts sum to the row count. `WINDOWS.md`'s rendered
table matches its JSON.

## Out of scope

- Renaming the PF-1 sub-blocks or the two conceded MC labels. Task 9 corrects the facts the decision
  rests on; the decision itself belongs to whoever owns `WINDOWS.md` ids 29 and 6.
- Building a new checker code for README's inventory counts. Task 6 stops the false claim; adding
  the code is a separate decision with its own scope.
- Reopening test 8. The opinion register passed on its own criterion; the structural finding the
  reader recorded is a prose judgement carried as an observation, not a gap.
- Re-measuring anything. No figure in this round was found wrong; every benchmark, conformance,
  trigger and routes number two readers checked reproduced exactly.
