---
plan: 12
phase: 06-legal-review-gate-launch
title: "Round 8 gap closure — ten shipped-file findings, one of them the launch gate itself, and the blind spot change 5 cannot see"
gap_closure: true
gap_ids: [G-06-30, G-06-31, G-06-32, G-06-33]
backlog_gap_ids: [G-06-34]
created: 2026-09-23
uat_round: "/gsd-verify-work 06 round 8, 2026-09-23, against 8eca37d"
planner: "inline (orchestrator) — the gsd-planner subagent was not dispatched; the Agent tool is prohibited in this session"
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05, 06-06, 06-07, 06-08, 06-09, 06-10, 06-11]
---

# Round 8 gap closure

Round 8 returned **ten findings in shipped files** and twenty-two in `.planning/`. Under the owner's
2026-09-23 gate scope only the ten block. This plan closes those ten (**Group E**), leaves
G-06-34 as tracked backlog, and fixes the one structural defect the round exposed in the standing
set's own instrument.

**One finding is not about prose.** B7: `LEGAL-REVIEW.md` tells a reader that two real-world name
collisions are open items to decide before wider distribution, and the register it names holds no
such item. That is the launch gate misreporting itself, and it is task E8's only subject.

## What this round measured about its own instruments

- **Change 1** (widened gap-closure range): marginal yield **1 of 10** shipped findings — the
  `bench-deal-brief.md`:28 literal, 0 added-line matches in the narrow `991f35a^..HEAD` and 1 in the
  widened `d67012e^..HEAD`. Third consecutive round of one. **Exit condition not met; keep it.**
  Separately, reader 7 returned nothing unique to itself for the first time. Record again in round 9.
- **Change 2** (second sweep reader): **zero** unique findings. Reader 6 returned three, all of
  which reader 5 or a file-named brief also returned. This is the **first** of the two consecutive
  zero rounds its exit condition needs. **Keep it; round 9 decides.**
- **Change 5** (post-SUMMARY literal re-run): **structurally incomplete, and this round proves it.**
  06-11 re-ran nineteen literals at `80641fd` and all held, yet its own `4c3e911` falsified two
  committed literals in files it never opened. Task E11 fixes the rule.
- **Refutations: 2**, after a round of none. Both came from readers whose Python execution was
  blocked, and both concerned the same sentence. Round 7's zero now reads as the anomaly 06-11
  predicted, not as reader improvement.

## Tasks

### E1 — README's "two kinds of number" (G-06-30, A1)
`README.md`:291 asserts two kinds; `:307` names a third that `:299-300`'s own definition of
inventory excludes and that sits outside the claim region. Restate `:291` to three kinds, naming the
third as the unenforced one, so `:302-304`'s "one of the three is checked" still reads over the
inventory items it belongs to. Do not touch `:307`.
**Verify:** `sed -n '291p;299,309p' README.md` reads coherently as one taxonomy; `python3 tools/check_repo.py` → 0 violations.

### E2 — README's 23-file gloss (G-06-30, refutation hardening)
Two readers independently derived 22 from `:181-184` because no `check_*` function opens the script.
Add the reason the 23rd is real — the interpreter's own load — so the class does not recur. This
closes a refuted finding, not a true one; the count stays 23.
**Verify:** audit-hook trace over a true `python3 tools/check_repo.py` subprocess returns 23 including `tools/check_repo.py`, matching the bullet item for item.

### E3 — the seven labels' locations (G-06-31, B1)
`LEGAL-REVIEW.md`:416-417's "`NUMBERING.md` and the two deal briefs" omits four shipped files
carrying `pain` under the repository's own `_source_label_pattern`. Either widen the enumeration or
scope the sentence to the six labels other than `pain` and state `pain`'s wider spread separately.
**Verify:** for each of the seven labels, the file list produced by `_source_label_pattern` over the tracked tree is covered by whatever the sentence ends up claiming. Run it, do not reason about it.

### E4 — the sweep that contradicts itself (G-06-31, B2)
`:399-400` ("nowhere else outside this file and the checker") and `:403` ("All seven appear
throughout `.planning/`") sit in one five-bullet list under `:358`'s tracked-tree sweep header.
Reconcile: scope `:399-400` to the shipped tree explicitly, or drop the "nowhere else" universal.
**Verify:** the two bullets state compatible things about the same sweep, and the header at `:358` names the scope both are read against.

### E5 — the date universal (G-06-31, B3)
`:70-71` says every check below was performed on 2026-09-21; `:198` records a live re-fetch on
2026-09-22. Restate over the dates that occur.
**Verify:** every date the record attaches to a performed check is inside whatever `:70-71` ends up asserting. Enumerate them first.

### E6 — the pattern's round count (G-06-31, B4)
`:1083` says "Seven rounds of the pattern" while `:951` and `:1014` number this phase's rounds 1 and
2 as the pattern's fourth and fifth. Qualify to this phase's rounds, or carry the pattern's own
ordinal — and if the ordinal is carried it becomes eleven at round 8, so prefer the qualifier.
Single site: `LEGAL-REVIEW.md:1083`. Authored `9656ca7`, 06-11.
**Verify:** `grep -rn "rounds of the pattern" LEGAL-REVIEW.md .planning/WINDOWS.md` — every hit agrees with `:951`/`:1014`.

### E7 — `owner.url`'s object, in both files (G-06-31, B5)
`owner` is a top-level key of `marketplace.json`; `plugins[0]` has none. Two sites say otherwise:
`LEGAL-REVIEW.md`:786 and `tools/check_repo.py`:3744 ("its plugin entry's same two fields plus
`owner.url`"). `check_repo.py`:6334 is already correct and `:734` is not wrong; leave both.
**Verify:** `python3 -c "import json;d=json.load(open('.claude-plugin/marketplace.json'));print('owner' in d, 'owner' in d['plugins'][0])"` → `True False`; then `grep -n "owner.url" LEGAL-REVIEW.md tools/check_repo.py` and read each hit against that.

### E8 — the two name collisions (G-06-31, B7) — the launch-gate task
`LEGAL-REVIEW.md`:74-75 and `:1067` state that **Ardent Digital** and **Gina Almeida** are open and
routed to `.planning/WINDOWS.md` for a decision before wider distribution. `grep -c` returns **0**
for each name in that file, and the only collision rows — ids 1 and 18 — both read `fixed`.
Decide which is true and make the record match:
- If the collisions are still open, append a ledger entry through the JSON fence and re-render, and
  point `:74-75` and `:1067` at its id.
- If they were decided, state where and when, and correct both sentences.
Do not close this by deleting the sentences. `:660-700` records both collisions as real, and one is
a person whose name and stated role both land close to the brief's invented character.
**Verify:** every name `LEGAL-REVIEW.md` says is routed to the ledger resolves to a ledger id whose status matches the word the review uses; `gsd-tools windows status` → `ok: true`.

### E9 — the reproduced ledger (G-06-31, B6)
Three sites assert a 32-entry register — `:1033`, `:1100`, `:1101-1102` — while `:1083` in the same
file cites ledger id 33 and the live fence reads `total_count: 33, open_count: 13`. Re-render all
three from the fence. E8 may add id 34; if so, render after E8, not before.
**Verify:** the reproduced table's last id, the three stated totals, and `gsd-tools windows status --raw` agree. Re-run after the final ledger write of this round, not at the task.

### E10 — the two falsified literals (G-06-32, C1 + C2)
- `evals/benchmark/bench-deal-brief.md`:28 — "`grep -c '^|.*20%'` returns four for each brief" now
  returns 4 and **5**. Authored `2e93a0c` (06-08), falsified by `4c3e911` (06-11).
- `tools/check_repo.py`:682-684 — "13 spelled cardinals" now returns **16** under the file's own
  `SPELLED_CARDINAL_RE`. Authored `fd1b17b` (04-07); 13 at `991f35a^`, 15 at `4c3e911`, 16 at HEAD.
  The sibling "5 raw regex matches ... for `skills/**`" is still exactly 5, so state both at HEAD.
Prefer restating by rule over restating by count wherever the rule is as clear.
**Verify:** re-run both commands at the commit that lands the edit, and again in E12.

### E11 — widen change 5 to the class it cannot currently see (G-06-32, structural)
Change 5 re-runs the literals **this round wrote**. Both C1 and C2 were written by earlier rounds,
were true when this round began, and were falsified by this round's edit to a third file. The rule
needs the reverse index: **for every file this round edits, re-run every committed command literal
anywhere in the tree whose result reads that file.** Record the rule in `.planning/WINDOWS.md`
through the JSON fence, and run it for this round in E12.
**Verify:** run the reverse index over 06-11's edit set as a back-test — it must return C1 and C2. A rule that does not rediscover the two findings that motivated it is not the rule.

### E12 — self-audit, then the post-SUMMARY pass at the widened scope
Read every sentence this round added, and every `.planning/` file it touched. Then, **after the
SUMMARY commit lands**, run change 5 at E11's widened scope. Round 8's own write-up already caught
one of its own this way (the `fc801a9` count, changed by the act of recording it) and one by
self-audit (the shipped-finding count, written 12 and corrected to 10 at three sites).
**Verify:** no `[x]` stands beside an UNVERIFIED requirement; LEG-04 and LEG-05 read `Pending`; all ten CI commands green; `gsd-tools windows status` → `ok: true`.

## Out of scope

**G-06-34's twenty-two `.planning/` findings.** Non-blocking under the 2026-09-23 decision. Seven
are new and fifteen are G-06-29's, unfixed by design. Touch a ledger row only where a task above
forces it open, and when forced, **edit the fence first and the reproduction second** — that
ordering is round 7's structural finding and it bound 06-11 twice.

The reopening condition was tested again and **not** triggered: every `.planning/`-vs-shipped
divergence this round has the record stale and the shipped file correct, or the record current and
the shipped reproduction stale. No defect propagated outward.

## Closure

This plan does not close the phase. `WINDOWS.md` id 12 and the LEG-04/LEG-05 checkboxes stay open
until a round of cold reads returns none; **eight have run and none has**. Group E is the blocking
work. Round 9 reads what this round wrote.
