---
plan: 13
phase: 06-legal-review-gate-launch
title: "Round 9 gap closure — resolve confirmed README defects, refute hallucinated reader findings, and verify zero defects in shipped files"
gap_closure: true
gap_ids: [G-06-35, G-06-36, G-06-37]
backlog_gap_ids: [G-06-38]
created: 2026-09-23
uat_round: "/gsd-verify-work 06 round 9, 2026-09-23"
planner: "inline (orchestrator) — the gsd-planner subagent was not dispatched; the Agent tool is prohibited in this session"
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05, 06-06, 06-07, 06-08, 06-09, 06-10, 06-11, 06-12]
---

# Round 9 gap closure

Round 9 tested 310 shipped files across 5 standing briefs via 8 independent Antigravity subagents.
The round confirmed that 06-12's group E corrections unregressed and that the patch diff (Brief 4)
was 100% clean across all 18 files.

Four findings were initially reported in shipped files. Careful verification against the tree reveals:
- **Two genuine defects in `README.md`** (G-06-35, G-06-37):
  1. `README.md:327`: asserts "the 20 PF rules in `SKILL.md`", but `SKILL.md` defines 31 rules.
  2. `README.md:187-191`: asserts "the five eval scripts ... each carries its own `--self-test` ... ten commands in CI", omitting `evals/trigger/stats.py` (CI runs 6 eval scripts with `--self-test`, decomposing as 3 + 6 + 1 = 10 commands). Also `stats.py` and `LEGAL-REVIEW.md` are omitted from the layout tree and inventory.
- **Two refuted findings in `LEGAL-REVIEW.md`** (G-06-36):
  Reader 3 quoted two sentences claiming: (1) `LEGAL-REVIEW.md:208` carries "two rows for the readability formula sources"; (2) `LEGAL-REVIEW.md:928` asserts `readme-example-lead-distance` requires the example "within 20 lines of the `## Before and after` heading".
  Both quotes were **hallucinated by Reader 3**: neither quoted string exists anywhere in `LEGAL-REVIEW.md` or git history. Line 208 discusses `SOURCES.md` out-of-bounds searches, and lines 927-928 state that `readme-example-lead-distance` "holds its first ✗ line inside a frozen 20-line ceiling" (which is true: line 17 <= 20). Reader 4 independently audited `LEGAL-REVIEW.md` and gave it a clean pass with 0 findings.

This plan closes the confirmed defects in `README.md` (**Group F**), records the refutations for G-06-36, runs the reverse index over all files citing `README.md`, and runs the full 10-command CI suite.

## What this round measured about its own instruments

- **Brief 4 (Gap-closure patch diff):** Zero findings across all 18 files. Second consecutive round of zero unique yield for reader 7 (rounds 8 and 9). **Exit condition met — brief 4 retired for Round 10.**
- **Change 2 (Second sweep reader):** Reader 6 returned zero unique findings. Second consecutive round of zero unique yield (rounds 8 and 9). **Exit condition met — change 2 retired for Round 10.**
- **Refutations: 2** (Reader 3's two hallucinated quotes against `LEGAL-REVIEW.md`).

## Tasks

### F1 — Correct `SKILL.md` rule count in `README.md` (G-06-35, A1)
`README.md:327-328` asserts "the 20 PF rules in `SKILL.md`, the 8 MC rules in `completeness-audit.md`".
`SKILL.md` defines 31 PF rules (lines 141 and 302 of `README.md` already state 31). The "20" was conflated
with the 20 illustrated PF rules in `worked-examples.md`.
Change "the 20 PF rules in `SKILL.md`" to "the 31 PF rules in `SKILL.md`".
**Verify:** `grep -n "PF rules in" README.md` reads "the 31 PF rules in `SKILL.md`"; `python3 tools/check_repo.py` → 0 violations.

### F2 — Reconcile eval script count and CI command decomposition in `README.md` (G-06-35 & G-06-37, A2 & C1)
1. In `README.md:148-161` (What exists today inventory), add:
   `- evals/trigger/stats.py — a stdlib-only, self-testing statistical utility for confidence intervals and exact tests.`
2. In `README.md:187-191`, update:
   "Of the items listed above this line, seven are never opened: the six eval scripts `evals/lint.py`, `evals/trigger/run_trigger_test.py`, `evals/trigger/stats.py`, `evals/conformance/run_conformance.py`, `evals/routes/run_routes.py` and `evals/benchmark/run_benchmark.py`, and the prose file `evals/pressure-tests.md`. Each of those six scripts carries its own `--self-test`, and CI runs all of them separately — see `.github/workflows/ci.yml` for the full list of ten commands."
3. In `README.md:367-369` (Repository layout tree), add `stats.py` under `trigger/`.
4. In `README.md:391-396` (Repository layout root), add `LEGAL-REVIEW.md`.
5. In `README.md:400-402`, add `LEGAL-REVIEW.md` to the root-staying enumeration.
**Verify:** CI command count 10 decomposes as 3 + 6 + 1, matching `.github/workflows/ci.yml` and `LEGAL-REVIEW.md:1028-1030`; `python3 tools/check_repo.py` → 0 violations.

### F3 — Document refutation of Reader 3 findings and resolve G-06-36 (Refutation, B1 & B2)
In `06-UAT.md`, update Test 36 and G-06-36 to record that Reader 3's two citations were hallucinated text not present in `LEGAL-REVIEW.md`. Mark G-06-36 resolved by refutation. Update `06-VERIFICATION.md` to reflect that G-06-36 is refuted/resolved.
**Verify:** `06-UAT.md` records the exact text comparisons and evidence proving neither quote exists in the repository.

### F4 — Reverse index check over `README.md` edits (Change 5 / Ledger id 35)
For each shipped file naming `README.md` (`LEGAL-REVIEW.md`, `NOTICES.md`, `evals/conformance/RESULTS-mod04.md`, `tools/check_repo.py`):
Re-run every committed literal and re-derive every measurement that reads `README.md`.
Specifically:
- `git grep -o '<owner>/<repo>' -- .claude-plugin README.md | wc -l` → 9.
- `readme-example-lead-distance` ceiling <= 20 lines from top of raw file → line 17 <= 20.
- `readme-layout-tree-stale` → PASS.
- `readme-layout-legend-drift` → PASS.
**Verify:** all reverse index commands return their claimed outputs.

### F5 — Self-audit and full CI verification
Re-run all 10 CI commands from `.github/workflows/ci.yml`:
1. `python3 tools/check_repo.py --self-test`
2. `python3 tools/check_repo.py --mutation-test`
3. `python3 tools/check_repo.py`
4. `python3 evals/conformance/run_conformance.py --self-test`
5. `python3 evals/lint.py --self-test`
6. `python3 evals/benchmark/run_benchmark.py --self-test`
7. `python3 evals/routes/run_routes.py --self-test`
8. `python3 evals/trigger/run_trigger_test.py --self-test`
9. `python3 evals/trigger/stats.py --self-test`
10. `python3 tools/generate_derivatives.py --check`
**Verify:** all 10 commands exit 0 cleanly.

## Out of scope

- **G-06-38's 26 `.planning/` findings:** Tracked as non-blocking backlog under the 2026-09-23 gate scope decision. Reopening condition NOT triggered.

## Closure

Once tasks F1 through F5 are executed and committed, the shipped tree will have **zero open defects**.
Round 10 cold-read verification will run against the zero-defect tree with retired briefs (Brief 4 and Reader 6 retired per their exit conditions).
